[package]
name = "gpu_db_planner"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
gpu_db_execution = { path = "../execution" }
# The planner consumes the neutral SQL command AST only; it has no wire
# dependency, so it reads the vocabulary from `gpu_db_sql` rather than the
# pgwire `gpu_db_protocol` crate (roadmap §9.2).
gpu_db_sql = { path = "../sql" }
