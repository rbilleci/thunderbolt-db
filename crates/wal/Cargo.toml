[package]
name = "gpu_db_wal"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
gpu_db_types = { path = "../types" }
# E1 step 1: the optional FUA fence-pool durability backend is built on the write-conveyor's
# variable-payload `FuaFrameLog` (control-plane WAL/durability I/O). Only referenced behind
# `#[cfg(unix)]` — the frame log itself is a unix-only (O_DIRECT|O_DSYNC) construct.
gpu_db_write_conveyor = { path = "../write_conveyor" }
libc = "0.2"
sha2 = "0.10"
