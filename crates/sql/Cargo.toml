[package]
name = "gpu_db_sql"
version.workspace = true
edition.workspace = true
license.workspace = true

# Neutral SQL vocabulary: the command AST, `SqlType`/`SqlValue`, the parser, and
# `ParseError`. Lives below both `gpu_db_engine` and `gpu_db_protocol` so the
# engine can consume the parsed SQL surface without depending on the wire
# (pgwire) crate (roadmap §9.2: invert the engine→protocol dependency).

[dependencies]
serde.workspace = true
thiserror.workspace = true
