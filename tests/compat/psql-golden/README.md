# psql golden compatibility suite

This suite exercises a real `psql`/libpq path and compares normalized output against golden artifacts.

## Run

```bash
export PGHOST=127.0.0.1
export PGPORT=5432
export PGDATABASE=postgres
export PGUSER=postgres
export PGPASSWORD=postgres

scripts/run_psql_golden.sh
```

Optional:
- `PSQL_BIN=/path/to/psql`
- `PSQL_GOLDEN_OUT_DIR=/tmp/psql-golden`
- `PSQL_GOLDEN_BOOT_CMD='cargo run -p gpu_db_protocol --bin gpu-db-server -- --listen 127.0.0.1:5432'`
- `PSQL_GOLDEN_BOOT_CWD=/path/to/repo-or-service`
- `PSQL_GOLDEN_STOP_CMD='pkill -f gpu-db-server'`
- `PSQL_GOLDEN_WAIT_HOST=127.0.0.1`
- `PSQL_GOLDEN_WAIT_PORT=5432`
- `PSQL_GOLDEN_WAIT_TIMEOUT_SEC=30`
- `PSQL_GOLDEN_REPORT=target/compat/psql-golden-report.json`

When `PSQL_GOLDEN_BOOT_CMD` is set, the harness starts the target service itself, waits for the configured TCP endpoint to accept connections, captures boot logs in `target/psql-golden/boot.log`, and writes a machine-readable scenario report to `PSQL_GOLDEN_REPORT` for CI scorecards.

## Update expected artifacts

1. Run the suite against the target endpoint.
2. Inspect `target/psql-golden/*.txt`.
3. If the new output is correct and deterministic, copy it into `tests/compat/psql-golden/expected/`.
4. Optionally add `tests/compat/psql-golden/expected/<scenario>.rc` when a scenario expects non-zero `psql` exit status (defaults to `0` when omitted).
5. Optionally add `tests/compat/psql-golden/scenarios/<scenario>.psqlargs` with one extra `psql` CLI argument per line when a scenario needs custom flags.

## Notes

- The harness intentionally strips volatile lines (timing/version/SSL banner noise).
- Keep scenario assertions stable and semantic, avoid transient text where possible.
- Current scenario coverage includes connect/simple-query, session reset probes, SQL prepare/execute/deallocate flow, transaction begin/commit/rollback flow, relational create/insert/select flows, metadata-backed catalog/type introspection, `pg_catalog.pg_tables` discovery, direct `pg_catalog.pg_namespace` lookup for the supported `public` namespace, empty `pg_catalog.pg_indexes` and `pg_catalog.pg_views` discovery for the current no-user-visible-SQL-index/no-SQL-view subset, empty `pg_catalog.pg_constraint` discovery for the current no-SQL-constraint subset, empty `pg_catalog.pg_attrdef` discovery for the current no-column-default subset, empty `pg_catalog.pg_description` discovery for the current no-comment subset, joined `pg_catalog.pg_class` / `pg_catalog.pg_namespace` relation metadata and table-name `IN (...)` plus namespace-only `public` relation subsets for supported `public` tables, joined `pg_catalog.pg_attribute` / `pg_catalog.pg_class` / `pg_catalog.pg_namespace` column metadata with formatted supported type names, real `psql \dt`, `\dt+`, `\dt+ <table>`, `\dt <prefix>*`, `\dt+ <prefix>*`, `\dt public.*`, `\dt+ public.*`, `\dt public.<prefix>*`, and `\dt+ public.<prefix>*` table listing, real `psql \d <table>` / `\d+ <table>` / `\d <prefix>*` / `\d+ <prefix>*` / `\d public.*` / `\d public.<prefix>*` / `\d+ public.<prefix>*` / `\d *.*` / `\d+ *.*` column display, real `psql \di` empty index listing, `\dv` / `\dv+` empty view listing, `\dm` / `\dm+` plus `\ds` / `\ds+` empty materialized-view/sequence listing, `\df` empty function listing, `\da` empty aggregate listing, `\dc` empty conversion listing, `\do` empty operator listing, `\dO` empty collation listing, `\dC` empty cast listing, `\dRp` empty publication listing, `\dRs` empty subscription listing, `\ddp` empty default-access-privilege listing, `\dd` empty object-description listing, `\du` bootstrap role listing, `\l` bootstrap database listing, `\db` bootstrap tablespace listing, `\dx` empty extension listing, `\dL` empty procedural-language listing, `\dD` / `\dD+` empty domain listing, and `\dA` bootstrap access-method listing for the current subset, real `psql \dn` and `\dn+ public` schema listing, real `psql \dT pg_catalog.int4` / `\dT pg_catalog.text` type display, `\dT pg_catalog.*` and `\dT+ pg_catalog.*` supported built-in type listing, first-slice `information_schema` table/column/schema introspection including richer supported table metadata, table-name `IN (...)` and exact table-name table subsets, base-table discovery with system-schema exclusion, all supported `public` table columns, column discovery with system-schema exclusion, table-name `IN (...)` column subsets, per-table column detail projection, rich column metadata, extended column numeric precision/radix/scale metadata for the supported subset, including exact table-filtered and table-name `IN (...)` extended column metadata, and empty `information_schema.table_constraints` / `information_schema.key_column_usage` discovery for the current no-SQL-constraint subset, extended-query bind execution including multirow parameterized SELECT, inferred `int4` and `text` bind parameter typing, inferred mixed `int4`/`text` conjunction parameters, inferred predicate-plus-`LIMIT` parameters, negative `LIMIT` bind rejection with zero-limit and later valid-limit recovery, invalid bind execution followed by later bound-portal `\gdesc` recovery, quoted-placeholder literal preservation, exact multi-digit placeholder-token substitution, zero-padded nonzero placeholder binding, apostrophe-escaped text bind literals, supported casted bind placeholders including casted `LIMIT` placeholders, zero-placeholder rejection/recovery, bound-portal and unbound parameterized-statement `\gdesc` description, bound-result `\gset`, expanded-output `\gx`, and crosstab rendering via `\crosstabview`, repeated unnamed rebind execution, parameter-count mismatch recovery, and invalid `int4` bind-value recovery, real `psql` `FETCH_COUNT` cursor-style SELECT retrieval, explicit `FETCH NEXT`, omitted-count `FETCH FORWARD`, `FETCH ... IN`, separator-less forward `FETCH`, `FETCH FORWARD ALL` / `FETCH ALL` cursor exhaustion, zero-count forward cursor movement, transaction-end cleanup for session-local cursors, unquoted cursor identifier case-folding, quoted cursor identifier preservation including semicolons and doubled-quote escapes across `DECLARE`/`FETCH`/`MOVE`/`CLOSE`, forward-only cursor `MOVE` including separator-less forms, duplicate cursor declaration rejection that preserves the existing cursor, non-forward cursor-direction rejection, explicit unsupported `FETCH_COUNT` plus unresolved `\bind` placeholder cursor traffic, `CLOSE ALL` cursor cleanup, real `psql \gdesc` result-description display over supported relational `SELECT` statements, explicit unsupported extended parameterized INSERT, parameterized JOIN SELECT, `COPY ... TO STDOUT` recovery, `COPY ... FROM STDIN` rejection before copy-in mode, and deterministic error/recovery paths.
- Scenario 44 covers catalog-qualified `information_schema.columns` extended metadata filters for `table_catalog = current_database()` backed by supported `public` session catalog metadata.
- Scenario 45 covers catalog-qualified `information_schema.tables` richer table metadata filters for `table_catalog = current_database()` backed by supported `public` session catalog metadata.
- Scenario 46 covers direct `pg_catalog.pg_namespace` lookup for the supported `public` namespace OID/name metadata.
- Scenario 47 covers real `psql \dn+ public` verbose schema introspection for the supported `public` namespace, returning owner metadata plus empty ACL/description fields for the current no-ACL/no-comment subset.
- Scenario 48 covers real plain `psql \d` relation listing for supported `public` session tables, backed by the session catalog relation metadata.
- Scenario 49 covers real `psql \dv` / `\dv+` view listing traffic, returning no rows for the current no-SQL-view subset while preserving supported table metadata.
- Scenario 50 covers real `psql \dm` / `\dm+` materialized-view and `\ds` / `\ds+` sequence listing traffic, returning no rows for the current no-materialized-view/no-sequence subset while preserving supported table metadata.
- Scenario 51 covers real `psql \df` function listing traffic, returning no rows for the current no-user-defined-function subset.
- Scenario 52 covers real `psql \du` role listing traffic, returning the bootstrap `postgres` role from a narrow `pg_catalog.pg_roles` compatibility slice while keeping role mutation out of scope.
- Scenario 53 covers real `psql \l` database listing traffic, returning the supported bootstrap `postgres` database from a narrow `pg_catalog.pg_database` compatibility slice while keeping database creation, templates, ACL mutation, and broader database catalog behavior out of scope.
- Scenario 54 covers real `psql \dx` extension listing traffic, returning no rows for the current no-extension subset while keeping extension install and extension catalog state out of scope.
- Scenario 55 covers real `psql \db` tablespace listing traffic, returning the bootstrap `pg_default` and `pg_global` metadata through a narrow `pg_catalog.pg_tablespace` compatibility slice while keeping tablespace creation, location management, options, and broader tablespace catalog behavior out of scope.
- Scenario 56 covers real `psql \dL` procedural-language listing traffic, returning no rows for the current no-procedural-language subset while keeping language creation and broader `pg_catalog.pg_language` behavior out of scope.
- Scenario 57 covers real `psql \dA` access-method listing traffic, returning the supported bootstrap `heap` table access method through a narrow `pg_catalog.pg_am` compatibility slice while keeping access-method creation, extension, and broader access-method catalog behavior out of scope.
- Scenario 58 covers real `psql \dT pg_catalog.*` and `\dT+ pg_catalog.*` type-listing traffic, returning the supported `int4`/`text` registry from a narrow `pg_catalog.pg_type` compatibility slice while keeping user-defined types and broader type catalog behavior out of scope.
- Scenario 59 covers real `psql \dD` and `\dD+` domain-listing traffic, returning no rows for the current no-domain subset while keeping domain creation, domain constraints, and broader domain catalog behavior out of scope.
- Scenario 60 covers real `psql \da` aggregate-listing traffic, returning no rows for the current no-user-defined-aggregate subset while keeping aggregate creation and broader function catalog behavior out of scope.
- Scenario 61 covers real `psql \dc` conversion-listing traffic, returning no rows for the current no-conversion subset while keeping encoding conversion creation and broader conversion catalog behavior out of scope.
- Scenario 62 covers real `psql \do` operator-listing traffic, returning no rows for the current no-user-defined-operator subset while keeping operator creation and broader operator catalog behavior out of scope.
- Scenario 63 covers real `psql \dO` collation-listing traffic, returning no rows for the current no-user-defined-collation subset while keeping collation creation and broader collation catalog behavior out of scope.
- Scenario 64 covers real `psql \dC` cast-listing traffic, returning no rows for the current no-user-defined-cast subset while keeping cast creation and broader cast catalog behavior out of scope.
- Scenario 65 covers real `psql \dRp` publication-listing traffic, returning no rows for the current no-publication subset while keeping publication creation and broader publication catalog behavior out of scope.
- Scenario 66 covers real `psql \dRs` subscription-listing traffic, returning no rows for the current no-subscription subset while keeping subscription creation and broader subscription catalog behavior out of scope.
- Scenario 67 covers real PostgreSQL 16 `psql` `FETCH_COUNT` cursor flow over a supported relational `SELECT`, proving session-local `DECLARE ... CURSOR FOR SELECT`, repeated `FETCH FORWARD n`, and cursor `CLOSE` handling return the same ordered rows as the normal SELECT path.
- Scenario 68 covers real PostgreSQL 16 `psql \ddp` default-access-privilege listing traffic, returning no rows for the current no-default-ACL subset while keeping default privilege mutation and broader ACL catalog behavior out of scope.
- Scenario 69 covers real PostgreSQL 16 `psql \gdesc` result-description traffic over a supported relational `SELECT`, including the follow-up `pg_catalog.format_type` formatter query over described `int4`/`text` result OIDs.
- Scenario 70 covers real PostgreSQL 16 `psql \dd` object-description listing traffic, returning no rows for the current no-comment/no-user-defined-object-description subset.
- Scenario 71 covers a common `information_schema.views` client probe for the supported `public` schema, returning no rows for the current no-SQL-view subset while keeping view definitions and view DDL out of scope.
- Scenario 72 covers a common `pg_catalog.pg_views` client probe for the supported `public` schema, returning no rows for the current no-SQL-view subset while keeping view definitions and view DDL out of scope.
- Scenario 73 covers a common `information_schema.tables` base-table discovery probe with system-schema exclusion, returning supported `public` session tables from catalog metadata.
- Scenario 74 covers a common `information_schema.columns` column-discovery probe with system-schema exclusion, returning supported `public` session-table columns from catalog metadata.
- Scenario 75 covers real PostgreSQL 16 `psql \dt *.*` and `\dt+ *.*` all-schema table-listing traffic, returning supported `public` session tables from catalog metadata while keeping unsupported schemas out of scope.
- Scenario 76 covers real PostgreSQL 16 `psql \d *.*` all-schema relation-description traffic, returning supported `public` session-table relation rows and reusing metadata-backed per-table describe probes while keeping unsupported schemas out of scope.
- Scenario 77 covers real PostgreSQL 16 `psql \d+ *.*` verbose all-schema relation-description traffic, returning supported `public` session-table relation rows and reusing metadata-backed verbose per-table describe probes for column storage/access-method display while keeping unsupported schemas out of scope.
- Scenario 78 covers real PostgreSQL 16 `psql \d+ public.*` verbose schema-wildcard relation-description traffic, returning supported `public` session-table relation rows and reusing metadata-backed verbose per-table describe probes for column storage/access-method display.
- Scenario 79 covers real PostgreSQL 16 `psql \dt public.<table>` exact schema-qualified table-listing traffic, returning only the requested supported `public` session table from catalog metadata.
- Scenario 80 covers real PostgreSQL 16 `psql \dt+ public.*` verbose schema-wildcard table-listing traffic, returning supported `public` session tables with catalog-backed persistence/access-method metadata while keeping heap size and description blank until those metadata surfaces exist.
- Scenario 81 covers real PostgreSQL 16 `psql \bind` multirow relational SELECT execution with a text-format parameter over the supported `int4` / `text` subset.
- Scenario 82 covers real PostgreSQL 16 `psql \bind` with omitted Parse parameter OIDs inferred from a supported `int4` predicate, including one invalid literal error followed by Sync recovery and a successful bound SELECT.
- Scenario 83 covers real PostgreSQL 16 `psql` `FETCH_COUNT` combined with `\bind`, explicitly rejecting the unresolved parameterized cursor declaration boundary while proving the session can still run a supported SELECT afterward.
- Raw extended `Parse`/`Bind`/`Execute` for supported parameterized `DECLARE ... CURSOR FOR SELECT` is covered by helper-level protocol tests rather than a golden scenario, including bound cursor-declaration portal `Describe` returning `NoData` and extended portal/statement cleanup leaving the installed session-local cursor fetchable, because PostgreSQL 16 `psql` still exposes the `FETCH_COUNT` plus `\bind` path as an unresolved-placeholder cursor declaration.
- Scenario 84 covers real PostgreSQL 16 `CLOSE ALL` cursor cleanup by proving a later fetch from the closed session-local cursor fails while later supported SELECT traffic still succeeds.
- Scenario 85 covers real PostgreSQL 16 `COPY ... TO STDOUT` traffic as an explicit unsupported COPY boundary and proves later supported SELECT traffic still succeeds in the same session. Scenario 127 covers real PostgreSQL 16 `COPY ... FROM STDIN` traffic, proving the endpoint rejects copy-in before entering frontend COPY data flow and later supported SELECT traffic still succeeds. Scenario 170 covers real PostgreSQL 16 `psql \copy ... FROM PROGRAM` client-side copy-in traffic, proving the same unsupported COPY boundary and recovery path for psql's meta-command flow.
- Raw frontend `CopyData`, `CopyDone`, `CopyFail`, and `FunctionCall` frames are covered by helper-level protocol tests because PostgreSQL 16 `psql` does not emit them from stable golden-suite flows; that coverage pins the unsupported SQLSTATE, skip-until-`Sync`, skipped side-effect, and later recovery behavior.
- Scenario 86 covers real PostgreSQL 16 parameterized extended `INSERT` traffic as an explicit unsupported non-SELECT boundary and proves later supported SELECT traffic still succeeds in the same session.
- Scenario 87 covers real PostgreSQL 16 parameterized extended JOIN SELECT traffic as an explicit unsupported broader-SELECT boundary and proves later supported SELECT traffic still succeeds in the same session.
- Scenario 88 covers real PostgreSQL 16 named cursor `CLOSE` against a missing cursor, returning a stable cursor-not-found error while proving an existing cursor and later supported SELECT traffic still work.
- Scenario 89 covers real PostgreSQL 16 `psql \bind` parameter-count mismatch handling for supported relational SELECT portals, returning a protocol violation before portal installation and proving a later correctly-bound SELECT still succeeds.
- Scenario 90 covers real PostgreSQL 16 `psql \bind` invalid `int4` text-input handling for supported relational SELECT portals, returning a stable invalid-text-representation error before portal installation and proving a later correctly-bound SELECT still succeeds.
- Scenario 91 covers real PostgreSQL 16 `psql \bind ... \gdesc` over a supported parameterized relational SELECT, proving a bound portal can be described with catalog-backed result metadata and a later correctly-bound execution still returns rows.
- Scenario 92 covers real PostgreSQL 16 `psql \bind` text-parameter execution over the supported relational SELECT subset and pins the empty-result portal path without fabricating rows.
- Scenario 93 covers real PostgreSQL 16 `psql \bind` text values that look like placeholders, proving `Ada$1` and `Missing$1` are treated as literal text data over the supported relational SELECT subset, including the empty-result portal path for the missing value.
- Scenario 94 covers repeated real PostgreSQL 16 `psql \bind` execution on unnamed extended-query state, proving exhausted unnamed portals can be replaced for later parameter values and that a bound-portal `\gdesc` does not prevent a later execution from returning the requested row.
- Scenario 95 covers real PostgreSQL 16 `psql \bind` execution with inferred predicate and `LIMIT` placeholders over an ordered supported relational `SELECT`. Scenario 168 covers negative extended `LIMIT` bind execution, proving it returns the stable PostgreSQL-compatible error while preserving zero-limit, later valid-limit, and SELECT recovery paths. Scenario 169 covers invalid bind execution before a later bound-portal `\gdesc`, proving error recovery preserves the prepared statement and later portal description/execution path.
- Scenario 96 covers real PostgreSQL 16 parameterized `psql \gdesc` without a preceding `\bind`, proving `Describe Statement` advertises catalog-backed result metadata for the supported relational `SELECT` subset without executing the statement.
- Scenario 130 covers real PostgreSQL 16 `psql \bind ... \gset` over a supported parameterized relational SELECT, proving extended-query rows and metadata can be consumed into client variables and reused by later supported SQL in the same session.
- Scenario 131 covers real PostgreSQL 16 `psql \bind ... \gx` over a supported parameterized relational SELECT, proving extended-query rows can be consumed by psql expanded-output rendering and later supported SQL still works in the same session.
- Scenario 132 covers real PostgreSQL 16 `psql \bind ... \crosstabview` over a supported parameterized relational SELECT, proving extended-query rows can be consumed by psql crosstab rendering and later supported SQL still works in the same session.
- Scenario 133 covers real PostgreSQL 16 `psql \gexec` over supported relational text results containing semicolon-terminated SQL, proving simple-query framing preserves semicolons inside SQL string literals and later supported SQL still works in the same session. Scenario 150 covers a semicolon-bearing quoted SQL prepared-statement name, proving simple-query framing also preserves semicolons inside quoted identifiers across `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE`. Scenario 175 covers parameterized `psql \bind ... \gexec` as an explicit unsupported extended meta-command boundary, proving it returns a stable bind-arity error and recovers for later supported SELECT traffic. Scenario 176 covers reused SQL-prepared placeholders through extended `EXECUTE <prepared>($1)` binds, proving `\gdesc`, invalid `int4` recovery, later execution, deallocate, and SELECT recovery stay truthful. Scenario 177 covers reordered outer placeholders through extended `EXECUTE <prepared>($2, $1)` binds, proving Parse/Describe infer the remapped outer `text`/`int4` OIDs and invalid reordered `int4` input preserves prepared-state recovery. Scenario 178 covers casted and parenthesized reordered outer placeholders through extended `EXECUTE <prepared>(($2)::pg_catalog.int4, ($1)::text)` binds, proving SQL `EXECUTE` argument normalization preserves the same remapped OID inference and recovery behavior. Scenario 179 covers mixed literal/placeholder SQL `EXECUTE` arguments through extended binds, proving only the remaining outer parameter is described/bound while invalid `int4`, later text-bound execution, deallocate, and SELECT recovery stay truthful. Scenario 180 covers conflicting reused outer placeholders in SQL-prepared extended binds, proving `EXECUTE <prepared>($1, $1)` is rejected when the reused placeholder would need incompatible SQL-prepared `int4` and `text` argument types, and that later supported SQL-prepared extended bind plus SELECT traffic recovers.
- Scenario 97 covers real PostgreSQL 16 `psql \bind` execution where SQL string literals contain `$1` / `$2`, proving quoted placeholder-looking tokens are not counted as protocol parameters and are not substituted during Bind.
- Scenario 98 covers real PostgreSQL 16 `psql \bind` execution where `$1` and `$10` appear in the same supported relational `SELECT`, proving Bind substitution targets exact unquoted placeholder tokens.
- Scenario 99 covers real PostgreSQL 16 `psql \bind` traffic with an unquoted `$0` placeholder, proving Parse rejects invalid zero-numbered parameters before statement installation and the session recovers for later supported SELECT traffic.
- Scenario 100 covers real PostgreSQL 16 `psql \bind` traffic with an unquoted `$00` placeholder, proving Parse rejects zero-padded zero-numbered parameters at the same boundary before statement installation.
- Scenario 101 covers real PostgreSQL 16 `psql \bind` traffic with zero-padded nonzero placeholders (`$01`, `$002`, `$0003`), proving Bind substitution uses numeric placeholder identity while zero-numbered placeholders remain rejected at Parse time.
- Scenario 102 covers real PostgreSQL 16 `psql \bind` traffic with apostrophes in text parameters, proving bound values are SQL-escaped as data and the supported relational parser accepts doubled-quote text literals generated by Bind.
- Scenario 103 covers real PostgreSQL 16 `psql \bind` traffic with placeholder-looking tokens inside line comments and nested block comments, proving commented `$n` tokens do not inflate parameter arity or participate in Bind substitution.
- Scenario 104 covers real PostgreSQL 16 `psql \bind` traffic with omitted Parse parameter OIDs inferred from a supported `text` predicate, proving repeated matching rows and empty-result portals stay truthful for inferred text parameters.
- Scenario 105 covers real PostgreSQL 16 `psql \bind` traffic with omitted Parse parameter OIDs inferred across a supported `int4`/`text` conjunction, proving invalid inferred `int4` input fails before portal installation and later correctly-bound execution still works in the same session.
- Scenario 106 covers real PostgreSQL 16 `psql \bind` traffic with a reused inferred `$1` placeholder across multiple supported `int4` predicates, proving invalid inferred `int4` input fails before portal installation and later correctly-bound execution reuses the same parameter value consistently.
- Scenario 107 covers real PostgreSQL 16 `psql \bind` traffic with commuted placeholders such as `$1 <= id` and `$2 = name`, proving omitted Parse OIDs are inferred from the catalog-backed column side and the bound relational SELECT executes after invalid-input recovery.
- Scenario 108 covers real PostgreSQL 16 `psql \bind` traffic with omitted Parse OIDs inferred across a supported top-level `OR` predicate, proving invalid inferred `int4` input fails before portal installation and later correctly-bound execution still works in the same session.
- Scenario 109 covers real PostgreSQL 16 `psql \bind` traffic with supported casted placeholders such as `$1::int4`, `$2::text`, and `pg_catalog`-qualified forms, proving bound literals remain inside the supported relational SELECT subset and invalid casted `int4` input recovers before correctly-bound execution.
- Scenario 110 covers real PostgreSQL 16 `psql \bind` traffic with a casted `LIMIT $n::pg_catalog.int4` placeholder, proving supported casted limit parameters parse, reject invalid `int4` input before portal installation, and recover for a correctly-bound execution.
- Scenario 111 covers real PostgreSQL 16 cursor lifecycle traffic with `FETCH FORWARD ALL` and `FETCH ALL`, proving supported relational cursor state exhausts remaining rows once and returns a truthful empty fetch afterward before `CLOSE` and later supported SELECT recovery. Scenario 112 covers `FETCH NEXT`, omitted-count `FETCH FORWARD`, `FETCH ... IN`, and explicit non-forward cursor-direction rejection with later supported SELECT recovery. Scenario 113 covers forward-only cursor `MOVE`, proving it advances supported relational cursor state without returning rows, reports truthful moved-row counts, rejects non-forward movement explicitly, and recovers for later supported SELECT traffic. Scenario 114 covers separator-less forward cursor grammar forms for `FETCH` and `MOVE`, plus explicit non-forward separator-less cursor rejection and later supported SELECT recovery. Scenario 115 covers unsupported `BINARY` and `SCROLL` cursor declaration options, proving they are rejected explicitly instead of installing malformed cursor names while supported `NO SCROLL` cursor traffic and later SELECT recovery still work. Scenario 116 covers `WITHOUT HOLD` cursor declaration grammar for supported forward-only relational SELECT cursors and explicit rejection of unsupported `WITH HOLD` cursors with later SELECT recovery. Scenario 117 covers PostgreSQL `ASENSITIVE` and `INSENSITIVE` cursor declarations for the supported forward-only relational SELECT cursor subset while preserving explicit unsupported binary-cursor rejection and later SELECT recovery. Scenario 118 covers zero-count forward cursor movement, proving `FETCH FORWARD 0` returns no rows without advancing state, `MOVE FORWARD 0` reports `MOVE 0`, and later supported cursor fetches plus SELECT recovery still work. Scenario 119 covers transaction-end cleanup for session-local `WITHOUT HOLD` cursors, proving `COMMIT` and `ROLLBACK` clear supported relational cursors, later `FETCH` calls return cursor-not-found errors, and supported SELECT recovery still works. Scenario 120 covers PostgreSQL-style unquoted cursor identifier case-folding across `DECLARE`, `FETCH`, `MOVE`, and `CLOSE`, proving mixed-case client cursor names address the same supported relational cursor while later missing-cursor and SELECT recovery still work. Scenario 121 covers quoted cursor identifier preservation, proving a quoted mixed-case cursor name with a space remains distinct from a different quoted case variant across `DECLARE`, `FETCH`, `MOVE`, and `CLOSE` while later supported SELECT recovery still works. Scenario 122 covers duplicate cursor declaration rejection, proving the existing supported relational cursor is preserved and later fetch/close plus SELECT recovery still work.
- Scenario 123 covers directionless numeric cursor movement, proving `FETCH 0 FROM`, `FETCH 2 FROM`, and `MOVE 1 FROM` preserve supported relational cursor position and later SELECT recovery still works. Scenario 162 covers quoted cursor identifiers containing semicolons, proving simple-query framing preserves the cursor name across `DECLARE`, `FETCH`, `MOVE`, and `CLOSE` before the expected post-close error and later SELECT recovery. Scenario 163 covers SQL comments inside supported `FETCH`, `MOVE`, and `CLOSE` cursor commands, proving comment-tolerant parsing preserves cursor lifecycle state and later SELECT recovery. Scenario 164 covers `\bind` text values containing spaces, semicolons, commas, and SQL comment markers, proving they remain literal data for supported relational SELECT predicates. Scenario 165 covers SQL comments around supported `DECLARE ... ASENSITIVE NO SCROLL CURSOR WITHOUT HOLD ... FOR SELECT` grammar, proving the commented declaration installs the supported relational cursor and preserves later fetch/move/close plus SELECT recovery.
- Scenario 124 covers forward-all cursor marker variants, proving `MOVE FORWARD ALL FROM`, `MOVE FORWARD n IN`, and `FETCH FORWARD ALL IN` preserve supported relational cursor state, report truthful counts, exhaust once, and recover for later supported SELECT traffic.
- Scenario 125 covers broader non-forward cursor directions, proving `FETCH FIRST`, `MOVE LAST`, `MOVE RELATIVE n`, and `MOVE ABSOLUTE n` return explicit unsupported-direction errors without advancing supported relational cursor state, and later cursor plus SELECT traffic still works. Scenario 126 covers marker-bearing omitted-count cursor forms, proving `FETCH FORWARD FROM`, `FETCH NEXT IN`, plain `FETCH FROM`, `MOVE FORWARD FROM`, and `MOVE NEXT IN` each advance the supported relational cursor by one row and recover for later supported SELECT traffic.
- Scenario 128 covers additional non-forward cursor directions, proving `FETCH PRIOR`, `FETCH BACKWARD ALL`, `MOVE PRIOR`, and `MOVE BACKWARD ALL` return explicit unsupported-direction errors without advancing supported relational cursor state, and later cursor plus SELECT traffic still works.
- Scenario 129 covers markerless omitted-count forward cursor forms, proving `FETCH FORWARD <cursor>`, `FETCH NEXT <cursor>`, `FETCH FORWARD ALL <cursor>`, `MOVE FORWARD <cursor>`, `MOVE NEXT <cursor>`, and `MOVE FORWARD ALL <cursor>` preserve supported relational cursor state and later SELECT recovery.
- Scenario 134 covers markerless `MOVE ALL <cursor>`, proving supported relational cursor movement advances to the end, reports the truthful moved-row count, returns no rows on the following fetch, and still recovers for later supported SELECT traffic.
- Scenario 135 covers marker-bearing `MOVE ALL FROM <cursor>` and `MOVE ALL IN <cursor>`, proving supported relational cursor movement advances to the end, reports truthful moved-row counts, returns no rows on following fetches, and still recovers for later supported SELECT traffic.
- Scenario 136 covers quoted cursor identifiers containing doubled-quote escapes, proving the decoded session-local cursor name remains stable across `DECLARE`, `FETCH`, `MOVE`, and `CLOSE`, then returns a cursor-not-found error after close while later supported SELECT traffic recovers.
- Scenario 158 covers marker-bearing zero-count movement and all-fetch cursor forms, proving `MOVE 0 FROM`, `MOVE 0 IN`, `FETCH ALL FROM`, and `FETCH ALL IN` preserve supported cursor state truthfully and recover for later supported SELECT traffic. Scenario 159 covers marker-bearing numeric `IN` forms, proving `FETCH 0 IN`, `FETCH n IN`, `MOVE 0 IN`, and `MOVE n IN` keep supported relational cursor state truthful across empty, partial, and remaining-row paths. Scenario 160 covers markerless numeric zero/count cursor forms, proving `FETCH 0 <cursor>`, `FETCH n <cursor>`, `MOVE 0 <cursor>`, and `MOVE n <cursor>` keep the same supported relational cursor truth boundary. Scenario 161 covers unsupported direction and declaration recovery, proving `FETCH BACKWARD n`, `MOVE ABSOLUTE n`, and unsupported `SCROLL CURSOR` declarations return explicit errors without advancing the supported forward-only cursor, and later cursor plus SELECT traffic still works.
- Scenario 137 covers SQL `PREPARE` / `EXECUTE` over the supported relational `SELECT` subset, proving real `psql` can prepare catalog-backed `int4`/`text` predicates, execute matching and empty-result paths, reject invalid `int4` input without losing the statement, deallocate it, and recover for later supported SELECT traffic. Scenario 138 covers SQL `PREPARE` without an explicit type list, proving catalog-backed parameter type inference, invalid inferred-`int4` recovery, `DEALLOCATE ALL` cleanup of SQL prepared state, and later supported SELECT recovery. Scenario 139 covers `DEALLOCATE PREPARE <name>` with a quoted SQL prepared-statement name, proving the name survives prepare/execute/deallocate and later supported SELECT traffic recovers after the expected missing-prepared-statement error. Scenario 140 covers duplicate SQL `PREPARE` names, proving duplicate rejection takes precedence over unsupported replacement payload validation and preserves the original prepared statement for later execution. Scenario 141 covers a quoted SQL prepared-statement name containing parentheses, proving the parser does not confuse identifier text for the parameter/type-list delimiter across `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE`. Scenario 142 covers a quoted SQL prepared-statement name containing ` AS `, proving the parser does not confuse identifier text for the `PREPARE ... AS ...` separator. Scenario 143 covers a quoted SQL prepared-statement name containing doubled-quote escapes, proving the decoded identifier remains stable across `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE`. Scenario 144 covers escaped SQL `EXECUTE` text arguments containing doubled apostrophes and commas, proving argument-list splitting and text literal decoding stay PostgreSQL-compatible for the supported prepared relational `SELECT` subset. Scenario 145 covers unsupported `NULL` SQL `EXECUTE` arguments, proving they return a SQL-prepared-specific unsupported-feature error, leave the prepared statement usable for later execution, and still recover after deallocation. Scenario 146 covers zero-parameter SQL prepared SELECTs, proving both `EXECUTE <name>` and `EXECUTE <name>()` execute the prepared statement before deallocation and later supported SELECT recovery. Scenario 147 covers too few and too many SQL `EXECUTE` arguments, proving the prepared statement remains usable for a correctly-shaped execution afterward. Scenario 148 covers parenthesized SQL `EXECUTE` int4/text literal arguments, proving matching and empty-result prepared executions plus deallocate, missing-prepared, and later SELECT recovery paths. Scenario 149 covers supported postfix-casted SQL `EXECUTE` int4/text literal arguments, including `pg_catalog`-qualified type names, while preserving empty-result, deallocate, missing-prepared, and later SELECT recovery paths. Scenario 150 covers semicolon-bearing quoted SQL prepared-statement names across `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE`. Scenario 151 covers SQL comments around supported `PREPARE` type lists, `EXECUTE` arguments, and `DEALLOCATE PREPARE` targets while preserving quoted identifier/comment-marker text and later supported SELECT recovery. Scenario 152 covers SQL comments containing delimiter-looking `AS` text before the real `PREPARE ... AS ...` separator, proving the supported prepared relational SELECT still prepares, executes, deallocates, and recovers. Scenario 153 covers newlines/tabs after SQL prepared-statement keywords, proving `PREPARE`, `EXECUTE`, `DEALLOCATE`, and `DEALLOCATE PREPARE` accept PostgreSQL whitespace while staying inside the supported prepared relational SELECT subset. Scenario 154 covers leading SQL comments before `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE`, proving comment-prefixed prepared-statement traffic still prepares, executes, deallocates, and recovers. Scenario 155 covers semicolons inside line and nested block comments around SQL prepared-statement traffic, proving simple-query framing preserves the intended `PREPARE` / `EXECUTE` / `DEALLOCATE PREPARE` boundaries and recovers for later supported SELECT traffic. Scenario 156 covers signed `int4` values across simple relational SELECT, extended `\bind`, and SQL `EXECUTE`, proving negative and zero parameters stay truthful for the supported subset before deallocate and later SELECT recovery. Scenario 157 covers `INT4` / `pg_catalog.text` DDL aliases and plus-signed `int4` literals across simple SELECTs, extended `\bind`, and SQL prepared execution. Scenario 166 covers inferred SQL-prepared `LIMIT $n` parameters over the supported relational SELECT subset, including invalid limit input recovery, casted limit execution, deallocation, missing-prepared errors, and later supported SELECT recovery. Scenario 167 covers negative SQL-prepared `LIMIT` execution, proving it returns a stable PostgreSQL-compatible error while preserving zero-limit, later valid-limit, deallocation, and SELECT recovery paths. Scenario 171 covers `psql \gdesc` over `EXECUTE <sql-prepared>`, proving extended Describe reports catalog-backed SQL prepared result metadata without executing the statement, while later SQL prepared execution and SELECT recovery still work. Scenario 172 covers `psql \bind` over `EXECUTE <sql-prepared>($1, $2)`, proving extended Parse/Describe infer parameter types from the SQL prepared statement, invalid bound `int4` input recovers without discarding the prepared statement, later bound execution succeeds, and SELECT recovery still works after deallocation. Scenario 173 covers the same SQL-prepared extended execution path through `\gset` and `\gx`, proving bound SQL-prepared rows can feed psql client variables and expanded-output rendering before deallocation and later supported SELECT recovery. Scenario 174 covers SQL-prepared extended `\crosstabview`, proving bound SQL-prepared rows can feed psql crosstab rendering before deallocation and later supported SELECT recovery. Scenario 177 covers `psql \bind` over reordered `EXECUTE <sql-prepared>($2, $1)`, proving outer placeholder type inference follows the SQL-prepared argument mapping and preserves invalid-`int4`, later valid execution, deallocate, and SELECT recovery behavior. Scenario 178 covers casted and parenthesized reordered outer placeholders in `EXECUTE <sql-prepared>(($2)::pg_catalog.int4, ($1)::text)`, proving SQL `EXECUTE` argument normalization preserves the remapped outer parameter inference and recovery behavior through real extended `psql \bind`. Scenario 179 covers mixed literal/placeholder SQL `EXECUTE` arguments through real extended `psql \bind`, proving Parse/Describe infer only the remaining outer parameter OID for `EXECUTE <prepared>($1, 'literal')` and `EXECUTE <prepared>(literal, $1)` while preserving invalid bind recovery, deallocate, and later SELECT recovery. Scenario 180 covers conflicting reused outer placeholders in SQL-prepared extended binds, proving `EXECUTE <prepared>($1, $1)` is rejected when the reused placeholder would need incompatible SQL-prepared `int4` and `text` argument types, and that later supported SQL-prepared extended bind plus SELECT traffic recovers.
- Raw explicit portal `Describe` ownership of `RowDescription`, raw `Describe Portal` after bounded `Execute.max_rows` suspension preserving resume position, raw Parse unsupported-parameter, oversized-OID, and unsupported-statement errors with skip-until-`Sync` recovery, named prepared-statement and named-portal duplicate handling, duplicate portal precedence over malformed payloads, missing raw `Bind` target statements with otherwise unsupported binary formats, raw binary `Bind` format rejection with skip-until-`Sync` recovery, missing raw `Describe Statement` / `Describe Portal`, `Close Statement` / `Close Portal`, and `Execute` portal targets with skip-until-`Sync` recovery, SQL `PREPARE` state isolation for extended `Close Statement`, successful `Close Portal` cleanup plus later missing-portal Execute recovery, and statement-close cascade invalidation of dependent portals before later Execute recovery are covered by helper-level protocol tests because PostgreSQL 16 `psql` does not emit stable named `Parse`/`Bind`/`Describe`/`Close`/missing-portal `Execute` metacommand traffic for the golden suite; unnamed replacement remains covered in the same lifecycle tests.
- Invalid raw UTF-8 bind payloads and mismatched raw parameter/result-format code counts, including skip-until-`Sync` recovery with skipped side effects and later supported SELECT traffic, are covered by helper-level protocol tests because PostgreSQL 16 `psql` does not emit those malformed wire shapes from stable golden-suite metacommands.
- CI now boots the repo-local compatibility endpoint with `cargo run -p gpu_db_protocol --bin gpu-db-server -- --listen 127.0.0.1:55432` before running the suite, so Q4 has a real service endpoint instead of a manual-only placeholder.
- If a wire-level extended-query flow is intentionally unsupported, encode that as an explicit expected failure in a dedicated scenario and set the expected `.rc` artifact.
