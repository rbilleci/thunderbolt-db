# psql golden compatibility suite

This suite exercises a real `psql`/libpq path and compares normalized output against golden artifacts.

Scenario 03 covers the supported relational create/insert/select path, including single-column filters, literal `BETWEEN` inclusive ranges, prefix-only text `LIKE 'prefix%'` predicates, narrow conjunctions, disjunctions, parenthesized nested `AND`/`OR` predicates, literal `IN (...)` membership over `int4`/`text` comparisons, `SELECT DISTINCT` duplicate elimination over selected columns, `COUNT(*)`, `SUM(int4_column)`, `AVG(int4_column)`, `MIN(column)`, `MAX(column)`, grouped `column, COUNT(*)`, grouped `column, SUM(int4_column)`, grouped `column, AVG(int4_column)`, grouped `column, MIN|MAX(column)` reporting, bounded `GROUP BY ... HAVING` filters over the grouped column plus `count`/`sum`/`avg`/`min`/`max` aggregate outputs, and ordered `LIMIT`/`OFFSET` pagination.

## Run

```bash
export PGHOST=127.0.0.1
export PGPORT=5432
export PGDATABASE=postgres
export PGUSER=postgres
export PGPASSWORD=postgres

scripts/run_psql_golden.sh
```

Optional:
- `PSQL_BIN=/path/to/psql`
- `PSQL_GOLDEN_OUT_DIR=/tmp/psql-golden`
- `PSQL_GOLDEN_BOOT_CMD='cargo run -p gpu_db_protocol --bin gpu-db-server -- --listen 127.0.0.1:5432'`
- `PSQL_GOLDEN_BOOT_CWD=/path/to/repo-or-service`
- `PSQL_GOLDEN_STOP_CMD='pkill -f gpu-db-server'`
- `PSQL_GOLDEN_WAIT_HOST=127.0.0.1`
- `PSQL_GOLDEN_WAIT_PORT=5432`
- `PSQL_GOLDEN_WAIT_TIMEOUT_SEC=30`
- `PSQL_GOLDEN_REPORT=target/compat/psql-golden-report.json`

When `PSQL_GOLDEN_BOOT_CMD` is set, the harness starts the target service itself, waits for the configured TCP endpoint to accept connections, captures boot logs in `target/psql-golden/boot.log`, and writes a machine-readable scenario report to `PSQL_GOLDEN_REPORT` for CI scorecards.

Scenario 02 covers PostgreSQL-style session reset/setup probes, including the asyncpg default reset cleanup sequence `SELECT pg_advisory_unlock_all(); CLOSE ALL; UNLISTEN *; RESET ALL;`, as bounded no-op compatibility that leaves later SQL prepared traffic usable.

Scenario 318 covers bounded `ALTER TABLE ... DROP CONSTRAINT` over supported primary-key and unique constraints, including `IF EXISTS`, metadata/comment cleanup in `psql \d` / `\dd`, duplicate inserts after removal, missing-target recovery, and unsupported cascade rejection.

## Update expected artifacts

1. Run the suite against the target endpoint.
2. Inspect `target/psql-golden/*.txt`.
3. If the new output is correct and deterministic, copy it into `tests/compat/psql-golden/expected/`.
4. Optionally add `tests/compat/psql-golden/expected/<scenario>.rc` when a scenario expects non-zero `psql` exit status (defaults to `0` when omitted).
5. Optionally add `tests/compat/psql-golden/scenarios/<scenario>.psqlargs` with one extra `psql` CLI argument per line when a scenario needs custom flags.

## Notes

- The harness intentionally strips volatile lines (timing/version/SSL banner noise).
- Keep scenario assertions stable and semantic, avoid transient text where possible.
- Current scenario coverage includes connect/simple-query, session reset probes, SQL prepare/execute/deallocate flow, transaction begin/commit/rollback flow, relational create/insert/select flows, bounded `DROP TABLE [IF EXISTS] public.<table>` and `TRUNCATE TABLE [ONLY] public.<table>` plus bootstrap-public schema clean-restore DDL recovery, metadata-backed catalog/type introspection, `pg_catalog.pg_tables` discovery, direct `pg_catalog.pg_namespace` lookup for the supported `public` namespace, `pg_catalog.pg_indexes` discovery for bounded metadata-only SQL indexes plus bounded `pg_catalog.pg_views` discovery for stored public views, `pg_catalog.pg_constraint` discovery for bounded primary-key and unique-constraint rows, metadata-backed `pg_catalog.pg_attrdef` discovery for supported literal column defaults, metadata-backed `pg_catalog.pg_description` discovery for supported table/column/index comments, joined `pg_catalog.pg_class` / `pg_catalog.pg_namespace` relation metadata and table-name `IN (...)` plus namespace-only `public` relation subsets for supported `public` tables, joined `pg_catalog.pg_attribute` / `pg_catalog.pg_class` / `pg_catalog.pg_namespace` column metadata with formatted supported type names, real `psql \dt`, `\dt+`, `\dt+ <table>`, `\dt <prefix>*`, `\dt+ <prefix>*`, `\dt public.*`, `\dt+ public.*`, `\dt public.<prefix>*`, and `\dt+ public.<prefix>*` table listing, real `psql \d <table>` / `\d+ <table>` / `\d <prefix>*` / `\d+ <prefix>*` / `\d public.*` / `\d public.<prefix>*` / `\d+ public.<prefix>*` / `\d *.*` / `\d+ *.*` column display, real `psql \di` index listing, `\di+` index descriptions, plus drop-to-empty behavior for bounded metadata-only SQL indexes, `\dv` / `\dv+` bounded stored-view listing, `\dm` / `\dm+` plus `\ds` / `\ds+` empty materialized-view/sequence listing, `\df` empty function listing, `\da` empty aggregate listing, `\dc` empty conversion listing, `\do` empty operator listing, `\dO` empty collation listing, `\dC` empty cast listing, `\dRp` empty publication listing, `\dRs` empty subscription listing, `\ddp` empty default-access-privilege listing, `\dd` table object-description listing for supported table comments, `\du` bootstrap role listing, `\l` bootstrap database listing, `\db` bootstrap tablespace listing, `\dx` empty extension listing, `\dL` empty procedural-language listing, `\dD` / `\dD+` empty domain listing, and `\dA` bootstrap access-method listing for the current subset, real `psql \dn` and `\dn+ public` schema listing, real `psql \dT pg_catalog.int4` / `\dT pg_catalog.text` type display, `\dT pg_catalog.*` and `\dT+ pg_catalog.*` supported built-in type listing, first-slice `information_schema` table/column/schema introspection including richer supported table metadata, table-name `IN (...)` and exact table-name table subsets, base-table discovery with system-schema exclusion, all supported `public` table columns, column discovery with system-schema exclusion, table-name `IN (...)` column subsets, per-table column detail projection, rich column metadata, extended column numeric precision/radix/scale metadata for the supported subset, including exact table-filtered and table-name `IN (...)` extended column metadata, and metadata-backed `information_schema.table_constraints` / `information_schema.key_column_usage` discovery for bounded primary-key and unique-constraint rows, extended-query bind execution including multirow parameterized SELECT, inferred `int4` and `text` bind parameter typing, inferred mixed `int4`/`text` conjunction parameters, inferred predicate-plus-`LIMIT` parameters, negative `LIMIT` bind rejection with zero-limit and later valid-limit recovery, invalid bind execution followed by later bound-portal `\gdesc` recovery, quoted-placeholder literal preservation, exact multi-digit placeholder-token substitution, zero-padded nonzero placeholder binding, apostrophe-escaped text bind literals, supported casted bind placeholders including casted `LIMIT` placeholders, zero-placeholder rejection/recovery, bound-portal and unbound parameterized-statement `\gdesc` description, bound-result `\gset`, expanded-output `\gx`, and crosstab rendering via `\crosstabview`, repeated unnamed rebind execution, parameter-count mismatch recovery, and invalid `int4` bind-value recovery, real `psql` `FETCH_COUNT` cursor-style SELECT retrieval, explicit `FETCH NEXT`, omitted-count `FETCH FORWARD`, `FETCH ... IN`, separator-less forward `FETCH`, `FETCH FORWARD ALL` / `FETCH ALL` cursor exhaustion, zero-count forward cursor movement, transaction-end cleanup for session-local cursors, unquoted cursor identifier case-folding, quoted cursor identifier preservation including semicolons and doubled-quote escapes across `DECLARE`/`FETCH`/`MOVE`/`CLOSE`, forward-only cursor `MOVE` including separator-less forms, duplicate cursor declaration rejection that preserves the existing cursor, `_psql_cursor` name-collision cleanup and recovery for psql `FETCH_COUNT`, non-forward cursor-direction rejection, explicit unsupported `FETCH_COUNT` plus unresolved `\bind` placeholder cursor traffic, `CLOSE ALL` cursor cleanup, real `psql \gdesc` result-description display over supported relational `SELECT` statements, extended parameterized INSERT/UPDATE/DELETE DML, explicit unsupported parameterized JOIN SELECT, `COPY ... TO STDOUT` recovery including leading-comment copy-out, extended `\gdesc` copy-out, and client-side `\copy ... TO PROGRAM`, `COPY ... FROM STDIN` import including client-side `\copy ... FROM PROGRAM`, the `pg_dump --inserts` boundary for `SELECT ... FROM ONLY public.<table>` cursors plus columnless table-order INSERT restore statements, and deterministic error/recovery paths.
- Scenario 44 covers catalog-qualified `information_schema.columns` extended metadata filters for `table_catalog = current_database()` backed by supported `public` session catalog metadata.
- Scenario 45 covers catalog-qualified `information_schema.tables` richer table metadata filters for `table_catalog = current_database()` backed by supported `public` session catalog metadata.
- Scenario 46 covers direct `pg_catalog.pg_namespace` lookup for the supported `public` namespace OID/name metadata.
- Scenario 47 covers real `psql \dn+ public` verbose schema introspection for the supported `public` namespace, returning owner metadata plus empty ACL/description fields before schema comments are set. Scenario 312 proves bounded `COMMENT ON SCHEMA public` appears in `\dn+ public`, `pg_catalog.obj_description(..., 'pg_namespace')`, and `\dd public` before `IS NULL` cleanup and unsupported-schema recovery.
- Scenario 48 covers real plain `psql \d` relation listing for supported `public` session tables, backed by the session catalog relation metadata.
- Scenario 49 covers real `psql \dv` / `\dv+` view listing traffic for the empty baseline, scenario 309 proves bounded stored views appear there once created, scenario 310 proves bounded `DROP VIEW [IF EXISTS] [public.]view` removes a stored view from those listings while preserving other views, scenario 311 proves `COMMENT ON VIEW` appears in `\dv+`, `pg_catalog.pg_description`, and `\dd` before `IS NULL` cleanup and dropped-view cleanup, and scenario 314 proves bounded `CREATE OR REPLACE VIEW` refreshes the stored definition while preserving the supported view comment.
- Scenario 50 covers real `psql \dm` / `\dm+` materialized-view and `\ds` / `\ds+` sequence listing traffic, returning no rows for the current no-materialized-view/no-sequence subset while preserving supported table metadata.
- Scenario 51 covers real `psql \df` function listing traffic, returning no rows for the current no-user-defined-function subset.
- Scenario 52 covers real `psql \du` role listing traffic, returning the bootstrap `postgres` role from a narrow `pg_catalog.pg_roles` compatibility slice while keeping role mutation out of scope. Scenario 315 proves bounded `COMMENT ON ROLE postgres` appears in `\du+` and `pg_catalog.shobj_description(..., 'pg_authid')` before `IS NULL` cleanup and unsupported-role recovery.
- Scenario 53 covers real `psql \l` database listing traffic, returning the supported bootstrap `postgres` database from a narrow `pg_catalog.pg_database` compatibility slice while keeping database creation, templates, ACL mutation, and broader database catalog behavior out of scope. Scenario 313 proves bounded `COMMENT ON DATABASE postgres` appears in `\l+` and `pg_catalog.shobj_description(..., 'pg_database')` before `IS NULL` cleanup and unsupported-database recovery.
- Scenario 54 covers real `psql \dx` extension listing traffic, returning no rows for the current no-extension subset while keeping extension install and extension catalog state out of scope.
- Scenario 55 covers real `psql \db` tablespace listing traffic, returning the bootstrap `pg_default` and `pg_global` metadata through a narrow `pg_catalog.pg_tablespace` compatibility slice while keeping tablespace creation, location management, options, and broader tablespace catalog behavior out of scope. Scenario 315 proves bounded `COMMENT ON TABLESPACE {pg_default|pg_global}` metadata appears in `\db+` and `pg_catalog.shobj_description(..., 'pg_tablespace')` before `IS NULL` cleanup and unsupported-tablespace recovery.
- Scenario 56 covers real `psql \dL` procedural-language listing traffic, returning no rows for the current no-procedural-language subset while keeping language creation and broader `pg_catalog.pg_language` behavior out of scope.
- Scenario 57 covers real `psql \dA` access-method listing traffic, returning the supported bootstrap `heap` table access method through a narrow `pg_catalog.pg_am` compatibility slice while keeping access-method creation, extension, and broader access-method catalog behavior out of scope.
- Scenario 58 covers real `psql \dT pg_catalog.*` and `\dT+ pg_catalog.*` type-listing traffic, returning the supported `int4`/`text` registry from a narrow `pg_catalog.pg_type` compatibility slice while keeping user-defined types and broader type catalog behavior out of scope.
- Scenario 59 covers real `psql \dD` and `\dD+` domain-listing traffic, returning no rows for the current no-domain subset while keeping domain creation, domain constraints, and broader domain catalog behavior out of scope.
- Scenario 60 covers real `psql \da` aggregate-listing traffic, returning no rows for the current no-user-defined-aggregate subset while keeping aggregate creation and broader function catalog behavior out of scope.
- Scenario 61 covers real `psql \dc` conversion-listing traffic, returning no rows for the current no-conversion subset while keeping encoding conversion creation and broader conversion catalog behavior out of scope.
- Scenario 62 covers real `psql \do` operator-listing traffic, returning no rows for the current no-user-defined-operator subset while keeping operator creation and broader operator catalog behavior out of scope.
- Scenario 63 covers real `psql \dO` collation-listing traffic, returning no rows for the current no-user-defined-collation subset while keeping collation creation and broader collation catalog behavior out of scope.
- Scenario 64 covers real `psql \dC` cast-listing traffic, returning no rows for the current no-user-defined-cast subset while keeping cast creation and broader cast catalog behavior out of scope.
- Scenario 65 covers real `psql \dRp` publication-listing traffic, returning no rows for the current no-publication subset while keeping publication creation and broader publication catalog behavior out of scope.
- Scenario 66 covers real `psql \dRs` subscription-listing traffic, returning no rows for the current no-subscription subset while keeping subscription creation and broader subscription catalog behavior out of scope.
- Scenario 67 covers real PostgreSQL 16 `psql` `FETCH_COUNT` cursor flow over a supported relational `SELECT`, proving session-local `DECLARE ... CURSOR FOR SELECT`, repeated `FETCH FORWARD n`, and cursor `CLOSE` handling return the same ordered rows as the normal SELECT path. Scenario 222 extends the direct relational SELECT cleanup boundary to non-empty batched results, scenario 223 pins the describe-only `\gdesc` path, scenario 224 pins the single-row `\gset` result-consumer path, scenario 225 pins the expanded-output `\gx` result-consumer path, scenario 226 pins the `\crosstabview` result-consumer path, scenario 231 pins generated-SQL `\gexec` consumption, scenarios 227-230 pin SQL-prepared `EXECUTE <name>(...) \gset` / `\gx` / `\crosstabview` / `\gdesc` cleanup, scenarios 233-237 pin zero-parameter SQL-prepared `EXECUTE <name>` cleanup through `\gexec`, `\gset`, `\gx`, `\crosstabview`, and `\gdesc`, scenario 238 pins extended `SELECT ... \bind ... \gdesc` metadata-only cleanup under `FETCH_COUNT`, scenario 264 pins the same metadata-only path when the pending bind value would be invalid for execution, scenarios 239-240 pin extended `SELECT ... \bind ... \gset` / `\gx` as explicitly unsupported parameterized cursor-declaration traffic, scenarios 241 and 256 pin `SELECT ... \bind ... \crosstabview` as supported cleanup traffic for non-empty and empty result shapes, scenario 258 pins invalid extended-bound `int4` input before crosstab rendering as a no-transient-cursor recovery path, scenario 259 pins invalid extended-bound `int4` input before expanded-output `\gx` rendering with later `\gx` recovery, scenario 260 pins invalid extended-bound `int4` input before `\crosstabview` rendering with later crosstab recovery, scenarios 242-243 pin extended-bound generated-SQL `\gexec` cleanup for non-empty and empty source rows, scenario 263 pins invalid extended-bound `int4` input before `FETCH_COUNT` generated-SQL `\gexec` as no-transient-cursor cleanup, scenario 266 pins invalid-looking extended-bound `int4` input before `FETCH_COUNT` `\gset` as an unsupported-cursor cleanup path, scenario 267 pins the matching plain `\g` invalid-looking-bind cleanup path before bind validation, scenarios 268-272 pin SQL-prepared `EXECUTE <name>($1) \bind ... \g` / `\gset` / `\gx` / `\crosstabview` / `\gexec` invalid-bind cleanup with prepared-statement recovery, scenarios 273-277 pin argument-bearing SQL-prepared `FETCH_COUNT` `\gset` / `\gx` / `\crosstabview` / `\gexec` / `\gdesc` argument-count cleanup, scenarios 289-293 pin argument-bearing negative-`LIMIT` `\gexec`, `\gset`, `\crosstabview`, `\gx`, and metadata-only `\gdesc` cleanup, scenarios 278-282 pin zero-parameter `\gdesc` / `\gset` / `\gx` / `\crosstabview` / `\gexec` too-many-argument cleanup variants, scenario 283 pins zero-parameter SQL-prepared literal negative-`LIMIT` cleanup, and scenarios 285-288 pin zero-parameter SQL-prepared literal negative-`LIMIT` `\gset` / `\gx` / `\crosstabview` / `\gexec` cleanup, scenarios 244-245 pin SQL-prepared generated-SQL `\gexec` cleanup for empty source rows including zero-parameter `EXECUTE <name>` traffic, scenarios 246-247 pin SQL-prepared generated-SQL unsupported COPY cleanup for zero-parameter and argument-bearing traffic, scenario 248 pins the matching extended-bound generated-COPY cleanup path, and scenario 249 pins direct generated-COPY cleanup; the cleanup paths leave no transient cursor and recover for later prepared, extended, or plain SELECT traffic.
- Scenarios 254-255 extend the unsupported extended `FETCH_COUNT` `\bind ... \gset` cleanup boundary to multirow and empty-result source shapes, proving the stable unsupported parameterized-cursor error leaves no transient `_psql_cursor` and later extended bound `\gdesc` plus execution recover. Scenario 266 extends the same `\gset` boundary to invalid-looking `int4` input, proving unsupported cursor declaration wins before bind validation and later client-variable capture recovers. Scenario 257 pins the matching empty-result `\bind ... \gx` cleanup boundary and later extended expanded-output recovery.
- Scenario 220 covers real PostgreSQL 16 `psql` `FETCH_COUNT` duplicate `_psql_cursor` cleanup, proving the implicit cursor declaration returns the duplicate-cursor error when the transient name is already occupied, psql cleanup leaves `_psql_cursor` absent, and later supported SELECT traffic recovers.
- Scenario 68 covers real PostgreSQL 16 `psql \ddp` default-access-privilege listing traffic, returning no rows for the current no-default-ACL subset while keeping default privilege mutation and broader ACL catalog behavior out of scope.
- Scenario 69 covers real PostgreSQL 16 `psql \gdesc` result-description traffic over a supported relational `SELECT`, including the follow-up `pg_catalog.format_type` formatter query over described `int4`/`text` result OIDs.
- Scenario 70 covers real PostgreSQL 16 `psql \dd` object-description listing traffic, returning no rows for the current no-broader-user-defined-object-description subset.
- Scenario 317 covers bounded table truncation, preserving table/index/constraint/comment metadata and unrelated objects while clearing visible rows, with missing/view/identity/cascade/multi-table boundaries. Scenario 305 covers supported `COMMENT ON TABLE`, `COMMENT ON COLUMN`, and `COMMENT ON INDEX` metadata for public tables and bounded metadata-only indexes, including `pg_catalog.pg_description`, `psql \d+` column descriptions, `psql \di+` index descriptions, `psql \dd` table descriptions, `IS NULL` cleanup, and missing target recovery. Scenario 311 covers the corresponding bounded `COMMENT ON VIEW` flow for stored public views, and scenario 312 covers the bounded `COMMENT ON SCHEMA public` flow for the supported namespace.
- Scenario 71 covers a common `information_schema.views` client probe for the supported `public` schema empty baseline, and scenario 309 proves bounded stored view definitions appear there once created.
- Scenario 72 covers a common `pg_catalog.pg_views` client probe for the supported `public` schema empty baseline, scenario 309 proves bounded stored view definitions appear there once created, scenario 310 proves bounded `DROP VIEW` removes the dropped view from `pg_catalog.pg_views`, and scenario 314 proves bounded `CREATE OR REPLACE VIEW` updates the exposed definition after replacement.
- Scenario 73 covers a common `information_schema.tables` base-table discovery probe with system-schema exclusion, returning supported `public` session tables from catalog metadata.
- Scenario 74 covers a common `information_schema.columns` column-discovery probe with system-schema exclusion, returning supported `public` session-table columns from catalog metadata.
- Scenario 75 covers real PostgreSQL 16 `psql \dt *.*` and `\dt+ *.*` all-schema table-listing traffic, returning supported `public` session tables from catalog metadata while keeping unsupported schemas out of scope.
- Scenario 76 covers real PostgreSQL 16 `psql \d *.*` all-schema relation-description traffic, returning supported `public` session-table relation rows and reusing metadata-backed per-table describe probes while keeping unsupported schemas out of scope.
- Scenario 77 covers real PostgreSQL 16 `psql \d+ *.*` verbose all-schema relation-description traffic, returning supported `public` session-table relation rows and reusing metadata-backed verbose per-table describe probes for column storage/access-method display while keeping unsupported schemas out of scope.
- Scenario 78 covers real PostgreSQL 16 `psql \d+ public.*` verbose schema-wildcard relation-description traffic, returning supported `public` session-table relation rows and reusing metadata-backed verbose per-table describe probes for column storage/access-method display.
- Scenario 79 covers real PostgreSQL 16 `psql \dt public.<table>` exact schema-qualified table-listing traffic, returning only the requested supported `public` session table from catalog metadata.
- Scenario 80 covers real PostgreSQL 16 `psql \dt+ public.*` verbose schema-wildcard table-listing traffic, returning supported `public` session tables with catalog-backed persistence/access-method metadata while reporting supported table comments while heap size remains blank until that metadata surface exists.
- Scenario 81 covers real PostgreSQL 16 `psql \bind` multirow relational SELECT execution with a text-format parameter over the supported `int4` / `text` subset.
- Scenario 82 covers real PostgreSQL 16 `psql \bind` with omitted Parse parameter OIDs inferred from a supported `int4` predicate, including one invalid literal error followed by Sync recovery and a successful bound SELECT.
- Scenario 250 covers real PostgreSQL 16 `psql \gdesc` over parameterized extended `INSERT`, filtered `UPDATE`, and filtered `DELETE`, proving DML descriptions return `NoData`, execute later through bound portals, and recover for SELECT traffic.
- Scenario 251 covers real PostgreSQL 16 `psql \bind ... \gset` over a supported extended SELECT that returns multiple rows, proving the client-side multirow `\gset` error still leaves the session usable for later supported extended-bound SELECT traffic. Scenario 252 pins the adjacent empty-result `\gset` client-error path with the same later extended-bound SELECT recovery. Scenario 253 proves a later supported `\bind ... \gdesc` metadata request and execution still work after the multirow `\gset` client error.
- Scenario 83 covers real PostgreSQL 16 `psql` `FETCH_COUNT` combined with `\bind`, explicitly rejecting the unresolved parameterized cursor declaration boundary while proving the session can still run a supported SELECT afterward. Scenario 216 extends that unsupported boundary to prove the failed psql cursor declaration installs no `_psql_cursor`, reports the expected missing-cursor error on a manual fetch probe, and then recovers for later supported cursor-style SELECT traffic in the same session. Scenario 198 covers `FETCH_COUNT` over supported SQL-prepared `EXECUTE <name>(...)` traffic, and scenario 199 extends that path to a quoted zero-parameter SQL prepared name, proving the endpoint installs a session-local cursor over the SQL-prepared relational SELECT result, fetches rows truthfully, deallocates cleanly, and recovers for later supported SELECT traffic. Scenario 217 extends the same real-client path to an empty SQL-prepared result, proving psql closes the transient `_psql_cursor`, later reports the expected missing-cursor error on a manual fetch probe, and then recovers for non-empty SQL-prepared execution, deallocation, and supported SELECT traffic. Scenarios 221 and 222 pin the matching direct relational SELECT cleanup paths, proving empty and non-empty `FETCH_COUNT` SELECTs close `_psql_cursor`, report the expected missing-cursor error on manual fetch probes, and recover for later cursor-style and plain SELECT traffic. Scenario 218 pins the negative-`LIMIT` variant, proving the failed SQL-prepared `FETCH_COUNT` execution installs no `_psql_cursor`, reports the expected missing-cursor error on a manual fetch probe, and preserves the prepared statement for later valid execution. Scenarios 289-292 extend that boundary to argument-bearing `\gexec`, `\gset`, `\crosstabview`, and `\gx`, proving generated SQL, client-variable capture, crosstab rendering, and expanded-output rendering are skipped after the negative-limit error and later valid `\gexec`, `\gset`, crosstab rendering, `\gx`, or direct execution recover. Scenario 283 pins the zero-parameter SQL-prepared literal negative-`LIMIT` variant, proving no transient cursor is installed, deallocation still succeeds, and later supported SELECT traffic recovers. Scenarios 285-288 pin the adjacent zero-parameter `\gset` / `\gx` / `\crosstabview` / `\gexec` failed-cursor variants with the same cleanup and recovery guarantee. Scenario 219 pins too-few and too-many SQL-prepared `FETCH_COUNT` argument mismatches, proving neither failure installs `_psql_cursor` and the prepared statement still executes/deallocates cleanly afterward. Scenarios 273-277 extend that cleanup boundary to `EXECUTE <name>(...) \gset`, `\gx`, `\crosstabview`, `\gexec`, and `\gdesc`, proving failed cursor declarations skip client-variable, expanded-output, crosstab, generated-SQL, and metadata consumption, leave no transient cursor, and preserve later correctly-shaped execution. Scenarios 278-282 pin zero-parameter `EXECUTE <name>(...) \gdesc` / `\gset` / `\gx` / `\crosstabview` / `\gexec` too-many-argument variants with the same no-transient-cursor and prepared-statement recovery guarantee. Scenario 201 covers invalid SQL-prepared `FETCH_COUNT` execution, and scenarios 268-272 cover the extended-bound `EXECUTE <name>($1) \bind ... \g` / `\gset` / `\gx` / `\crosstabview` / `\gexec` variants, proving failed cursor declarations do not install a cursor or discard the prepared statement before later valid execution. Scenario 202 covers SQL-prepared extended `\gdesc` literal argument validation, proving invalid literal input returns the parameter error instead of a generic unsupported-SELECT error and later describe/execute/deallocate/SELECT traffic recovers. Scenario 203 covers real `psql \gdesc` over a supported `DECLARE ... CURSOR FOR SELECT`, proving the extended Describe path returns `NoData` without installing the cursor until a later declaration executes and the same session can fetch, close, and run a supported SELECT. Scenario 204 covers `DECLARE ... CURSOR FOR EXECUTE <sql-prepared>(literal) \gdesc`, proving invalid inner SQL-prepared literals return the same precise parameter error, install no cursor, and recover for later valid declaration/fetch/close plus SELECT traffic. Scenario 205 covers `DECLARE ... CURSOR FOR EXECUTE <sql-prepared>($1)` through real `psql \bind`, proving extended Describe returns `NoData`, later Execute installs the cursor, and fetch/close/deallocate plus supported SELECT recovery stay truthful. Scenario 208 extends that cursor-declaration path to SQL-prepared negative `LIMIT` binds, proving `\gdesc` returns `NoData` without installing a cursor, the later Execute reports the stable negative-limit error, and a valid cursor declaration/fetch/close still recovers. Scenario 210 covers direct `DECLARE ... CURSOR FOR SELECT ... LIMIT -1`, proving negative-limit error specificity, no bad cursor installation, and later cursor/SELECT recovery. Scenario 211 covers negative cursor counts (`FETCH -1` / `MOVE -1`) as explicit unsupported non-forward cursor traffic while preserving forward cursor position, close behavior, and later SELECT recovery. Scenario 212 covers `\gdesc` over unsupported `BINARY`/`SCROLL` cursor declarations, proving extended Parse returns the same explicit cursor-option unsupported error as simple query traffic without installing bad cursor state.
- Raw extended `Parse`/`Bind`/`Execute` for supported parameterized `DECLARE ... CURSOR FOR SELECT` and `DECLARE ... CURSOR FOR EXECUTE <sql-prepared>($n)` is covered by helper-level protocol tests plus real-client scenario 205 for the SQL-prepared outer-bind path, including bound cursor-declaration portal `Describe` returning `NoData`, SQL-prepared cursor declaration OID inference from SQL prepared state, explicit conflicting Parse OID rejection before statement installation, and extended portal/statement cleanup leaving the installed session-local cursor fetchable. The PostgreSQL 16 `psql` `FETCH_COUNT` plus `\bind` edge still exposes unresolved-placeholder cursor declaration traffic and remains explicitly unsupported for some consumers; scenarios 216, 239-240, and 254-255 plus 257 pin cleanup/recovery for unsupported `\gset` and `\gx` result-consumer traffic, while scenarios 241, 256, and 242 prove `\crosstabview` and generated-SQL `\gexec` consume supported rows, including an empty crosstab result shape, and still clean up the transient cursor.
- Raw extended `Bind` result-format-code arity validation for SQL-prepared `EXECUTE <prepared>($n)` portals is covered by helper-level protocol tests because PostgreSQL 16 `psql` does not expose malformed result-format vectors from stable golden-suite flows; malformed SQL-prepared result format vectors now fail before portal installation while valid per-column text vectors remain accepted.
- Scenario 84 covers real PostgreSQL 16 `CLOSE ALL` cursor cleanup by proving a later fetch from the closed session-local cursor fails while later supported SELECT traffic still succeeds.
- Scenario 85 covers real PostgreSQL 16 `COPY ... TO STDOUT` traffic as a supported simple-query copy-out export and proves later supported SELECT traffic still succeeds in the same session. Scenario 200 extends copy-out export to SQL statements with leading line/block comments. Scenario 215 covers `psql \copy ... TO PROGRAM` client-side copy-out traffic, proving the generated `COPY ... TO STDOUT` streams supported rows and reports `COPY n`. Scenarios 246-247 cover SQL-prepared `FETCH_COUNT` `\gexec` generated COPY export, and scenario 249 covers direct `FETCH_COUNT` generated COPY export while preserving transient cursor cleanup and later supported traffic. Scenario 213 covers real `psql \gdesc` over comment-prefixed `COPY ... TO STDOUT`, proving extended Parse still returns the stable unsupported COPY error instead of installing bad extended state. Scenario 127 covers real PostgreSQL 16 `COPY ... FROM STDIN` import traffic, scenario 170 covers real PostgreSQL 16 `psql \copy ... FROM PROGRAM` client-side copy-in import, scenario 296 covers direct `COPY ... FROM STDIN WITH CSV` plus `COPY ... TO STDOUT WITH CSV` round-trip traffic, scenario 297 covers matching `psql \copy ... WITH CSV` client-side program flows, scenarios 298-299 cover direct and `psql \copy` CSV `HEADER` import/export flows, scenarios 300-301 cover direct and `psql \copy` parenthesized CSV options with custom `DELIMITER`, scenarios 302-303 cover direct and `psql \copy` parenthesized CSV options with custom `QUOTE`/`ESCAPE`, scenario 304 covers supported literal column defaults through insert, `ALTER COLUMN SET/DROP DEFAULT`, `information_schema.columns`, `pg_catalog.pg_attrdef`, and real `psql \d`, scenario 319 covers bounded default-backed `ALTER TABLE ... ADD COLUMN` row rewrite, catalog visibility, unsupported no-default/duplicate/view boundaries, and later insert defaults, scenario 321 covers bounded `ALTER TABLE ... RENAME COLUMN` catalog/index metadata retargeting, preserved defaults/comments, missing/duplicate/view rejection, and later reads/inserts through renamed columns, scenario 320 covers bounded non-dependent `ALTER TABLE ... DROP COLUMN` row rewrite, column/default/comment catalog cleanup, dependency/view rejection, and later default-backed inserts, scenario 307 covers bounded single-column primary-key constraints through `information_schema`, `pg_catalog.pg_constraint`, real `psql \d`, and duplicate rejection, scenario 308 covers bounded single-column unique constraints through column, table, and pg_dump-style `ALTER TABLE ... ADD CONSTRAINT ... UNIQUE` forms plus `information_schema`, `pg_catalog.pg_constraint`, `COMMENT ON CONSTRAINT`, real `psql \d`/`\dd`, and duplicate rejection, scenario 214 covers real `psql \gdesc` over `COPY ... FROM STDIN`, and scenario 248 covers extended-bound generated COPY, preserving explicit unsupported COPY boundaries for extended/metadata shapes.
- Scenario 309 covers bounded stored SQL views: `CREATE VIEW public.<name> AS <supported SELECT>` stores the definition, `SELECT * FROM view` expands it read-only, `pg_catalog.pg_views`, `information_schema.views`, and real `psql \dv` / `\dv+` expose the view, and duplicate/missing-base/view-on-view/filtered-view-select boundaries recover for later supported table traffic. The `pg-dump` compatibility smoke now covers the matching real-tooling path: pg-dump catalog metadata exposes bounded view relation/attribute/dependency rows plus `pg_get_viewdef`, and plain/custom/directory/tar restore modes preserve the supported view definition and `SELECT * FROM view`. Scenario 310 covers bounded stored-view deletion: `DROP VIEW [IF EXISTS] [public.]view` removes the view from `pg_catalog.pg_views`, `information_schema.views`, and `psql \dv` / `\dv+`, preserves the base table and other views, rejects table targets and missing views truthfully, and leaves cascade/multi-view/materialized-view semantics outside the supported slice. Scenario 314 covers bounded stored-view replacement: `CREATE OR REPLACE VIEW [public.]view AS <supported SELECT>` refreshes one supported base-table view definition, preserves existing view comments in `\dv+`, rejects unsupported replacement sources without corrupting the prior definition, and recovers for later supported table traffic.
- Raw frontend `CopyData`, `CopyDone`, `CopyFail`, and `FunctionCall` frames are covered by helper-level protocol tests because PostgreSQL 16 `psql` does not emit them from stable golden-suite flows; that coverage pins the unsupported SQLSTATE, skip-until-`Sync`, skipped side-effect, and later recovery behavior.
- P3 current-scope closeout treats frontend FunctionCall, broader binary formats/types beyond the selected-driver `int4`/`text` subset, and advanced cursor/portal behavior as post-P3 backlog unless a named target client/workload emits or requires that exact surface.
- Scenario 86 covers real PostgreSQL 16 parameterized extended `INSERT`, filtered `UPDATE`, and filtered `DELETE` traffic as supported DML over public `int4`/`text` tables, including invalid-bind recovery and later SELECT traffic.
- Scenario 87 covers real PostgreSQL 16 parameterized extended JOIN SELECT traffic as an explicit unsupported broader-SELECT boundary and proves later supported SELECT traffic still succeeds in the same session.
- Scenario 88 covers real PostgreSQL 16 named cursor `CLOSE` against a missing cursor, returning a stable cursor-not-found error while proving an existing cursor and later supported SELECT traffic still work.
- Scenario 89 covers real PostgreSQL 16 `psql \bind` parameter-count mismatch handling for supported relational SELECT portals, returning a protocol violation before portal installation and proving a later correctly-bound SELECT still succeeds.
- Scenario 90 covers real PostgreSQL 16 `psql \bind` invalid `int4` text-input handling for supported relational SELECT portals, returning a stable invalid-text-representation error before portal installation and proving a later correctly-bound SELECT still succeeds.
- Scenario 91 covers real PostgreSQL 16 `psql \bind ... \gdesc` over a supported parameterized relational SELECT, proving a bound portal can be described with catalog-backed result metadata and a later correctly-bound execution still returns rows.
- Scenario 92 covers real PostgreSQL 16 `psql \bind` text-parameter execution over the supported relational SELECT subset and pins the empty-result portal path without fabricating rows.
- Scenario 93 covers real PostgreSQL 16 `psql \bind` text values that look like placeholders, proving `Ada$1` and `Missing$1` are treated as literal text data over the supported relational SELECT subset, including the empty-result portal path for the missing value.
- Scenario 94 covers repeated real PostgreSQL 16 `psql \bind` execution on unnamed extended-query state, proving exhausted unnamed portals can be replaced for later parameter values and that a bound-portal `\gdesc` does not prevent a later execution from returning the requested row.
- Scenario 95 covers real PostgreSQL 16 `psql \bind` execution with inferred predicate and `LIMIT` placeholders over an ordered supported relational `SELECT`. Scenario 168 covers negative extended `LIMIT` bind execution, proving it returns the stable PostgreSQL-compatible error while preserving zero-limit, later valid-limit, and SELECT recovery paths. Scenario 206 covers the related bound-portal `\gdesc` path, proving a supported relational SELECT with a negative bound `LIMIT` still returns result metadata before the later execution error and recovery. Scenario 194 covers SQL prepared `LIMIT` parameters through extended `EXECUTE <prepared>($1, $2)` binds, proving mixed `text`/`int4` inference, invalid limit text recovery, negative-limit recovery, zero-limit execution, later valid execution, deallocation, and SELECT recovery. Scenario 207 pins the same metadata-before-execution-error boundary for SQL-prepared `EXECUTE <prepared>($1, $2) \bind ... -1 \gdesc`, proving Describe does not execute the negative `LIMIT` portal while later Execute still emits the stable error and recovers. Scenario 209 pins SQL-prepared `\gdesc` as metadata-only for outer-bound values while preserving later bound execution and SELECT recovery. Scenario 169 covers invalid bind execution before a later bound-portal `\gdesc`, proving error recovery preserves the prepared statement and later portal description/execution path.
- Scenario 96 covers real PostgreSQL 16 parameterized `psql \gdesc` without a preceding `\bind`, proving `Describe Statement` advertises catalog-backed result metadata for the supported relational `SELECT` subset without executing the statement.
- Scenario 130 covers real PostgreSQL 16 `psql \bind ... \gset` over a supported parameterized relational SELECT, proving extended-query rows and metadata can be consumed into client variables and reused by later supported SQL in the same session. Scenarios 251-252 pin the adjacent multirow and empty-result `\gset` client-error paths and later extended-bound SELECT recovery. Scenario 261 pins invalid `int4` bind input before `\gset`, proving the bind-time error installs no bad portal and later correctly-bound `\gset` variable capture plus supported SELECT traffic recover on the same connection.
- Scenario 131 covers real PostgreSQL 16 `psql \bind ... \gx` over a supported parameterized relational SELECT, proving extended-query rows can be consumed by psql expanded-output rendering and later supported SQL still works in the same session.
- Scenario 132 covers real PostgreSQL 16 `psql \bind ... \crosstabview` over a supported parameterized relational SELECT, proving extended-query rows can be consumed by psql crosstab rendering and later supported SQL still works in the same session.
- Scenario 133 covers real PostgreSQL 16 `psql \gexec` over supported relational text results containing semicolon-terminated SQL, proving simple-query framing preserves semicolons inside SQL string literals and later supported SQL still works in the same session. Scenario 231 covers the same generated-SQL result-consumer path under direct `FETCH_COUNT`, proving batched source rows execute their generated supported SELECTs, psql closes `_psql_cursor`, a manual fetch probe reports the expected missing-cursor error, and later supported SQL recovers. Scenarios 232 and 233 extend that cleanup boundary to SQL-prepared `EXECUTE <prepared>(...) \gexec` plus zero-parameter `EXECUTE <prepared> \gexec`, scenarios 244-245 pin the matching empty-source SQL-prepared and zero-parameter SQL-prepared no-op paths, scenarios 246-247 pin generated COPY TO STDOUT export for zero-parameter and argument-bearing SQL-prepared traffic, scenarios 242-243 extend supported and empty generated-SQL execution to extended-bound source rows under `FETCH_COUNT`, scenario 263 pins invalid extended-bound `int4` input before `FETCH_COUNT` generated-SQL `\gexec` as no-transient-cursor cleanup, scenario 248 pins extended-bound generated unsupported COPY cleanup while preserving later extended SELECT recovery, and scenario 249 pins the matching direct generated-COPY export path while preserving later plain SELECT recovery. Scenario 262 pins invalid extended-bound `int4` input before generated-SQL `\gexec`, proving no bad portal is installed; the same scenario also preserves the explicit unsupported non-`FETCH_COUNT` parameterized `\gexec` boundary and later supported SELECT recovery. Scenario 264 pins `\gdesc` as a metadata-only extended path under `FETCH_COUNT`, even when a pending bind value would fail at execution time. Scenario 150 covers a semicolon-bearing quoted SQL prepared-statement name, proving simple-query framing also preserves semicolons inside quoted identifiers across `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE`. Scenario 175 covers non-`FETCH_COUNT` parameterized `psql \bind ... \gexec` as an explicit unsupported extended meta-command boundary, proving it returns a stable bind-arity error and recovers for later supported SELECT traffic. Scenario 176 covers reused SQL-prepared placeholders through extended `EXECUTE <prepared>($1)` binds, proving `\gdesc`, invalid `int4` recovery, later execution, deallocate, and SELECT recovery stay truthful. Scenario 177 covers reordered outer placeholders through extended `EXECUTE <prepared>($2, $1)` binds, proving Parse/Describe infer the remapped outer `text`/`int4` OIDs and invalid reordered `int4` input preserves prepared-state recovery. Scenario 178 covers casted and parenthesized reordered outer placeholders through extended `EXECUTE <prepared>(($2)::pg_catalog.int4, ($1)::text)` binds, proving SQL `EXECUTE` argument normalization preserves the same remapped OID inference and recovery behavior. Scenario 179 covers mixed literal/placeholder SQL `EXECUTE` arguments through extended binds, proving only the remaining outer parameter is described/bound while invalid `int4`, later text-bound execution, deallocate, and SELECT recovery stay truthful. Scenario 180 covers conflicting reused outer placeholders in SQL-prepared extended binds, proving `EXECUTE <prepared>($1, $1)` is rejected when the reused placeholder would need incompatible SQL-prepared `int4` and `text` argument types, and that later supported SQL-prepared extended bind plus SELECT traffic recovers. Scenario 181 covers sparse outer placeholders through extended SQL-prepared binds, proving an unused lower-numbered bind slot is accepted without type coercion while the used `int4` placeholder still validates, executes, deallocates, and recovers.
- Scenario 182 covers compatible reused outer placeholders in SQL-prepared extended binds, proving `EXECUTE <prepared>($1, $1)` accepts a single shared outer bind when both SQL-prepared arguments are `int4`, while invalid shared input, matching rows, empty results, deallocation, and later SELECT recovery stay truthful.
- Scenario 183 covers PostgreSQL escape string text literals in SQL-prepared extended binds, proving `EXECUTE <prepared>(E'...', $1)` decodes the supported `text` argument as data while Parse/Describe infer only the remaining outer `int4` placeholder and invalid-input recovery preserves the prepared statement. Scenario 190 extends that path to PostgreSQL hex/octal escape string text literals plus malformed-escape recovery. Scenario 191 covers newline-separated adjacent standard string literals through the same SQL-prepared extended-bind boundary, and scenario 196 extends that boundary to adjacent string literals separated by newline-bearing SQL comments. Scenario 192 covers PostgreSQL `CAST(... AS ...)` SQL `EXECUTE` arguments for the supported `int4`/`text` subset. Scenario 194 covers the same SQL-prepared extended-bind path when one prepared argument drives a supported relational `LIMIT`.
- Scenario 184 covers PostgreSQL dollar-quoted text literals in SQL-prepared extended binds, proving `EXECUTE <prepared>($tag$...$tag$, $1)` and `EXECUTE <prepared>($$...$$, $1)` decode the supported `text` argument as data while Parse/Describe infer only the remaining outer `int4` placeholder and invalid-input recovery preserves the prepared statement. Scenario 193 extends that path to dollar-quoted text containing `::` markers plus an outer supported postfix cast, proving cast scanning keeps marker-looking text as data while unsupported outer cast targets remain rejected and recoverable. Scenario 197 extends the same boundary to parenthesized dollar-quoted literals whose bodies contain unmatched parentheses, proving argument normalization keeps those parentheses as text data.
- Scenario 185 covers comment-looking text inside those dollar-quoted SQL `EXECUTE` arguments, proving `--` and `/* ... */` markers inside `$tag$...$tag$` / `$$...$$` data do not get stripped before Parse/Describe inference or Execute recovery.
- Scenario 186 covers supported PostgreSQL typed string literals in SQL-prepared extended binds, proving `text '...'`, `pg_catalog.text $$...$$`, and `int4 '...'` SQL `EXECUTE` arguments decode as data while Parse/Describe infer only the remaining outer placeholders and invalid-input recovery preserves the prepared statement.
- Scenario 187 covers supported typed-literal aliases in SQL-prepared extended binds, proving `TEXT '...'`, `integer '...'`, `pg_catalog.integer '...'`, and parenthesized `pg_catalog.integer '...'` arguments decode through the same real `psql \bind` path while invalid-input recovery preserves the prepared statement.
- Scenario 188 covers PostgreSQL Unicode escape string text literals in SQL-prepared extended binds, proving `U&'...'` default backslash escapes decode as supported text data while malformed Unicode escapes are rejected before later supported execution and SELECT recovery. Scenario 189 extends that real-client path to custom `UESCAPE` Unicode text literals. Scenario 195 covers PostgreSQL national-character string literals, proving `N'...'` and typed `text n'...'` arguments decode as supported text data through real extended `psql \bind` recovery.
- Scenario 97 covers real PostgreSQL 16 `psql \bind` execution where SQL string literals contain `$1` / `$2`, proving quoted placeholder-looking tokens are not counted as protocol parameters and are not substituted during Bind.
- Scenario 98 covers real PostgreSQL 16 `psql \bind` execution where `$1` and `$10` appear in the same supported relational `SELECT`, proving Bind substitution targets exact unquoted placeholder tokens.
- Scenario 99 covers real PostgreSQL 16 `psql \bind` traffic with an unquoted `$0` placeholder, proving Parse rejects invalid zero-numbered parameters before statement installation and the session recovers for later supported SELECT traffic.
- Scenario 100 covers real PostgreSQL 16 `psql \bind` traffic with an unquoted `$00` placeholder, proving Parse rejects zero-padded zero-numbered parameters at the same boundary before statement installation.
- Scenario 101 covers real PostgreSQL 16 `psql \bind` traffic with zero-padded nonzero placeholders (`$01`, `$002`, `$0003`), proving Bind substitution uses numeric placeholder identity while zero-numbered placeholders remain rejected at Parse time.
- Scenario 102 covers real PostgreSQL 16 `psql \bind` traffic with apostrophes in text parameters, proving bound values are SQL-escaped as data and the supported relational parser accepts doubled-quote text literals generated by Bind.
- Scenario 103 covers real PostgreSQL 16 `psql \bind` traffic with placeholder-looking tokens inside line comments and nested block comments, proving commented `$n` tokens do not inflate parameter arity or participate in Bind substitution.
- Scenario 104 covers real PostgreSQL 16 `psql \bind` traffic with omitted Parse parameter OIDs inferred from a supported `text` predicate, proving repeated matching rows and empty-result portals stay truthful for inferred text parameters.
- Scenario 105 covers real PostgreSQL 16 `psql \bind` traffic with omitted Parse parameter OIDs inferred across a supported `int4`/`text` conjunction, proving invalid inferred `int4` input fails before portal installation and later correctly-bound execution still works in the same session.
- Scenario 106 covers real PostgreSQL 16 `psql \bind` traffic with a reused inferred `$1` placeholder across multiple supported `int4` predicates, proving invalid inferred `int4` input fails before portal installation and later correctly-bound execution reuses the same parameter value consistently.
- Scenario 107 covers real PostgreSQL 16 `psql \bind` traffic with commuted placeholders such as `$1 <= id` and `$2 = name`, proving omitted Parse OIDs are inferred from the catalog-backed column side and the bound relational SELECT executes after invalid-input recovery.
- Scenario 108 covers real PostgreSQL 16 `psql \bind` traffic with omitted Parse OIDs inferred across a supported top-level `OR` predicate, proving invalid inferred `int4` input fails before portal installation and later correctly-bound execution still works in the same session.
- Scenario 109 covers real PostgreSQL 16 `psql \bind` traffic with supported casted placeholders such as `$1::int4`, `$2::text`, and `pg_catalog`-qualified forms, proving bound literals remain inside the supported relational SELECT subset and invalid casted `int4` input recovers before correctly-bound execution.
- Scenario 110 covers real PostgreSQL 16 `psql \bind` traffic with a casted `LIMIT $n::pg_catalog.int4` placeholder, proving supported casted limit parameters parse, reject invalid `int4` input before portal installation, and recover for a correctly-bound execution.
- Scenario 111 covers real PostgreSQL 16 cursor lifecycle traffic with `FETCH FORWARD ALL` and `FETCH ALL`, proving supported relational cursor state exhausts remaining rows once and returns a truthful empty fetch afterward before `CLOSE` and later supported SELECT recovery. Scenario 112 covers `FETCH NEXT`, omitted-count `FETCH FORWARD`, `FETCH ... IN`, and explicit non-forward cursor-direction rejection with later supported SELECT recovery. Scenario 113 covers forward-only cursor `MOVE`, proving it advances supported relational cursor state without returning rows, reports truthful moved-row counts, rejects non-forward movement explicitly, and recovers for later supported SELECT traffic. Scenario 114 covers separator-less forward cursor grammar forms for `FETCH` and `MOVE`, plus explicit non-forward separator-less cursor rejection and later supported SELECT recovery. Scenario 115 covers unsupported `BINARY` and `SCROLL` cursor declaration options, proving they are rejected explicitly instead of installing malformed cursor names while supported `NO SCROLL` cursor traffic and later SELECT recovery still work. Scenario 116 covers `WITHOUT HOLD` cursor declaration grammar for supported forward-only relational SELECT cursors and explicit rejection of unsupported `WITH HOLD` cursors with later SELECT recovery. Scenario 117 covers PostgreSQL `ASENSITIVE` and `INSENSITIVE` cursor declarations for the supported forward-only relational SELECT cursor subset while preserving explicit unsupported binary-cursor rejection and later SELECT recovery. Scenario 118 covers zero-count forward cursor movement, proving `FETCH FORWARD 0` returns no rows without advancing state, `MOVE FORWARD 0` reports `MOVE 0`, and later supported cursor fetches plus SELECT recovery still work. Scenario 119 covers transaction-end cleanup for session-local `WITHOUT HOLD` cursors, proving `COMMIT` and `ROLLBACK` clear supported relational cursors, later `FETCH` calls return cursor-not-found errors, and supported SELECT recovery still works. Scenario 120 covers PostgreSQL-style unquoted cursor identifier case-folding across `DECLARE`, `FETCH`, `MOVE`, and `CLOSE`, proving mixed-case client cursor names address the same supported relational cursor while later missing-cursor and SELECT recovery still work. Scenario 121 covers quoted cursor identifier preservation, proving a quoted mixed-case cursor name with a space remains distinct from a different quoted case variant across `DECLARE`, `FETCH`, `MOVE`, and `CLOSE` while later supported SELECT recovery still works. Scenario 122 covers duplicate cursor declaration rejection, proving the existing supported relational cursor is preserved and later fetch/close plus SELECT recovery still work.
- Scenario 123 covers directionless numeric cursor movement, proving `FETCH 0 FROM`, `FETCH 2 FROM`, and `MOVE 1 FROM` preserve supported relational cursor position and later SELECT recovery still works. Scenario 162 covers quoted cursor identifiers containing semicolons, proving simple-query framing preserves the cursor name across `DECLARE`, `FETCH`, `MOVE`, and `CLOSE` before the expected post-close error and later SELECT recovery. Scenario 163 covers SQL comments inside supported `FETCH`, `MOVE`, and `CLOSE` cursor commands, proving comment-tolerant parsing preserves cursor lifecycle state and later SELECT recovery. Scenario 164 covers `\bind` text values containing spaces, semicolons, commas, and SQL comment markers, proving they remain literal data for supported relational SELECT predicates. Scenario 165 covers SQL comments around supported `DECLARE ... ASENSITIVE NO SCROLL CURSOR WITHOUT HOLD ... FOR SELECT` grammar, proving the commented declaration installs the supported relational cursor and preserves later fetch/move/close plus SELECT recovery.
- Scenario 124 covers forward-all cursor marker variants, proving `MOVE FORWARD ALL FROM`, `MOVE FORWARD n IN`, and `FETCH FORWARD ALL IN` preserve supported relational cursor state, report truthful counts, exhaust once, and recover for later supported SELECT traffic.
- Scenario 125 covers broader non-forward cursor directions, proving `FETCH FIRST`, `MOVE LAST`, `MOVE RELATIVE n`, and `MOVE ABSOLUTE n` return explicit unsupported-direction errors without advancing supported relational cursor state, and later cursor plus SELECT traffic still works. Scenario 126 covers marker-bearing omitted-count cursor forms, proving `FETCH FORWARD FROM`, `FETCH NEXT IN`, plain `FETCH FROM`, `MOVE FORWARD FROM`, and `MOVE NEXT IN` each advance the supported relational cursor by one row and recover for later supported SELECT traffic.
- Scenario 128 covers additional non-forward cursor directions, proving `FETCH PRIOR`, `FETCH BACKWARD ALL`, `MOVE PRIOR`, and `MOVE BACKWARD ALL` return explicit unsupported-direction errors without advancing supported relational cursor state, and later cursor plus SELECT traffic still works.
- Scenario 129 covers markerless omitted-count forward cursor forms, proving `FETCH FORWARD <cursor>`, `FETCH NEXT <cursor>`, `FETCH FORWARD ALL <cursor>`, `MOVE FORWARD <cursor>`, `MOVE NEXT <cursor>`, and `MOVE FORWARD ALL <cursor>` preserve supported relational cursor state and later SELECT recovery.
- Scenario 134 covers markerless `MOVE ALL <cursor>`, proving supported relational cursor movement advances to the end, reports the truthful moved-row count, returns no rows on the following fetch, and still recovers for later supported SELECT traffic.
- Scenario 135 covers marker-bearing `MOVE ALL FROM <cursor>` and `MOVE ALL IN <cursor>`, proving supported relational cursor movement advances to the end, reports truthful moved-row counts, returns no rows on following fetches, and still recovers for later supported SELECT traffic.
- Scenario 136 covers quoted cursor identifiers containing doubled-quote escapes, proving the decoded session-local cursor name remains stable across `DECLARE`, `FETCH`, `MOVE`, and `CLOSE`, then returns a cursor-not-found error after close while later supported SELECT traffic recovers.
- Scenario 158 covers marker-bearing zero-count movement and all-fetch cursor forms, proving `MOVE 0 FROM`, `MOVE 0 IN`, `FETCH ALL FROM`, and `FETCH ALL IN` preserve supported cursor state truthfully and recover for later supported SELECT traffic. Scenario 159 covers marker-bearing numeric `IN` forms, proving `FETCH 0 IN`, `FETCH n IN`, `MOVE 0 IN`, and `MOVE n IN` keep supported relational cursor state truthful across empty, partial, and remaining-row paths. Scenario 160 covers markerless numeric zero/count cursor forms, proving `FETCH 0 <cursor>`, `FETCH n <cursor>`, `MOVE 0 <cursor>`, and `MOVE n <cursor>` keep the same supported relational cursor truth boundary. Scenario 161 covers unsupported direction and declaration recovery, proving `FETCH BACKWARD n`, `MOVE ABSOLUTE n`, and unsupported `SCROLL CURSOR` declarations return explicit errors without advancing the supported forward-only cursor, and later cursor plus SELECT traffic still works.
- Scenario 137 covers SQL `PREPARE` / `EXECUTE` over the supported relational `SELECT` subset, proving real `psql` can prepare catalog-backed `int4`/`text` predicates, execute matching and empty-result paths, reject invalid `int4` input without losing the statement, deallocate it, and recover for later supported SELECT traffic. Scenario 138 covers SQL `PREPARE` without an explicit type list, proving catalog-backed parameter type inference, invalid inferred-`int4` recovery, `DEALLOCATE ALL` cleanup of SQL prepared state, and later supported SELECT recovery. Scenario 139 covers `DEALLOCATE PREPARE <name>` with a quoted SQL prepared-statement name, proving the name survives prepare/execute/deallocate and later supported SELECT traffic recovers after the expected missing-prepared-statement error. Scenario 140 covers duplicate SQL `PREPARE` names, proving duplicate rejection takes precedence over unsupported replacement payload validation and preserves the original prepared statement for later execution. Scenario 141 covers a quoted SQL prepared-statement name containing parentheses, proving the parser does not confuse identifier text for the parameter/type-list delimiter across `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE`. Scenario 142 covers a quoted SQL prepared-statement name containing ` AS `, proving the parser does not confuse identifier text for the `PREPARE ... AS ...` separator. Scenario 143 covers a quoted SQL prepared-statement name containing doubled-quote escapes, proving the decoded identifier remains stable across `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE`. Scenario 144 covers escaped SQL `EXECUTE` text arguments containing doubled apostrophes and commas, proving argument-list splitting and text literal decoding stay PostgreSQL-compatible for the supported prepared relational `SELECT` subset. Scenario 145 covers unsupported `NULL` SQL `EXECUTE` arguments, proving they return a SQL-prepared-specific unsupported-feature error, leave the prepared statement usable for later execution, and still recover after deallocation. Scenario 146 covers zero-parameter SQL prepared SELECTs, proving both `EXECUTE <name>` and `EXECUTE <name>()` execute the prepared statement before deallocation and later supported SELECT recovery. Scenario 147 covers too few and too many SQL `EXECUTE` arguments, proving the prepared statement remains usable for a correctly-shaped execution afterward. Scenario 148 covers parenthesized SQL `EXECUTE` int4/text literal arguments, proving matching and empty-result prepared executions plus deallocate, missing-prepared, and later SELECT recovery paths. Scenario 149 covers supported postfix-casted SQL `EXECUTE` int4/text literal arguments, including `pg_catalog`-qualified type names, while preserving empty-result, deallocate, missing-prepared, and later SELECT recovery paths. Scenario 150 covers semicolon-bearing quoted SQL prepared-statement names across `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE`. Scenario 151 covers SQL comments around supported `PREPARE` type lists, `EXECUTE` arguments, and `DEALLOCATE PREPARE` targets while preserving quoted identifier/comment-marker text and later supported SELECT recovery. Scenario 152 covers SQL comments containing delimiter-looking `AS` text before the real `PREPARE ... AS ...` separator, proving the supported prepared relational SELECT still prepares, executes, deallocates, and recovers. Scenario 153 covers newlines/tabs after SQL prepared-statement keywords, proving `PREPARE`, `EXECUTE`, `DEALLOCATE`, and `DEALLOCATE PREPARE` accept PostgreSQL whitespace while staying inside the supported prepared relational SELECT subset. Scenario 154 covers leading SQL comments before `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE`, proving comment-prefixed prepared-statement traffic still prepares, executes, deallocates, and recovers. Scenario 155 covers semicolons inside line and nested block comments around SQL prepared-statement traffic, proving simple-query framing preserves the intended `PREPARE` / `EXECUTE` / `DEALLOCATE PREPARE` boundaries and recovers for later supported SELECT traffic. Scenario 156 covers signed `int4` values across simple relational SELECT, extended `\bind`, and SQL `EXECUTE`, proving negative and zero parameters stay truthful for the supported subset before deallocate and later SELECT recovery. Scenario 157 covers `INT4` / `pg_catalog.text` DDL aliases and plus-signed `int4` literals across simple SELECTs, extended `\bind`, and SQL prepared execution. Scenario 166 covers inferred SQL-prepared `LIMIT $n` parameters over the supported relational SELECT subset, including invalid limit input recovery, casted limit execution, deallocation, missing-prepared errors, and later supported SELECT recovery. Scenario 167 covers negative SQL-prepared `LIMIT` execution, proving it returns a stable PostgreSQL-compatible error while preserving zero-limit, later valid-limit, deallocation, and SELECT recovery paths. Scenario 171 covers `psql \gdesc` over `EXECUTE <sql-prepared>`, proving extended Describe reports catalog-backed SQL prepared result metadata without executing the statement, while later SQL prepared execution and SELECT recovery still work. Scenario 172 covers `psql \bind` over `EXECUTE <sql-prepared>($1, $2)`, proving extended Parse/Describe infer parameter types from the SQL prepared statement, invalid bound `int4` input recovers without discarding the prepared statement, later bound execution succeeds, and SELECT recovery still works after deallocation. Scenario 173 covers the same SQL-prepared extended execution path through `\gset` and `\gx`, proving bound SQL-prepared rows can feed psql client variables and expanded-output rendering before deallocation and later supported SELECT recovery. Scenario 174 covers SQL-prepared extended `\crosstabview`, proving bound SQL-prepared rows can feed psql crosstab rendering before deallocation and later supported SELECT recovery. Scenario 177 covers `psql \bind` over reordered `EXECUTE <sql-prepared>($2, $1)`, proving outer placeholder type inference follows the SQL-prepared argument mapping and preserves invalid-`int4`, later valid execution, deallocate, and SELECT recovery behavior. Scenario 178 covers casted and parenthesized reordered outer placeholders in `EXECUTE <sql-prepared>(($2)::pg_catalog.int4, ($1)::text)`, proving SQL `EXECUTE` argument normalization preserves the remapped outer parameter inference and recovery behavior through real extended `psql \bind`. Scenario 179 covers mixed literal/placeholder SQL `EXECUTE` arguments through real extended `psql \bind`, proving Parse/Describe infer only the remaining outer parameter OID for `EXECUTE <prepared>($1, 'literal')` and `EXECUTE <prepared>(literal, $1)` while preserving invalid bind recovery, deallocate, and later SELECT recovery. Scenario 180 covers conflicting reused outer placeholders in SQL-prepared extended binds, proving `EXECUTE <prepared>($1, $1)` is rejected when the reused placeholder would need incompatible SQL-prepared `int4` and `text` argument types, and that later supported SQL-prepared extended bind plus SELECT traffic recovers. Scenario 181 covers sparse outer placeholders in SQL-prepared extended binds, proving an unused lower-numbered bind slot is not coerced before the used placeholder executes and validates normally.
- Scenario 182 covers compatible reused outer placeholders in SQL-prepared extended binds, proving same-typed `EXECUTE <prepared>($1, $1)` argument mapping shares one outer `int4` bind slot without losing invalid-input recovery, matching/empty-result execution, deallocation, or later SELECT recovery.
- Scenario 183 covers PostgreSQL escape string text literals in SQL-prepared extended binds, proving mixed `E'...'` literal plus outer-placeholder execution stays inside the supported `text`/`int4` relational SELECT subset with `\gdesc`, invalid bind recovery, deallocation, and later SELECT recovery. Scenario 190 extends that path to hex/octal escape string text literals and malformed-escape recovery. Scenario 191 covers newline-separated adjacent standard string literals and same-line adjacent-literal rejection/recovery, and scenario 196 covers adjacent string literals separated by newline-bearing SQL comments while preserving no-newline comment-separated rejection/recovery. Scenario 192 covers `CAST(... AS ...)` SQL `EXECUTE` arguments for supported `int4`/`text` literal and placeholder values, plus unsupported cast-target rejection/recovery.
- Scenario 184 covers PostgreSQL dollar-quoted text literals in SQL-prepared extended binds, proving mixed `$tag$...$tag$` / `$$...$$` literals plus an outer placeholder stay inside the supported `text`/`int4` relational SELECT subset with `\gdesc`, invalid bind recovery, deallocation, and later SELECT recovery. Scenario 197 covers parenthesized dollar-quoted literals with unmatched literal-body parentheses through the same real-client path.
- Scenario 185 covers PostgreSQL comment markers inside dollar-quoted SQL-prepared extended-bind literals, proving the same supported subset treats `--` and `/* ... */` as `text` data, preserves invalid bind recovery, deallocates cleanly, and recovers for later SELECT traffic.
- Scenario 186 covers supported PostgreSQL typed string literals in SQL-prepared extended binds, proving `text '...'`, `pg_catalog.text $$...$$`, and `int4 '...'` literals stay inside the supported `text`/`int4` relational SELECT subset with `\gdesc`, invalid bind recovery, deallocation, and later SELECT recovery.
- Scenario 187 covers supported typed-literal aliases in SQL-prepared extended binds, proving `TEXT '...'`, `integer '...'`, `pg_catalog.integer '...'`, and parenthesized `pg_catalog.integer '...'` literals stay inside the same supported subset with `\gdesc`, invalid bind recovery, deallocation, and later SELECT recovery.
- Scenario 188 covers PostgreSQL Unicode escape string text literals in SQL-prepared extended binds, proving `U&'...'` default backslash escapes stay inside the supported `text` subset with `\gdesc`, invalid bind recovery, malformed-literal rejection, deallocation, and later SELECT recovery. Scenario 189 covers custom `UESCAPE` text literals through the same SQL-prepared extended-bind boundary. Scenario 195 covers national-character string literals through the same boundary, proving `N'...'` and typed `text n'...'` values decode as supported `text` data while later bind, deallocate, and SELECT recovery stay stable. Scenario 265 covers invalid-looking extended-bound `int4` input under `FETCH_COUNT` before `\gx`, proving the unsupported parameterized-cursor boundary wins before bind-value validation, installs no transient `_psql_cursor`, and later expanded-output execution recovers on the same connection. Scenario 266 pins the matching `\gset` unsupported-cursor cleanup path with later client-variable capture recovery.
- Raw explicit portal `Describe` ownership of `RowDescription`, raw bounded `Execute.max_rows` final completion counts, raw `Describe Portal` after bounded suspension preserving resume position, raw Parse unsupported-parameter, oversized-OID, unsupported-statement, and SQL-prepared cursor declaration OID-conflict errors with skip-until-`Sync` recovery, named prepared-statement and named-portal duplicate handling, duplicate portal precedence over malformed payloads, missing raw `Bind` target statements before binary format validation, supported raw binary `int4`/`text` parameter/result `Bind` coverage, malformed binary `int4` payload rejection, and malformed parameter/result format-count skip-until-`Sync` recovery, missing raw `Describe Statement` / `Describe Portal` and `Execute` portal targets with skip-until-`Sync` recovery, PostgreSQL-compatible `CloseComplete` for missing raw `Close Statement` / `Close Portal` targets without entering error recovery, SQL `PREPARE` state isolation for extended `Close Statement`, successful `Close Portal` cleanup plus later missing-portal Execute recovery, and statement-close cascade invalidation of dependent portals before later Execute recovery are covered by helper-level protocol tests because PostgreSQL 16 `psql` does not emit stable named `Parse`/`Bind`/`Describe`/`Close`/missing-portal `Execute` metacommand traffic for the golden suite; unnamed replacement remains covered in the same lifecycle tests.
- Invalid raw UTF-8 bind payloads and mismatched raw parameter/result-format code counts, including skip-until-`Sync` recovery with skipped side effects and later supported SELECT traffic, are covered by helper-level protocol tests because PostgreSQL 16 `psql` does not emit those malformed wire shapes from stable golden-suite metacommands.
- CI now boots the repo-local compatibility endpoint with `cargo run -p gpu_db_protocol --bin gpu-db-server -- --listen 127.0.0.1:55432` before running the suite, so Q4 has a real service endpoint instead of a manual-only placeholder.
- If a wire-level extended-query flow is intentionally unsupported, encode that as an explicit expected failure in a dedicated scenario and set the expected `.rc` artifact.
