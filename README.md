# gpu-database-engine

Bootstrap implementation workspace for the pre-NVIDIA phase.

PostgreSQL compatibility work is in the post-CUDA P1-P7 hardening loop. Current P3 coverage prioritizes real `psql` extended query, cursor, prepared statement, portal, `FETCH_COUNT`, error recovery, selected-driver binary `int4`/`text` transfer, and first simple-query COPY TO STDOUT export and COPY FROM STDIN import plus explicit unsupported broader-binary/function-call behavior for the supported relational `SELECT` plus `int4`/`text` subset. The golden suite now also pins SQL-prepared `FETCH_COUNT` + extended `EXECUTE <name>($1) \bind ... \g` / `\gset` / `\gx` / `\crosstabview` / `\gexec` invalid-bind cleanup plus argument-count cleanup for argument-bearing `EXECUTE <name>(...) \gdesc` / `\gset` / `\gx` / `\crosstabview` / `\gexec`, argument-bearing negative-`LIMIT` `\gdesc` / `\gset` / `\gx` / `\crosstabview` / `\gexec` cleanup, zero-parameter `EXECUTE <name>(...) \gdesc` / `\gset` / `\gx` / `\crosstabview` / `\gexec`, zero-parameter literal negative-`LIMIT`, zero-parameter literal negative-`LIMIT` `\gdesc` metadata-only cleanup, zero-parameter literal negative-`LIMIT` `\gset` / `\gx` / `\crosstabview` / `\gexec` cleanup, and the bounded clean-restore DDL path used by `pg_restore --clean --if-exists` for supported public tables: failed cursor declarations install no transient `_psql_cursor`, describe-only traffic returns metadata without executing the bad limit, generated-SQL execution and expanded-output rendering are skipped on the bad limit, client-variable and crosstab consumption are skipped on failed negative limits, stale public tables can be dropped and recreated by real restore tooling, the prepared statement can be cleaned up, and later supported traffic recovers.

P3 current scope is closed as of the 2026-05-16 closeout pass once the checked-in validation gates remain green: `protocol.extended_query` and `protocol.error_paths` are green, the real PostgreSQL 16 psql golden suite is green, the `tokio-postgres` smoke harness proves startup/simple/prepared/empty/error-recovery/reconnect behavior for the supported subset, minimal binary `int4`/`text` Bind/result transfer is covered for that named driver lane, and the `sqlx` smoke harness proves startup/raw SQL setup, prepared/bound SELECT, empty results, unsupported COPY option recovery, and pooled reconnect through a second Rust async driver. The checked-in `node-postgres` compatibility smoke now covers the same app-shaped startup/simple/prepared-bind/empty/error-recovery/pool/reconnect/unsupported COPY option contract from a JavaScript client, and the checked-in `asyncpg` smoke covers startup, raw SQL setup, prepared/bound SELECT, empty results, unsupported COPY option recovery, reconnect, and one-connection pool reuse through asyncpg's default reset cleanup SQL. Post-P3 DML coverage now includes PostgreSQL extended-protocol `Parse`/`Bind`/`Describe`/`Execute` support for bounded parameterized `INSERT`, filtered `UPDATE`, and filtered `DELETE` over supported public `int4`/`text` tables, including `NoData` metadata descriptions, command tags, bind-time type recovery, and later SELECT recovery. Post-P3 bulk-data coverage now includes a checked-in PostgreSQL 16 `tests/compat/pg-dump/run.sh` smoke for supported `public` `int4`/`text` tables: the compatibility endpoint can expose the bounded catalog surface needed for public-schema discovery, emit `CREATE SCHEMA public`, schema-qualified `CREATE TABLE`, and COPY data, restore a plain `pg_dump --format=plain --no-owner --no-privileges --schema=public` dump through real `psql` into a fresh `--shared-catalog` endpoint, restore a split plain `pg_dump --schema-only` followed by `pg_dump --data-only` through real `psql` against the pre-created supported schema, restore a COPY-free plain `pg_dump --format=plain --inserts --rows-per-insert=2 --no-owner --no-privileges --schema=public` dump through real `psql`, restore `pg_dump --format=custom`, `--format=directory`, and `--format=tar` archives through real `pg_restore --no-owner --no-privileges`, restore the directory archive with `pg_restore --jobs=2` without concurrent shared-catalog sessions clobbering table data, and run custom-archive `pg_restore --clean --if-exists` over stale supported public tables so restored rows replace pre-existing rows. Broader binary formats/types, binary/query/extended COPY, roles/ACL fidelity, indexes/constraints/sequences/views/functions, unsupported schemas/object kinds, schema/data split archive jobs, broad DROP/schema semantics beyond the clean-restore public-table path, frontend FunctionCall, advisory lock semantics beyond asyncpg reset cleanup no-ops, and advanced cursor/portal behavior are classified as post-P3 backlog unless a named client/workload or SQL feature reopens that exact surface.

## Current scope

- Replication-shaped local commit path
- WAL-before-visibility invariant tests
- WAL durability + queue watermarks (flushed, buffered, unflushed, pending depth/capacity, pending age/deadline), commit/apply/visibility lag gauges, explicit backlog/gap blocker flags, active transaction depth, and failover/admission readiness flags (`quiescent_for_failover`, `follower_promotion_ready`, `mutation_admission_saturated`)
- Engine truth surface via `Engine::status_snapshot()` for served snapshot identity/frontier, active fallback reasons + parity rollups, and replication/readiness health
- Engine telemetry snapshot + sink publication API for replication lag, durable WAL/snapshot frontier, write-path readiness/backlog state, and runtime metrics
- Operational replication smoke coverage now includes `scripts/run_replication_multiprocess_smoke.sh`, which builds a packaged example and launches independent follower child processes while the parent leader sends real TCP AppendEntries frames, `scripts/run_replication_service_smoke.sh`, which keeps two follower service processes alive across multiple sequential AppendEntries connections before controlled shutdown, `scripts/run_replication_supervised_restart_smoke.sh`, which restarts one follower service and replays the durable prefix while a second service stays live, and `scripts/run_replication_container_smoke.sh`, which runs those follower services in local Docker containers while a host leader drives real TCP AppendEntries; production service-manager supervision and Kubernetes deployment remain explicit gaps.
- MVCC benchmark reporting via `MvccBenchmarkReport::from_results(...)`, summarizing GPU-executed workload rate, CPU fallback rate, H2D/D2H bytes, kernel execution totals, and batch wait totals from deterministic workload runs
- Thin MVCC execution slice via `Engine::execute_mvcc_query()` covering snapshot-bound full scan / key lookup / key-batch fan-in / explicit multi-source `Concat`, `ConcatDistinct`, `IntersectDistinct`, `IntersectAll`, `ExceptDistinct`, `ExceptAll`, `SymmetricDifferenceDistinct`, and `SymmetricDifferenceAll` composition / generic `FollowValueChain { keys, plan, provenance }` linear nested-join expansion / `FollowValueChainBranches { keys, plans, fan_in, provenance }` plus `FollowValueChainLabeledBranches { keys, branches, fan_in, provenance }` seed-grouped or first-non-empty branch expansion / legacy value→key reference helpers layered on that same chain mechanism / optional key-prefix/range or source-aware key/value or branch-label filtering, explicit multi-frame provenance filters/projection plus reusable frame-bundle controls on nested-join rows including exact ordered bundle-path equality, ordered bundle-subpath matching, anchored bundle-slice matching, counted bundle-membership thresholds, exact/ranged first/last/nth occurrence matching, exact/ranged same-subpath ordinal plus adjacent and first/last-to-ordinal occurrence-distance helpers, exact/ranged first/last-to-ordinal same-subpath offset helpers, exact/ranged ordinal-pair same-subpath offset helpers, exact/ranged ordinal mixed-subpath occurrence-distance helpers, exact/ranged first/last-to-ordinal mixed-subpath occurrence-distance helpers, exact/ranged nth-offset matching, exact/ranged first/last-to-ordinal mixed-subpath offset helpers, and first/last exact/ranged mixed-occurrence offsets, first/last mixed-subpath occurrence-distance helpers, reusable occurrence-offset and occurrence-distance projection/order helpers, and bundle-relative positional segment equality, source-aware ordering, limit, and key/value or join-side source-value/branch-label/provenance-value/provenance-path-summary projection through the execution layer with explicit CPU fallback parity tracking (`GPU-123`)
- MVCC execution now passes through an explicit backend hook (`CpuMvccExecutionBackend` today), so a future `CudaBackend` can attach behind `Engine::execute_mvcc_query()` without changing `MvccReadQuery`, `MvccReadResult`, or `MvccReadRow { source_key, key, value }`
- CUDA-transition prep now also includes a regression-proven first-slice backend handoff: full scan / key lookup / key-batch lookup plus simple filter shapes can execute through an alternate backend while unsupported shapes fall back through the CPU reference pipeline with the same tracked `GpuMvccReadParityGap` accounting, and the current truth surface now classifies the first unsupported boundary explicitly (`unsupported_source`, `unsupported_order`, `unsupported_projection`, `unsupported_filter`, `empty_logical_filter_tree`) so CUDA routing can explain why a query missed the first slice
- CUDA hardware onboarding has driver-level launch paths in `CudaDriverRuntime`: `launch_smoke_add_one(...)` covers PTX module load, device allocation, kernel launch, synchronization, and D2H copy, while `filter_all_mask(...)`, `filter_equal_u32_mask(...)`, `filter_equal_bytes_mask(...)`, `filter_bytes_range_mask(...)`, and `mvcc_visibility_mask(...)` are now integrated into `CudaMvccExecutionBackend` for filterless reads, exact numeric/string `MvccReadFilter::ValueEquals`, key-prefix filters, general bytewise key ranges, nested `All`/`Any` combinations of those supported predicates, single-frame `ProvenanceKeyPrefix` / `ProvenanceValueEquals` predicates plus bundle key/value/key-value equality, counted membership, key-prefix predicates, stable bundle path predicates (`PathEquals`, `PathContains`, `PathCountAtLeast`, `PathPairAtDistance`, path prefix/suffix/slice/segment equality, and bundle length), same-subpath plus mixed-subpath occurrence path predicates, and source-key/source-value/branch-label filters over CPU-resolved rows, and device-side MVCC visibility masking over supported full-scan/key-lookup shapes. `CudaMvccRowBatch` now records the first gate-1 SoA transfer contract for key/value offset buffers, flattened bytes, MVCC visibility bounds, and provenance handles, with CUDA row-length and visibility kernels proving H2D -> kernel -> D2H row-batch access on local hardware. Supported CUDA full scans now feed all MVCC versions into the device visibility kernel so filterless full-scan row selection no longer depends on CPU-visible preselection; supported `KeyLookup` feeds all MVCC versions to CUDA and uses a device key-equality mask plus device visibility mask, while first `KeyBatchLookup` shapes compact the all-version row set to requested keys before one CUDA visibility/filter/order/projection pass, preserving batch request order without transferring unrelated table rows. `FollowValueChain`, `FollowValueChainBranches`, and `FollowValueChainLabeledBranches` current-row joins and prefix-terminal expansions now have native CUDA source resolution: seed and hop rows are selected from all MVCC versions with CUDA key masks plus device visibility masks, prefix terminals expand through a CUDA key-prefix mask, branch fan-in is applied per seed, branch labels are preserved in the unchanged host row contract, and provenance assembly stays on the host. Gate 5 has started with CUDA-backed key/value ordering for full-scan/key-lookup/key-batch/native-`Concat` shapes plus `Concat` over CUDA-resolvable native child sources, including nested set/multiset composition, preserving source-list order while each child performs source/visibility/filter masking through CUDA; supported CUDA paths now also apply post-filter/post-order limits before projection, and CPU-resolved provenance sources, including `Concat` plus distinct/intersect/except/symmetric-difference composition over CPU-resolved children, can report CUDA execution with source-relative filters, source/branch ordering, source/branch projection, provenance order, and provenance projection after CUDA source/visibility/filter masking. Native distinct/intersect/except/symmetric-difference sources whose children are already CUDA-resolvable now use CUDA source/visibility masks for each child before preserving the existing host set/multiset semantics, with classifier/backend/local CUDA coverage now enumerating every current set/multiset variant. The autonomous CUDA loop now tracks completion through ordered gates: GPU row format/transfer contract, native scan/visibility parity, native point/key lookup parity, provenance/filter expansion parity, ordering/projection/composition coverage, benchmark/telemetry/fallback-rate regression gates, and GPU CI or a reproducible runner.
- CPU-first reference engine skeleton
- Device-aware execution abstractions

## MVCC vertical slice (current bootstrap shape)

- Engine entry point: `Engine::execute_mvcc_query(&MvccReadQuery)`
- Supported sources:
  - `MvccReadSource::FullScan`
  - `MvccReadSource::KeyLookup { key }`
  - `MvccReadSource::KeyBatchLookup { keys }` (fan-in multi-source lookup; preserves request order before downstream filter/order/limit)
  - `MvccReadSource::Concat { sources }` (explicit composition primitive that concatenates existing source shapes in source-list order before downstream filter/order/limit)
  - `MvccReadSource::ConcatDistinct { sources }` (ordered deduplicating composition primitive; keeps the first occurrence of each resolved row while preserving distinct source-provenance rows)
  - `MvccReadSource::IntersectDistinct { sources }` (ordered intersection primitive; emits first-source rows whose exact resolved-row identity appears in every subsource, with source provenance treated as part of identity)
  - `MvccReadSource::IntersectAll { sources }` (ordered multiset intersection primitive; emits first-source rows up to the minimum exact resolved-row identity count across subsources, with source provenance treated as part of identity)
  - `MvccReadSource::ExceptDistinct { sources }` (ordered subtraction primitive; emits first-source rows whose exact resolved-row identity does not appear in any remaining subsource, with source provenance treated as part of identity)
  - `MvccReadSource::ExceptAll { sources }` (ordered multiset subtraction primitive; emits first-source rows after subtracting exact resolved-row identity multiplicity contributed by remaining subsources, with source provenance treated as part of identity)
  - `MvccReadSource::SymmetricDifferenceDistinct { sources }` (ordered unique-presence primitive; emits rows whose exact resolved-row identity appears in exactly one subsource, preserving first appearance order and treating source provenance as part of identity)
  - `MvccReadSource::SymmetricDifferenceAll { sources }` (ordered multiset symmetric-difference primitive; iteratively cancels exact resolved-row identity multiplicity source-by-source and emits the remaining imbalance in first appearance order, with source provenance treated as part of identity)
  - `MvccReadSource::FollowValueChain { keys, plan, provenance }` (generic source-preserving linear nested-join helper; follows `plan.value_key_hops` visible value→key hops from each visible seed row, then either returns the current row or expands visible keys matching the current row's value as a prefix)
  - `MvccReadSource::FollowValueChainBranches { keys, plans, fan_in, provenance }` (generic source-preserving branch helper; resolves multiple linear value-chain plans per visible seed row under an explicit per-seed fan-in policy such as `AllBranches` or `FirstNonEmptyBranch`)
  - `MvccReadSource::FollowValueChainLabeledBranches { keys, branches, fan_in, provenance }` (branch-labeled nested-join helper; preserves the same branch fan-in semantics while attaching stable branch labels that downstream filters/order/projections can inspect without fabricating extra row state)
  - `MvccSourceProvenance::Seed` preserves the original seed row as join provenance for source-aware filters, ordering, and projections.
  - `MvccSourceProvenance::TerminalInput` retargets source-aware filters, ordering, and projections to the row that fed the terminal resolution step (for example the profile row whose value drove a prefix fan-out).
  - Nested-join rows now retain an internal provenance path, so explicit frame-aware filters/projection can inspect `Seed`, `TerminalInput`, or any `ValueHop(n)` frame without changing the external row contract.
  - `MvccReadSource::FollowValueKeyRefs { keys }` (join-adjacent foreign-key-style expansion from seed row values to referenced keys)
  - `MvccReadSource::FollowValueKeyPrefixes { keys }` (prefix-driven foreign-key-style expansion from seed row values to visible target-key ranges)
  - `MvccReadSource::FollowValueKeyRefPrefixes { keys }` (source-preserving two-hop expansion: seed value -> referenced row -> prefix-driven target fan-out)
  - `MvccReadSource::FollowValueKeyRefValueKeyRefs { keys }` (source-preserving three-hop expansion: seed value -> referenced row -> referenced row value -> final referenced row)
  - `MvccReadSource::FollowValueKeyRefValueKeyPrefixes { keys }` (source-preserving chained fan-out: seed value -> referenced row -> referenced row value -> visible prefix-driven target expansion)
  - `MvccReadSource::FollowValueKeyRefValueKeyRefPrefixes { keys }` (source-preserving four-hop fan-out: seed value -> referenced row -> referenced row value -> referenced row -> visible prefix-driven target expansion)
  - `MvccReadSource::FollowValueKeyRefValueKeyRefValueKeyRefs { keys }` (source-preserving deeper terminal chain: seed value -> referenced row -> referenced row value -> referenced row -> referenced row value -> referenced row -> final visible target row)
  - `MvccReadSource::FollowValueKeyRefValueKeyRefValueKeyPrefixes { keys }` (source-preserving deeper fan-out chain: seed value -> referenced row -> referenced row value -> referenced row -> referenced row value -> visible referenced row -> visible prefix-driven target expansion)
  - `MvccReadSource::FollowValueKeyRefValueKeyRefValueKeyRefPrefixes { keys }` (source-preserving deeper nested fan-out chain: seed value -> referenced row -> referenced row value -> referenced row -> referenced row value -> visible referenced row -> visible referenced-row value -> visible prefix-driven target expansion)
- Supported snapshot rule:
  - `visibility.read_txn_id` selects the MVCC snapshot frontier
- Supported filters:
  - `MvccReadFilter::KeyPrefix(prefix)`
  - `MvccReadFilter::SourceKeyPrefix(prefix)`
  - `MvccReadFilter::ProvenanceKeyPrefix { frame, prefix }`
  - `MvccReadFilter::ProvenanceBundleKeyEquals { bundle, expected }`
  - `MvccReadFilter::ProvenanceBundleKeyCountAtLeast { bundle, expected, min_count }`
  - `MvccReadFilter::ProvenanceBundleKeyPrefix { bundle, prefix }`
  - `MvccReadFilter::BranchLabelEquals(label)`
  - `MvccReadFilter::KeyRange { start_inclusive, end_exclusive }`
  - `MvccReadFilter::ValueEquals(value)`
  - `MvccReadFilter::SourceValueEquals(value)`
  - `MvccReadFilter::ProvenanceValueEquals { frame, expected }`
  - `MvccReadFilter::ProvenanceBundleValueEquals { bundle, expected }`
  - `MvccReadFilter::ProvenanceBundleValueCountAtLeast { bundle, expected, min_count }`
  - `MvccReadFilter::ProvenanceBundleKeyValueEquals { bundle, key, value }`
  - `MvccReadFilter::ProvenanceBundleKeyValueCountAtLeast { bundle, key, value, min_count }`
  - `MvccReadFilter::ProvenanceBundlePathEquals { bundle, summary, expected }`
  - `MvccReadFilter::ProvenanceBundlePathContains { bundle, summary, expected }` (ordered contiguous subpath matching over named key/value/`key=value` provenance bundles)
  - `MvccReadFilter::ProvenanceBundlePathCountAtLeast { bundle, summary, expected, min_count }` (ordered repeated-subpath counting over named key/value/`key=value` provenance bundles)
  - `MvccReadFilter::ProvenanceBundlePathPairAtDistance { bundle, summary, left, right, distance }` (relative-position matching for bundle segments separated by an exact distance)
  - `MvccReadFilter::ProvenanceBundlePathSuffixEquals { bundle, summary, expected }` (whole-bundle suffix matching over named key/value/`key=value` provenance bundles)
  - `MvccReadFilter::ProvenanceBundlePathPrefixEquals { bundle, summary, expected }` (whole-bundle prefix matching over named key/value/`key=value` provenance bundles)
  - `MvccReadFilter::ProvenanceBundlePathSliceEquals { bundle, summary, start, expected }` (anchored contiguous subpath matching at a chosen bundle-relative offset over named key/value/`key=value` provenance bundles)
  - `MvccReadFilter::ProvenanceBundlePathFirstOccurrenceAt { bundle, summary, start, expected }` (require the first ordered contiguous occurrence of a key/value/`key=value` subpath to begin at an exact bundle-relative offset)
  - `MvccReadFilter::ProvenanceBundlePathFirstOccurrenceWithin { bundle, summary, start_min, start_max, expected }` (require the first ordered contiguous occurrence of a key/value/`key=value` subpath to begin within an inclusive bundle-relative offset range)
  - `MvccReadFilter::ProvenanceBundlePathLastOccurrenceAt { bundle, summary, start, expected }` (require the last ordered contiguous occurrence of a key/value/`key=value` subpath to begin at an exact bundle-relative offset)
  - `MvccReadFilter::ProvenanceBundlePathLastOccurrenceWithin { bundle, summary, start_min, start_max, expected }` (require the last ordered contiguous occurrence of a key/value/`key=value` subpath to begin within an inclusive bundle-relative offset range)
  - `MvccReadFilter::ProvenanceBundlePathOccurrenceAt { bundle, summary, occurrence_index, start, expected }` (require an exact zero-based ordered contiguous occurrence of a key/value/`key=value` subpath to begin at an exact bundle-relative offset)
  - `MvccReadFilter::ProvenanceBundlePathOccurrenceWithin { bundle, summary, occurrence_index, start_min, start_max, expected }` (require an exact zero-based ordered contiguous occurrence of a key/value/`key=value` subpath to begin within an inclusive bundle-relative offset range)
  - `MvccReadFilter::ProvenanceBundlePathOccurrenceDistance { bundle, summary, left_occurrence_index, right_occurrence_index, distance, expected }` (require two exact zero-based ordered contiguous occurrences of the same key/value/`key=value` subpath to begin an exact distance apart)
  - `MvccReadFilter::ProvenanceBundlePathOccurrenceDistanceWithin { bundle, summary, left_occurrence_index, right_occurrence_index, min_distance, max_distance, expected }` (require two exact zero-based ordered contiguous occurrences of the same key/value/`key=value` subpath to begin within an inclusive distance range)
  - `MvccReadFilter::ProvenanceBundlePathFirstOccurrenceDistance { bundle, summary, distance, expected }` (require the first two ordered contiguous occurrences of the same key/value/`key=value` subpath to begin an exact distance apart)
  - `MvccReadFilter::ProvenanceBundlePathFirstOccurrenceDistanceWithin { bundle, summary, min_distance, max_distance, expected }` (require the first two ordered contiguous occurrences of the same key/value/`key=value` subpath to begin within an inclusive distance range)
  - `MvccReadFilter::ProvenanceBundlePathLastOccurrenceDistance { bundle, summary, distance, expected }` (require the last two ordered contiguous occurrences of the same key/value/`key=value` subpath to begin an exact distance apart)
  - `MvccReadFilter::ProvenanceBundlePathLastOccurrenceDistanceWithin { bundle, summary, min_distance, max_distance, expected }` (require the last two ordered contiguous occurrences of the same key/value/`key=value` subpath to begin within an inclusive distance range)
  - `MvccReadFilter::ProvenanceBundlePathFirstOccurrenceToOccurrenceDistance { bundle, summary, occurrence_index, distance, expected }` (require the first and an exact zero-based later occurrence of the same key/value/`key=value` subpath to begin an exact distance apart)
  - `MvccReadFilter::ProvenanceBundlePathFirstOccurrenceToOccurrenceDistanceWithin { bundle, summary, occurrence_index, min_distance, max_distance, expected }` (require the first and an exact zero-based later occurrence of the same key/value/`key=value` subpath to begin within an inclusive distance range)
  - `MvccReadFilter::ProvenanceBundlePathFirstOccurrenceToOccurrenceAt { bundle, summary, occurrence_index, first_start, occurrence_start, expected }` (require the first and an exact zero-based later occurrence of the same key/value/`key=value` subpath to begin at exact bundle-relative offsets)
  - `MvccReadFilter::ProvenanceBundlePathFirstOccurrenceToOccurrenceWithin { bundle, summary, occurrence_index, first_start_min, first_start_max, occurrence_start_min, occurrence_start_max, expected }` (require the first and an exact zero-based later occurrence of the same key/value/`key=value` subpath to begin within inclusive bundle-relative offset ranges)
  - `MvccReadFilter::ProvenanceBundlePathOccurrenceToLastDistance { bundle, summary, occurrence_index, distance, expected }` (require an exact zero-based occurrence and the last occurrence of the same key/value/`key=value` subpath to begin an exact distance apart)
  - `MvccReadFilter::ProvenanceBundlePathOccurrenceToLastDistanceWithin { bundle, summary, occurrence_index, min_distance, max_distance, expected }` (require an exact zero-based occurrence and the last occurrence of the same key/value/`key=value` subpath to begin within an inclusive distance range)
  - `MvccReadFilter::ProvenanceBundlePathOccurrenceToLastAt { bundle, summary, occurrence_index, occurrence_start, last_start, expected }` (require an exact zero-based occurrence and the last occurrence of the same key/value/`key=value` subpath to begin at exact bundle-relative offsets)
  - `MvccReadFilter::ProvenanceBundlePathOccurrenceToLastWithin { bundle, summary, occurrence_index, occurrence_start_min, occurrence_start_max, last_start_min, last_start_max, expected }` (require an exact zero-based occurrence and the last occurrence of the same key/value/`key=value` subpath to begin within inclusive bundle-relative offset ranges)
  - `MvccReadFilter::ProvenanceBundlePathOccurrencePairAt { bundle, summary, left_occurrence_index, left_start, right_occurrence_index, right_start, expected }` (require two exact zero-based occurrences of the same key/value/`key=value` subpath to begin at exact bundle-relative offsets)
  - `MvccReadFilter::ProvenanceBundlePathOccurrencePairWithin { bundle, summary, left_occurrence_index, left_start_min, left_start_max, right_occurrence_index, right_start_min, right_start_max, expected }` (require two exact zero-based occurrences of the same key/value/`key=value` subpath to begin within inclusive bundle-relative offset ranges)
  - `MvccReadFilter::ProvenanceBundlePathMixedOccurrenceDistance { bundle, summary, left_occurrence_index, left_expected, right_occurrence_index, right_expected, distance }` (require two exact zero-based ordered contiguous occurrences of different key/value/`key=value` subpaths to begin an exact distance apart)
  - `MvccReadFilter::ProvenanceBundlePathMixedOccurrenceDistanceWithin { bundle, summary, left_occurrence_index, left_expected, right_occurrence_index, right_expected, min_distance, max_distance }` (require two exact zero-based ordered contiguous occurrences of different key/value/`key=value` subpaths to begin within an inclusive distance range)
  - `MvccReadFilter::ProvenanceBundlePathFirstMixedOccurrenceDistance { bundle, summary, left_expected, right_expected, distance }` (require the first ordered contiguous occurrences of two different key/value/`key=value` subpaths to begin an exact distance apart)
  - `MvccReadFilter::ProvenanceBundlePathFirstMixedOccurrenceDistanceWithin { bundle, summary, left_expected, right_expected, min_distance, max_distance }` (require the first ordered contiguous occurrences of two different key/value/`key=value` subpaths to begin within an inclusive distance range)
  - `MvccReadFilter::ProvenanceBundlePathLastMixedOccurrenceDistance { bundle, summary, left_expected, right_expected, distance }` (require the last ordered contiguous occurrences of two different key/value/`key=value` subpaths to begin an exact distance apart)
  - `MvccReadFilter::ProvenanceBundlePathLastMixedOccurrenceDistanceWithin { bundle, summary, left_expected, right_expected, min_distance, max_distance }` (require the last ordered contiguous occurrences of two different key/value/`key=value` subpaths to begin within an inclusive distance range)
  - `MvccReadFilter::ProvenanceBundlePathFirstMixedOccurrenceToOccurrenceDistance { bundle, summary, left_expected, right_occurrence_index, right_expected, distance }` (require the first occurrence of one key/value/`key=value` subpath and an exact zero-based later occurrence of another to begin an exact distance apart)
  - `MvccReadFilter::ProvenanceBundlePathFirstMixedOccurrenceToOccurrenceDistanceWithin { bundle, summary, left_expected, right_occurrence_index, right_expected, min_distance, max_distance }` (require the first occurrence of one key/value/`key=value` subpath and an exact zero-based later occurrence of another to begin within an inclusive distance range)
  - `MvccReadFilter::ProvenanceBundlePathOccurrenceToLastMixedDistance { bundle, summary, left_occurrence_index, left_expected, right_expected, distance }` (require an exact zero-based occurrence of one key/value/`key=value` subpath and the last occurrence of another to begin an exact distance apart)
  - `MvccReadFilter::ProvenanceBundlePathOccurrenceToLastMixedDistanceWithin { bundle, summary, left_occurrence_index, left_expected, right_expected, min_distance, max_distance }` (require an exact zero-based occurrence of one key/value/`key=value` subpath and the last occurrence of another to begin within an inclusive distance range)
  - `MvccReadFilter::ProvenanceBundlePathFirstMixedOccurrenceToOccurrenceAt { bundle, summary, left_expected, left_start, right_occurrence_index, right_expected, right_start }` (require the first occurrence of one key/value/`key=value` subpath plus an exact zero-based later occurrence of another to begin at exact bundle-relative offsets)
  - `MvccReadFilter::ProvenanceBundlePathFirstMixedOccurrenceToOccurrenceWithin { bundle, summary, left_expected, left_start_min, left_start_max, right_occurrence_index, right_expected, right_start_min, right_start_max }` (require the first occurrence of one key/value/`key=value` subpath plus an exact zero-based later occurrence of another to begin within inclusive bundle-relative offset ranges)
  - `MvccReadFilter::ProvenanceBundlePathOccurrenceToLastMixedAt { bundle, summary, left_occurrence_index, left_expected, left_start, right_expected, right_start }` (require an exact zero-based occurrence of one key/value/`key=value` subpath plus the last occurrence of another to begin at exact bundle-relative offsets)
  - `MvccReadFilter::ProvenanceBundlePathOccurrenceToLastMixedWithin { bundle, summary, left_occurrence_index, left_expected, left_start_min, left_start_max, right_expected, right_start_min, right_start_max }` (require an exact zero-based occurrence of one key/value/`key=value` subpath plus the last occurrence of another to begin within inclusive bundle-relative offset ranges)
  - `MvccReadFilter::ProvenanceBundlePathMixedOccurrenceAt { bundle, summary, left_occurrence_index, left_expected, left_start, right_occurrence_index, right_expected, right_start }` (require exact zero-based ordered contiguous mixed subpath occurrences to begin at exact bundle-relative offsets)
  - `MvccReadFilter::ProvenanceBundlePathMixedOccurrenceWithin { bundle, summary, left_occurrence_index, left_expected, left_start_min, left_start_max, right_occurrence_index, right_expected, right_start_min, right_start_max }` (require exact zero-based ordered contiguous mixed subpath occurrences to begin within inclusive bundle-relative offset ranges)
  - `MvccReadFilter::ProvenanceBundlePathFirstMixedOccurrenceAt { bundle, summary, left_expected, left_start, right_expected, right_start }` (require the first ordered contiguous mixed subpath occurrences to begin at exact bundle-relative offsets)
  - `MvccReadFilter::ProvenanceBundlePathFirstMixedOccurrenceWithin { bundle, summary, left_expected, left_start_min, left_start_max, right_expected, right_start_min, right_start_max }` (require the first ordered contiguous mixed subpath occurrences to begin within inclusive bundle-relative offset ranges)
  - `MvccReadFilter::ProvenanceBundlePathLastMixedOccurrenceAt { bundle, summary, left_expected, left_start, right_expected, right_start }` (require the last ordered contiguous mixed subpath occurrences to begin at exact bundle-relative offsets)
  - `MvccReadFilter::ProvenanceBundlePathLastMixedOccurrenceWithin { bundle, summary, left_expected, left_start_min, left_start_max, right_expected, right_start_min, right_start_max }` (require the last ordered contiguous mixed subpath occurrences to begin within inclusive bundle-relative offset ranges)
  - `MvccReadFilter::ProvenanceBundlePathSegmentEquals { bundle, summary, index, expected }` (bundle-relative positional equality over named key/value/`key=value` provenance bundles)
  - `MvccReadFilter::ProvenanceBundleLenEquals { bundle, expected_len }` (exact whole-bundle cardinality checks over named provenance bundles)
  - `MvccReadFilter::All([...])`
  - `MvccReadFilter::Any([...])`
- Supported projections:
  - `MvccProjection::KeyValue`
  - `MvccProjection::KeyOnly`
  - `MvccProjection::ValueOnly`
  - `MvccProjection::BranchLabelTargetValue` (branch-aware projection that mirrors the resolved branch label into `key` while surfacing the resolved target row's value in `value`)
  - `MvccProjection::SourceKeyTargetValue` (join-adjacent result shape that mirrors the original seed key into `key` while projecting the resolved target row's value)
  - `MvccProjection::SourceValueOnly` (join-adjacent result shape that projects only the original seed row's value)
  - `MvccProjection::TargetKeySourceValue` (join-adjacent result shape that keeps the target key while projecting the original seed row's value)
  - `MvccProjection::TargetKeyProvenanceValue { frame }` (join-adjacent result shape that keeps the resolved target key while projecting the value from an explicit provenance frame such as `Seed`, `TerminalInput`, or `ValueHop(n)`)
  - `MvccProjection::TargetKeyProvenanceSummary { summary }` (join-adjacent result shape that keeps the resolved target key while projecting a lightweight provenance-path summary such as joined keys, joined values, or joined `key=value` hops)
  - `MvccProjection::TargetKeyProvenanceBundleSummary { bundle, summary }` (join-adjacent result shape that keeps the resolved target key while projecting a lightweight summary of a named provenance-frame bundle such as `SeedThroughTerminalInput` or `FullPath`)
  - `MvccProjection::TargetKeyProvenanceBundleOccurrenceOffset { bundle, summary, expected, occurrence }` (join-adjacent result shape that keeps the resolved target key while projecting the first, last, or exact zero-based occurrence offset of a named key/value/`key=value` bundle subpath)
  - `MvccProjection::TargetKeyProvenanceBundleOccurrenceDistance { bundle, summary, left_expected, left_occurrence, right_expected, right_occurrence }` (join-adjacent result shape that keeps the resolved target key while projecting the scalar distance between two selected key/value/`key=value` bundle-subpath occurrences, including mixed-subpath pairs)
  - `MvccProjection::TargetKeyProvenanceBundleMixedOccurrenceOffsetPair { bundle, summary, left_expected, left_occurrence, right_expected, right_occurrence }` (join-adjacent result shape that keeps the resolved target key while projecting a stable `left,right` pair of mixed-subpath bundle-relative offsets)
- Result row shape:
  - `MvccReadRow { source_key, key, value }`
  - `source_key` is populated for join-adjacent expansion sources so source-preserving joins can keep seed provenance visible while join-side projections reuse the same engine-facing result contract.
- Supported ordering:
  - `MvccReadOrder::KeyAsc`
  - `MvccReadOrder::KeyDesc`
  - `MvccReadOrder::ValueAsc`
  - `MvccReadOrder::ValueDesc`
  - `MvccReadOrder::BranchLabelAsc`
  - `MvccReadOrder::BranchLabelDesc`
  - `MvccReadOrder::SourceKeyAsc`
  - `MvccReadOrder::SourceKeyDesc`
  - `MvccReadOrder::SourceValueAsc`
  - `MvccReadOrder::SourceValueDesc`
  - `MvccReadOrder::ProvenanceKeyAsc { frame }`
  - `MvccReadOrder::ProvenanceKeyDesc { frame }`
  - `MvccReadOrder::ProvenanceValueAsc { frame }`
  - `MvccReadOrder::ProvenanceValueDesc { frame }`
  - `MvccReadOrder::ProvenanceBundleKeyPathAsc { bundle }`
  - `MvccReadOrder::ProvenanceBundleKeyPathDesc { bundle }`
  - `MvccReadOrder::ProvenanceBundleValuePathAsc { bundle }`
  - `MvccReadOrder::ProvenanceBundleValuePathDesc { bundle }`
  - `MvccReadOrder::ProvenanceBundlePathOccurrenceOffsetAsc { bundle, summary, expected, occurrence }`
  - `MvccReadOrder::ProvenanceBundlePathOccurrenceOffsetDesc { bundle, summary, expected, occurrence }`
  - `MvccReadOrder::ProvenanceBundlePathOccurrenceDistanceAsc { bundle, summary, left_expected, left_occurrence, right_expected, right_occurrence }`
  - `MvccReadOrder::ProvenanceBundlePathOccurrenceDistanceDesc { bundle, summary, left_expected, left_occurrence, right_expected, right_occurrence }`
  - `MvccReadOrder::ProvenanceBundlePathMixedOccurrenceOffsetPairAsc { bundle, summary, left_expected, left_occurrence, right_expected, right_occurrence }`
  - `MvccReadOrder::ProvenanceBundlePathMixedOccurrenceOffsetPairDesc { bundle, summary, left_expected, left_occurrence, right_expected, right_occurrence }`
- Optional row cap:
  - `limit: Some(n)` applies after visibility + filter + ordering stages
- Current device strategy:
  - planned target: GPU default device
  - executed target: CPU reference semantics via `ScanOperator` → `FilterOperator` → `SortOperator` → `LimitOperator` → `ProjectOperator`
  - fallback reason: `FallbackReason::GpuMvccReadParityGap` (`GPU-123`)
  - CUDA transition primitives: `CudaDriverRuntime::filter_all_mask(...)`, `filter_equal_u32_mask(...)`, `filter_equal_bytes_mask(...)`, `filter_bytes_range_mask(...)`, `mvcc_row_batch_lengths(...)`, and `mvcc_visibility_mask(...)` now prove real device mask generation plus row-batch H2D -> kernel -> D2H inspection paths; filterless reads, exact `ValueEquals`, key-prefix filters, general key ranges, key ordering for full-scan/key-lookup shapes, single-frame provenance key-prefix/value-equality filters, provenance bundle key/value/key-value equality, counted membership, key-prefix filters, stable bundle path predicates (`PathEquals`, `PathContains`, `PathCountAtLeast`, `PathPairAtDistance`, path prefix/suffix/slice/segment equality, and bundle length), and same-subpath plus mixed-subpath occurrence path predicates over CPU-resolved provenance rows, nested `All`/`Any` trees composed from supported predicates, provenance order/projection over CPU-resolved provenance rows after CUDA mask execution, CUDA-backed `Concat` over CUDA-resolvable native child sources including nested set/multiset composition, native distinct/intersect/except/symmetric-difference composition over CUDA-resolved child sources, native `FollowValueChain` / branch fan-in / labeled-branch source resolution, and device-side MVCC visibility masking over supported first-slice shapes can report GPU execution on CUDA-capable hardware. Supported full scans, key lookups, first key-batch lookups, value-chain branch sources, and native composition children feed all MVCC versions to CUDA before source/visibility masking; fallback re-resolves CPU-visible rows to preserve reference correctness.
- Deterministic workload fixtures:
  - `tests/fixtures/mvcc-read-workload.txt`
  - `tests/fixtures/mvcc-full-scan-workload.txt`
  - `tests/fixtures/mvcc-source-composition-workload.txt`
- Next obvious extension boundary:
  - no further MVCC surface growth is required for the no-GPU bootstrap by default; the next step is bootstrap closeout / CUDA-transition prep using the deterministic point-lookup, full-scan/simple-filter, and source-composition fixtures, explicit fallback truth, and documented engine-facing contract.
  - supported point-lookup and full-scan/simple-filter fixture shapes already have explicit backend-swap parity regressions, so `CudaBackend` can be attached and judged against CPU truth without rewriting `MvccReadQuery`, `MvccReadResult`, or `MvccReadRow`.

## Quickstart

```bash
cargo fmt --all
cargo clippy --all-targets --all-features -- -D warnings
cargo test --all-features
scripts/run_cuda_parity.sh
```

## Command notes

- `SET key=value`, `SET key TO value`, `SET LOCAL key=value`, and `SET SESSION key=value` are equivalent.
- `DEL key`, `DELETE key`, and `DELETE FROM key` are equivalent.
- `BEGIN|COMMIT|ROLLBACK` also accept `WORK` and `TRANSACTION` aliases.
- `COMMIT|ROLLBACK|END ... AND [NO] CHAIN` forms are accepted; `AND CHAIN` reopens transaction context by allocating a fresh transaction id after the terminal transition.
- `END` maps to `COMMIT`; `ABORT` maps to `ROLLBACK`.
- `START TRANSACTION` and `START WORK` map to `BEGIN`; optional `READ ONLY` / `READ WRITE`, `[NOT] DEFERRABLE`, and `ISOLATION LEVEL {SERIALIZABLE|REPEATABLE READ|READ COMMITTED|READ UNCOMMITTED}` suffixes are accepted on `BEGIN`/`START` aliases (including comma-separated mode lists) and currently map to plain `BEGIN` behavior.
- `CHECKPOINT`, `FLUSH`, `FLUSH WAL`, `FLUSH LOG`, `FLUSH WRITE AHEAD`, `FLUSH WRITE AHEAD {LOG|WAL}`, `FLUSH WRITE-AHEAD`, `FLUSH WRITE-AHEAD {LOG|WAL}`, `FLUSH WRITEAHEAD`, `FLUSH WRITEAHEAD {LOG|WAL}`, `FLUSH WRITE_AHEAD`, `FLUSH WRITE_AHEAD {LOG|WAL}`, `FLUSH WRITE_AHEAD_LOG`, and `FLUSH WRITE_AHEAD_WAL` are equivalent admin/coordination commands and are tracked as CPU fallback metric events.
- Replication backlog blocker labels can be decoded from delimited text streams (CSV-like, multiline, and JSON-like arrays with quoted labels).
- `RESET {ALL|ROLE|AUTHORIZATION|AUTH|SESSION AUTHORIZATION|SESSION AUTH}`, `DISCARD {ALL|TEMP|TEMPORARY|TEMP TABLE[S]|TEMPORARY TABLE[S]|PLANS|SEQUENCES}`, `DEALLOCATE {ALL|name|PREPARE|PREPARED name}`, `SET ROLE {NONE|DEFAULT|name}` (including quoted names), `SET SESSION AUTHORIZATION value` / `SET SESSION AUTH value` (including quoted names), `SET TRANSACTION ...`, `SET SESSION CHARACTERISTICS AS TRANSACTION ...`, `CLOSE {ALL|name}`, `LISTEN channel`, `NOTIFY channel[, payload]`, `UNLISTEN [*|ALL|channel]`, and `SELECT pg_advisory_unlock_all()` are accepted as CPU-routed session-control no-ops in the bootstrap engine so PostgreSQL-style client reset probes do not fail parser validation.
- P1 relational SQL bootstrap support is intentionally narrow and structured: `CREATE TABLE name (column INT|INTEGER|TEXT, ...)`, `INSERT INTO name (columns...) VALUES (...)`, `UPDATE name SET column = literal [, ...] WHERE column (=|<|<=|>|>=) literal | column BETWEEN literal AND literal | column IN (literal, ...) | text_column LIKE 'prefix%' [AND ...] [OR ...]`, `DELETE FROM name WHERE column (=|<|<=|>|>=) literal | column BETWEEN literal AND literal | column IN (literal, ...) | text_column LIKE 'prefix%' [AND ...] [OR ...]`, and `SELECT {*|columns} FROM name [WHERE column (=|<|<=|>|>=) literal | column BETWEEN literal AND literal | column IN (literal, ...) | text_column LIKE 'prefix%' [AND ...] [OR ...]] [ORDER BY column [ASC|DESC]] [LIMIT n] [OFFSET n]`. Engine-facing relational reads lower through the MVCC read path with CPU reference execution and explicit `GpuMvccReadParityGap` fallback where SQL finalization still exceeds the bridge; engine-facing relational updates and deletes use the same WAL-before-visibility mutation path and MVCC version/tombstone semantics, with WAL replay rebuilding the visible table state. The psql compatibility endpoint exercises the same parsed command shapes for real-client golden coverage. Joins, expressions beyond supported literal comparisons/ranges/membership/prefix predicates and literal assignments, `NOT BETWEEN`, `NOT IN`, subquery `IN`, NULLs/defaults, update expressions/arithmetic, schema-qualified names beyond supported `public` aliases, constraints, broad type coercion, and catalog introspection are still outside this P1 slice.
- P2 catalog work has started with an engine-owned `public` schema catalog. Created relational tables receive stable user relation OIDs starting at `16384`; columns receive stable ids, `attnum`, table OID references, and PostgreSQL-compatible type metadata for `int4` (OID `23`, fixed width `4`) and `text` (OID `25`, variable width). This metadata is now used by relational insert validation, row decoding, catalog-bound select projection/filter/order planning, result column descriptions, and bootstrap recovery from the durable WAL prefix. The compatibility endpoint also has dynamic first-slice `pg_catalog.pg_class`/`pg_catalog.pg_attribute` introspection for supported session tables, including relation OIDs plus column `attnum`/`atttypid`/`attlen` metadata, first-slice `pg_catalog.pg_type` introspection for the supported `int4`/`text` type registry, `pg_catalog.pg_tables` discovery, direct `pg_catalog.pg_namespace` lookup for the supported `public` namespace, empty `pg_catalog.pg_indexes` discovery for the current no-SQL-index subset, joined `pg_catalog.pg_class` / `pg_catalog.pg_namespace` relation metadata including table-name `IN (...)` subset filtering and namespace-only `public` relation lookup, and joined `pg_catalog.pg_attribute` / `pg_catalog.pg_class` / `pg_catalog.pg_namespace` column metadata with formatted supported type names and nullable flags for supported `public` session tables, real `psql \dt` / `\dt+` / `\dt+ <table>` / `\dt <prefix>*` / `\dt+ <prefix>*` / `\dt public.*` / `\dt+ public.*` / `\dt public.<prefix>*` / `\dt+ public.<table>` / `\dt+ public.<prefix>*` / `\d <table>` / `\d+ <table>` / `\d <prefix>*` / `\d+ <prefix>*` / `\d public.*` / `\d public.<prefix>*` / `\d+ public.<prefix>*` / `\dp <table>` / `\z public.<pattern>` coverage including schema-qualified `public.<table>` describes, real `psql \di` empty index-listing coverage, `\dv` / `\dv+` empty view-listing coverage, `\dm` / `\dm+` plus `\ds` / `\ds+` empty materialized-view/sequence-listing coverage, `\df` empty function-listing coverage, `\da` empty aggregate-listing coverage, `\dc` empty conversion-listing coverage, `\do` empty operator-listing coverage, `\dO` empty collation-listing coverage, `\dC` empty cast-listing coverage, `\dRp` empty publication-listing coverage, `\dRs` empty subscription-listing coverage, `\ddp` empty default-access-privilege coverage, `\dd` empty object-description coverage, `\dx` empty extension-listing coverage, `\dL` empty procedural-language-listing coverage, and `\dD` / `\dD+` empty domain-listing coverage for the current no-SQL-index/no-SQL-view/no-materialized-view/no-sequence/no-user-defined-function/no-user-defined-aggregate/no-conversion/no-user-defined-operator/no-user-defined-collation/no-user-defined-cast/no-publication/no-subscription/no-default-ACL/no-object-description/no-extension/no-procedural-language/no-domain subset, `\du` role-listing coverage for the bootstrap `postgres` role, `\l` database-listing coverage for the bootstrap `postgres` database, and `\db` tablespace-listing coverage for the bootstrap `pg_default` / `pg_global` metadata, plus `\dA` access-method listing coverage for supported heap table access metadata, real `psql \dn` and `\dn+ public` public-schema coverage, real `psql \dT pg_catalog.int4` / `\dT pg_catalog.text` supported-type coverage, `\dT pg_catalog.*` / `\dT+ pg_catalog.*` supported built-in type-listing coverage, and first-slice `information_schema.tables` / `information_schema.columns` / `information_schema.schemata` / `information_schema.table_constraints` / `information_schema.key_column_usage` plus `pg_catalog.pg_constraint`, `pg_catalog.pg_attrdef`, and `pg_catalog.pg_description` coverage backed by session catalog metadata, including richer table catalog/insertability/type-shape fields, single-table and table-name `IN (...)` subset table discovery, exact and catalog-qualified exact table-name filtering for richer table metadata, table-name `IN (...)` subset column enumeration, per-table column detail projection, table-filtered, catalog-qualified table-filtered, and table-name `IN (...)` extended column metadata, all-supported-public-table column enumeration, supported column nullability/default/type-identity fields and `int4` numeric precision/radix/scale metadata, plus truthfully empty information-schema and pg-catalog constraint/default/comment rows for the current no-SQL-constraint/no-column-default/no-comment subset. `\dp`/`\z` currently report supported plain public tables with empty access/column privilege and policy fields because grants, column ACLs, and row policies are not implemented; `\ddp` and `\dd` return no rows until default ACL/object comment metadata exists; verbose schema and table listings intentionally leave access privileges/description and heap size/description blank until those metadata surfaces exist. The current limitation is explicit: user-visible SQL indexes, SQL views, SQL materialized views, SQL sequences, SQL functions, SQL aggregates, SQL operators, user-defined collations, user-defined casts, encoding conversions, SQL publications, SQL subscriptions, SQL extensions, procedural languages, SQL constraints, column defaults, comments, object descriptions, domains, user-defined types, PostgreSQL heap-size/description metadata, database creation/templates/ACL mutation, default ACL mutation, role/ACL/policy mutation, tablespace creation/location/options, and advanced PostgreSQL catalog surfaces remain pending.
- Real PostgreSQL 16 plain `psql \d` relation listing now also works for supported `public` session tables, using the same session catalog rows as table-listing introspection while keeping unsupported relation kinds absent until the engine exposes them. Real `psql \dv` / `\dv+`, `\dm` / `\dm+`, `\ds` / `\ds+`, `\df`, `\da`, `\dc`, `\do`, `\dO`, `\dC`, `\dRp`, `\dRs`, `\ddp`, `\dd`, `\dx`, `\dL`, and `\dD` / `\dD+` listing traffic is covered as empty results for the current no-SQL-view/no-materialized-view/no-sequence/no-user-defined-function/no-user-defined-aggregate/no-conversion/no-user-defined-operator/no-user-defined-collation/no-user-defined-cast/no-publication/no-subscription/no-default-ACL/no-object-description/no-extension/no-procedural-language/no-domain subset; `\du` now lists the bootstrap `postgres` role through a narrow `pg_catalog.pg_roles` compatibility slice; `\l` lists the supported bootstrap `postgres` database through a narrow `pg_catalog.pg_database` compatibility slice; `\db` lists the bootstrap `pg_default` and `pg_global` tablespaces through a narrow `pg_catalog.pg_tablespace` compatibility slice; `\dA` lists the supported bootstrap `heap` table access method through a narrow `pg_catalog.pg_am` compatibility slice; `\dT pg_catalog.*` and `\dT+ pg_catalog.*` list the supported `int4`/`text` type registry through a narrow `pg_catalog.pg_type` compatibility slice.
- Common `information_schema.views` and `pg_catalog.pg_views` discovery for the supported `public` schema now return truthful empty results for the current no-SQL-view subset, keeping view definitions and view DDL out of scope.
- Common `information_schema.tables` base-table discovery that filters out `pg_catalog` and `information_schema` now returns supported `public` session tables from catalog metadata.
- Common `information_schema.columns` column discovery that filters out `pg_catalog` and `information_schema` now returns supported `public` session-table columns from catalog metadata.
- Real PostgreSQL 16 `psql \dt *.*` and `\dt+ *.*` all-schema table listing now returns supported `public` session tables from catalog metadata, while broader namespace behavior remains bounded to the current supported schema/table subset.
- Real PostgreSQL 16 `psql \d *.*` and `\d+ *.*` all-schema relation description now returns supported `public` session-table relation rows from catalog metadata and reuses the existing per-table describe probes for supported column/type display, including verbose column storage/access-method fields for `\d+`, while broader namespace and relation-kind behavior remains bounded to the current supported subset.
- Real PostgreSQL 16 `psql \d+ public.*` verbose schema-wildcard relation description is pinned by golden coverage for supported `public` session tables, using the namespace-scoped relation lookup plus metadata-backed verbose per-table describe probes.
- Real PostgreSQL 16 `psql \dt public.<table>` exact schema-qualified table listing is pinned by golden coverage for supported `public` session tables, returning only the requested table from catalog metadata.
- Real PostgreSQL 16 `psql \dt+ public.*` verbose schema-wildcard table listing is pinned by golden coverage for supported `public` session tables, returning catalog-backed persistence/access-method fields while heap size/description remain blank until those metadata surfaces exist.
- P3 wire-protocol work has started with a narrow real extended-query path in the compatibility endpoint. `Parse`, `Bind`, `Describe`, `Execute`, `Sync`, and `Close` now support session-local prepared statements and portals for text-format `int4`/`text` parameters and text-format results over the current relational `SELECT` subset; statement close cascades to dependent portals and invalidates later Execute attempts against those portals, portal close stays local while leaving the prepared statement reusable and later missing-portal Execute recovery intact, nonexistent extended statement/portal `Close` targets return PostgreSQL-compatible `CloseComplete` without entering error recovery, extended `Close Statement` does not remove SQL `PREPARE` state, named prepared statement and named portal duplicates return PostgreSQL-compatible duplicate-object errors before payload validation while preserving existing portal state, SQL `PREPARE` duplicate names are rejected before validating an unsupported replacement payload while preserving the original SQL prepared statement, unnamed prepared statements and unnamed portals remain replaceable, missing `Bind` target statements return `26000` before parameter/result format validation can mask the lifecycle error, bind parameter arity and supported `int4` text-input syntax are checked at Bind time against the prepared query placeholder/type contract without installing a bad portal, text parameter/result format code lists must be zero codes, one broadcast code, or one code per parameter/result column, invalid UTF-8 text bind values and unsupported NULL bind values return stable SQLSTATE errors without installing the portal, omitted Parse parameter OIDs are inferred for supported catalog-backed SELECT predicates/limits while quoted `$n` string literals and commented `$n` tokens are not counted as protocol parameters, Bind substitution now replaces unquoted `$n` placeholder tokens by numeric identity so multi-digit placeholders such as `$10` cannot be corrupted by `$1` and zero-padded nonzero placeholders such as `$01` bind as parameter 1, supported casted placeholders such as `$1::int4`, `$2::text`, casted `LIMIT` parameters, and `pg_catalog`-qualified forms execute as the same text-format `int4`/`text` subset, unquoted zero-numbered placeholders such as `$0` and `$00` are rejected at Parse time without installing a statement, oversized Parse parameter-OID lists are rejected before statement installation so `Describe Statement` cannot advertise phantom parameters, unsupported parameter OIDs, oversized parameter-OID lists, unsupported non-DML non-SELECT statements, and broader unsupported parameterized SELECT shapes are rejected at Parse time without installing bad prepared statements, described portals execute data rows without replaying `RowDescription`, matching the extended-protocol boundary where explicit `Describe` owns result metadata, bound parameterized portals and unbound parameterized statements can be described through real `psql \gdesc` flows with catalog-backed result metadata, real `psql \bind ... \gset` can consume supported extended SELECT rows into client variables, real `psql \bind ... \gx` can render supported extended SELECT rows through psql expanded output, and real `psql \bind ... \crosstabview` can consume supported extended SELECT rows through psql crosstab rendering, nonzero `Execute.max_rows` batches supported relational `SELECT` portals with `PortalSuspended` until later `Execute` frames consume the remaining rows, final `CommandComplete` reports the portal completed-row count while repeated executes after exhaustion report zero rows, `Describe Portal` after a suspended max-row execution returns metadata without rewinding or advancing the portal cursor, and unlimited `Execute.max_rows=0` now also advances the same portal result cursor to exhaustion instead of replaying rows on later Execute frames. Extended-query errors skip later messages until `Sync` so clients do not receive duplicate errors from a failed pipeline before ReadyForQuery recovery; raw Parse failures, missing `Describe Statement` / `Describe Portal`, missing `Execute` portal targets, and mismatched parameter/result format-code counts are pinned to that same recovery boundary with stable `26000` / `34000` / `08P01` errors. Real PostgreSQL 16 `psql` `FETCH_COUNT` cursor flow is also covered for supported relational `SELECT` statements through session-local `DECLARE ... CURSOR FOR SELECT`, `DECLARE ... CURSOR WITHOUT HOLD FOR SELECT`, `DECLARE ... ASENSITIVE/INSENSITIVE ... CURSOR ... FOR SELECT`, `FETCH FORWARD n`, `FETCH NEXT`, omitted-count `FETCH FORWARD`, `FETCH ... IN`, separator-less forward `FETCH` forms, `FETCH FORWARD ALL`, `FETCH ALL`, zero-count `FETCH FORWARD 0`, transaction-end cleanup for session-local cursors, unquoted cursor identifier case-folding and quoted cursor identifier preservation including semicolons and doubled-quote escapes across `DECLARE`/`FETCH`/`MOVE`/`CLOSE`, forward-only `MOVE` including separator-less and zero-count forms, cursor `CLOSE`, duplicate cursor declaration rejection that preserves the existing cursor, missing named-cursor close errors, and `CLOSE ALL` handling backed by the same SELECT result path; `FETCH_COUNT` combined with unresolved `\bind` placeholders, non-forward cursor/fetch/move directions, and unsupported `BINARY`/`SCROLL`/`WITH HOLD` cursor declaration options are explicitly rejected as unsupported cursor traffic and the session remains usable afterward. Real PostgreSQL 16 `psql \gdesc` result-description flow is covered for supported relational `SELECT` statements, including the follow-up `pg_catalog.format_type` formatter query over described result OIDs for the supported `int4`/`text` type subset; SQL-prepared extended `EXECUTE <prepared>(literal)` descriptions now return precise literal parameter validation errors and recover for later `\gdesc`, execution, deallocation, and supported SELECT traffic. Real `psql` extended parameterized `INSERT`, filtered `UPDATE`, and filtered `DELETE` are covered; parameterized JOIN SELECT and extended `COPY ... TO STDOUT` including leading-comment copy-out, unquoted zero-numbered placeholders, zero-padded nonzero placeholder binding, apostrophe-bearing text bind execution, ignored line/nested-block commented placeholder tokens, omitted-OID inferred text binds, mixed omitted-OID `int4`/`text` conjunction and top-level `OR` binds, reused inferred placeholders across multiple supported predicates, and commuted omitted-OID placeholders such as `$1 <= id` / `$2 = name`, and supported casted placeholders such as `$1::int4` / `$2::text` plus casted `LIMIT` placeholders are now explicitly covered while preserving later supported SELECT traffic in the same session. Minimal binary `int4`/`text` parameter/result formats are supported for selected driver traffic; broader binary formats/types, unsolicited frontend COPY data frames, frontend FunctionCall flow, and broader portal/cursor behavior still return explicit unsupported-feature errors. The psql golden suite includes PostgreSQL 16-compatible `\bind`, successful text-parameter bind plus placeholder-looking text, quoted placeholder-looking SQL literals, multi-digit placeholder tokens, zero-padded nonzero placeholder binding, apostrophe-escaped text bind literals, commented placeholder-looking tokens, and zero-numbered placeholder rejection/recovery, inferred text predicates, inferred mixed `int4`/`text` conjunction and top-level `OR` predicates, reused inferred placeholders, commuted inferred placeholders, casted bind placeholders including casted `LIMIT` parameters, inferred predicate-plus-`LIMIT` parameters, parameterized statement `\gdesc`, bound-result `\gset`, bound expanded-output `\gx`, bound crosstab rendering via `\crosstabview`, empty-result execution, repeated unnamed rebind execution, and bound-portal `\gdesc`, bind parameter-count mismatch recovery, invalid `int4` bind-value recovery, invalid bind execution followed by later bound-portal `\gdesc` recovery, `FETCH_COUNT`, `FETCH NEXT`, omitted-count `FETCH FORWARD`, `FETCH ... IN`, separator-less forward `FETCH`, `FETCH FORWARD ALL`, `FETCH ALL`, zero-count cursor movement, transaction-end cleanup for session-local cursors, unquoted cursor identifier case-folding, quoted cursor identifier preservation including semicolons and doubled-quote escapes, forward-only `MOVE` including separator-less forms, non-forward cursor rejection, unsupported cursor declaration options, cursor `WITHOUT HOLD`, cursor `ASENSITIVE`/`INSENSITIVE`, `CLOSE ALL`, duplicate cursor declaration rejection, missing named cursor `CLOSE`, `\gdesc`, inferred-bind-type error/recovery, duplicate SQL `PREPARE` precedence over unsupported replacement payload validation, unsupported parameterized cursor, extended DML, unsupported extended JOIN SELECT, and unsupported COPY option recovery scenarios that reach real client extended/cursor/error paths; named parse/bind duplicate semantics including duplicate portal precedence over malformed payloads, missing raw `Bind` target statements before binary format validation, supported raw binary `int4`/`text` parameter/result `Bind` coverage, malformed binary `int4` payload rejection, and malformed parameter/result format-count skip-until-`Sync` recovery, raw Parse unsupported-parameter, oversized-OID, zero-placeholder, and unsupported-statement errors with skip-until-`Sync` recovery, missing raw `Describe` and `Execute` targets with skip-until-`Sync` recovery, missing raw `Close` targets completing without error recovery, SQL-prepared-state isolation and dependent-portal invalidation for extended `Close Statement`, successful raw `Close Portal` cleanup plus later missing-portal Execute recovery, invalid raw UTF-8 and NULL bind payloads, mismatched raw parameter/result-format code counts with skip-until-`Sync` recovery, unlimited raw Execute exhaustion, raw bounded Execute final completion counts, raw portal `Describe` after bounded Execute suspension preserving resume position, raw frontend `CopyData`/`CopyDone`/`CopyFail` skip-until-`Sync` recovery, raw frontend `FunctionCall` skip-until-`Sync` recovery with skipped side effects, and FunctionCall frame parsing remains covered by helper-level lifecycle/error-path tests because they are client-version dependent or not emitted by `psql`.
- P3 raw extended-protocol cursor declaration support now covers `Parse`/`Bind`/`Execute` for `DECLARE ... CURSOR FOR SELECT` over the supported relational `SELECT` subset with text-format `int4`/`text` parameters. Helper-level wire coverage also pins bound cursor-declaration portal `Describe` as `NoData`, rejects malformed multi-entry result-format vectors for those `NoData` portals before installation while accepting valid text-format broadcast shape, rejects explicit Parse OIDs that conflict with SQL-prepared cursor-declaration parameter inference before statement installation, and proves extended portal/statement cleanup leaves the installed session-local cursor fetchable. Real PostgreSQL 16 `psql \bind` now pins `DECLARE ... CURSOR FOR EXECUTE <sql-prepared>($1)` through the same `NoData` Describe and later Execute installation path; the `FETCH_COUNT` plus `\bind` unresolved-placeholder edge remains an explicit unsupported scenario and now also proves no `_psql_cursor` is installed before later cursor-style SELECT recovery, including unsupported `\gset` and `\gx` result-consumer variants plus supported `\crosstabview` and generated-SQL `\gexec` cleanup. Real `psql` `FETCH_COUNT` over SQL-prepared `EXECUTE <name>(...)` now also pins invalid argument recovery, plain plus `\gdesc` / `\gset` / `\gx` / `\crosstabview` / `\gexec` argument-count mismatch cleanup, negative-`LIMIT` cleanup including argument-bearing `\gset` and `\gexec`, the zero-parameter literal form, and zero-parameter `\gdesc` metadata-only plus `\gset` / `\gx` / `\crosstabview` / `\gexec` failed-cursor variants, empty-result cleanup, and extended-bound `EXECUTE <name>($1) \bind ... \g` / `\gset` / `\gx` / `\crosstabview` / `\gexec` invalid-bind cleanup, proving failed cursor declarations leave no transient `_psql_cursor`, describe-only traffic does not execute bad limits, generated-SQL execution is skipped on bad limits, client-variable capture is skipped on failed negative limits, and deallocation or later valid execution remains stable as applicable.
- P3 real-client `FETCH_COUNT` cursor lifecycle coverage now also pins the `_psql_cursor` name-collision path: if a user-visible session cursor already owns psql's transient cursor name, a later `FETCH_COUNT` SELECT gets the stable duplicate-cursor error, psql cleanup observes the cursor as absent, and later supported SELECT traffic in the same session still recovers.
- P3 real-client `FETCH_COUNT` empty-result coverage now also pins direct supported relational `SELECT` traffic: an empty cursor-style SELECT closes psql's transient `_psql_cursor`, a manual fetch probe reports the expected missing-cursor error, and later supported cursor-style and plain SELECT traffic in the same session still recovers.
- P3 real-client `FETCH_COUNT` generated-SQL coverage now also pins SQL-prepared empty-source `EXECUTE <name>(...) \gexec` and zero-parameter `EXECUTE <name> \gexec`: empty generated-command results leave psql's transient `_psql_cursor` absent, preserve the prepared statement for later execution and deallocation, and recover for later supported SELECT traffic.
- P3 real-client `FETCH_COUNT` generated-SQL coverage now also pins direct `SELECT ... \gexec`, SQL-prepared `EXECUTE <name>(...) \gexec`, and extended-bound `SELECT ... $1 \bind ... \gexec` when the generated command is explicitly unsupported `COPY`, including argument-bearing prepared statements: the supported COPY TO STDOUT data is streamed without leaving transient cursor state, psql's transient `_psql_cursor` is absent afterward, and later supported SELECT traffic recovers through plain, SQL-prepared, or extended-bound paths as appropriate.
- P3 real-client `FETCH_COUNT` SQL-prepared cleanup coverage now includes zero-parameter `EXECUTE <name> \gset`, `\gx`, `\crosstabview`, and `\gdesc`, proving client-variable capture, expanded-output rendering, crosstab rendering, and metadata-only description consume or bypass psql's transient cursor, leave `_psql_cursor` absent, and preserve later `EXECUTE <name>()`, deallocation, and supported SELECT recovery.
- P3 real-client `FETCH_COUNT` non-empty-result coverage now pins the matching direct supported relational `SELECT` path: after batched row retrieval, psql closes its transient `_psql_cursor`, a manual fetch probe reports the expected missing-cursor error, and later supported cursor-style plus plain SELECT traffic still recovers.
- P3 real-client `FETCH_COUNT` describe-only coverage now pins the metadata path too: direct `SELECT ... \gdesc`, extended `SELECT ... $1 \bind ... \gdesc`, and SQL-prepared `EXECUTE <name>(...) \gdesc` under `FETCH_COUNT` return only result metadata, leave psql's transient `_psql_cursor` absent, report the expected missing-cursor error on a manual fetch probe, and recover for later extended, cursor-style, SQL-prepared, or plain SELECT traffic.
- P3 real-client `FETCH_COUNT` result-consumer coverage now also pins crosstab rendering alongside expanded output: `SELECT ... \gx` and `SELECT ... \crosstabview` under `FETCH_COUNT` consume supported relational rows, leave psql's transient `_psql_cursor` absent, report the expected missing-cursor error on a manual fetch probe, and recover for later cursor-style plus plain SELECT traffic. Extended-bound `\gset`, `\gx`, `\crosstabview`, and generated-SQL `\gexec` now also pin invalid `int4` bind input as no-bad-portal or no-transient-cursor recovery paths before later supported SELECT traffic. SQL-prepared `EXECUTE <name>(...) \gset` and extended-bound SQL-prepared `EXECUTE <name>($1) \bind ... \crosstabview` / `\gexec` prove the same cleanup boundary while preserving the prepared statement for later supported execution and deallocation.
- P3 real-client cursor declaration description now also pins `psql \gdesc` over supported `DECLARE ... CURSOR FOR SELECT` traffic: the endpoint returns `NoData` for the declaration description, tracks the described portal state internally, does not install the cursor during description, and later declaration/fetch/close plus supported SELECT recovery remain truthful.
- P3 cursor-declared SQL-prepared `EXECUTE` descriptions now reuse the same literal argument validation as direct SQL-prepared `EXECUTE ... \gdesc`: invalid inner `int4` literals return the precise parameter error instead of the generic unsupported-SELECT fallback, no cursor is installed, and later valid declaration/fetch/close plus supported SELECT recovery remain truthful.
- P3 cursor-declared SQL-prepared `EXECUTE` now also pins the negative-`LIMIT` Describe/Execute split for real `psql \bind`: `DECLARE ... CURSOR FOR EXECUTE <prepared>($1) \bind -1 \gdesc` returns `NoData` without installing the cursor, the later Execute returns the stable negative-limit error, and a valid cursor declaration/fetch/close plus deallocation and supported SELECT recovery still work.
- P3 direct cursor declarations now preserve the same negative-`LIMIT` error boundary as supported SELECT execution: real PostgreSQL 16 `psql` traffic for `DECLARE ... CURSOR FOR SELECT ... LIMIT -1` returns the stable `LIMIT must not be negative` error, installs no cursor, and a later valid cursor declaration/fetch/close plus supported SELECT traffic still recovers. Negative cursor counts (`FETCH -1` / `MOVE -1`) are also pinned as explicit unsupported non-forward cursor traffic that preserves cursor position for later forward movement, close, and SELECT recovery. Real `psql \gdesc` over unsupported `BINARY`/`SCROLL` cursor declarations now fails at extended Parse with the explicit cursor-option unsupported error instead of the generic extended-SELECT fallback, installs no bad cursor state, and preserves later supported cursor/SELECT recovery.
- P3 COPY handling keeps real PostgreSQL 16 `psql \gdesc` over `COPY ... TO STDOUT` explicit and unsupported: extended Parse returns the stable unsupported COPY error instead of the generic extended-SELECT fallback, installs no statement/portal state, and later supported SELECT traffic recovers.
- P3 SQL prepared statement follow-up coverage now pins inferred `LIMIT $n` parameters for supported relational `SELECT` statements: real PostgreSQL 16 `psql` traffic prepares and executes a limited result, rejects invalid limit input without losing the prepared statement, accepts a later casted limit argument, returns a stable PostgreSQL-style error for negative limit execution while preserving zero-limit and later valid-limit executions, deallocates cleanly, and recovers for later supported SELECT traffic. The extended `\bind` path now pins the same negative `LIMIT` error boundary while allowing `\gdesc` to describe supported relational result metadata before the later execution error, including SQL-prepared `EXECUTE <prepared>($1, $2)` portals, preserving zero-limit, later valid-limit, and SELECT recovery behavior.
- P3 SQL-prepared extended-bind coverage now pins SQL prepared `LIMIT` parameters through real extended `EXECUTE <prepared>($1, $2)` binds: Parse/Describe infer mixed `text`/`int4` outer placeholders, invalid limit text and negative limit execution recover without losing prepared state, zero-limit returns no rows, later valid execution succeeds, and deallocation plus SELECT recovery remain stable.
- P3 SQL-prepared extended-bind coverage now also pins reused placeholders in the SQL prepared statement itself: real PostgreSQL 16 `psql` traffic binds `EXECUTE <prepared>($1)` where the prepared relational `SELECT` uses `$1` in multiple predicates, describes the result with `\gdesc`, rejects invalid `int4` input without losing the prepared statement, executes a later valid bind, deallocates cleanly, and recovers for later supported SELECT traffic.
- P3 SQL-prepared extended-bind coverage now also pins reordered outer placeholders: real PostgreSQL 16 `psql` traffic binds `EXECUTE <prepared>($2, $1)`, proving extended Parse/Describe infer outer `text`/`int4` parameter OIDs from the SQL prepared argument mapping, invalid reordered `int4` input preserves prepared state, later execution succeeds, and SELECT recovery still works after deallocation.
- P3 SQL-prepared extended-bind coverage now also pins casted and parenthesized reordered outer placeholders: real PostgreSQL 16 `psql` traffic binds `EXECUTE <prepared>(($2)::pg_catalog.int4, ($1)::text)`, proving supported SQL `EXECUTE` argument normalization feeds the same remapped outer `text`/`int4` OID inference, invalid casted `int4` input preserves prepared state, later execution succeeds, and SELECT recovery still works after deallocation.
- P3 SQL-prepared extended-bind coverage now also pins mixed literal and placeholder SQL `EXECUTE` arguments: real PostgreSQL 16 `psql` traffic binds `EXECUTE <prepared>($1, 'literal')` and `EXECUTE <prepared>(literal, $1)`, proving Parse/Describe infer only the remaining outer parameter OID, invalid bound `int4` input preserves prepared state, later text-bound execution succeeds, and SELECT recovery still works after deallocation.
- P3 SQL-prepared extended-bind coverage now also rejects conflicting reused outer placeholders: real PostgreSQL 16 `psql` traffic with `EXECUTE <prepared>($1, $1)` against incompatible SQL-prepared `int4`/`text` arguments returns a stable parameter-type conflict before installing the extended statement, then a supported SQL-prepared extended bind and later SELECT traffic still recover.
- P3 SQL-prepared extended-bind coverage now also pins sparse outer placeholders: real PostgreSQL 16 `psql` traffic binds `EXECUTE <prepared>($2, 'literal')`, preserves a supplied but unused `$1` bind slot without bogus type coercion, rejects invalid used `int4` input, then executes a later valid sparse bind and recovers for SELECT traffic after deallocation.
- P3 SQL-prepared extended-bind coverage now also pins compatible reused outer placeholders: real PostgreSQL 16 `psql` traffic binds `EXECUTE <prepared>($1, $1)` against same-typed SQL-prepared `int4` arguments, describes the result, rejects invalid shared `int4` input without losing prepared state, executes matching and empty-result paths, deallocates cleanly, and recovers for later supported SELECT traffic.
- P3 SQL-prepared extended-bind coverage now also pins PostgreSQL escape string literals as mixed SQL `EXECUTE` arguments: real PostgreSQL 16 `psql` traffic binds `EXECUTE <prepared>(E'...', $1)`, decodes the supported `text` argument as data including simple backslash plus hex/octal escape forms, describes only the remaining outer `int4` bind, preserves invalid-input and malformed-escape recovery, deallocates cleanly, and recovers for later supported SELECT traffic.
- P3 SQL-prepared extended-bind coverage now also pins PostgreSQL newline-separated adjacent standard string literals as mixed SQL `EXECUTE` arguments: real PostgreSQL 16 `psql` traffic binds `EXECUTE <prepared>('Ada'\n' Lovelace', $1)` and typed `text 'Grace'\n' Hopper'`, decodes them as supported `text` data, rejects same-line adjacent string literals explicitly for this subset, preserves invalid bind recovery, deallocates cleanly, and recovers for later SELECT traffic.
- P3 SQL-prepared extended-bind coverage now also preserves newline-bearing SQL comments as whitespace between adjacent string literals: real PostgreSQL 16 `psql` traffic binds `EXECUTE <prepared>('Ada' /*\n... */ ' Lovelace', $1)` and line-comment-separated text literals, decodes them as supported `text` data, while no-newline comment-separated adjacent literals remain rejected and recoverable for the current subset.
- P3 SQL-prepared extended-bind coverage now also pins PostgreSQL `CAST(... AS ...)` arguments for the supported `int4`/`text` subset: real PostgreSQL 16 `psql` traffic binds `EXECUTE <prepared>(CAST($1 AS pg_catalog.int4), CAST('Ada' AS text))`, infers/describes only real outer placeholders, rejects unsupported cast target types explicitly, preserves invalid bind recovery, and recovers for later supported execution.
- P3 SQL-prepared extended-bind coverage now also pins PostgreSQL dollar-quoted text literals as mixed SQL `EXECUTE` arguments: real PostgreSQL 16 `psql` traffic binds `EXECUTE <prepared>($tag$...$tag$, $1)` and `EXECUTE <prepared>($$...$$, $1)`, decodes the supported `text` argument as data, describes only the remaining outer `int4` bind, preserves invalid-input recovery, deallocates cleanly, and recovers for later supported SELECT traffic.
- P3 SQL-prepared extended-bind coverage now also keeps PostgreSQL comment markers inside dollar-quoted SQL `EXECUTE` arguments as literal text: real PostgreSQL 16 `psql` traffic binds `EXECUTE <prepared>($tag$...--...$tag$, $1)` and `EXECUTE <prepared>($$.../*...*/...$$, $1)`, so comment stripping no longer corrupts supported `text` values before Parse/Describe inference or Execute recovery.
- P3 SQL-prepared extended-bind coverage now also keeps PostgreSQL cast markers inside dollar-quoted SQL `EXECUTE` arguments as literal text while still honoring an outer supported postfix cast: real PostgreSQL 16 `psql` traffic binds `EXECUTE <prepared>($tag$Ada::literal$tag$::text, ...)`, preserves `::` inside the dollar body as data, rejects unsupported outer cast targets explicitly, and recovers for later supported execution.
- P3 SQL-prepared extended-bind coverage now also keeps unmatched parentheses inside parenthesized dollar-quoted SQL `EXECUTE` arguments as literal text: real PostgreSQL 16 `psql` traffic binds `EXECUTE <prepared>(($tag$Ada (literal$tag$), $1)` and `CAST(($$Grace ) literal$$) AS text)`, proving argument normalization does not treat dollar-literal body parentheses as SQL grouping while preserving invalid-bind recovery and later SELECT recovery.
- P3 cursor coverage now also pins real PostgreSQL 16 `psql` `FETCH_COUNT` over SQL prepared execution: `PREPARE <name>(int4) AS SELECT ...`, `\set FETCH_COUNT 1`, and `EXECUTE <name>(...)` now install a session-local forward cursor over the supported SQL-prepared relational `SELECT` result, fetch rows truthfully, deallocate cleanly, and recover for later SELECT traffic.
- P3 cursor coverage now also pins zero-parameter quoted SQL prepared execution through `FETCH_COUNT`: `PREPARE "Exec Cursor Zero" AS SELECT ...` plus `EXECUTE "Exec Cursor Zero"` installs a session-local forward cursor over the supported SQL-prepared result. Raw extended `Parse`/`Bind`/`Describe`/`Execute` and real PostgreSQL 16 `psql \bind` now also accept `DECLARE ... CURSOR FOR EXECUTE <prepared>($1)` for the supported SQL-prepared relational `SELECT` subset, infer the outer `int4`/`text` bind metadata from SQL prepared state, describe the cursor-declaration portal as `NoData`, and install the session-local cursor at Execute time.
- P3 SQL-prepared extended-bind coverage now also pins PostgreSQL typed string literals for the supported `int4`/`text` subset: real PostgreSQL 16 `psql` traffic binds `EXECUTE <prepared>(text '...', $1)`, `EXECUTE <prepared>(pg_catalog.text $$...$$, $1)`, and `EXECUTE <prepared>($1, int4 '...')`, proving those typed literal arguments decode as data while remaining outer placeholders are inferred/described/executed with invalid-bind recovery.
- P3 SQL-prepared extended-bind coverage now also pins supported typed-literal aliases through the same real-client path: `TEXT '...'`, `integer '...'`, `pg_catalog.integer '...'`, and parenthesized `pg_catalog.integer '...'` SQL `EXECUTE` arguments stay inside the supported `int4`/`text` subset, preserve `\gdesc`, invalid-bind recovery, deallocation, and later SELECT recovery.
- P3 SQL-prepared extended-bind coverage now also pins PostgreSQL Unicode escape string text literals through the same real-client path: `U&'...'` SQL `EXECUTE` arguments decode supported default backslash and custom `UESCAPE` Unicode escapes as text data, infer/describe only the remaining outer placeholders, reject malformed Unicode escapes explicitly, and preserve later supported execution plus SELECT recovery.
- P3 SQL-prepared extended-bind coverage now also pins PostgreSQL national-character string literals through the same real-client path: `N'...'` and typed `text n'...'` SQL `EXECUTE` arguments decode as supported `text` data, infer/describe only the remaining outer `int4` placeholder, preserve invalid-bind recovery, deallocate cleanly, and recover for later SELECT traffic.
- P3 follow-up coverage now pins PostgreSQL-style directionless numeric cursor movement for the supported relational cursor subset: real PostgreSQL 16 `psql` traffic with `FETCH 0 FROM <cursor>`, `FETCH 2 FROM <cursor>`, and `MOVE 1 FROM <cursor>` preserves cursor position truthfully and still recovers for later supported SELECT traffic.
- P3 follow-up coverage now also pins forward-all marker variants for supported relational cursors: real PostgreSQL 16 `psql` traffic with `MOVE FORWARD ALL FROM <cursor>`, `MOVE FORWARD n IN <cursor>`, and `FETCH FORWARD ALL IN <cursor>` reports truthful movement/fetch counts, exhausts the supported cursor state once, and still recovers for later supported SELECT traffic.
- P3 follow-up coverage now pins broader non-forward cursor directions as explicit unsupported behavior: real PostgreSQL 16 `psql` traffic with `FETCH FIRST`, `MOVE LAST`, `MOVE RELATIVE n`, and `MOVE ABSOLUTE n` returns stable unsupported-direction errors without advancing the supported relational cursor, and later supported cursor/SELECT traffic still works.
- P3 follow-up coverage now pins marker-bearing omitted-count cursor movement for supported relational cursors: real PostgreSQL 16 `psql` traffic with `FETCH FORWARD FROM`, `FETCH NEXT IN`, plain `FETCH FROM`, `MOVE FORWARD FROM`, and `MOVE NEXT IN` advances by one row per command and still recovers for later supported SELECT traffic.
- P3 follow-up coverage now pins markerless omitted-count cursor movement for supported relational cursors: real PostgreSQL 16 `psql` traffic with `FETCH FORWARD <cursor>`, `FETCH NEXT <cursor>`, `FETCH FORWARD ALL <cursor>`, `MOVE FORWARD <cursor>`, `MOVE NEXT <cursor>`, and `MOVE FORWARD ALL <cursor>` preserves truthful forward-only cursor state and later supported SELECT recovery.
- P3 follow-up coverage now pins additional non-forward cursor directions as explicit unsupported behavior: real PostgreSQL 16 `psql` traffic with `FETCH PRIOR`, `FETCH BACKWARD ALL`, `MOVE PRIOR`, and `MOVE BACKWARD ALL` returns stable unsupported-direction errors without advancing the supported relational cursor, and later supported cursor/SELECT traffic still works.
- Post-P3 COPY import coverage now pins real PostgreSQL 16 `psql` `COPY ... FROM STDIN` as a supported simple-query copy-in path: the endpoint enters COPY data mode, imports supported `int4`/`text` rows, reports `COPY n`, and later supported SELECT traffic still works.
- P3 follow-up coverage now also pins real PostgreSQL 16 `psql \gdesc` over `COPY ... FROM STDIN`: extended Parse returns the stable unsupported COPY error before any copy-in data flow or extended statement/portal state is installed, and later supported SELECT traffic recovers.
- Post-P3 COPY import coverage now also pins real PostgreSQL 16 `psql \copy ... FROM PROGRAM` client-side copy-in traffic: the generated `COPY ... FROM STDIN` imports supported rows and later supported SELECT traffic still works.
- Post-P3 COPY export coverage now pins real PostgreSQL 16 `psql \copy ... TO PROGRAM` client-side copy-out traffic: the generated `COPY ... TO STDOUT` request streams supported table rows and reports `COPY n` while later supported SELECT traffic still works.
- P3 follow-up coverage now pins real PostgreSQL 16 `psql \gexec` over supported relational text results containing semicolon-terminated SQL: simple-query framing preserves semicolons inside SQL string literals, the generated supported SELECT executes, and later supported SELECT traffic still works. The same generated-SQL consumer is now covered under direct, extended-bound `FETCH_COUNT`, and SQL-prepared `FETCH_COUNT`, proving batched source rows execute generated supported SELECTs, empty extended-bound and SQL-prepared source rows no-op truthfully including zero-parameter prepared traffic, invalid extended-bound source input leaves no transient cursor state, generated supported COPY TO STDOUT commands stream table rows for zero-parameter and argument-bearing SQL-prepared traffic without leaving transient cursor state, psql closes `_psql_cursor`, a manual fetch probe reports the expected missing-cursor error, and later supported SELECT traffic or SQL-prepared execution/deallocation recovers. The non-`FETCH_COUNT` `\bind ... \gexec` parameterized meta-command edge is explicitly pinned as unsupported for now: it returns a stable bind-arity error and recovers for later supported SELECT traffic.
- P3 `FETCH_COUNT` extended-bind cleanup coverage now also pins `SELECT ... $1 \bind ... \gset` as an explicit unsupported parameterized cursor-declaration edge across single-row, empty-result, multirow, and invalid-looking-bind source shapes: real PostgreSQL 16 `psql` receives the stable unsupported-parameterized-cursor error before bind-value validation, leaves no transient `_psql_cursor`, and later extended bound metadata/execution or client-variable capture traffic recovers.
- P3 `FETCH_COUNT` extended-bind cleanup coverage now also pins the matching empty-result `SELECT ... $1 \bind ... \gx` unsupported cursor-declaration edge plus invalid-looking `int4` input before `\gx`: real PostgreSQL 16 `psql` receives the stable unsupported-parameterized-cursor error before bind-value validation, leaves no transient `_psql_cursor`, and later extended bound expanded-output traffic recovers.
- P3 extended-bind expanded-output coverage now also pins invalid `int4` input before `\gx` rendering: real PostgreSQL 16 `psql` receives the stable bind-input error, installs no bad portal, and later extended bound expanded-output traffic recovers on the same connection.
- P3 follow-up coverage now pins markerless `MOVE ALL <cursor>` as a real-client cursor exhaustion form: real PostgreSQL 16 `psql` traffic advances the supported relational cursor to the end, reports the truthful moved-row count, and leaves later fetch plus supported SELECT recovery intact.
- P3 follow-up coverage now pins marker-bearing `MOVE ALL FROM <cursor>` and `MOVE ALL IN <cursor>` as real-client cursor exhaustion forms: supported relational cursor movement advances to the end, reports truthful moved-row counts, and preserves later fetch plus supported SELECT recovery.
- P3 follow-up coverage now pins marker-bearing zero-count movement and all-fetch cursor forms: real PostgreSQL 16 `psql` traffic with `MOVE 0 FROM`, `MOVE 0 IN`, `FETCH ALL FROM`, and `FETCH ALL IN` preserves cursor position, exhausts rows once, leaves later fetches empty, and still recovers for supported SELECT traffic.
- P3 follow-up coverage now pins marker-bearing numeric `IN` cursor forms: real PostgreSQL 16 `psql` traffic with `FETCH 0 IN`, `FETCH n IN`, `MOVE 0 IN`, and `MOVE n IN` preserves supported relational cursor state across empty, partial, and remaining-row paths before later supported SELECT recovery.
- P3 follow-up coverage now pins markerless numeric zero/count cursor forms: real PostgreSQL 16 `psql` traffic with `FETCH 0 <cursor>`, `FETCH n <cursor>`, `MOVE 0 <cursor>`, and `MOVE n <cursor>` preserves supported relational cursor state across empty, partial, and remaining-row paths before later supported SELECT recovery.
- P3 follow-up coverage now pins unsupported cursor direction recovery: real PostgreSQL 16 `psql` traffic with `FETCH BACKWARD n`, `MOVE ABSOLUTE n`, and unsupported `SCROLL CURSOR` declarations returns explicit unsupported errors without advancing the supported forward-only cursor, and later cursor plus supported SELECT traffic still works.
- P3 follow-up coverage now pins quoted cursor identifiers containing semicolons: real PostgreSQL 16 `psql` traffic preserves the session-local cursor name through simple-query framing across `DECLARE`, `FETCH`, `MOVE`, and `CLOSE`, returns cursor-not-found after close, and still recovers for later supported SELECT traffic.
- P3 follow-up coverage now pins SQL comments inside supported cursor commands: real PostgreSQL 16 `psql` traffic with commented `FETCH`, `MOVE`, and `CLOSE` commands preserves the session-local cursor lifecycle, returns cursor-not-found after close, and still recovers for later supported SELECT traffic.
- P3 follow-up coverage now pins SQL comments inside supported cursor declarations: real PostgreSQL 16 `psql` traffic with comments around `DECLARE ... ASENSITIVE NO SCROLL CURSOR WITHOUT HOLD ... FOR SELECT` grammar installs the supported relational cursor, preserves truthful fetch/move/close behavior, and still recovers for later supported SELECT traffic.
- P3 follow-up coverage now pins quoted cursor identifiers containing doubled-quote escapes: real PostgreSQL 16 `psql` traffic preserves the decoded session-local cursor name across `DECLARE`, `FETCH`, `MOVE`, and `CLOSE`, returns cursor-not-found after close, and still recovers for later supported SELECT traffic.
- P3 follow-up coverage now pins SQL `PREPARE` / `EXECUTE` over the supported relational `SELECT` subset: real PostgreSQL 16 `psql` traffic prepares catalog-backed `int4`/`text` predicates, executes matching and empty-result paths, rejects invalid `int4` input without removing the prepared statement, deallocates the statement, and recovers for later supported SELECT traffic.
- P3 follow-up coverage now pins real `psql \gdesc` over SQL `EXECUTE <prepared>`: extended Describe can report catalog-backed result metadata for a supported SQL-prepared relational `SELECT` without executing it, then the same SQL prepared statement still executes and later SELECT traffic recovers.
- P3 follow-up coverage now pins extended `\bind` over SQL prepared execution: real PostgreSQL 16 `psql` traffic can `PREPARE` a supported relational SELECT, then run `EXECUTE <prepared>($1, $2)` through Parse/Bind/Describe/Execute with parameter OIDs inferred from the SQL prepared statement, invalid bound `int4` recovery, later successful execution, deallocate, and SELECT recovery.
- P3 SQL-prepared `\gdesc` coverage now also pins metadata-only behavior for outer-bound values: real PostgreSQL 16 `psql` can describe `EXECUTE <prepared>($1, $2) \bind ... \gdesc` without executing the SQL-prepared body, and raw Bind coverage revalidates inferred SQL-prepared outer parameter types before installing a portal.
- P3 follow-up coverage now pins SQL-prepared extended result consumers: real PostgreSQL 16 `psql` traffic can run `EXECUTE <prepared>($1, ...)` through `\bind ... \gset`, `\bind ... \gx`, and `\bind ... \crosstabview`, proving SQL-prepared rows can feed client variables, expanded-output rendering, and crosstab rendering before deallocate and later SELECT recovery.
- P3 raw extended Bind validation now applies result-format-code arity checks to SQL-prepared `EXECUTE <prepared>($n)` portals as well as direct relational `SELECT` portals, so malformed result format vectors fail before portal installation while valid per-column text format vectors remain accepted.
- P3 follow-up coverage now pins SQL `PREPARE` omitted type-list inference and `DEALLOCATE ALL`: real PostgreSQL 16 `psql` traffic prepares a catalog-backed relational `SELECT` without explicit parameter types, infers supported `int4`/`text` predicates, recovers after invalid inferred `int4` input, clears SQL prepared state with `DEALLOCATE ALL`, and recovers for later supported SELECT traffic.
- P3 follow-up coverage now pins `DEALLOCATE PREPARE <name>` for SQL prepared statements: quoted prepared-statement names are preserved through `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE`, the prepared state is removed truthfully, and later supported SELECT traffic recovers.
- P3 follow-up coverage now pins quoted SQL prepared-statement names containing delimiter-looking text and escaped quotes: real PostgreSQL 16 `psql` traffic proves `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE` split `AS` plus optional type/argument lists outside quoted identifier text, including names containing parentheses, ` AS `, or doubled-quote escapes, while preserving later supported SELECT recovery.
- P3 follow-up coverage now pins escaped SQL `EXECUTE` text arguments: real PostgreSQL 16 `psql` traffic proves doubled-apostrophe and comma-bearing text literals split and decode correctly for supported SQL prepared relational `SELECT` execution, including empty-result, deallocate, missing-prepared, and later SELECT recovery paths.
- P3 follow-up coverage now pins unsupported `NULL` SQL `EXECUTE` arguments: real PostgreSQL 16 `psql` traffic proves `NULL` stays outside the current prepared relational `SELECT` subset with a SQL-prepared-specific unsupported-feature error, while the prepared statement remains reusable and later supported SELECT recovery still works.
- P3 follow-up coverage now pins zero-parameter SQL prepared statements: real PostgreSQL 16 `psql` traffic proves `PREPARE <name> AS SELECT ...` can execute through both `EXECUTE <name>` and `EXECUTE <name>()`, deallocate cleanly, and recover for later supported SELECT traffic.
- P3 follow-up coverage now pins SQL prepared argument-count recovery: real PostgreSQL 16 `psql` traffic proves too few and too many `EXECUTE` arguments return the stable parameter-count error without discarding the prepared statement, then a correctly-shaped execution, deallocate, missing-prepared error, and later supported SELECT recovery all still work.
- P3 follow-up coverage now pins real `psql` `FETCH_COUNT` duplicate `_psql_cursor` recovery: when an application cursor already occupies psql's transient cursor name, the implicit cursor declaration returns the duplicate-cursor error, psql's cleanup path leaves `_psql_cursor` absent, and supported SELECT traffic still recovers.
- P3 follow-up coverage now pins real `psql` `FETCH_COUNT` with `\gset`: a single-row supported relational SELECT can feed psql client variables, leaves the transient `_psql_cursor` absent afterward, reports the expected missing-cursor error on a manual fetch probe, and recovers for later supported SELECT traffic.
- P3 follow-up coverage now pins SQL-prepared `FETCH_COUNT` describe/result-consumer cleanup through `\gdesc`, `\gset`, `\gx`, `\crosstabview`, and `\gexec`, plus `\gdesc`, `\gset`, `\gx`, `\crosstabview`, and `\gexec` argument-count cleanup: supported `EXECUTE <name>(...)` and zero-parameter `EXECUTE <name>` results return metadata, feed psql client variables, expanded output, crosstab rendering, or generated supported SELECT execution, leave the transient `_psql_cursor` absent afterward, and preserve the prepared statement for later execution, deallocation, and supported SELECT recovery. The argument-bearing path now also covers negative-`LIMIT` `\gset` and `\gexec` failed-cursor cleanup, proving client-variable capture and generated SQL are skipped until a later valid execution. The zero-parameter path now covers `\gdesc` / `\gset` / `\gx` / `\crosstabview` / `\gexec` argument-count cleanup plus literal negative-`LIMIT` execution cleanup, `\gdesc` metadata-only cleanup, and `\gset` / `\gx` / `\crosstabview` / `\gexec` failed-cursor cleanup; extended bound `SELECT ... \bind ... \gdesc` is also pinned as metadata-only under `FETCH_COUNT`, including a non-executed bind value that would be invalid for execution, with no transient cursor left behind and later bound description/execution still usable.
- P3 follow-up coverage now pins empty-result extended-bound `FETCH_COUNT` `\crosstabview`: real PostgreSQL 16 `psql` renders the empty crosstab result, leaves no transient `_psql_cursor`, reports the expected missing-cursor error on a manual fetch probe, and later supported extended-bound crosstab traffic recovers on the same connection.
- P3 follow-up coverage now pins invalid extended-bound `\crosstabview` recovery: real PostgreSQL 16 `psql` reports the stable invalid-`int4` bind error before crosstab rendering, installs no bad portal, and later correctly-bound crosstab rendering succeeds on the same connection.
- P3 follow-up coverage now pins real `psql \gdesc` over extended parameterized `INSERT`, filtered `UPDATE`, and filtered `DELETE`: Describe returns `NoData` without row side effects, later Execute returns truthful DML command tags, and the same session can still use relational `SELECT` afterward.
- P3 follow-up coverage now pins real `psql \bind ... \gset` when a supported extended SELECT returns multiple rows: psql reports the client-side multirow `\gset` error after consuming the result, and the same connection still recovers for later supported extended-bound SELECT traffic.
- P3 follow-up coverage now also pins the adjacent empty-result `psql \bind ... \gset` path: psql reports the client-side no-row `\gset` error after consuming the supported extended SELECT result, and the same connection still recovers for later supported extended-bound SELECT traffic.
- P3 follow-up coverage now pins metadata recovery after a client-side `\gset` result-consumer error: after a multirow `psql \bind ... \gset` error, a later supported `\bind ... \gdesc` still returns catalog-backed metadata and subsequent execution succeeds on the same connection.
- P3 follow-up coverage now pins parenthesized SQL prepared arguments: real PostgreSQL 16 `psql` traffic proves parenthesized `int4` and `text` literal arguments in `EXECUTE <name>((...), (...))` decode into the supported prepared relational `SELECT` subset, including matching, empty-result, deallocate, missing-prepared, and later SELECT recovery paths.
- P3 follow-up coverage now pins supported postfix-casted SQL prepared arguments: real PostgreSQL 16 `psql` traffic proves `EXECUTE <name>(2::int4, 'text'::pg_catalog.text)` style arguments decode into the supported prepared relational `SELECT` subset, including matching, empty-result, deallocate, missing-prepared, and later SELECT recovery paths.
- P3 follow-up coverage now pins semicolon-bearing quoted SQL prepared names: simple-query framing preserves semicolons inside quoted identifiers, so real PostgreSQL 16 `psql` traffic can `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE` a supported relational SELECT named `"lookup;semi"` before recovering for later supported SELECT traffic.
- P3 follow-up coverage now pins SQL prepared-statement comments: real PostgreSQL 16 `psql` traffic can use SQL comments around supported `PREPARE` type lists, `EXECUTE` argument lists, and `DEALLOCATE PREPARE` targets while preserving quoted identifier/comment-marker text and later supported SELECT recovery.
- P3 follow-up coverage now pins SQL prepared-statement `AS` separator scanning through comments: real PostgreSQL 16 `psql` traffic can prepare a supported relational SELECT when line or nested block comments before the true `AS` separator contain delimiter-looking `AS` text, then execute, deallocate, hit the expected missing-prepared error, and recover for later supported SELECT traffic.
- P3 follow-up coverage now pins PostgreSQL whitespace after SQL prepared-statement keywords: real PostgreSQL 16 `psql` traffic can put newlines or tabs after `PREPARE`, `EXECUTE`, `DEALLOCATE`, and `DEALLOCATE PREPARE` while staying inside the supported relational prepared-`SELECT` subset and preserving missing-prepared plus later SELECT recovery.
- P3 follow-up coverage now pins leading comments before SQL prepared-statement commands: real PostgreSQL 16 `psql` traffic can prefix supported `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE` statements with line or nested block comments while preserving prepare/execute/deallocate behavior and later supported SELECT recovery.
- P3 follow-up coverage now pins semicolons inside SQL comments for prepared-statement traffic: simple-query framing preserves semicolons inside line and nested block comments, so supported SQL `PREPARE` / `EXECUTE` / `DEALLOCATE PREPARE` batches keep their intended statement boundaries and recover for later supported SELECT traffic.
- P3 follow-up coverage now pins signed `int4` parameter traffic: real PostgreSQL 16 `psql` simple SELECTs, extended `\bind`, and SQL `PREPARE` / `EXECUTE` all preserve negative and zero `int4` values over the supported relational SELECT subset before deallocate and later SELECT recovery.
- P3/P2 follow-up coverage now pins `INT4` / `pg_catalog.text` DDL aliases plus plus-signed `int4` literals across simple relational `SELECT`, extended `\bind`, and SQL `PREPARE` / `EXECUTE` traffic over the supported PostgreSQL-compatible subset.
- P3 follow-up coverage now pins delimiter-like text Bind values: real PostgreSQL 16 `psql` `\bind` parameters containing spaces, semicolons, commas, line-comment markers, and block-comment markers remain literal text data over supported relational `SELECT` predicates.
- P4 SQL-to-GPU bridge work has started at the SQL result boundary. Relational `SELECT * FROM table` now lowers through the MVCC CUDA probe path and can report `executed_target = gpu(0)` with SQL-level CPU/GPU parity coverage on local NVIDIA hardware. Supported equality predicates use the relational equality index as a `KeyBatchLookup` source, same-column equality `OR` groups and literal `IN (...)` membership use that same index-backed key-batch path, unordered `LIMIT` is pushed into the MVCC/CUDA query when it is safe to apply before SQL ordering, projection-only SQL result shaping no longer counts as GPU fallback after the row fetch executes on GPU, supported decoded-column `ORDER BY` uses an ordered key-batch access path with `LIMIT` pushdown, `LIMIT`/`OFFSET` pagination fetches the needed prefix and skips rows during host result shaping without reporting a GPU parity fallback, `SELECT DISTINCT` over selected columns removes duplicate projected rows after GPU row fetch without reporting a parity fallback, and supported `COUNT(*)`, `SUM(int4_column)`, `AVG(int4_column)`, `MIN(column)`, `MAX(column)`, grouped `column, COUNT(*)`, grouped `column, SUM(int4_column)`, grouped `column, AVG(int4_column)`, grouped `column, MIN|MAX(column)`, and bounded grouped `HAVING` result filtering report aggregate rows after GPU row fetch without reporting a parity fallback. Narrow literal range predicates (`<`, `<=`, `>`, `>=`) now use a filtered key-batch bridge, literal `BETWEEN` predicates normalize to an inclusive range conjunction, prefix-only text `LIKE 'prefix%'` predicates use filtered/ordered key-batch bridges, narrow `AND` conjunctions use a conjunctive key-batch bridge, and `OR` groups of supported predicates, including parenthesized nested `AND`/`OR` forms normalized to filter groups, use disjunctive key-batch bridges instead of reporting a SQL GPU parity fallback. CUDA-probed MVCC reads now publish SQL-visible row-batch H2D bytes and a GPU execution timing sample into engine metrics. `RelationalSqlGpuBridgeReport::from_results(...)` summarizes SQL-level GPU execution and CPU fallback rates for the supported relational mix.
- P5 storage/indexing work has started with a durable WAL segment boundary plus an in-memory relational equality index maintained from WAL-applied inserts and rebuilt during durable WAL recovery. `Engine::persist_durable_wal_to_file(...)` writes the flushed WAL prefix to a checksummed segment, and `Engine::recover_from_durable_wal_file(...)` replays that segment so relational catalog descriptors, table rows, and equality-index access paths survive a restart-shaped test. `Engine::persist_durable_wal_checkpoint(...)` / `Engine::recover_from_durable_wal_checkpoint(...)` add the first checkpoint-control metadata layer: a control file identifies the segment and validates durable record count plus last durable transaction id before replay. `Engine::persist_durable_wal_archive(...)` / `Engine::recover_from_durable_wal_archive(...)` add a bounded multi-segment local archive proof: the manifest records ordered segment paths, record counts, transaction ranges, overall durable count, last durable transaction id, and engine-written per-transaction timestamps before replaying the same committed prefix. `Engine::ingest_durable_wal_archive_segment(...)` adds local streaming-style segment ingestion: an already-written checksummed segment is validated against the current archive, transaction ids must advance beyond the durable boundary, timestamp metadata is preserved or required consistently, and the manifest is rewritten only after validation. `Engine::recover_from_durable_wal_archive_to_txn(...)` adds the first transaction-bound PITR target proof by replaying only the validated archive prefix ending at an exact archived transaction id and rejecting targets before the first record, beyond the durable archive, or between archived transaction boundaries. `Engine::recover_from_durable_wal_archive_to_timestamp_micros(...)` adds exact timestamp-bound PITR for engine-written archives, rejecting missing timestamp metadata, before-first, beyond-durable, between-boundary, and ambiguous timestamp targets before replay. `Engine::recover_from_durable_wal_checkpoint_and_archive_to_txn(...)` / `Engine::recover_from_durable_wal_checkpoint_and_archive_to_timestamp_micros(...)` add the first local base-backup restore proof: a validated checkpoint-control base backup is combined with a validated archive suffix only if the archive overlaps and byte-for-byte matches the base boundary, then recovery replays to the exact transaction or timestamp target. `Engine::plan_durable_wal_archive_retention_to_txn(...)` / `Engine::apply_durable_wal_archive_retention_to_txn(...)` and `Engine::plan_durable_wal_archive_retention_to_timestamp_micros(...)` / `Engine::apply_durable_wal_archive_retention_to_timestamp_micros(...)` add local PITR-branch suffix cleanup: the archive is fully validated, the manifest and segment files are rewritten to the exact target transaction or timestamp prefix, retained timestamp metadata is preserved, and obsolete post-target segment files are removed only after the new manifest is installed. `Engine::plan_durable_wal_archive_retention_from_checkpoint(...)` / `Engine::apply_durable_wal_archive_retention_from_checkpoint(...)` add the local base-backup-aware archive retention window proof: a checkpoint-control base backup is validated against the archive, the archive is rewritten to retain the base-boundary record plus durable suffix, and base-plus-archive transaction/timestamp restore still works after older pre-base archive records are pruned. `Engine::fork_durable_wal_archive_timeline_to_txn(...)` / `Engine::fork_durable_wal_archive_timeline_to_timestamp_micros(...)` add local PITR timeline-branch proof: an exact transaction or timestamp target is forked into a branch archive manifest containing only the selected durable prefix, with sidecar timeline id, optional parent id, fork boundary, source manifest, and branch manifest metadata. `Engine::export_durable_wal_archive_object_backup(...)` / `Engine::restore_durable_wal_archive_object_backup(...)` add a file-backed object-bundle backup proof: a validated archive is exported as a backup manifest plus manifest/segment objects with byte lengths and checksums, and restore verifies every object before installing a restored archive manifest and segment set. Supported `WHERE column = literal` relational reads now use a `KeyBatchLookup` access path over indexed row keys before final SQL verification, and `RelationalSelectResult::access_path` reports whether execution used `FullTableScan`, `EqualityIndex`, or an ordered key batch. Current retention is still conservative but has tested pruning boundaries: `Engine::checkpoint_vacuum_mvcc_versions(...)` can prune tuple versions deleted at or before a durable safe transaction id, refuses to cross active transactions or the flushed WAL boundary, and leaves WAL replay able to reconstruct pruned historical versions; WAL archive cleanup supports transaction/timestamp-target suffix cleanup and checkpoint-backed base-window cleanup. Physical page-image base backups, production object-storage APIs, production timeline management, and automatic background cleanup remain outside the current proof.
- P6 operational replication work has a reproducible packaged-local 3-node Raft smoke harness at `scripts/run_replication_cluster_smoke.sh` plus `scripts/run_replication_packaged_smoke.sh`. The harness proves leader write admission, follower catch-up, read-after-apply, deterministic request-vote election, old-leader write rejection after failover, and elected-leader continuation using typed `AppendEntriesRequest` / `AppendEntriesResponse` messages. Those messages now have a tested binary frame codec and reusable single-request TCP send/serve helpers; the smoke path uses that TCP helper for the failover append-entry replication step. Operator output includes `OperationalDeploymentPreflightReport` lines that fail the scripts if the smoke proof, TCP append-entries transport evidence, deterministic election evidence, packaged-local entrypoint evidence, or deployment-scope evidence drifts. `scripts/run_replication_multiprocess_smoke.sh` launches independent follower child processes for bounded TCP catch-up/read-after-apply, `scripts/run_replication_service_smoke.sh` advances that to follower service processes that remain alive across multiple sequential AppendEntries connections, apply committed entries, report catch-up/read-after-apply, and exit through controlled shutdown, and `scripts/run_replication_container_smoke.sh` packages those follower services into local Docker containers on a user-defined network while a host leader verifies committed apply/read-after-apply and controlled shutdown. Production supervision and Kubernetes deployment remain open.
- P7 performance-proof work has started with `scripts/run_p7_relational_benchmark.sh`, which runs app-shaped indexed lookup workloads, including repeated point lookups and one batched `OR` lookup, plus analytical full-table scan, narrow analytical range/conjunctive-filter, narrow analytical disjunctive-filter, and aggregate/distinct workloads over the supported relational SQL subset. The report at `docs/testing/reports/p7-relational-benchmark-2026-05-11.md` validates correctness and GPU routing evidence, including 0% SQL fallback, CPU/GPU-probe totals and QPS, per-workload p50/p95/max latency, CPU-vs-GPU total ratios, per-engine CUDA driver probe runtime caching, normalized H2D/D2H transfer pressure, and non-zero SQL-level H2D/timing telemetry for the benchmark mix after equality predicate bridge pushdown, bridge-level decoded-column ordering pushdown, range predicate key-batch bridging, literal `BETWEEN` normalization to conjunction, prefix-only text `LIKE` key-batch bridging, conjunction key-batch bridging, disjunction key-batch bridging, parenthesized nested boolean normalization, supported `SELECT DISTINCT` duplicate elimination, supported `COUNT(*)` / `SUM(int4)` / `AVG(int4)` / `MIN` / `MAX` aggregate reporting, and compact key-batch H2D transfer for requested rows. `scripts/run_p7_batching_sweep.sh` adds a reproducible lookup-size sweep; `docs/testing/reports/p7-batching-sweep-2026-05-17.md` records lookup sizes 1, 4, 16, and 64 on the local NVIDIA host, all with 0% SQL fallback, correctness validation, and access-path evidence showing app-style same-column `OR` lookups use an index-backed ordered key batch. The sweep shows a single supported `OR` query keeps GPU-probe latency near one probe while repeated point lookups scale roughly linearly with lookup count, but both batched and analytical workloads still remain slower than the CPU baseline. `scripts/run_p7_gpu_residency_baseline.sh` adds a reproducible residency baseline at `docs/testing/reports/p7-gpu-residency-baseline-2026-05-17.md`: it measures cold per-query H2D probe traffic, zero-transfer resident table-data snapshot SELECT probes for the app lookup workload and supported aggregate/distinct SQL shapes, retained-device-memory `COUNT(*)`, int4 equality-predicate `COUNT(*)`, int4 range-predicate `COUNT(*)`, int4 `SUM`, int4 predicate-projection, and bounded int4 ordered-projection kernel probes over the resident allocation, and a post-mutation probe; validates CPU/GPU correctness; records a resident-byte accounting snapshot (`resident_bytes_current=48341` for the local 1,000-row fixture) that is valid before mutation and invalidated by the later WAL-applied insert; records a retained real CUDA device allocation/copy handle for the encoded resident snapshot when local driver hardware is available (`resident_device_memory_query_kernel_supported=bounded_count_all_int4_equality_count_int4_range_count_int4_sum_int4_projection_and_int4_ordered_projection`, `resident_device_memory_proof_supported=true`, `resident_device_memory_retained=true`, `resident_device_memory_allocated_bytes=65135` on the RTX 3090 run); records manual refresh-cost metadata (`resident_refresh_row_delta=1`, `resident_refresh_byte_delta=58`) when the snapshot is repopulated after that mutation; records memory-pressure fallback metadata when the default GPU is marked pressured; and proves deterministic resident-snapshot budget admission/eviction with oversized-snapshot rejection. `Engine::status_snapshot()` and `Engine::telemetry_snapshot()` expose the resident tables, per-GPU resident bytes and configured budgets, validity/invalidation state, admission/eviction metadata, and retained device-memory proof metadata so operators can inspect the current P7 residency truth surface without private engine state. Current P7 claims stay limited to routing/correctness telemetry, measured batching benefit over repeated GPU probes, aggregate/distinct benchmark evidence, resident accounting plus invalidation/refresh-cost/memory-pressure/budget metadata, operator-visible residency status, a retained real allocation/copy handle, bounded resident snapshot execution for the supported lookup and aggregate/distinct shapes, and retained-device-memory `COUNT(*)`, int4 equality-predicate `COUNT(*)`, int4 range-predicate `COUNT(*)`, int4 `SUM`, int4 predicate-projection, and bounded int4 ordered-projection query-kernel proofs; broad workload-level GPU advantage and broad production CUDA cache behavior remain unsupported until grouped aggregate kernels read directly from retained device-memory handles. Follow-up work is deeper batching/transfer layout, production GPU-resident execution semantics, richer expressions beyond literal comparison/range/membership/prefix predicates and bounded aggregate reporting, and driver-level kernel/transfer timing refinement beyond initial driver probing.

## Safety invariant

The engine preserves **WAL-before-visibility**: a state transition is never visible to readers before its corresponding WAL record is durably flushed.
