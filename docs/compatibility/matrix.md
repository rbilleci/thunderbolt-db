# Compatibility Matrix (Phase-Gated)

This matrix makes compatibility intent explicit per delivery phase while preserving the project invariants:
- GPU-first planning with deterministic CPU fallback
- WAL-before-visibility durability boundary
- Role-aware write gating for replication safety

## Legend

- ✅ Supported in phase
- 🟡 Partial/limited support
- 🚫 Not supported in phase
- 📌 Planned (post-phase)

## Postgres Protocol + SQL Surface

| Capability | v0 | v0.5 | v1 | Notes |
|---|---:|---:|---:|---|
| Text command parser for key-value mutations (`SET`, `DEL`) | ✅ | ✅ | ✅ | Current bootstrap command surface for deterministic testing |
| Transaction control command parsing (`BEGIN`, `COMMIT`, `ROLLBACK`) | ✅ | ✅ | ✅ | Counted as non-GPU-eligible fallbacks in runtime telemetry |
| Admin flush command (`CHECKPOINT`/`FLUSH`) | ✅ | ✅ | ✅ | Drains pending deterministic batch |
| Session-control parser no-op aliases (`RESET`/`DISCARD`/`DEALLOCATE`/`SET ROLE`/`SET SESSION AUTH*`/`SET SESSION CHARACTERISTICS AS TRANSACTION ...`/`CLOSE {ALL|name}`/`LISTEN`/`NOTIFY`/`UNLISTEN` incl. `UNLISTEN ALL`, plus reset-cleanup `SELECT pg_advisory_unlock_all()`) | ✅ | ✅ | ✅ | PostgreSQL-style client reset/setup probes accepted without mutating state; advisory lock semantics beyond asyncpg reset cleanup remain out of scope |
| Minimal relational SQL (`CREATE TABLE`, `INSERT`, filtered `UPDATE`/`DELETE`, bounded `TRUNCATE`, narrow `SELECT`) | 🟡 | 🟡 | ✅ | P1 bootstrap slice uses structured parsed plans; engine reads lower through MVCC execution with CPU reference fallback, engine filtered updates and deletes use the WAL-before-visibility mutation path with MVCC version/tombstone semantics, and psql golden covers create/insert/update/delete/select including single-column literal comparison filters, literal `BETWEEN` inclusive range predicates, prefix-only text `LIKE 'prefix%'` predicates, narrow `AND` conjunctions, top-level `OR` groups, supported parenthesized nested `AND`/`OR` predicates, literal `IN (...)` membership normalized to filter groups, literal update assignments, literal and bounded sequence-backed column defaults, bounded default drops, literal and sequence-backed default-backed column adds, bounded table renames, bounded column renames, and bounded non-dependent column drops for supported `int4`/`text` columns, `SELECT DISTINCT` duplicate elimination over selected columns, `COUNT(*)`, `SUM(int4)`, `AVG(int4)`, `MIN`/`MAX` over supported columns, grouped `column, COUNT(*)`, grouped `column, SUM(int4)`, grouped `column, AVG(int4)`, grouped `column, MIN|MAX(column)` reporting, bounded `GROUP BY ... HAVING` filters over the grouped column plus `count`/`sum`/`avg`/`min`/`max` outputs, and ordered `LIMIT`/`OFFSET` pagination plus bounded `TRUNCATE TABLE [ONLY] [public.]table` / `TRUNCATE [public.]table` clearing visible rows through the WAL-before-visibility mutation path while preserving table metadata, with bounded `RESTART IDENTITY` resetting only sequences referenced by supported table column defaults |
| Engine catalog/type spine for relational tables | 🟡 | 🟡 | ✅ | First P2 slice stores created tables in `public` with stable relation OIDs, column ids/attnums, and `int4`/`text` type metadata; durable-WAL bootstrap replay restores catalog plus rows, and dynamic `pg_catalog.pg_class`/`pg_catalog.pg_attribute` introspection exists for supported session tables including relation OIDs and column `attnum`/`atttypid`/`attlen`; first-slice `pg_catalog.pg_type` introspection exposes the supported `int4`/`text` type registry; `pg_catalog.pg_tables` discovery plus direct `pg_catalog.pg_namespace` lookup for the supported `public` namespace and joined `pg_catalog.pg_class` / `pg_catalog.pg_namespace` relation metadata, including table-name `IN (...)` subset filtering and namespace-only `public` relation lookup, returns supported `public` session tables, `pg_catalog.pg_indexes` discovery reflects bounded metadata-backed `CREATE [UNIQUE] INDEX name ON table (column)` rows for supported public tables, enforces single-column unique index values on create plus later `INSERT`/`COPY FROM`/filtered `UPDATE`, removes indexes after supported `DROP INDEX [IF EXISTS] [public.]index [, ...]`, retargets non-constraint indexes after supported `ALTER INDEX [public.]index RENAME TO new_index`, and retargets table/index/constraint/comment/default metadata after supported `ALTER TABLE [IF EXISTS] [ONLY] [public.]table RENAME TO new_table`, and joined `pg_catalog.pg_attribute` / `pg_catalog.pg_class` / `pg_catalog.pg_namespace` column metadata returns supported column ordinals, names, formatted type names, and nullable flags; real `psql \dt`, `\dt+`, `\dt+ <table>`, `\dt <prefix>*`, `\dt+ <prefix>*`, schema-qualified `\dt public.*` / `\dt+ public.*` / `\dt public.<prefix>*` / `\dt+ public.<table>` / `\dt+ public.<prefix>*`, `\d <table>`, `\d+ <table>`, `\d <prefix>*`, `\d+ <prefix>*`, `\d public.*`, schema-qualified `\d public.<table>` / `\d+ public.<table>` / `\d public.<prefix>*` / `\d+ public.<prefix>*`, `\dp <table>`, schema-qualified `\z public.<pattern>`, `\di` and `\di+` index listings for bounded SQL indexes, including renamed non-constraint indexes and indexes retargeted by bounded table rename, `\dv` / `\dv+` bounded stored-view listing including renamed views, `\dm` / `\dm+` bounded materialized-view listing, `\ds` / `\ds+` bounded sequence listing plus bounded sequence value functions, `\df` / `\df+` bounded function listing, `\da` empty aggregate listing, `\dc` empty conversion listing, `\do` empty operator listing, `\dO` empty collation listing, `\dC` empty cast listing, `\dRp` bounded publication listing, `\dRs` bounded disabled subscription listing, `\ddp` empty default-access-privilege listing, `\dd` object-description listing for supported table/constraint/view/materialized-view/sequence/domain/publication/subscription comments, `\dx` bootstrap `plpgsql` extension listing plus idempotent `CREATE EXTENSION IF NOT EXISTS plpgsql`, `\dL` bootstrap `plpgsql` procedural-language listing, `\db` bounded tablespace listing, `\dA` bootstrap access-method listing, `\dn`, `\dn+ public`, `\dT pg_catalog.int4` / `\dT pg_catalog.text`, and `\dT pg_catalog.*` / `\dT+ pg_catalog.*` display works for supported session objects/types; first-slice `information_schema.tables`, `information_schema.columns`, `information_schema.schemata`, `information_schema.table_constraints`, and `information_schema.key_column_usage` introspection works for supported tables/schema, including richer table catalog/insertability/type-shape fields, table-name `IN (...)` subset plus exact-name and catalog-qualified exact-name filtered table discovery, all-column, table-name `IN (...)` subset, and per-table detail projection enumeration across supported `public` table columns, table-filtered, catalog-qualified table-filtered, and table-name `IN (...)` extended column metadata, column nullability/default/type-identity plus `int4` numeric precision/radix/scale fields, metadata-backed primary-key, unique-constraint, check-constraint, and foreign-key rows in information-schema plus pg-catalog constraint views, metadata-backed literal and bounded sequence-backed column defaults plus bounded default drops, literal and sequence-backed default-backed column adds, and bounded table renames in information-schema, pg-catalog, and `psql \d` output plus supported table/column/index/primary-key/unique/check-constraint/foreign-key/view/materialized-view/sequence comments in pg-catalog and `psql \d+`/`\dd` output; `\dp`/`\z` report bounded relation ACL metadata for supported public tables, stored views, materialized views, and sequences while column-privilege and row-policy fields stay empty until those features exist, `\ddp` reports bounded default table privileges when configured, while `\dd` reports supported table, primary-key/unique/check-constraint/foreign-key, view, materialized-view, sequence, domain, public-schema, publication, and subscription comments, `\dv+` reports supported view comments including after bounded view rename, `\dm+` reports supported materialized-view comments including after bounded materialized-view rename, `\dn+ public` reports supported public-schema comments, and `\di+` reports supported index comments including renamed non-constraint indexes while shared-object comments are bounded to the supported database/role/tablespace subset and broader object comments remain pending, verbose schema listings report bounded public-schema ACL metadata plus supported `COMMENT ON SCHEMA public` descriptions, and verbose table listings now report supported table comments plus deterministic compatibility heap-size strings for supported public tables; SQL index methods beyond the default `USING btree` spelling emitted by pg_dump, opclasses/options, expression/partial/multi-column indexes, physical index storage, `DROP INDEX CONCURRENTLY`, constraint-backed index renames via `ALTER INDEX`, broader index dependency semantics, `CREATE OR REPLACE VIEW` boundaries beyond supported base and layered stored views, view cascade/dependency semantics beyond bounded stored-view `DROP VIEW [IF EXISTS] [public.]view [, ...]`, bounded stored-view rename, bounded dependent-parent rejection, and bounded table-rename view-dependency rejection, materialized-view concurrent-refresh/no-data-refresh/dependency/storage semantics, sequence options beyond default +1/no-bound/cache-1, identity/ownership/dependency integration beyond bounded sequence-backed int4 defaults, arguments/overloads/table-referencing bodies/languages/options beyond bounded literal zero-argument SQL function execution, SQL aggregates, SQL operators, user-defined collations, user-defined casts, encoding conversions, domain constraints/defaults/not-null/collations beyond bounded domain-backed table columns, SQL constraints beyond bounded single-column primary keys, unique constraints, single-column literal check constraints, and bounded single-column foreign keys, NULL/no-default/identity/generated/expression defaults beyond bounded sequence-backed int4 defaults, shared-object comments beyond the supported database/role/tablespace subset, broader logical-replication object comments beyond bounded publications/subscriptions, procedural language execution/creation beyond metadata-only bootstrap `plpgsql`, user-defined types, broader physical PostgreSQL heap-size accounting and broader object-description metadata, permission enforcement, broader role/policy mutation beyond bounded role metadata and bounded relation ACL/default-table-ACL metadata, default ACL mutation beyond bounded table defaults, broader object descriptions, publication streaming/options/filters beyond bounded metadata, subscription streaming/slots/workers/enabled subscriptions beyond bounded disabled metadata, extension install/drop/update beyond idempotent bootstrap `CREATE EXTENSION IF NOT EXISTS plpgsql`, relation/database storage placement, filesystem-backed tablespace placement/options, and advanced PostgreSQL catalog surfaces remain pending |
| P2 follow-up note | 🟡 | 🟡 | ✅ | Real PostgreSQL 16 plain `psql \d` relation listing is now covered for supported `public` session tables by the same metadata-backed relation rows used by table-listing introspection; unsupported relation kinds remain absent until the engine exposes them; real PostgreSQL 16 `psql \dv` / `\dv+` list bounded stored views, `\dm` / `\dm+` list bounded materialized-view snapshots, `\ds` / `\ds+` list bounded sequences, `\dRp` lists bounded publication metadata, `\dRs` lists bounded disabled subscription metadata, `\dd` lists bounded publication/subscription comments, and real `\df` / `\df+` lists bounded zero-argument SQL functions with literal-body `SELECT [public.]name()` execution and `\da`, `\dc`, `\do`, `\dO`, and `\dC` remain empty for the current aggregate/conversion/operator/collation/cast subset; `\ddp` reports bounded default table privileges when configured; `\dD` / `\dD+` lists bounded public domains created with `CREATE DOMAIN [public.]name AS {int4|text}`, including supported comments, domain-backed table-column catalog metadata, dependency-aware drop rejection, and drop cleanup; `\dd` reports supported relation, schema, domain, publication, and subscription comments while broader object-description fidelity remains pending; `\dx` and `\dL` expose metadata-only bootstrap `plpgsql` extension/language rows; `\du` / `\du+` list the bootstrap `postgres` role through a narrow `pg_catalog.pg_roles` compatibility slice including bounded role comments; `\l` / `\l+` list the supported bootstrap `postgres` database through a narrow `pg_catalog.pg_database` compatibility slice including bounded database comments; `\db` / `\db+` list bootstrap and bounded metadata-only application tablespace rows through a narrow `pg_catalog.pg_tablespace` compatibility slice including bounded tablespace comments; `\dA` lists the supported `heap` table access method through a narrow `pg_catalog.pg_am` compatibility slice; `\dT pg_catalog.*` and `\dT+ pg_catalog.*` list the supported `int4`/`text` type registry through a narrow `pg_catalog.pg_type` compatibility slice |
| P2 function catalog note | 🚫 | 🟡 | ✅ | Bounded `CREATE FUNCTION [public.]function() RETURNS {int4|text} LANGUAGE sql AS literal`, `COMMENT ON FUNCTION [public.]function() IS {literal|NULL}`, `ALTER FUNCTION [public.]function() RENAME TO new_function`, `SELECT [public.]function()` for stored bodies that are exactly `SELECT {int4_literal|text_literal}`, and `DROP FUNCTION [IF EXISTS] [public.]function()` are engine-owned and WAL-replayed for public zero-argument SQL function catalog rows. The supported slice allocates stable function OIDs, stores return type and source text, preserves OID/body/return type and retargets supported comments across bounded renames, exposes rows through real PostgreSQL 16 `psql \df`, `\df+`, direct `pg_catalog.pg_proc`, and `pg_catalog.pg_description`, rejects duplicate or missing required targets before partial mutation, executes literal bodies as one typed row, and removes supported comments on drop. Arguments, overloads, table-referencing bodies, non-SQL languages, volatility/security/options, dependency semantics, aggregates, operators, and broader function behavior remain pending |
| P2 sequence catalog note | 🟡 | 🟡 | ✅ | Bounded `CREATE SEQUENCE [public.]sequence`, `COMMENT ON SEQUENCE [public.]sequence IS {literal|NULL}`, `ALTER SEQUENCE [public.]sequence RENAME TO new_sequence`, `DROP SEQUENCE [IF EXISTS] [public.]sequence [, ...]`, bounded `nextval`/session-local `currval`/`setval` value functions, bounded `SERIAL` / `SERIAL4` implicit sequence-backed `int4` defaults, bounded explicit `DEFAULT nextval('public.sequence'::regclass)` column defaults, and bounded `ADD COLUMN int4 DEFAULT nextval('public.sequence'::regclass)` rewrites are engine-owned and WAL-replayed for public sequences. The supported slice allocates stable relation OIDs, exposes relation kind `s` through direct `pg_catalog.pg_class` / `pg_catalog.pg_namespace` queries, lists renamed objects and retargeted comments through real PostgreSQL 16 `psql \ds`, `\ds+`, `\dd`, and `pg_catalog.pg_description`, maintains default +1 sequence value state across WAL replay and shared-catalog sessions, exposes sequence-backed defaults through `psql \d`, `pg_catalog.pg_attrdef`, and `information_schema.columns`, rejects table/view/materialized-view/missing regclass targets before mutation, and removes supported sequence comments on drop. Real PostgreSQL 16 `pg_dump` / `pg_restore` now round-trips bounded sequences through pg-dump's default `CREATE SEQUENCE ... START WITH 1 INCREMENT BY 1 NO MINVALUE NO MAXVALUE CACHE 1`, `COMMENT ON SEQUENCE`, and actual `setval(...)` state replay across the checked-in plain/custom/directory/tar, clean, and split restore smoke. Sequence options beyond that default spelling, ownership/dependencies, identity integration, `CASCADE`/`RESTRICT`, unsupported schemas, and broader dependency semantics remain pending |
| P2 materialized-view catalog note | 🟡 | 🟡 | ✅ | Bounded `CREATE MATERIALIZED VIEW [public.]view AS <supported base-table SELECT> [WITH [NO] DATA]`, `REFRESH MATERIALIZED VIEW [public.]view [WITH DATA]`, `COMMENT ON MATERIALIZED VIEW [public.]view IS {literal|NULL}`, `ALTER MATERIALIZED VIEW [public.]view RENAME TO new_view`, and `DROP MATERIALIZED VIEW [IF EXISTS] [public.]view [, ...]` are engine-owned and WAL-replayed for public snapshot relations. The supported slice stores captured `int4`/`text` rows and columns, supports pg-dump-compatible no-data creation with empty snapshots, preserves `SELECT * FROM materialized_view` snapshot reads after base-table writes until refresh, refreshes rows from the original supported base-table SELECT while preserving OID/comment metadata, exposes relation kind `m` through direct `pg_catalog.pg_class` / `pg_catalog.pg_namespace` queries, lists refreshed/renamed objects and retargeted comments through real PostgreSQL 16 `psql \dm`, `\dm+`, `\dd`, and `pg_catalog.pg_description`, rejects table/view/sequence targets and duplicate names before partial mutation, and removes supported comments on drop. `REFRESH MATERIALIZED VIEW CONCURRENTLY`, refresh `WITH NO DATA`, materialized views over views/materialized views, dependency cascades, physical storage/indexing, unsupported schemas, and broader rewrite semantics remain pending |
| P2 shared-object comments note | 🚫 | 🟡 | ✅ | Bounded `COMMENT ON DATABASE`, `COMMENT ON ROLE`, and `COMMENT ON TABLESPACE <supported_tablespace>` accept literal-or-NULL comments for supported shared-object metadata, persist through engine WAL replay and shared protocol catalog state, appear through real `psql \l+` / `\du+` / `\db+` plus supported catalog probes, and reject unsupported targets. Security labels and broader shared-object comments remain pending |
| P2 shared-object rename note | 🚫 | 🟡 | ✅ | Bounded metadata-only `ALTER ROLE name RENAME TO new_name`, `ALTER DATABASE name RENAME TO new_name`, and `ALTER TABLESPACE name RENAME TO new_name` are engine-owned and WAL-replayed for application roles/databases/tablespaces. The supported slice preserves OIDs, database/listing metadata, tablespace locations, role login flags, supported comments, and bounded ACL/default-privilege grantees, appears through real `psql \du`/`\du+`, `\l`/`\l+`, `\db`/`\db+`, and supported catalog probes, and rejects bootstrap-object renames, missing sources, duplicate targets, and unsupported option/action shapes. Authentication/passwords, role membership/ownership, database routing/storage/templates, filesystem-backed tablespace placement/options, security labels, permission enforcement, and broader shared-object semantics remain pending |
| P2 database catalog note | 🚫 | 🟡 | ✅ | Bounded metadata-only `CREATE DATABASE name`, `DROP DATABASE [IF EXISTS] name [, ...]`, `COMMENT ON DATABASE name`, and `GRANT`/`REVOKE {CONNECT|TEMPORARY|ALL [PRIVILEGES]} ON DATABASE name` are engine-owned and WAL-replayed for application database catalog rows. Created databases appear through real `psql \l` / `\l+` and direct `pg_catalog.pg_database` probes, supported comments and ACL metadata are removed on drop, and supported created-role ACL grantees retarget on role rename. Connection routing to created databases, per-database storage namespaces, templates/cloning, owners/full ACL fidelity, encoding/locale/tablespace options, `ALTER DATABASE`, `DROP DATABASE FORCE`, permission enforcement, grant options, and broader database semantics remain pending |
| P2 tablespace catalog note | 🚫 | 🟡 | ✅ | Bounded metadata-only `CREATE TABLESPACE name LOCATION 'literal'`, `DROP TABLESPACE [IF EXISTS] name [, ...]`, `COMMENT ON TABLESPACE name`, and `GRANT`/`REVOKE {CREATE|ALL [PRIVILEGES]} ON TABLESPACE name` are engine-owned and WAL-replayed for application tablespace catalog rows. Created tablespaces appear through real `psql \db` / `\db+` and direct `pg_catalog.pg_tablespace` probes, supported comments and ACL metadata are removed on drop, and supported created-role ACL grantees retarget on role rename. Relation/database storage placement, filesystem directory validation or creation, owners/full ACL fidelity, options, `ALTER TABLESPACE`, security labels, permission enforcement, grant options, and broader tablespace semantics remain pending |
| P2 role catalog note | 🚫 | 🟡 | ✅ | Bounded `CREATE ROLE` / `CREATE USER` with optional `LOGIN`/`NOLOGIN`, `DROP ROLE` / `DROP USER [IF EXISTS]`, and `COMMENT ON ROLE` are engine-owned and WAL-replayed for application-role metadata. Created roles appear through real `psql \du` / `\du+` and direct `pg_catalog.pg_roles`, can be used as bounded table/schema/default-table ACL grantees, and are protected from drop while supported comments or ACL metadata still reference them. Passwords, memberships, authentication, ownership changes, permission enforcement, security labels, and broader role options remain pending |
| P2 public-schema lifecycle note | 🚫 | 🟡 | ✅ | Bounded `CREATE SCHEMA [IF NOT EXISTS] public` and `DROP SCHEMA [IF EXISTS] public` are engine-owned and WAL-replayed for the bootstrap public namespace. The supported slice keeps `public` visible through real `psql \dn` / `\dn+ public`, direct namespace catalog probes, and `information_schema.schemata` after create/recreate, rejects duplicate creates without `IF NOT EXISTS`, rejects dropping non-empty `public` before mutation, removes supported schema comments and schema ACL metadata on successful empty drop, and preserves pg-dump clean-restore recovery with truthful non-empty schema errors. Unsupported schema names, `AUTHORIZATION`, `CASCADE`/`RESTRICT`, default schema ACL semantics, schema moves, and multi-schema object support remain pending |
| P2 view catalog note | 🟡 | 🟡 | ✅ | Common `information_schema.views` and `pg_catalog.pg_views` discovery for the supported `public` schema expose bounded stored view definitions, `DROP VIEW [IF EXISTS] [public.]view [, ...]` removes supported stored views from shared catalog sessions, `pg_catalog.pg_views`, `information_schema.views`, and real `psql \dv` / `\dv+` listings while preserving base tables and other views, and `COMMENT ON VIEW [public.]view IS <literal-or-NULL>` persists through shared catalog and engine WAL replay, appears in `pg_catalog.pg_description`, `psql \dv+`, `psql \dd`, and pg_dump/restore, and is cleaned up on supported `DROP VIEW`. The pg-dump catalog path exposes bounded view relation/attribute/definition/dependency metadata so real `pg_dump` / `pg_restore` round-trip supported base-table views and bounded child views over supported parent views. `CREATE OR REPLACE VIEW [public.]view AS <supported SELECT>` now replaces one supported view while preserving existing view OID/comment metadata and rejecting unsupported replacements or replacements of parent views that still have dependents without corrupting the prior definition. Bounded `ALTER VIEW [public.]view RENAME TO new_view` retargets supported stored-view metadata and view comments through shared catalog sessions, engine WAL replay, `information_schema.views`, `pg_catalog.pg_views`, `pg_catalog.pg_description`, real `psql \dv+`/`\dd`, and `SELECT * FROM renamed_view`. Bounded layered views over supported stored parent views are supported; cycles and replacing, renaming, or dropping a parent view while child views depend on it are rejected. Cascading dependency actions, updateable views, materialized views over views, and broader view catalog behavior remain pending |
| P2 table-drop note | 🟡 | 🟡 | ✅ | Bounded `DROP TABLE [IF EXISTS] [public.]table [, ...]` is engine-owned and WAL-replayed for supported public base tables. The supported slice removes table catalog rows, visible row versions, table/column/index/constraint comments, metadata-only indexes, unique/primary-key constraint metadata, and residency metadata, rejects missing tables without `IF EXISTS`, duplicate table names, and view targets before partial mutation, preserves unrelated tables/views/shared-object comments, and is covered by real PostgreSQL 16 `psql` golden catalog/listing traffic. `CASCADE`/`RESTRICT`, dependency-aware view/index/constraint cascades beyond this bounded cleanup, unsupported schemas, and broader table-drop semantics remain pending |
| P2 table-truncate note | 🟡 | 🟡 | ✅ | Bounded `TRUNCATE TABLE [ONLY] [public.]table` and `TRUNCATE [public.]table` are engine-owned and WAL-replayed for supported public base tables, with bounded `RESTART IDENTITY` resetting only public sequences referenced by supported sequence-backed `int4` column defaults. The supported slice clears visible row versions through the WAL-before-visibility mutation path, preserves table/index/primary-key/unique-constraint/comment/default metadata plus unrelated tables/views/shared-object comments, rejects missing tables and view targets, and is covered by real PostgreSQL 16 `psql` golden catalog/listing traffic. Multi-table truncates, `CONTINUE IDENTITY`, `CASCADE`/`RESTRICT`, trigger/FK/identity/owned-by dependency semantics beyond supported default references, unsupported schemas, and broader truncate dependency semantics remain pending |
| P2 relation ACL metadata note | 🚫 | 🟡 | ✅ | Bounded `GRANT {SELECT\|INSERT\|UPDATE\|DELETE\|ALL [PRIVILEGES]} ON [TABLE] [public.]table TO {PUBLIC\|postgres\|created_role}` and matching `REVOKE ... FROM ...` are engine-owned metadata operations for supported public base tables. Bounded `GRANT`/`REVOKE` metadata is also supported for `ON VIEW [public.]view`, `ON MATERIALIZED VIEW [public.]view`, and `ON SEQUENCE [public.]sequence` over supported public relation objects, persists through WAL replay and shared protocol catalog sessions, retargets across supported rename flows, cleans up on supported drops, and renders through real PostgreSQL 16 `psql \dp` / `\z` exact and schema-pattern listings. Bounded `GRANT {USAGE\|CREATE\|ALL [PRIVILEGES]} ON SCHEMA public TO {PUBLIC\|postgres\|created_role}` and matching `REVOKE ... FROM ...` persist public-schema ACL metadata, render through real `psql \dn+ public` plus pg-dump namespace `nspacl` discovery, and clean up on supported `DROP SCHEMA public`. Bounded `ALTER DEFAULT PRIVILEGES [FOR ROLE postgres] [IN SCHEMA public] GRANT\|REVOKE ... ON TABLES TO\|FROM {PUBLIC\|postgres\|created_role}` persists default table ACL metadata, renders through `psql \ddp`, and applies only to subsequently created supported public base tables. Permission enforcement, passwords/authentication, memberships, grant options, column ACLs, row policies, default schema ACLs, ACLs for shared objects and default privileges for non-table object kinds, unsupported schemas/grantees/privileges, ownership changes, and broader PostgreSQL ACL semantics remain pending |
| P2 table-rename note | 🟡 | 🟡 | ✅ | Bounded `ALTER TABLE [IF EXISTS] [ONLY] [public.]table RENAME TO new_table` is engine-owned and WAL-replayed for supported public base tables. The supported slice moves visible row keys under the new table name, retargets table catalog metadata plus primary-key/unique/index/default/comment metadata, preserves table OID, column attnums/defaults, equality-index access paths, and duplicate enforcement under the renamed table, rejects duplicate targets, view/materialized-view targets, missing tables without `IF EXISTS`, dependent stored views, schema-qualified targets, and cascade/restrict syntax, and is covered by real PostgreSQL 16 `psql` golden catalog/listing traffic. Schema moves, dependency cascade rewriting, ACL/ownership semantics, and broader relation-rename semantics remain pending |
| P2 constraint-drop note | 🟡 | 🟡 | ✅ | Bounded `ALTER TABLE [IF EXISTS] [ONLY] [public.]table DROP CONSTRAINT [IF EXISTS] constraint` is engine-owned and WAL-replayed for metadata-backed single-column primary-key, unique, check, and foreign-key constraints on supported public base tables. The supported slice removes the backing constraint index or check/foreign-key metadata plus supported index/constraint comments, preserves table rows and unrelated metadata, allows values after the constraint is gone, and is covered by real PostgreSQL 16 `psql` golden traffic. Multi-action ALTER TABLE, `CASCADE`/`RESTRICT`, unsupported schemas, and broader dependency semantics remain pending |
| P2 constraint-rename note | 🟡 | 🟡 | ✅ | Bounded `ALTER TABLE [IF EXISTS] [ONLY] [public.]table RENAME CONSTRAINT old_name TO new_name` is engine-owned and WAL-replayed for metadata-backed single-column primary-key, unique, check, and foreign-key constraints on supported public base tables. The supported slice retargets backing index/check/foreign-key metadata plus supported index/constraint comments, preserves duplicate/check/FK enforcement under the new name, rejects missing-table/missing-constraint/duplicate-target/view targets, and is covered by real PostgreSQL 16 `psql` golden traffic. Multi-action ALTER TABLE, `CASCADE`/`RESTRICT`, unsupported schemas, and broader dependency semantics remain pending |
| P2 check-constraint note | 🟡 | 🟡 | ✅ | Bounded table `CHECK` constraints are engine-owned and WAL-replayed for one supported `int4`/`text` column compared to a literal with `=`, `<`, `<=`, `>`, or `>=`. The supported slice accepts `CREATE TABLE ... [CONSTRAINT name] CHECK (...)` and `ALTER TABLE ... ADD CONSTRAINT name CHECK (...)`, validates existing rows before add, enforces before visible `INSERT`, `COPY FROM`, and filtered `UPDATE`, exposes metadata through `information_schema.table_constraints`, `pg_catalog.pg_constraint`, `pg_catalog.pg_description`, and real `psql \d+`, and participates in supported comment, rename, drop, and dependent column-drop rejection flows. Multi-column/expression checks, `NOT VALID`, `VALIDATE CONSTRAINT`, domain checks, cascade/restrict behavior, unsupported schemas, and broader PostgreSQL expression semantics remain pending |
| P2 foreign-key note | 🟡 | 🟡 | ✅ | Bounded single-column foreign keys are engine-owned and WAL-replayed for `ALTER TABLE [ONLY] [public.]child ADD CONSTRAINT name FOREIGN KEY (child_col) REFERENCES [public.]parent(parent_col)` when the referenced column has a supported primary-key or unique constraint and both columns have the same supported type. The supported slice validates existing child rows before add, enforces child-side `INSERT`, `COPY FROM`, and filtered `UPDATE`, protects parent deletes and referenced-key updates before visibility, exposes metadata through `information_schema.table_constraints`, `information_schema.key_column_usage`, `pg_catalog.pg_constraint`, `pg_catalog.pg_description`, and real `psql \d+` child/parent describe output, and participates in supported comment, rename, drop, table-rename, column-rename, and dependent drop rejection flows. Cascading actions, deferrable constraints, multi-column/self-referential FKs, NULL/MATCH semantics, unsupported schemas, and broader dependency/cascade behavior remain pending |
| P2 column-default lifecycle note | 🟡 | 🟡 | ✅ | Bounded `ALTER TABLE [ONLY] [public.]table ALTER [COLUMN] column DROP DEFAULT` is engine-owned and WAL-replayed for supported literal and sequence-backed `int4`/`text` column defaults, including implicit `SERIAL` defaults. The supported slice clears metadata-backed defaults from `information_schema.columns`, `pg_catalog.pg_attrdef`, and real `psql \d`, preserves rows, sequence state, and unrelated table/index/constraint/comment metadata, rejects omitted-column inserts before WAL append/visibility after the drop instead of applying stale defaults, and is covered by real PostgreSQL 16 psql golden traffic. No-default column adds, ADD COLUMN SERIAL, identity, generated, expression, NULL, sequence ownership/dependency semantics, and broader ALTER COLUMN semantics remain pending |
| P2 column-rename/drop note | 🟡 | 🟡 | ✅ | Bounded `ALTER TABLE [ONLY] [public.]table RENAME [COLUMN] old_name TO new_name` is engine-owned and WAL-replayed for supported public base tables. The supported rename slice retargets column catalog metadata plus primary-key/unique/check/index metadata, preserves row values, defaults, attnums, and column comments, rejects missing/duplicate/view targets, and is covered by real PostgreSQL 16 psql golden traffic. Bounded `ALTER TABLE [ONLY] [public.]table DROP [COLUMN] column` is engine-owned and WAL-replayed for non-indexed/non-constrained columns on supported public base tables. The supported drop slice rewrites visible rows before catalog exposure, removes the dropped column/default/comment metadata, compacts remaining column ordinals while preserving later-column comments, keeps remaining defaults usable for omitted inserts, rejects missing/view/index-or-constraint/check-dependent columns, and is covered by real PostgreSQL 16 psql golden traffic. Multi-column drops, `CASCADE`/`RESTRICT`, dependency-aware column drops, type changes, and broader ALTER TABLE semantics remain pending |
| P2 table discovery note | 🟡 | 🟡 | ✅ | Common `information_schema.tables` base-table discovery with `table_type = 'BASE TABLE'` and system-schema exclusion returns supported `public` session tables from catalog metadata; broader relation kinds and unsupported schemas remain out of scope |
| P2 column discovery note | 🟡 | 🟡 | ✅ | Common `information_schema.columns` discovery with system-schema exclusion returns supported `public` session-table columns from catalog metadata; unsupported schemas and broader PostgreSQL type/catalog surfaces remain out of scope |
| P2 all-schema table listing note | 🟡 | 🟡 | ✅ | Real PostgreSQL 16 `psql \dt *.*` and `\dt+ *.*` all-schema table listing returns supported `public` session tables from catalog metadata; unsupported schemas and broader relation kinds remain out of scope |
| P2 all-schema relation describe note | 🟡 | 🟡 | ✅ | Real PostgreSQL 16 `psql \d *.*` and `\d+ *.*` all-schema relation description returns supported `public` session-table relation rows from catalog metadata, then reuses existing per-table describe probes for supported columns/types and verbose column storage/access-method display; unsupported schemas and broader relation kinds remain out of scope |
| P2 exact schema table listing note | 🟡 | 🟡 | ✅ | Real PostgreSQL 16 `psql \dt public.<table>` exact schema-qualified table listing returns only the requested supported `public` session table from catalog metadata; unsupported schemas and broader relation kinds remain out of scope |
| P2 verbose schema table listing note | 🟡 | 🟡 | ✅ | Real PostgreSQL 16 `psql \dt+ public.*` verbose schema-wildcard table listing returns supported `public` session tables with catalog-backed persistence/access-method metadata; heap size and description remain blank until those metadata surfaces exist |
| P2 verbose schema-wildcard describe note | 🟡 | 🟡 | ✅ | Real PostgreSQL 16 `psql \d+ public.*` verbose schema-wildcard relation description is covered for supported `public` session tables, reusing namespace-scoped relation lookup plus metadata-backed verbose column/type/storage/access-method probes |
| Full PostgreSQL wire protocol compatibility | 🚫 | 🚫 | 🟡 | P3 first slice supports `Parse`/`Bind`/`Describe`/`Execute`/`Sync`/`Close` for text-format prepared relational `SELECT` portals with `int4`/`text` parameters, session-local statement/portal close lifecycle including statement-close cascade invalidation for dependent portals, successful portal-close cleanup with prepared-statement reuse plus later missing-portal Execute recovery, PostgreSQL-compatible `CloseComplete` for nonexistent extended statement/portal names without entering error recovery, and SQL `PREPARE` state isolation for extended `Close Statement`, PostgreSQL-compatible duplicate rejection for named prepared statements and named portals before payload validation while preserving existing named portal state and unnamed replacement semantics, missing `Bind` target statements reported as `26000` before binary format validation, Bind-time parameter arity and supported `int4` text-input checks that reject malformed portals before installation, PostgreSQL-compatible text parameter/result format code counts of zero, one broadcast code, or exactly one per parameter/result column, stable invalid-UTF-8 and unsupported-NULL text bind rejection without installing portals, inferred `int4`/`text` parameter OIDs for supported catalog-backed SELECT predicates/limits when clients omit Parse OIDs while quoted `$n` string literals and commented `$n` tokens are not counted or substituted as protocol parameters, exact unquoted placeholder-token replacement preserves multi-digit tokens such as `$10`, zero-padded nonzero placeholder tokens such as `$01` bind by numeric parameter identity, apostrophe-bearing text parameters are escaped as SQL string data, and unquoted zero-numbered placeholders such as `$0` and `$00` are rejected before statement installation, Parse-time rejection of unsupported parameter OIDs, oversized explicit parameter-OID lists, unsupported non-DML non-SELECT extended statements, and broader unsupported parameterized SELECT shapes before statement installation, explicit portal `Describe` is the only source of extended-protocol `RowDescription` while later `Execute` emits only row data, `PortalSuspended`, or `CommandComplete`, plus bound-portal and unbound parameterized-statement description through psql `\gdesc`, bound-result consumption through psql `\bind ... \gset`, expanded-output rendering through psql `\bind ... \gx`, and crosstab rendering through psql `\bind ... \crosstabview`, successful text-parameter `\bind` execution plus placeholder-looking text values, quoted placeholder-looking SQL literals, multi-digit placeholder tokens, zero-padded nonzero placeholder binding, apostrophe-escaped text bind literals, line/nested-block commented placeholder tokens, zero-numbered placeholder rejection/recovery, inferred text predicates, mixed inferred `int4`/`text` conjunction and top-level `OR` predicates with invalid-`int4` recovery, reused inferred placeholders across multiple supported `int4` predicates, commuted inferred placeholders such as `$1 <= id` / `$2 = name`, inferred predicate-plus-`LIMIT` parameters, negative `LIMIT` bind rejection with zero-limit and later valid-limit recovery, bound-portal `\gdesc` metadata for supported relational SELECTs whose later execution rejects a negative bound `LIMIT`, invalid bind execution followed by later bound-portal `\gdesc` recovery, empty-result portals, and repeated unnamed rebind execution for supported relational SELECTs, nonzero `Execute.max_rows` batching for supported relational `SELECT` portals via `PortalSuspended` plus later `Execute` resume with final `CommandComplete` reporting the completed portal row count, raw `Describe Portal` after bounded execution suspension as metadata-only state that preserves the resume position, and unlimited `Execute.max_rows=0` exhaustion through the same portal result cursor; extended-query errors, including raw Parse failures, missing `Describe Statement` / `Describe Portal`, missing `Execute` portal targets, and mismatched parameter/result format-code counts, skip later messages until `Sync`, then return ReadyForQuery; real PostgreSQL 16 `psql` `FETCH_COUNT` cursor flow now works for supported relational `SELECT` statements via session-local `DECLARE ... CURSOR FOR SELECT`, `DECLARE ... CURSOR WITHOUT HOLD FOR SELECT`, `DECLARE ... ASENSITIVE/INSENSITIVE ... CURSOR ... FOR SELECT`, `FETCH FORWARD n`, `FETCH NEXT`, omitted-count `FETCH FORWARD`, `FETCH ... IN`, separator-less forward `FETCH` forms, `FETCH FORWARD ALL`, `FETCH ALL`, zero-count `FETCH FORWARD 0`, transaction-end cleanup for session-local cursors, unquoted cursor identifier case-folding and quoted cursor identifier preservation including semicolons and doubled-quote escapes across `DECLARE`/`FETCH`/`MOVE`/`CLOSE`, forward-only `MOVE` including separator-less and zero-count forms, cursor `CLOSE`, duplicate cursor declaration rejection that preserves the existing cursor, named-cursor close missing-name errors, and `CLOSE ALL` handling, while `FETCH_COUNT` plus unresolved `\bind` placeholders, non-forward cursor/fetch/move directions, including `FETCH PRIOR`, `FETCH BACKWARD ALL`, `MOVE PRIOR`, and `MOVE BACKWARD ALL`, and unsupported `BINARY`/`SCROLL`/`WITH HOLD` cursor declaration options are explicitly rejected as unsupported cursor traffic; real PostgreSQL 16 `psql \gdesc` now works for supported relational `SELECT` descriptions, including parameterized statement descriptions without execution and psql's follow-up `pg_catalog.format_type` query over described `int4`/`text` result OIDs; real `psql` extended parameterized `INSERT`, filtered `UPDATE`, and filtered `DELETE` execute over supported public `int4`/`text` tables with command tags, bind-time type recovery, `NoData` `\gdesc` metadata, and later SELECT recovery; parameterized JOIN SELECT remains explicitly rejected with session recovery; minimal simple-query `COPY <table> TO STDOUT` streams supported table rows over PostgreSQL CopyOut messages, minimal simple-query `COPY <table> TO STDOUT WITH CSV`, `COPY <table> TO STDOUT WITH CSV HEADER`, and parenthesized `COPY <table> TO STDOUT WITH (FORMAT csv, HEADER, DELIMITER '|')` and `COPY <table> TO STDOUT WITH (FORMAT csv, HEADER, DELIMITER ';', QUOTE '|', ESCAPE '\')` stream supported CSV rows, minimal simple-query `COPY <table> FROM STDIN` imports supported text rows over PostgreSQL CopyIn messages, and minimal simple-query `COPY <table> FROM STDIN WITH CSV`, `COPY <table> FROM STDIN WITH CSV HEADER`, plus parenthesized `COPY <table> FROM STDIN WITH (FORMAT csv, HEADER, DELIMITER '|')` and `COPY <table> FROM STDIN WITH (FORMAT csv, HEADER, DELIMITER ';', QUOTE '|', ESCAPE '\')` import supported CSV rows; minimal binary `int4`/`text` parameter/result formats are supported for selected driver traffic, while broader COPY options such as NULL/FORCE settings, binary/query/extended COPY, broader binary formats/types, unsolicited frontend COPY data frames, frontend FunctionCall flow, and advanced portal/cursor behavior remain unsupported with named unsupported-feature errors; raw binary `Bind` coverage now pins supported `int4`/`text` binary encode/decode plus malformed format-count recovery, and raw frontend `CopyData`, `CopyDone`, `CopyFail`, and `FunctionCall` coverage pins unsupported SQLSTATE plus skip-until-`Sync` recovery and skipped side effects |
| P3 current-scope closeout | 🚫 | 🚫 | 🟡 | Closed for the supported relational `SELECT` plus `int4`/`text` protocol scope on 2026-05-16 once `protocol.extended_query`, `protocol.error_paths`, and real psql golden coverage were green. The first selected-driver reopening is also closed for `tokio-postgres`: startup/simple query/prepared SELECT, empty results, unsupported COPY option recovery, reconnect, and minimal binary `int4`/`text` Bind/result transfer are covered. Follow-up `sqlx`, `node-postgres`, and `asyncpg` gates cover startup/raw SQL or simple setup, prepared/bound SELECT, empty results, unsupported COPY option recovery, and reconnect or bounded pool behavior for the same supported subset. Post-P3 backlog: binary/query/extended COPY for fuller bulk import/export or dump/restore targets, broader binary parameter/result formats beyond supported `int4`/`text`, frontend FunctionCall, default asyncpg pool reset cleanup SQL, and scrollable/backward/holdable or otherwise advanced cursor/portal behavior required by an observed client. |
| P3 SQL-prepared limit-bind note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 `psql` extended `EXECUTE <prepared>($1, $2)` binds now cover SQL prepared relational `SELECT` statements whose second prepared argument feeds `LIMIT`: Parse/Describe infer mixed `text`/`int4` outer placeholders, invalid limit text and negative limit execution recover without losing prepared state, zero-limit returns no rows, later valid execution succeeds, and deallocation plus SELECT recovery remain stable. |
| P3 raw parameterized cursor declaration note | 🚫 | 🚫 | 🟡 | Raw extended `Parse`/`Bind`/`Execute` now accepts `DECLARE ... CURSOR FOR SELECT` and `DECLARE ... CURSOR FOR EXECUTE <sql-prepared>($n)` for the supported relational `SELECT` subset with text-format `int4`/`text` parameters, installing the session-local cursor at Execute time. Helper-level wire coverage also pins bound cursor-declaration portal `Describe` as `NoData`, marks successful `NoData` portal descriptions as described without executing them, rejects malformed multi-entry result-format vectors for those `NoData` portals before portal installation while accepting the valid broadcast text-format shape, SQL-prepared cursor declarations infer outer bind metadata from SQL prepared state, and extended portal/statement cleanup does not remove the installed session-local cursor. Real PostgreSQL 16 `psql \gdesc` over supported `DECLARE ... CURSOR FOR SELECT` now pins the same metadata-only `NoData` boundary before later declaration/fetch/close recovery; `DECLARE ... CURSOR FOR EXECUTE <sql-prepared>(literal) \gdesc` also returns precise SQL-prepared literal parameter errors without installing a cursor, `DECLARE ... CURSOR FOR EXECUTE <sql-prepared>($1)` through real `psql \bind` proves outer-bind NoData description plus later cursor installation/fetch/close recovery, and the SQL-prepared negative-`LIMIT` variant proves `NoData` Describe remains metadata-only while the later cursor Execute emits the stable negative-limit error and recovers for a valid cursor. Direct supported cursor declarations now also preserve negative-`LIMIT` error specificity: `DECLARE ... CURSOR FOR SELECT ... LIMIT -1` returns `LIMIT must not be negative`, installs no cursor, and later valid cursor/SELECT traffic recovers; negative cursor counts (`FETCH -1` / `MOVE -1`) remain explicitly unsupported as non-forward cursor traffic while preserving forward cursor state. Unsupported cursor declaration options now return the same explicit cursor-option unsupported error during extended Parse for real `psql \gdesc` over `BINARY`/`SCROLL` cursor declarations, instead of falling through to a generic extended-SELECT rejection. Real PostgreSQL 16 `psql` `FETCH_COUNT` now also works over supported SQL-prepared relational `EXECUTE <name>(...)` traffic, including quoted zero-parameter prepared names, by installing a session-local forward cursor over the SQL-prepared result; invalid, plain plus `\gset` / `\gx` / `\crosstabview` argument-count mismatch, negative-`LIMIT` including argument-bearing `\gexec`, zero-parameter literal negative-`LIMIT`, and zero-parameter `\gdesc` metadata-only plus `\gset` / `\gx` / `\crosstabview` / `\gexec` cleanup variants, and extended-bound invalid-bind SQL-prepared cursor execution through regular, client-variable, expanded-output, and crosstab consumers installs no `_psql_cursor` and leaves prepared statement cleanup or later valid execution stable as applicable; `FETCH_COUNT` plus unresolved `\bind` placeholders still emits unresolved cursor-placeholder traffic, installs no `_psql_cursor`, and remains explicitly unsupported in the golden suite while recovering for later cursor-style SELECT traffic; a real-client `_psql_cursor` name collision is pinned as duplicate-cursor failure followed by psql cleanup that leaves the transient cursor absent and later supported SELECT recovery intact. |
| P3 `FETCH_COUNT` describe-only cleanup note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 `psql` `FETCH_COUNT` over a supported relational `SELECT ... \gdesc` and extended `SELECT ... $1 \bind ... \gdesc` now returns result metadata without leaving psql's transient `_psql_cursor` installed; `\gdesc` remains metadata-only even when a pending `\bind` value would fail at execution time, a manual fetch probe reports the expected missing-cursor error, and later extended, cursor-style, plus plain supported SELECT traffic recovers |
| P3 `FETCH_COUNT` extended-bind result-consumer note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 `psql` `FETCH_COUNT` combined with plain extended `SELECT ... $1 \bind ... \g`, `\gset`, or `\gx` is explicitly rejected as unsupported parameterized cursor-declaration traffic, with plain `\g` pinned for invalid-looking-bind cleanup, `\gset` pinned across single-row, empty-result, multirow, and invalid-looking-bind source shapes, and `\gx` pinned across non-empty, empty, and invalid-looking-bind source shapes before bind-value validation; `SELECT ... $1 \bind ... \crosstabview` consumes supported non-empty and empty result shapes, rejects invalid `int4` bind input before transient cursor installation, and recovers for later crosstab traffic; `SELECT ... $1 \bind ... \gexec` executes generated supported SELECTs from non-empty cursor-style source rows, truthfully no-ops for empty source rows, and rejects invalid `int4` bind input before transient cursor installation; all paths leave no transient `_psql_cursor` and later extended bound SELECT traffic recovers |
| P3 extended-bind result-consumer error recovery note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 `psql` `SELECT ... $1 \bind ... \gset`, `\gx`, `\crosstabview`, and generated-SQL `\gexec` now pin invalid `int4` bind input as a stable bind-input error before result-consumer execution, install no bad portal, and recover for later supported result-consumer or SELECT traffic on the same connection |
| P3 `FETCH_COUNT` result-consumer cleanup note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 `psql` `FETCH_COUNT` over supported relational `SELECT ... \gset`, `SELECT ... \gx`, `SELECT ... \crosstabview`, and generated-SQL `SELECT ... \gexec` traffic now feeds client variables, expanded-output rendering, crosstab rendering, or generated supported SELECT execution, leaves psql's transient `_psql_cursor` absent after the consumer completes, reports the expected missing-cursor error on a manual fetch probe, and recovers for later plain supported SELECT traffic; SQL-prepared `EXECUTE <name>(...) \gdesc`, `EXECUTE <name>(...) \gset`, `EXECUTE <name>(...) \gx`, `EXECUTE <name>(...) \crosstabview`, `EXECUTE <name>(...) \gexec`, zero-parameter `EXECUTE <name> \gexec`, zero-parameter `EXECUTE <name> \gset`, zero-parameter `EXECUTE <name> \gx`, zero-parameter `EXECUTE <name> \crosstabview`, zero-parameter `EXECUTE <name> \gdesc`, argument-bearing SQL-prepared `EXECUTE <name>(...) \gdesc` / `\gset` / `\gx` / `\crosstabview` / `\gexec` argument-count cleanup, argument-bearing negative-`LIMIT` `\gdesc` / `\gset` / `\gx` / `\crosstabview` / `\gexec` cleanup, zero-parameter `EXECUTE <name>(...) \gdesc` / `\gset` / `\gx` / `\crosstabview` / `\gexec` too-many-argument cleanup, zero-parameter literal negative-`LIMIT` execution cleanup, zero-parameter literal negative-`LIMIT` `\gdesc` metadata-only cleanup, zero-parameter literal negative-`LIMIT` `\gset` / `\gx` / `\crosstabview` / `\gexec` failed-cursor cleanup, and invalid extended-bound `EXECUTE <name>($1) \bind ... \g` / `\gset` / `\gx` / `\crosstabview` / `\gexec` are covered by the same cleanup boundary while preserving prepared-statement cleanup and later supported traffic; empty generated-SQL source rows now no-op truthfully for both argument-bearing and zero-parameter SQL-prepared `\gexec`, invalid extended-bound generated-SQL input leaves no transient cursor, and generated supported COPY TO STDOUT commands stream table rows for zero-parameter and argument-bearing SQL-prepared traffic while leaving no transient cursor |
| P3 text Bind delimiter note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 `psql \bind` text parameters containing spaces, semicolons, commas, line-comment markers, and block-comment markers remain literal text data for supported relational `SELECT` predicates; binary text formats and broader PostgreSQL type/input behavior remain unsupported |
| P3 cursor movement note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 directionless numeric cursor movement plus forward-all and omitted-count marker/markerless variants are pinned for the supported relational cursor subset: `FETCH 0 FROM <cursor>`, `FETCH 2 FROM <cursor>`, `FETCH 0 IN <cursor>`, `FETCH n IN <cursor>`, markerless `FETCH 0 <cursor>` / `FETCH n <cursor>`, `MOVE 1 FROM <cursor>`, `MOVE 0 FROM <cursor>`, `MOVE 0 IN <cursor>`, `MOVE n IN <cursor>`, markerless `MOVE 0 <cursor>` / `MOVE n <cursor>`, `MOVE FORWARD ALL FROM <cursor>`, `MOVE FORWARD n IN <cursor>`, `FETCH FORWARD ALL IN <cursor>`, `FETCH ALL FROM <cursor>`, `FETCH ALL IN <cursor>`, `FETCH FORWARD FROM <cursor>`, `FETCH NEXT IN <cursor>`, `FETCH FROM <cursor>`, `MOVE FORWARD FROM <cursor>`, `MOVE NEXT IN <cursor>`, `FETCH FORWARD <cursor>`, `FETCH NEXT <cursor>`, `FETCH FORWARD ALL <cursor>`, `MOVE FORWARD <cursor>`, `MOVE NEXT <cursor>`, `MOVE FORWARD ALL <cursor>`, markerless `MOVE ALL <cursor>`, and marker-bearing `MOVE ALL FROM <cursor>` / `MOVE ALL IN <cursor>` preserve cursor position truthfully and later supported SELECT traffic still works; broader non-forward directions such as `FETCH FIRST`, `FETCH PRIOR`, `FETCH BACKWARD n`, `FETCH BACKWARD ALL`, `MOVE LAST`, `MOVE PRIOR`, `MOVE BACKWARD ALL`, `MOVE RELATIVE n`, and `MOVE ABSOLUTE n` are explicitly rejected without advancing supported cursor state, and unsupported `SCROLL CURSOR` declarations leave later cursor/SELECT traffic recoverable |
| P3 cursor comment note | 🚫 | 🚫 | 🟡 | SQL comments inside supported `DECLARE ... CURSOR ... FOR SELECT`, `FETCH`, `MOVE`, and `CLOSE` cursor commands are accepted for the supported relational cursor subset while preserving cursor lifecycle state, truthful movement/fetch results, post-close cursor-not-found errors, and later supported SELECT recovery |
| P3 fetch-count result-cleanup note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 `psql FETCH_COUNT` over both empty and non-empty supported relational `SELECT` results closes psql's transient `_psql_cursor`, reports the expected missing-cursor error on a manual fetch probe, and recovers for later supported cursor-style plus plain SELECT traffic |
| Post-P3 COPY export note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 `psql` simple-query `COPY <supported_table> TO STDOUT`, leading-comment copy-out, client-side `\copy ... TO PROGRAM`, and generated `COPY ... TO STDOUT` through direct and SQL-prepared `FETCH_COUNT` `\gexec` stream supported `int4`/`text` public-table rows over PostgreSQL CopyOut messages and recover for later supported SELECT traffic. Simple-query SQL copy-in and client-side `\copy ... FROM PROGRAM` are supported for text and CSV `int4`/`text` public tables, including CSV `HEADER` and parenthesized one-character CSV `DELIMITER`, `QUOTE`, and `ESCAPE` flows. A checked-in `tests/compat/pg-dump/run.sh` smoke now proves PostgreSQL 16 `pg_dump --format=plain --no-owner --no-privileges --schema=public` can discover supported `public` `int4`/`text` tables, bounded stored views, bounded materialized views, bounded sequences, and bounded public domains/domain-backed table columns, dump `CREATE SCHEMA public`, `CREATE DOMAIN public.<domain> AS {integer|text}` plus domain comments, schema-qualified `CREATE TABLE` with literal `int4`/`text` column defaults and domain-backed columns, `CREATE VIEW public.<view> AS <supported SELECT>` including bounded child views over supported parent views, `CREATE MATERIALIZED VIEW public.<view> AS <supported SELECT> WITH NO DATA` plus materialized-view refresh data, bounded `CREATE SEQUENCE public.<sequence>` plus actual sequence-state `setval` replay, COPY data, metadata-only `CREATE INDEX name ON table (column)` definitions, and supported `COMMENT ON TABLE`/`COMMENT ON COLUMN`/`COMMENT ON INDEX`/`COMMENT ON CONSTRAINT` metadata for bounded primary keys plus `COMMENT ON VIEW`/`COMMENT ON MATERIALIZED VIEW`/`COMMENT ON SEQUENCE`/`COMMENT ON DOMAIN`, then restore it through real `psql` into a fresh `--shared-catalog` endpoint; the same smoke proves split plain `pg_dump --schema-only` plus `pg_dump --data-only` restores through real `psql`, COPY-free `pg_dump --format=plain --inserts --rows-per-insert=2 --no-owner --no-privileges --schema=public` dumps restore through real `psql`, `pg_dump --format=custom`, `--format=directory`, and `--format=tar` archives restore through real `pg_restore --no-owner --no-privileges`, custom/directory/tar archives restore when split into `pg_restore --schema-only` followed by `pg_restore --data-only`; split schema/data restores recreate materialized-view definitions/comments while leaving snapshots empty until an explicit refresh because PostgreSQL 16 data-only restore does not replay materialized-view refreshes, `pg_restore --jobs=2` for the directory archive, and custom-archive `pg_restore --clean --if-exists` over stale supported public tables/indexes/views/materialized views/sequences/domains into supported endpoints. Extended `\gdesc` copy-out/copy-in, extended-bound generated COPY, binary COPY, query COPY, NULL/FORCE COPY options, ownership/full ACL fidelity, constraints beyond bounded single-column primary keys, unique constraints, single-column literal check constraints, and bounded single-column foreign keys, sequence options/identity/ownership/dependency semantics beyond bounded value functions and sequence-backed int4 defaults, domain constraints/defaults/not-null/collations beyond bounded domain-backed table columns, view cascade/dependency semantics, broader function semantics, NULL/no-default/identity/generated/expression defaults beyond bounded sequence-backed int4 defaults, unsupported schemas/object kinds, broad DROP/schema/table/index dependency semantics beyond bounded public-table cleanup, bounded stored-view cleanup, and `COPY ... TO STDOUT \gdesc` remain explicitly unsupported before installing bad extended state |
| P3 simple-query framing note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 `psql \gexec` over supported relational text results containing semicolon-terminated SQL is pinned, including direct, extended-bound, SQL-prepared, SQL-prepared empty-source, zero-parameter SQL-prepared empty-source, direct generated COPY TO STDOUT, SQL-prepared generated COPY TO STDOUT, and extended-bound generated unsupported COPY FROM STDIN `FETCH_COUNT` cursor-style source retrieval; simple-query splitting preserves semicolons inside SQL string literals, quoted identifiers, and line/nested block comments, including semicolon-bearing SQL prepared-statement names and prepared-statement comment traffic, executes the generated supported SELECT when source rows exist, streams generated supported COPY TO STDOUT or rejects unsupported generated COPY explicitly, leaves transient `_psql_cursor` cleanup truthful under `FETCH_COUNT`, and later supported SQL remains usable. Non-`FETCH_COUNT` parameterized `psql \bind ... \gexec` is explicitly unsupported for now, returning a stable bind-arity error while preserving later supported SELECT recovery |
| P3 SQL prepared statement note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 SQL `PREPARE` / `EXECUTE` over supported relational `SELECT` statements is pinned for catalog-backed `int4`/`text` parameter predicates, `psql FETCH_COUNT` cursor-style retrieval over supported SQL-prepared `EXECUTE <name>(...)` results including quoted zero-parameter prepared names, argument-count failed-cursor cleanup including zero-parameter `\gdesc` / `\gset` / `\gx` / `\crosstabview` / `\gexec` too-many-argument cleanup, negative-`LIMIT` failed-cursor cleanup including argument-bearing `\gx` and generated-SQL `\gexec`, zero-parameter literal negative-`LIMIT`, zero-parameter literal negative-`LIMIT` `\gdesc` metadata-only, and `\gset` / `\gx` / `\crosstabview` / `\gexec` cleanup variants, and empty-result transient cursor cleanup, extended cursor declarations over `EXECUTE <prepared>($n)` in helper-level raw wire coverage including explicit conflicting Parse OID rejection before statement installation, extended `psql \gdesc` metadata description over `EXECUTE <prepared>` without executing the SQL prepared statement including literal argument validation errors, outer-bound metadata-only descriptions, and later recovery, extended `psql \bind` execution of SQL-prepared statements through `EXECUTE <prepared>($1, ...)` with SQL-prepared parameter OID inference, reused SQL-prepared placeholders through extended `EXECUTE <prepared>($1)` bind/describe/execute recovery, reordered outer placeholders through extended `EXECUTE <prepared>($2, $1)` bind/describe/execute recovery with remapped outer `text`/`int4` OID inference, casted and parenthesized reordered outer placeholders through extended `EXECUTE <prepared>(($2)::pg_catalog.int4, ($1)::text)` bind/describe/execute recovery with the same remapped OID inference, mixed literal/placeholder SQL `EXECUTE` arguments through extended binds where Parse/Describe infer only the remaining outer parameter OID, PostgreSQL escape string text literals such as `EXECUTE <prepared>(E'...', $1)` including hex/octal escapes, Unicode escape string text literals such as `EXECUTE <prepared>(U&'...', $1)` with default backslash and custom `UESCAPE` escapes, national-character string literals such as `EXECUTE <prepared>(N'...', $1)` and typed `text n'...'`, dollar-quoted text literals such as `EXECUTE <prepared>($tag$...$tag$, $1)`, newline-separated adjacent standard string literals such as `EXECUTE <prepared>('Ada'\n' Lovelace', $1)`, newline-bearing comment-separated adjacent literals such as `EXECUTE <prepared>('Ada' /*\n... */ ' Lovelace', $1)`, and supported typed string literals such as `EXECUTE <prepared>(text '...', $1)` / `EXECUTE <prepared>($1, int4 '...')` plus supported aliases such as `TEXT '...'`, `integer '...'`, and `pg_catalog.integer '...'` through the same mixed literal/placeholder path, including dollar-quoted text containing `--`, `/* ... */`, `::`, and unmatched parenthesis markers that must remain literal data during comment stripping, cast scanning, and parenthesized-argument normalization, compatible reused outer placeholders such as `EXECUTE <prepared>($1, $1)` over same-typed SQL-prepared arguments bind once and validate both uses, conflicting reused outer placeholders such as `EXECUTE <prepared>` with one outer placeholder mapped to incompatible SQL-prepared `int4` and `text` arguments are rejected before statement installation, sparse outer placeholders such as `EXECUTE <prepared>($2, 'literal')` preserve unused lower-numbered bind slots without bogus type coercion while validating the used placeholder, SQL-prepared extended results consumed by `\gset`, `\gx`, and `\crosstabview`, inferred predicate-plus-`LIMIT` parameters, negative `LIMIT` execution rejection with zero-limit and later valid-limit recovery, bound-portal `\gdesc` metadata for SQL-prepared negative `LIMIT` portals before later execution rejects the bound limit, zero-parameter prepared SELECTs through both `EXECUTE <name>` and `EXECUTE <name>()`, omitted type-list inference, quoted prepared-statement names including names containing parentheses, ` AS ` delimiter-looking text, doubled-quote escapes, and semicolons, escaped text `EXECUTE` arguments containing doubled apostrophes and commas, parenthesized, supported postfix-casted, and supported `CAST(... AS ...)` SQL `EXECUTE` int4/text literal or placeholder arguments including `LIMIT` values, SQL comments around supported `PREPARE` type lists, `EXECUTE` argument lists, and `DEALLOCATE PREPARE` targets, SQL comments containing delimiter-looking `AS` text or semicolons before the true statement separator, newlines/tabs after `PREPARE`, `EXECUTE`, `DEALLOCATE`, and `DEALLOCATE PREPARE` keywords, leading SQL comments before `PREPARE`, `EXECUTE`, and `DEALLOCATE PREPARE` statements, duplicate prepared-name rejection before replacement payload validation, matching and empty-result execution, invalid inferred-`int4` input recovery including `LIMIT`, malformed escape/Unicode literal rejection, same-line and no-newline comment-separated adjacent string literal rejection/recovery, unsupported cast-target rejection/recovery, SQL `EXECUTE` argument-count mismatch recovery that preserves the prepared statement, explicit unsupported `NULL` SQL `EXECUTE` arguments that preserve the prepared statement for later supported execution, `DEALLOCATE`, `DEALLOCATE PREPARE <name>`, `DEALLOCATE ALL`, and later supported SELECT recovery; raw Bind validation also revalidates inferred SQL-prepared outer parameter types before portal installation; broader SQL prepared statement expression support remains pending |
| P3 raw Bind format arity note | 🚫 | 🚫 | 🟡 | Helper-level raw wire coverage validates Bind result-format-code arity for SQL-prepared `EXECUTE <prepared>($n)` portals using supported result-column metadata and for supported cursor-declaration `NoData` portals using a zero-column result contract, so malformed result-format vectors fail before portal installation while valid text-format vector shapes remain accepted; PostgreSQL 16 `psql` does not expose malformed result-format vectors from stable golden-suite flows |
| P3 extended mutation Describe note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 `psql \gdesc` over extended parameterized `INSERT`, filtered `UPDATE`, and filtered `DELETE` traffic returns `NoData` without side effects, and later bound Execute returns truthful DML command tags plus SELECT recovery |
| P3 extended gset multirow recovery note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 `psql \bind ... \gset` over a supported extended SELECT that returns multiple rows is pinned as a client-side result-consumer error path: psql reports the multirow `\gset` error after consuming the result, and later supported extended-bound SELECT traffic recovers on the same connection |
| P3 extended gset empty recovery note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 `psql \bind ... \gset` over a supported extended SELECT that returns no rows is pinned as the adjacent client-side result-consumer error path: psql reports the no-row `\gset` error after consuming the result, and later supported extended-bound SELECT traffic recovers on the same connection |
| P3 extended gset error then describe recovery note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 `psql \bind ... \gset` over a multirow supported SELECT is pinned across the next metadata request: after the client-side `\gset` error, a later supported `\bind ... \gdesc` still returns catalog-backed result metadata and execution remains usable on the same connection |
| P3 extended gset invalid-bind recovery note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 `psql \bind ... \gset` over a supported extended SELECT is pinned across invalid `int4` bind input: the bind-time error installs no bad portal, and later correctly-bound `\gset` variable capture plus supported SELECT traffic recover on the same connection |
| P3 extended crosstab invalid-bind recovery note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 `psql \bind ... \crosstabview` over a supported extended SELECT is pinned across invalid `int4` bind input: the bind-time error installs no bad portal, and later correctly-bound crosstab rendering succeeds on the same connection |
| P3/P2 int4 client literal note | 🚫 | 🚫 | 🟡 | Real PostgreSQL 16 `psql` traffic can create supported tables with `INT4` and `pg_catalog.text` type names, then use plus-signed `int4` literals through simple relational `SELECT`, extended `\bind`, and SQL `PREPARE` / `EXECUTE`; broader PostgreSQL type grammar remains pending |
| Async PostgreSQL driver compatibility matrix | 🚫 | 📌 | 🟡 | Checked-in app-shaped smoke suites exist for `tokio-postgres`, `sqlx`, `node-postgres`, and `asyncpg` against the supported SQL/protocol subset. `tokio-postgres` covers connect/startup negotiation, simple query, prepared/bind query with `int4` parameter and `int4`/`text` results, empty results, explicit unsupported COPY option handling, recovery, and reconnect. `sqlx` covers startup, raw SQL setup over the simple-query path, prepared/bound relational SELECT, empty results, explicit unsupported COPY option handling, recovery, and pooled reconnect. `node-postgres` covers startup, simple query, named prepared/bound SELECT, empty results, explicit unsupported COPY option handling, recovery, pool query, and reconnect from a JavaScript client. `asyncpg` covers startup, raw SQL setup, prepared/bound relational SELECT, empty results, explicit unsupported COPY option handling, recovery, reconnect, and a one-connection pool query using the driver's default reset cleanup SQL. Future driver gates remain open for `pgx` and JDBC/R2DBC. Compatibility beyond the supported subset remains future work, especially COPY streaming, broader binary formats/types, richer type/OID metadata, advanced portal behavior, TLS/GSS/cancel/notification depth, real advisory-lock semantics beyond reset cleanup no-ops, and broad SQL grammar. |
| Broad PostgreSQL SQL grammar coverage | 🚫 | 🚫 | 🟡 | Incremental after core replication + durability gates |

## Storage + Durability

| Capability | v0 | v0.5 | v1 | Notes |
|---|---:|---:|---:|---|
| WAL append + flush before visibility | ✅ | ✅ | ✅ | Non-negotiable invariant with regression tests |
| Commit/apply/visible index monotonicity | ✅ | ✅ | ✅ | Covered by engine and replication tests |
| WAL flush failure rollback of unapplied tail | ✅ | ✅ | ✅ | Ensures failed durability does not become visible |
| Crash recovery replay runbook | 🟡 | 🟡 | ✅ | P5 durable storage now writes the flushed WAL prefix to a checksummed local segment, can install a checkpoint-control file that identifies that segment and validates durable record count plus last transaction id, can persist/recover an ordered multi-segment local WAL archive manifest with per-segment record-count and transaction-range validation, can ingest a newly arrived checksummed WAL segment into that archive after continuity/timestamp validation, can recover a validated archive prefix to an exact archived transaction id or exact engine-written timestamp boundary, can combine a checkpoint-control base backup with a validated overlapping archive suffix to an exact transaction or timestamp target, can plan/apply local transaction-target and timestamp-target archive cleanup that rewrites the manifest to the retained durable prefix before removing obsolete post-target segment files while preserving retained timestamp metadata, can plan/apply checkpoint-backed base-window archive cleanup that retains the base-boundary record plus durable suffix while preserving base-plus-archive transaction/timestamp restore, can plan/apply checkpoint-backed PITR-window cleanup selection that validates archive timestamps and rejects unsafe recent-base windows before mutation, can fork exact transaction/timestamp targets into local branch archives with registry-checked sidecar timeline identity/ancestry metadata, can select/recover a named registered local timeline after registry/sidecar/branch archive validation, can prune a local timeline registry to one retained target plus ancestors after validating registered sidecars/branch archives and then remove only unreferenced sidecars/manifests/segments, can dry-run/apply scheduler-safe local maintenance cleanup through a checked operator preflight that validates checkpoint-backed archive retention and registered-timeline pruning together before mutation while preserving retained registered-timeline recovery, rejects stale sidecars and unsafe recent-base windows before archive/registry mutation, and can export/restore a checked file-backed object-bundle backup with per-object byte-length/checksum validation, manifest-object/backup-metadata cross-checks, and staged segment install before restored manifest install. Replay restores relational catalog plus table data at the committed boundary. Physical page-image base backups, production object-storage APIs, automated production timeline failover orchestration beyond local registered-target selection/pruning, and live background archive-cleanup scheduling remain open |
| Relational equality access path | 🚫 | 🟡 | ✅ | First P5 slice maintains an in-memory equality index from WAL-applied inserts, rebuilds it during durable WAL/file replay, and uses it for supported `WHERE column = literal` reads through MVCC key-batch lookup |
| MVCC retention/vacuum boundary | 🚫 | 🟡 | ✅ | First P5 checkpoint-vacuum slice prunes versions deleted at or before a durable safe transaction id, rejects boundaries crossing active transactions or unflushed WAL, and leaves WAL replay able to reconstruct pruned historical versions |

## Replication + Roles

| Capability | v0 | v0.5 | v1 | Notes |
|---|---:|---:|---:|---|
| `LocalReplicator` leader/follower role gates | ✅ | ✅ | ✅ | Followers reject writes |
| Commit/applied watermark tracking | ✅ | ✅ | ✅ | Snapshot metadata hooks present |
| Raft replicator scaffolding interface | 🟡 | ✅ | ✅ | Interface-first before full deployment |
| 3-node Raft operation + catch-up | 🚫 | 🟡 | ✅ | P6 now has a reproducible packaged-local 3-node smoke harness proving leader write, follower catch-up, and read-after-apply through typed `AppendEntriesRequest` / `AppendEntriesResponse` messages with a tested binary frame codec and reusable single-request TCP send/serve helpers; `OperationalDeploymentPreflightReport` emits stable pass/fail, commit/apply/caught-up evidence, TCP append-entries transport evidence, deterministic request-vote election evidence, packaged-local entrypoint evidence, scope, and explicit deployment-gap lines. A bounded packaged multi-process smoke launches independent follower child processes and proves TCP AppendEntries catch-up/read-after-apply across process boundaries. A packaged service smoke keeps two follower service processes alive across multiple sequential AppendEntries connections, applies committed entries, reports catch-up/read-after-apply, and exits through controlled shutdown. A bounded supervised-restart smoke restarts one follower service, replays the full durable prefix after restart, keeps a second follower service live, and verifies both followers catch up before controlled shutdown. A local Docker container smoke packages those follower services into two containers, drives real TCP AppendEntries from the host leader, verifies committed apply/read-after-apply, and exits through controlled shutdown. A bounded container-restart smoke restarts one follower container, replays the full durable prefix, and keeps a second follower container live. A Docker Compose restart smoke starts both follower services from a checked-in Compose file, restarts one Compose service, re-discovers its host port, replays the full durable prefix, and keeps a second service live. Systemd verification validates checked-in follower unit/environment artifacts, unit syntax, and the follower-service ExecStart contract. Kubernetes verification validates checked-in two-follower Deployment/Service manifests against the same follower-service command/env/port contract. `scripts/run_replication_deployment_preflight.sh` aggregates the service, systemd, Kubernetes, and Compose restart checks into one bounded local deployment gate; live production service-manager supervision and live Kubernetes rollout remain open |
| Failover readiness basics | 🚫 | 🟡 | ✅ | First P6 smoke path covers deterministic request-vote election, old-leader write rejection after leader transition, and elected-leader continuation; the operator report requires matching promoted-leader/follower commit/apply indexes, old-leader rejection, promoted-node `Leader` role, and election vote count meeting quorum |

## CPU/GPU Execution

| Capability | v0 | v0.5 | v1 | Notes |
|---|---:|---:|---:|---|
| Deterministic batch ordering | ✅ | ✅ | ✅ | ADR-002 |
| Device routing abstraction (`DeviceRouter`) | ✅ | ✅ | ✅ | Explicit reasons for fallback |
| Fallback reason telemetry (`Unavailable`, `QueueSaturated`, `MemoryPressure`, `NotGpuEligible`) | ✅ | ✅ | ✅ | Metrics + tests in place |
| SQL-to-GPU bridge for relational reads | 🚫 | 🟡 | ✅ | P4 slices can run plain relational `SELECT * FROM table` row fetches through the MVCC CUDA probe path with SQL-level parity and bridge-rate reporting; supported equality predicates, same-column equality `OR` groups, and literal `IN (...)` membership now lower through the relational equality-index `KeyBatchLookup`, unordered `LIMIT` is pushed into MVCC/CUDA execution, projection-only SQL result shaping no longer counts as GPU fallback after GPU row fetch, supported decoded-column `ORDER BY` uses an ordered key-batch access path with `LIMIT` pushdown, ordered `LIMIT`/`OFFSET` pagination fetches the needed prefix and skips rows during host result shaping without reporting `GpuMvccReadParityGap`, and `SELECT DISTINCT` plus supported `COUNT(*)`/`SUM(int4)`/`AVG(int4)`/`MIN`/`MAX`, grouped count/sum/avg/min/max reporting, and bounded grouped `HAVING` result filtering shape rows after GPU row fetch without reporting a parity gap; narrow literal range predicates use a filtered key-batch bridge, literal `BETWEEN` predicates normalize to inclusive range conjunctions, prefix-only text `LIKE` predicates use filtered/ordered key-batch bridges, narrow `AND` conjunctions of supported predicates use a conjunctive key-batch bridge, and other top-level `OR` groups of supported predicates, including parenthesized groups, use a disjunctive key-batch bridge instead of reporting `GpuMvccReadParityGap` |
| Production GPU execution kernels for OLTP subset | 🚫 | 🟡 | ✅ | Must preserve deterministic replay |
| CPU/GPU parity validation harness | 🟡 | 🟡 | ✅ | Detailed plan in `docs/testing/parity-and-jepsen-plan.md` |

## Observability + Operations

| Capability | v0 | v0.5 | v1 | Notes |
|---|---:|---:|---:|---|
| Runtime commit/fallback/flush counters | ✅ | ✅ | ✅ | Includes latest-reason signals |
| SLO-aligned observability baseline | 🟡 | ✅ | ✅ | Expanded with replication rollout |
| Relational workload performance proof | 🚫 | 🟡 | ✅ | P7 reports cover indexed app-style lookup workloads, analytical full scan, analytical range filter, conjunctive analytical filter, and disjunctive analytical filter with correctness validation, fallback rates, device info, per-engine CUDA driver probe runtime caching, SQL-visible H2D/D2H counters plus normalized transfer pressure, access-path evidence, CPU/GPU-probe timing, p50/p95/max latencies, and CPU-vs-GPU total ratios; after equality/range/`BETWEEN`/conjunction/disjunction predicate bridge pushdown, same-column equality `OR` index batching, and compact requested-row H2D transfer for key-batch reads, the benchmark mix records 0% SQL fallback. The 2026-05-17 residency baseline validates cold/resident-snapshot/post-mutation CPU/GPU correctness, records resident-byte accounting, a retained real CUDA allocation/copy handle for encoded resident snapshot bytes on the local RTX 3090, retained-device-memory `COUNT(*)`, int4 equality-predicate `COUNT(*)`, int4 membership-predicate `COUNT(*)`, int4 range-predicate `COUNT(*)`, int4 `BETWEEN`-predicate `COUNT(*)`, retained int4 `AND`/`OR` filter-group `COUNT(*)`, retained text prefix `LIKE` `COUNT(*)`, int4 scalar `SUM`/`AVG`/`MIN`/`MAX`, int4 filtered scalar `SUM`/`AVG`/`MIN`/`MAX`, int4 `BETWEEN` scalar `SUM`/`AVG`/`MIN`/`MAX`, int4 predicate-projection, int4 paginated distinct projection, int4 paginated filtered distinct projection, bounded int4 paginated filtered ordered-projection, int4 grouped COUNT/SUM/AVG/MIN/MAX with grouped `HAVING`, and int4 filtered grouped COUNT/SUM/AVG/MIN/MAX with filtered grouped `HAVING` kernel/probe proofs over that allocation, WAL-applied mutation invalidation metadata, manual post-mutation refresh-cost metadata, memory-pressure fallback metadata, deterministic resident-snapshot budget admission/eviction, oversized-snapshot rejection, and bounded resident table-data snapshot SELECT probes with zero per-query H2D transfer for the supported public table lookup and aggregate/distinct shapes. Engine status/telemetry now reports resident tables, cache state, per-GPU bytes/budgets, validity/invalidation, admission/eviction/rejection decision facts, refresh metadata, retained device-memory proof metadata, and latest resident route decisions with query-shape, cache-state, byte-cost, refresh, and D2H-row facts through `RelationalResidentCache`; accepted resident route decisions now execute by default through normal `Engine::execute_relational_select(...)` for retained-device-memory `COUNT(*)`, supported count predicates, unfiltered/filtered/`BETWEEN` int4 scalar aggregates, bounded int4 grouped aggregate shapes with grouped `HAVING`, and bounded int4 range-predicate projections while non-accepted routes fall back to the existing MVCC/CUDA-probe path. `Engine::warm_relational_residency_with_policy(...)` now provides bounded operator-triggered warmup/policy evidence for named or all supported public base tables, optional per-GPU budget application, invalidated-entry refresh, memory-pressure skips, deterministic admission/eviction, and route-readiness reporting; broad production CUDA cache behavior still requires richer expressions beyond literal comparison/range/membership/prefix predicates and bounded aggregate/projection reporting, production background cache orchestration/daemon scheduling, durable GPU pages, and driver-level kernel/transfer timing refinement |
| Security/compliance control mapping | 🟡 | ✅ | ✅ | See architecture doc 07 |
| Backup/PITR/DR test gates | 🟡 | 🟡 | ✅ | See architecture doc 08 |

## Explicit Non-Goals Through v1

- Full PostgreSQL extension ecosystem compatibility
- Advanced online reconfiguration automation
- Broad multi-region orchestration automation

## Update Policy

Update this matrix whenever:
1. a roadmap phase shifts,
2. an ADR changes a compatibility boundary, or
3. a feature moves between 🚫/🟡/✅.

Keep entries short and test-gated where possible.
