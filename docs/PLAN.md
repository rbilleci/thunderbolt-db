# PLAN — Unified Work Ledger

This is the **only document allowed to own open, deferred, blocked, or sequenced project work**.
`STATUS.md` owns current facts, `HANDOVER.md` owns the short resume baton, `ARCHITECTURE.md` and
`docs/design/` own design, and `DECISIONS.md` owns rationale. Action language elsewhere must reference a
task ID here or be explicitly historical.

## How to use this ledger

- States: **NOW** (active focus), **NEXT** (ready after NOW), **BLOCKED** (named prerequisite), **PARKED**
  (deliberately outside the current horizon), and **VERIFY** (old finding must be checked against the tree).
- A deferred task must name its trigger. Completed work is removed from this file and summarized in
  `STATUS.md` or the implementation archive.
- Future agents update one row here rather than creating a new plan, checklist, proposal sequence, or open
  board. Design documents may be linked as evidence but never override this ledger.
- Every implementation slice follows `CHARTER.md`, includes non-vacuous GPU execution evidence, and runs the
  relevant correctness/performance gates in `AGENTS.md`.

## Current focus

Maintain one active priority path; do not start another feature slice while its acceptance is unresolved. The
BENCH-001 preflight made three existing tasks explicit prerequisite owners. READ-002's canonical compound-key
milestone and PRODUCT-002's canonical SQL/type milestone passed on 2026-07-19; their broader post-benchmark breadth
does not delay the architecture evidence gate. The active sequence now starts at PRODUCT-001:

1. **PRODUCT-001 — canonical serving and write-path unification.** Carry the accepted PRODUCT-002 routes through
   the engine-backed Parse/Bind/Execute path, prepared R1/W1 and atomic T8/T32 sessions, and crash-durable
   synchronous acknowledgement. Enforce every manifest numeric route envelope, including pre-WAL route-class
   derivation and admission. Every live database-local logical mutation reached through autocommit, predeclared
   execution, interactive `COMMIT`, generic DML/DDL, or COPY crosses one typed transaction-admission interface before
   any `commit_seq` or WAL claim. One authoritative coordinator owns global logical order, canonical WAL/range
   mapping, durable/applied joining, atomic publication, and publication-covered terminal eligibility; pgwire only
   encodes and sends that outcome. ADR-014 sequence/system transitions enter the same coordinator as their own typed
   records rather than being folded into a user transaction they must outlive. Read-only work is never sequenced.
   Recovery never claims or re-appends work: one replay-only interface consumes canonical records in their existing
   order and performs the sole startup publication. Exactly one product pgwire binary and one public facade
   mutation/transaction submission boundary may reach the coordinator. Covered, classic, serialized, explicit-
   commit, COPY, and recovery-specific components are private preparation/apply/replay strategies behind that
   boundary; no superseded public write entry or embedded product-like endpoint remains. Lane activation must
   coexist with general SQL and explicit transactions; an intent-only engine is not a product answer.

   The legacy/target/API/consumer inventory, exact parsed-command facade-to-engine admission slice, and General
   atomic resource/user-transaction foundation are accepted. Arbitrary parsed programs remain `General`, support
   more than 32 operations, are statically bounded before staging, and currently expose explicit repeatable-read
   characteristics; post-WAL uncertainty is indeterminate and recovery-owned. W1/T8/T32 may become reachable only
   from the later engine-owned prepared-route proof, including READ COMMITTED semantics. Lower preparation/apply
   strategies remain plural, but the independently accepted authority slice now has one canonical commit mutex,
   replicator, `WalBuffer`, transaction-status index,
   direct apply order, checkpoint boundary, and contiguous publication join now own all live traffic. Optimized
   intent lanes own no sequence, physical WAL, apply queue, in-flight publication bridge, checkpoint writer, or
   visibility frontier; mixed classic/general/optimized traffic uses one crash-recoverable order, and fresh traffic
   creates no `.lane-*` files. Historical physical-lane input is consumed only during startup and a nonempty prefix
   remains read-only until offline migration. The independently accepted typed prepared-command foundation now
   parses `$n` into owned AST slots and binds typed values directly; its transitional SQL rendering is used only for
   existing WAL/retry identity and never reparsed on live execution. The canonical engine-backed server now owns
   one Parse/Bind/Describe/Execute/Close dispatcher shared by its sync, sequential-baseline, and async ingresses;
   Parse resolves neutral parameter/result metadata against one engine catalog snapshot, Bind is effect-free, and
   each portal executes its prepared AST at most once while supporting bounded suspension. The independently
   accepted engine-owned prepared route now derives W1/T8/T32 only as bounded optimizations, keeps indexed programs
   above 32 operations on uncapped `General`, implements READ COMMITTED/REPEATABLE READ plus read-your-writes, and
   retains exact ABA-safe source/index allocations with fail-closed replacement, purge, conflict, and pre-WAL
   validation proofs. The independently accepted facade slice now has exactly one public execution method,
   `SharedEngine::submit`, with typed text, prepared, batch-eligible read, session-close, and deterministic
   instrumentation variants. `EngineFacade`, `SessionId`, borrowed/stateless/session/batched/prepared free
   execution functions, the old sequential server loop, and public hook functions are deleted; enqueue is
   crate-private, batchers are engine-instance-bound, and every variant preserves transaction ownership. The
   independently accepted Describe-revalidation slice now validates statement and portal parameter/result metadata
   against the current catalog before either blocking or async ingress can encode it; stale parameter OIDs and result
   shapes fail without emitting cached metadata, while missing-object and failed-transaction precedence is preserved.
   The independently accepted transactional-DDL compatibility foundation stages one `CREATE TABLE` in a
   transaction-private catalog, exposes it only to its creating transaction, and commits it through the existing
   explicit-transaction WAL/publication claimant; an extended implicit cycle commits at Sync or rolls back after a
   later error. The independently accepted composite envelope now composes that CREATE with DML in either supported
   order, resolves/coalesces one typed transaction record, closes exact final GPU/index allocations before WAL, and
   performs one table-batched catalog/data publication plus recovery. It still rejects multiple DDL before a second
   private effect. The independently accepted transaction-private Parse/Describe slice now carries the connection's
   engine-bound session through effect-free metadata resolution, isolation-level statement snapshots, Bind/Execute,
   commit/rollback revalidation, and failed-rowless cached Describe; exact private-table DML validation remains
   narrow and published targets retain their generation proof. The independently accepted SQLx/simple-query slice
   moves SQLx onto the canonical server, executes bounded literal/NULL projections on GPU, gives every idle
   multi-statement segment exact PostgreSQL transaction-control and whole-message syntax precedence, and retains one
   exact quote/comment/dollar-aware scanner plus original WAL source identity. The independently accepted COPY slice
   moves simple and extended text/CSV COPY FROM/TO plus tokio-postgres coverage onto the canonical server. COPY FROM
   retains an exact engine/transaction/relation proof through zero-row or nonempty completion, repeats definitive
   constraint validation under the commit lock before WAL, and enters the sole facade admission and canonical
   publication owner. Simple COPY TO synthesizes its exact parsed SELECT, while extended COPY TO retains and
   executes its exact bound SELECT AST. The independently accepted connection-security slice now gives the product
   binary an explicit local-development trust profile plus a fail-closed production rustls/SCRAM-SHA-256 profile;
   PostgreSQL SASLprep/raw fallback, uniform wrong-user proof work, strict transcript validation, bounded framing,
   and both-ingress late-auth recovery wrap the existing dispatcher without adding an execution or commit owner.
   The accepted generation-scoped keyed-cancellation slice emits BackendKeyData, handles exact direct/TLS cancellation,
   cancels queued async metadata/facade/COPY work, preserves every post-submission error plus published mutation
   outcome, and moves node-postgres to the canonical target. Its first frozen-tree audit rejected error relabelling,
   queued CopyDone publication, uncancellable extended-metadata permit waits, and blocking/TLS malformed-cancel
   responses. A second frozen-tree audit accepted those direct repairs but found the same error-relabelling class in
   COPY/metadata/batched branches, four ordinary-permit async COPY entries, and overclaimed queue-path tests. Both
   rejection rounds were repaired. A third frozen-tree audit then found that synthetic simple-query cancellation
   left an explicit transaction usable and that a cached-row post-encoding check could overwrite an encoder error.
   Central failed-transaction ownership, a success-only encoded-response classifier, and deterministic blocking/
   async plus error-sentinel tests repaired both findings. A fourth frozen-tree audit accepted those families but
   found that queued async extended Sync and simple-query automatic COMMIT could still publish staged DML after
   cancellation. The active generation now reaches every implicit COMMIT (including simple COPY), pre-admission
   cancellation rolls back, post-admission COMMIT outcomes remain authoritative, and raw/lower-level zero-permit
   staged-row tests prove no publication plus reuse. A fifth frozen-tree audit accepted those repairs but found a
   missing post-session-lock guard around mutation-capable `BatchedText` fallback and pipelined simple-COPY frames
   surviving cancellation into ordinary dispatch. The final token check now follows that exact mutex, a barrier
   proves the race non-vacuously, and a narrow simple-COPY drain state plus simultaneous-ready transport/raw
   multi-frame tests prove one `57014`/Ready pair, zero publication, and reuse. Refreshed full gates pass, and a sixth
   fresh independent frozen-tree audit returned **ACCEPT** with no blocking findings. The independently accepted
   prepared-statement/portal and transaction-state compatibility slice moves asyncpg, psycopg, pgx, and JDBC plus
   the aggregate tokio-postgres gate onto the canonical server. It adds canonical startup parameters required by those unchanged
   clients, typed text/binary TEXT Bind decoding, effect-free pool cleanup and isolation metadata, exact portal
   suspension/exhaustion, and explicit failed-transaction rollback proof without adding a facade, WAL, or publication
   owner. Its first report card caught a measurable point-read regression from inserting the new command in the
   middle of the shared enum; the repaired append-only command invariant restores both cache regimes to the accepted
   baseline. The independently accepted psql/GPU-catalog/R2DBC slice now serves PostgreSQL 16 psql scenarios
   04/06/07 and the unchanged R2DBC `pg_catalog.pg_type` extension-autodetection query through the canonical server;
   R2DBC is no longer a legacy-catalog baseline and autodetection remains enabled. Versioned catalog rows are pinned
   at one statement snapshot, uploaded as transient device relations, and filtered, projected, grouped, ordered, and
   joined on the GPU. Explicit system/public relation identity, public-shadow and visibility semantics, RangeVar
   aliases with immutable OID provenance, mixed wildcard projection, LIKE prefixes, typed casts/arrays/aggregates,
   prepared AST execution, batcher engine ownership, and old typed-WAL view bodies all have focused fail-closed and
   actual-GPU coverage. No facade, transaction, sequence/WAL claimant, publication owner, or CPU relational product
   path was added.

   The independently accepted PostgreSQL 16 pg_dump/restore slice now generates and restores plain, custom,
   directory, tar, parallel-directory, clean/if-exists, insert-style, and split schema/data archives against the
   canonical server. Exact versioned dump programs retain quoted literal/identifier semantics, encode complete
   query-independent catalog candidates from one pinned generation, then run requested-OID joins, catalog joins,
   filters, aggregate, final scalar projection/gather, and ordering through typed GPU plans. Authoritatively empty
   programs carry an explicit catalog-model proof; semantic near misses fail closed. Sequence state is
   snapshot-pinned across drop/recreate ABA; bounded dump session
   controls preserve transaction and failed-transaction semantics; pg_dumpall globals restore non-bootstrap role
   LOGIN state, tablespaces, comments, and ACLs without widening unmodeled role privileges. Both harnesses own and
   clear exact child identities, reject occupied ports, and bind every restored relation ACL assertion to its
   object. Sequence ACLs use the relation-kind `rwU` owner baseline, source and restored sequence ACL output must
   match exactly, and a newly created default-privilege probe is checked in isolation. Subscription COUNT uses the
   block-reduced device equal-count scan for zero and nonzero results rather than host index cardinality. The slice
   adds no facade, transaction-id, WAL, recovery, sequence, commit, publication, or host-relational product owner,
   and the legacy server receives no new compatibility behavior.

   Final gates pass SQL 52, active engine 516, and a globally isolated include-ignored engine differential of 1,058
   tests. The nine-test catalog/facade HAZARD cohort passes three sequential and two concurrent rounds (45/45) with
   zero CUDA 700/716/717; psql 04/06/07, standalone R2DBC, and the full eight-driver aggregate pass. Workspace
   all-target/all-feature check, strict Clippy, format, shell, diff, dependency, and source-size gates are clean; the
   touched production/test maxima are 1,996/2,987 lines. The preceding two-layer/two-cache report card remains
   applicable because the audit repairs do not change a read kernel, residency layout, typed point-read route, or
   its result path. Successive rejection rounds were repaired and re-audited; the final independent implementation
   audit returned **ACCEPT** with no blocking finding at base
   `e937d5c737cee84a7ccb89c0f8e7dde8663aa84c`, index tree
   `47a52bda0ff64253f956e75cb61e8081bc759fd0`, and cached-diff SHA-256
   `b2ce5ac798fb1e7105e8b594bab514a70eae9da18c9be5072d8f698cac784991`, with 56 staged paths and no unstaged drift.

   The accepted pg_dump/restore slice passes SQL 57, active engine 525, facade 75 plus the 13-test concurrency cohort,
   canonical server 76, protocol 71 + 127, all 18 pg_dump restore/metadata/privilege cases, and pg_dumpall globals
   restore. Workspace check, strict workspace Clippy, format/shell/diff, source inventory, actual-GPU sequence ABA,
   and the complete application-driver aggregate are green. The mandatory repaired-tree card's clean isolated build
   completes both layers and cache regimes at 230.154M/s, p50 156us in-L2 and 198.870M/s, p50 201us out-of-L2 after
   a 2,129.4s build. Against the prior accepted 229.397M/s and 200.834M/s card, that is +0.3%/-1.0% and not a
   material regression. The former shared-target 201.959M/s/174.844M/s artifact reused a GCC-13.3-built AWS-LC
   archive on the GCC 15.2 host; two exact-source clean builds were byte-identical and restored 227–230M/s targeted
   throughput. The report-card runner now uses a fresh isolated target and records the compiler plus exact executable
   hashes before direct invocation. The two initial rejection rounds repaired recognition, transaction/session,
   snapshot, role, harness, and evidence
   defects. A third architecture audit then rejected the host-finalized dump catalog seam; the repair replaces
   query-time host filtering/join/count decisions with typed device plans and adds an actual-GPU cross-route
   differential. A fourth three-lane audit rejected host-derived subscription COUNT, sequence/default ACL fidelity,
   and contradictory acceptance/source-size evidence; the device-count, relation-kind ACL, exact-object harness,
   and documentation repairs above close those findings. Fresh architecture, semantics/security, and
   evidence/documentation lanes all returned **ACCEPT** with no blocker or implementation drift on full candidate
   tree `c32d6090b31a017197e828c539250c955e658d6e` and full binary-diff SHA-256
   `1dc026bba261f7e1e0a2114ec52ab2b74f34188640c3d5aa7a104024c78379dc` across 66 paths. The accepted immutable
   implementation target is base `744d2e1113f403afff1e88bc175f11f6cda2dc7e`, code/test index tree
   `91efd8ba72c666e1b51881257930f778a8c71d1d`, and cached binary-diff SHA-256
   `5671761b1fd4a62eab7457f9a8e731a3b807c3c2f58c6d0477bd826c5c8a96d8` across 62 code/test paths. This accepted
   slice stops here without starting the next PRODUCT-001 boundary.

   Three inherited base defects remain explicitly owned by **PRODUCT-001** rather than being mislabeled as slice
   regressions: `gpu_chunk_class_check_and_foreign_keys_stay_device_native`,
   `transaction_fk_commit_waits_across_classic_wave_tail_handoff`, and
   `gpu_single_wide_unique_indexes_elide_validate_collisions_and_recover`. The active next boundary closes the
   remaining compatibility proofs: broader transactional DDL, relaxation of the conservative full-generation
   conflict that serializes a staged CREATE after any intervening commit, remaining SQLSTATE/type-codec breadth,
   named client suites, and mixed recovery coverage. Only after those proofs are independently accepted does the
   legacy/P8 compatibility-and-deletion slice re-inventory the still-live psql/preflight/benchmark consumers,
   migrate or explicitly disposition each behavior, and delete the superseded listener plus independently callable
   P8 protocol adapters in the same audited slice. Do not retain a second product-like execution or write owner as
   a compatibility shortcut. Bounded/offloaded response streaming remains SCALE-001 and must be explicit while the
   async task still formats materialized results.

   Before migration, inventory every legacy server behavior, product target, public write API, test suite, and live
   consumer. Deletion requires the canonical server to pass the existing psql golden, pg_dump/restore, tokio-
   postgres, SQLx, node-postgres, TLS/SCRAM, COPY, cancellation, prepared-statement/portal, transaction-state, and
   catalog/introspection gates. Unsupported behavior needs an accepted compatibility deviation or PLAN owner and
   must fail before side effects. No slice may land an independently callable product path or commit owner even
   temporarily; move ownership and remove the old public entry in the same slice. Final source/build guards enumerate
   and enforce the sole server target, facade boundary, WAL claimant, and publication owner. Each independently
   reviewable slice receives a fresh read-only adversarial audit by a different agent over its focused gates and
   sabotage evidence; fix and re-audit every finding to ACCEPT before the next slice starts or merges, followed by a
   fresh whole-tree ownership audit after all deletions. PRODUCT-001 remains open until serving, mixed-traffic/
   recovery, compatibility-migration, deletion, and source-ownership gates all pass.

   **Ownership boundary:** RETIRE-002 reverse-gather/deauthorization repair remains a private representation-repair
   strategy behind the sole publication owner; PRODUCT-001 neither widens nor deletes it. Representation-only
   residency/compaction/repair publication preserves logical contents and `visible_next` and cannot bypass WAL for a
   logical mutation. DUR-001 checkpoint/PITR policy remains sequenced after the accepted BENCH baseline.
2. **COPY-001 — PostgreSQL-compatible GPU-native bulk COPY.** Start only after PRODUCT-001 proves the sole server,
   facade admission boundary, WAL claimant, and publication owner. This is necessary both for product ingestion and
   for development velocity: the canonical report card still constructs its 48M-row out-of-L2 fixture through about
   48,000 SQL-text INSERT/autocommit operations, and the current accepted evidence records **2,138.3s insert + 0.0s
   final residency**. INSERT chunks of 250/1,000/10,000 did not materially change per-row cost, so another larger SQL
   string or an outer transaction is not the target. Keep PostgreSQL `COPY ... FROM STDIN` and `psql \copy` as the
   public surface; do not add a benchmark-only direct-write authority.

   Parse the COPY command once and stream bounded, backpressured `CopyData` into typed columnar staging chunks. Retain
   PostgreSQL 16 text/CSV behavior and add standard binary COPY for the supported type vocabulary, including column
   lists, NULL markers, defaults/identity and sequence effects, quoting/escape/header/delimiter rules, exact row
   counts, and PostgreSQL error/transaction behavior. Lower each chunk to device-ready encoded append, MVCC sidecar,
   constraint, and maintained-index batches. The implementation must not retain an O(total rows)
   `Vec<Vec<SqlValue>>`, render the load back into a giant SQL INSERT for WAL, or reparse SQL during live apply or
   recovery. Hidden GPU preparation may advance by bounded chunks, but chunks never become user commits: an
   autocommit COPY owns one user transaction, canonical typed WAL/outcome chain, and atomic publication, while COPY
   inside an explicit transaction remains one statement in that transaction and publishes only with its other effects
   at COMMIT. Cancellation, `CopyFail`, constraint/type failure, disconnect, or pre-WAL resource refusal publishes
   nothing, while every acknowledged COPY remains RPO 0 and fresh-context recoverable.

   Acceptance compares the same inputs and failures against PostgreSQL 16 through real `psql \copy` plus simple and
   extended pgwire clients in autocommit and explicit transactions. Cover text, CSV, and binary formats; supported
   fixed/variable-width and NULL values; column/default mapping; CHECK/UNIQUE/FK enforcement; `CopyDone`/`CopyFail`,
   cancellation, SQLSTATE, command tag/count, rollback, retry identity, durable reopen, and replay-equivalent logical
   rows, identities, visibility, indexes, and catalog state. Prove bounded host staging (configured chunk owners plus
   fixed metadata, independent of total rows), non-vacuous GPU batch apply, no host relational authority, and no
   regression in COPY TO or ordinary INSERT/transaction semantics.

   The development-cycle gate uses the unchanged report-card `accounts(id int4, balance int4)` data and cache/read
   workload. On the canonical RTX PRO 6000 host under the report card's existing local/in-memory, no-fsync durability
   profile, load and publish the 48M-row Section C fixture through the canonical COPY path in **<=180s and >=270,000
   rows/s** (at least 10x faster than the frozen 2,138.3s setup baseline). After the script's recorded isolated clean
   build, complete both layers and cache regimes in **<=10 minutes**, including fixed cool-down gaps, and complete the
   entire canonical fresh-target invocation in **<=20 minutes** with build and execution time reported separately.
   Record parse/decode, staging, H2D, GPU validation/encode/apply/index, WAL/durability, publication, peak host/device
   bytes, and end-to-end COPY throughput through permanent `probe-timing` instrumentation. After small-fixture
   INSERT-versus-COPY semantic and physical-layout invariants pass, switch only Layer 2 fixture construction to this
   path; keep row counts, cache regimes, measured read route, samples, result contracts, and fixed baseline unchanged.
3. **BENCH-001 — runner and campaign.** Use COPY-001 for the deterministic 34M-row seed, then build the fixed
   open-loop sustained and `B01`–`B10` driver, tuned PostgreSQL profile, quiet-window qualification, and versioned
   artifacts; execute the immutable comparison. Publish the exact host, GPU/driver/runtime, durability, and PostgreSQL
   configuration. Never substitute the older P8 `order_line` microbenchmarks or add a benchmark-only write authority.
4. **DUR-001 — checkpoint/PITR policy.** Start only after preserving the accepted pre-DUR BENCH result, then rerun
   the affected cohorts to measure checkpoint-policy overhead.

**ROUTE-001** and **SCALE-001** remain blocked on BENCH-001 evidence; their broader route and connection-scale work
does not unblock the immutable banking campaign. **CFG-001** is touch-triggered: reckon a flag when its owning
subsystem is already being changed, rather than opening a separate competing workstream.

Physical multi-GPU work (**MULTI-001/002/003**) is user-deferred until every non-MULTI plan item is complete or
the user explicitly promotes it; its hardware gates remain mandatory and are parked at the end of the ledger.

## STRUCT-001 — oversized-file remediation method

The source-size standard is [`CODE_SIZE.md`](CODE_SIZE.md). The corrected 2026-07-12 baseline had **30 files outside
its analysis envelopes**: 18 production files over 2,000 lines, eight test files over 3,000 lines, and four examples
or tools over 3,000 lines. Every baseline outlier plus one subsequently crossed test threshold has a completed
audited disposition. The standalone **STRUCT-001** queue is **zero files**; `engine_expr.rs` is the sole accepted
registered exception. Two later-crossing engine files are explicitly reopened and owned by active **PRODUCT-001**
below, so they are not a hidden STRUCT queue or accepted exceptions.

### Analysis packet required for each file

Before editing an outlier, put a compact packet in its implementation commit/PR and distill the result into
that file's disposition cell in the inventory below:

1. current line count, class, and generated/archive status;
2. responsibility map by major types/functions/tests/kernels and the invariant owned by each cluster;
3. dependency map covering callers, imports/re-exports, shared state, feature gates, unsafe/device boundaries,
   build integration, and external API consumers;
4. change-history/co-change evidence sufficient to distinguish real cohesion from accidental accumulation;
5. one disposition: decompose, retain by registered exception, regenerate, archive, or delete;
6. for decomposition, the target module map, dependency direction, stable facade/re-exports, test moves, risks,
   and exact compile/test/GPU/performance gates.

Execute one behavior-preserving ownership extraction at a time. Keep semantic rewrites separate, update every
code/build/test/doc reference in the same slice, search for old paths and symbols, and do not create numbered
shards, catch-all modules, cycles, or a compensating `pub(crate)` sprawl. Target new modules below 1,500 lines.
Critical outliers over 5,000 lines receive priority; a pure structural move uses targeted correctness gates,
while any runtime/kernel/residency/result-path change also uses the full applicable `AGENTS.md` gates.

### Ordered inventory

Completed disposition cells preserve slice-time line counts and paths as historical evidence; later retirements
may delete a listed module. The fresh inventory command and `CODE_SIZE.md` exception registry are the current size
authorities, while completed deletion facts live in `STATUS.md`.

**Wave 1 — critical production context and dependency hubs.** Analyze facades and embedded implementation first
so extraction preserves crate APIs, GPU/kernel ownership, cache behavior, and stable symbol ABIs.

| Lines | File | Disposition / evidence |
|---:|---|---|
| 1,792 | `crates/execution/src/lib.rs` | **DISPOSITION COMPLETE — execution root now 1,792 lines and satisfies the production envelope.** Handwritten production facade with distinct routing policy, CUDA runtime/context/driver layers, resident-memory ownership, MVCC batch encoding, point-read submission/result ownership, write locate/apply, and scan/aggregate/join/sort/expression kernel families. Public root symbols are consumed by engine, planner, metrics, observability, examples, and server tests; stable crate-root re-exports are the facade. **Completed modules:** `routing.rs` (241), `mvcc_batch.rs` (208), `runtime_contract.rs`, sub-3,000-line test seams, `cuda_context.rs` (998), `cuda_driver.rs` (886), `staged_filter.rs` (1,447), `staged_mvcc.rs` (592), `resident_memory.rs` (444), `resident_header.rs` (92), `resident_text.rs` (310 after STRUCT-001FS/FV safety hardening), `resident_scalar.rs` (1,083), `resident_compare_ordered.rs` (1,088) plus `resident_compare_ordered.ptx` (803), `expression_filter.rs` (393), `resident_sort.rs` (1,206) plus `resident_argsort.ptx` (490 lines/four entries) and test-only `argsort_test_support.rs` (768), `resident_count.rs` (1,066), `resident_gather.rs` (258), `resident_filter.rs` (1,221 after STRUCT-001T safety hardening), `resident_aggregate.rs` (866 after STRUCT-001V safety hardening), `resident_group.rs` (1,282), `group_input.rs` (639), `write_locate.rs` (865 after STRUCT-001Z safety hardening), `write_apply.rs` (1,256 after STRUCT-001AB safety hardening), `resident_sidecar.rs` (1,102 after STRUCT-001AD safety hardening), `unique_coordinate.rs` (195), `point_read_submission.rs` (626), `point_read_submit.rs` (1,329), `point_read_dense.rs` (1,406), `point_read_bloom.rs` (206), `point_read_text.rs` (1,205 after STRUCT-001M/N correctness hardening), `point_read_rows.rs` (475), and `point_read_compound.rs` (1,107). STRUCT-001Z made the extracted write/visible-locate boundary total: typed same-context owners, exact index/version extents, on-device packed-slot bounds, reserved fail-closed count sentinels, and guaranteed launched-error draining. The exact execution inventory is 121 tests with 50 active and 71 GPU-ignored; five affected GPU gates passed 15 sequential and 10 concurrent invocations, and independent audit is clean. The remaining cross-context orchestration boundary is promoted as **MULTI-002** rather than hidden behind host probing. STRUCT-001AA isolated fused append publication, incremental int4 index insertion, and device compound-key folding with stable facade. STRUCT-001AB replaced raw addresses with typed owners/exact spans, fixed cross-block header publication ordering, added device text bounds and launched-error drains, and closed cleanly under the 120-test suite plus 24/16 HAZARD matrix. STRUCT-001AC isolated the five resident sidecar layout launchers with stable inherent APIs. STRUCT-001AD made those safe APIs total: owned same-context sources, exact source/destination spans, explicit bool/validity state writes, actual text-blob ownership and device validation, alias rejection, checked engine geometry, and failure drains. Its 124-test suite, 18/12 final HAZARD matrix, and independent re-audit are clean; the physical cross-context branch is **MULTI-003**. STRUCT-001AE isolated the bounded device uniqueness verdict exactly; STRUCT-001AF removed its exposed optimizer-sensitive ordered-index recast allocation and restored the report-card ratio. STRUCT-001AG isolated the exact row-count header PTX/launcher behind the unchanged facade; its direct context/cache gate passed 3 sequential and 2 concurrent invocations, independent re-audit is clean, and the canonical two-layer/two-cache card remained green. STRUCT-001FD isolated the normalized-exact join-coordinate window/rank/shift owner in the rustfmt-clean 1,076-line `join_window.rs` child, reducing the root to 17,032 lines. The only visibility change is the narrow `pub(super)` window-launcher bridge required by the existing root sort owner; crate-root public types and inherent APIs are unchanged. The three core GPU gates passed nine sequential plus six concurrent invocations with zero CUDA 700/716/717. Both execution modes passed 48/78, both engine modes passed 505/487, and workspace all-target check, strict execution/engine clippy, dependency/scoped source/reference/format checks, and independent audit are clean. PTX, ABI, geometry, partition/peer/NULL, shift, window, rank, error, synchronization, allocation, and bounded-readback behavior are source-equivalent, so the report card was not applicable. STRUCT-001FE isolated the normalized-exact device materialization/concatenation owner in the 904-line `join_materialize.rs` child and bounded final fixed/bool/text projection in the 782-line `join_projection.rs` child, reducing the root to 15,921 lines. A pre-edit size simulation rejected a single 1,668-line leaf; the final leaves use exact explicit imports with no visibility change or cycle. Four direct/engine/streaming GPU gates passed 12 sequential plus eight concurrent invocations with zero CUDA 700/716/717. Both execution modes passed 48/78, both engine modes passed 505/487, and workspace all-target check, strict execution/engine clippy, dependency/scoped source/reference/format checks, and independent re-audit are clean. All five PTX blobs, APIs, layouts, validation, geometry, NULL/OUTER/type/empty/error/allocation/ownership/readback behavior are source-equivalent, so the report card was not applicable. STRUCT-001FF isolated the normalized-exact stable device-coordinate sort method and merge launcher/PTX in the 417-line `join_sort.rs` child, reducing the root to 15,542 lines. The shared `CudaJoinOrderKey` remains uniquely at the root facade to avoid a cycle; the former root window-launcher bridge is now the explicit one-way `join_sort -> join_window` dependency, with no new visibility. Three direct/engine/streaming GPU sort gates passed nine sequential plus six concurrent invocations with zero CUDA 700/716/717. Both execution modes passed 48/78, both engine modes passed 505/487, and workspace all-target check, strict execution/engine clippy, dependency/scoped source/reference/format checks, and independent audit are clean. Rust/PTX, comparator, stable-tie, NULL/OUTER, direction, descriptor, context, merge, geometry, synchronization, allocation, error, and small/window behavior are source-equivalent, so the report card was not applicable. STRUCT-001FG isolated the normalized-exact GPU OUTER-join match bitmap and unmatched-coordinate completion owner in the rustfmt-clean 507-line `join_outer.rs` child, reducing the root to 15,144 lines. Narrow `pub(super)` bitmap fields preserve the unchanged parent fixed-join launcher's prior effective access; the crate-root type and inherent APIs remain stable, with no sibling dependency or cycle. Five direct/LEFT/RIGHT/FULL/type-wide/streaming GPU gates passed 15 sequential plus ten concurrent invocations with zero CUDA 700/716/717. Both execution modes passed 48/78, both engine modes passed 505/487, and workspace all-target check, strict execution/engine clippy, dependency/scoped source/reference/format checks, and independent audit are clean. Rust/PTX, GPU allocation/zeroing, atomic N:N collapse, bounded two-pass complement cardinality, scalar D2H, accumulated-coordinate D2D, OUTER padding, validation, geometry, synchronization, errors, and accounting are source-equivalent, so the report card was not applicable. STRUCT-001FH isolated the normalized-exact device-resident join-coordinate identity and post-join filter owner in the rustfmt-clean 428-line `join_filter.rs` child, reducing the root to 14,761 lines. Shared `CudaJoinCoordinatesU32` and `CudaPredicateMaskI32` contracts remain uniquely root-owned for fixed-join, expression, and other coordinate consumers; the child uses explicit imports with no visibility bridge, sibling dependency, or cycle. Five direct/OUTER-WHERE/Kleene/real-NULL-versus-pad/streaming GPU gates passed 15 sequential plus ten concurrent invocations with zero CUDA 700/716/717. Both execution modes passed 48/78, both engine modes passed 505/487, and workspace all-target check, strict execution/engine clippy, dependency/scoped source/reference/format checks, and independent audit are clean. Rust/PTX, identity eligibility, real/pad mask descriptors, descriptor H2D, two-pass compaction, bounded scalar D2H, coordinate D2D, validation, empty paths, ordering, geometry, synchronization, errors, and allocation accounting are source-equivalent, so the report card was not applicable. STRUCT-001FI isolated the normalized-exact accumulated fixed/text/composite coordinate join in the rustfmt-clean 1,309-line `join_fixed.rs` child, reducing the root to 13,489 lines. Shared key, coordinate, and predicate contracts remain uniquely root-owned; the existing root re-export supplies `CudaMatchBitmapU32` without a sibling path, visibility bridge, or cycle. Five direct/typed-NULL/N:N-text-numeric/streaming GPU gates passed 15 sequential plus ten concurrent invocations with zero CUDA 700/716/717. Both execution modes passed 48/78, both engine modes passed 505/487, and workspace all-target check, strict execution/engine clippy, dependency/scoped source/reference/format checks, and independent audit are clean. Rust/PTX, same-context and descriptor validation, 4/8/16/text/composite keys, eligibility and NULL exclusion, hash build/probe, owned/persistent match marking, INNER/OUTER count-and-emit, padding, bounded scalar readback, emitted-count verification, geometry, ordering, errors, synchronization, and allocation lifetime are source-equivalent, so the report card was not applicable. STRUCT-001FJ isolated the normalized-exact host-staged unique/N:N int/text hash-join benchmark/reference owner in the rustfmt-clean 1,390-line `staged_hash_join.rs` child, reducing the root to 12,112 lines. `HashJoinOutcome` remains root-re-exported; consumer audit proved the only non-test caller is the standard Layer-1 roofline example, and the stale transient-residency route comment now names the actual production coordinate API. Four direct GPU families passed 12 sequential plus eight concurrent invocations with zero CUDA 700/716/717. Both execution modes passed 48/78, both engine modes passed 505/487, and workspace all-target check, strict execution/engine clippy, dependency/scoped source/reference/format checks are clean. Rust/PTX, key/validity uploads, collision byte verification, duplicate verdicts, N:N chains, two-pass sizing, NULL exclusion, geometry, synchronization, errors, ordering, and pair readback are source-equivalent. Extraction audit found no moved-source regression but exposed an inherited undersized-validity device-read hazard, promoted immediately as STRUCT-001FK; FJ itself did not require the report card. STRUCT-001FK then made all four staged APIs total: `None` remains all-valid, every present bitmap must contain exactly `row_count.div_ceil(32)` words, malformed inputs fail before any early return/allocation/launch, and zero rows accept exactly zero words. The retained GPU contract gate covers short, empty-for-nonempty, oversized, zero-row, and positive NULL-exclusion behavior across unique/N:N int/text joins; it and the existing four-family matrix passed 15 sequential plus ten concurrent invocations without device faults. Both execution modes passed 48/79, both engine modes passed 505/487, all static gates and independent re-audit are clean, and Layer-1 remained stable (`hash_join_inner_i64` 256.0 to 257.8 M-element/s; resident payload control 6359.4 to 6313.5 M-element/s). The child remains bounded at 1,403 lines. STRUCT-001FL then isolated the shared resident payload-key/order-key descriptors and opaque device-coordinate owner in the rustfmt-clean 85-line `join_contract.rs` child, reducing the execution root to 12,035 lines. The private module preserves the three crate-root public names by re-export; narrow `pub(super)` coordinate fields and test readback match their former root-private effective scope, while `CudaPredicateMaskI32` remains with expression execution. Direct composite-coordinate, production three-way SQL, and over-budget streaming GPU joins passed nine sequential plus six concurrent invocations with zero device faults. Both execution modes passed 48/79, both engine modes passed 505/487, workspace all-target check, strict execution/engine clippy, dependency/scoped source/reference/format checks, and independent audit are clean. Type layout, derives, docs, accessors, unsafe test readback, external consumers, runtime, kernels, synchronization, allocations, and result behavior are source-equivalent, so the report card was not applicable. STRUCT-001FM then isolated the device predicate-mask type, five resident APIs, ordered mask compaction, retained-mask construction, row-range PTX, and mask-AND PTX in the rustfmt-clean 385-line `predicate_mask.rs` child, reducing the execution root to 11,682 lines. The stable crate-root type/API remains opaque externally; narrow `pub(super)` fields preserve the fixed/filter join consumers, and one root-private compact import preserves exactly nine typed-filter call sites without a sibling cycle. Direct predicate VM, OUTER-WHERE 3VL, and over-budget streaming-filter GPU routes passed nine sequential plus six concurrent invocations with zero device faults. Both execution modes passed 48/79, both engine modes passed 505/487, workspace all-target check, strict execution/engine clippy, dependency/scoped source/reference/format checks, and independent audit are clean. Both PTX blobs, symbols/arguments, arithmetic-VM calls, probe scope, buffer ownership transfer, context/allocation/launch/synchronization, empty/error behavior, and ordered results are source-equivalent, so the report card was not applicable. STRUCT-001FN then isolated the normalized-exact postfix VM types/interpreter in the rustfmt-clean 1,069-line `expression_vm.rs` child and shared checked text-window validation in the 41-line neutral `resident_window.rs` leaf, reducing the root to 10,624 lines. The neutral leaf prevents an expression/filter/predicate dependency cycle; stable crate-root type re-exports and one private runner import preserve the facade. Five i32/i64/i128/varlen/production GPU routes passed 15 sequential plus ten concurrent invocations. Both execution modes passed 48/79, both engine modes passed 505/487, all static/boundary gates and independent audit are clean. PTX, all referenced symbols and ABIs, dispatch, stack/lease/overflow, transfer, synchronization, empty/error, and result behavior are source-equivalent, so the report card was not applicable. Audit promoted the inherited constant-mask host-fill debt as STRUCT-001FO. STRUCT-001FO then replaced the inherited O(rows) host constant-mask vector/H2D with a `cuMemsetD32Async` fill of exactly `n` 0/1 i32 words on the leased pooled stream. The symbol is ConstMask-lazy, stream errors drain before lease reuse, and non-constant VM routes are unchanged. A permanent two-cache Layer-1 measurement improved from 10,106us/3.3 GB/s to 37us/905.1 GB/s IN-L2 (~274x) and 80,271us/3.3 GB/s to 188us/1428.0 GB/s OUT-OF-L2 (~427x), with stable rooflines. The canonical card confirmed 36us/944.9 GB/s and 187us/1431.8 GB/s and completed both production point-read cache regimes. The TRUE/FALSE production SQL gate passed three sequential plus two concurrent invocations; both suite modes, static/boundary checks, and independent audit are clean. STRUCT-001FP then isolated the normalized-exact group device view, owned derived buffer, and nine arith/bool/pack/widen/upload/wide-key/distinct launchers in the rustfmt-clean 920-line `derived_column.rs` child, reducing the root to 9,738 lines and `group_input.rs` to 618. Stable public type paths/inherent APIs remain; `group_input -> derived_column` is the sole child edge with no reverse dependency. Six validation tests and five arith/bool/composite/text-distinct/streaming GPU routes passed 15 sequential plus ten concurrent invocations. Both execution modes passed 48/79, both engine modes passed 505/487, static/boundary gates and independent audit are clean. PTX, seven symbols/arguments, layouts, extents, context identity, transfers, synchronization, errors, results, and leases are source-equivalent, so the report card was not applicable. STRUCT-001FQ then made the expression, derived-column, and grouped safe APIs total before device access: checked aligned allocation windows, typed VM stack transitions and exact Value/Mask/TwoValues terminals, bounded two-column text blobs in host preflight and PTX, typed same-context derived/wide-key views, exact destination and validity geometry, exact fixed/text DISTINCT permutations and row domains, and drained device validation of actual text offsets. The public derived buffer no longer exposes a raw device address. The rustfmt-clean leaves are `expression_vm.rs` (1,361), `expression_vm/tests.rs` (222), `derived_column.rs` (1,269), `resident_window.rs` (64), `group_input.rs` (636), and `predicate_mask.rs` (393); the root is now 9,750 lines. Eight safety/arith/bool/composite/distinct/streaming GPU routes passed 24 sequential plus 16 concurrent final-tree invocations without CUDA 700/716/717. Both execution modes passed 53/80, both engine modes passed 505/487, workspace all-target/all-feature check, strict execution/engine clippy, dependency/read-policy/scoped format/diff gates, the full two-layer/two-cache report card, and independent audit are clean. The audit discovered the omitted 9,745-line hand-maintained expression PTX hub and promoted it first as STRUCT-001FR. STRUCT-001FW then isolated the normalized-exact resident TEXT prefix-count and bounded selected-row projection owner in the rustfmt-clean 310-line `resident_text.rs` child, reducing the root to 9,332 lines. Stable crate-root inherent API paths remain externally callable; the private child adds no visibility bridge and depends one-way on the neutral text-window validator plus parent context/stream services. The direct TEXT gate passed three sequential plus two concurrent invocations, and mixed retained, grouped-TEXT, and ordered-TEXT consumers passed nine sequential plus six concurrent command invocations without device faults. Both execution modes passed 54/81, both engine modes passed 505/487, workspace all-target/all-feature check, strict execution/engine clippy, source/reference/format/diff gates, and independent audit are clean. The PTX file, symbol/ABI/body, validation, context, pooled-stream/lease, bounded D2H, ordering/duplicate, UTF-8, and error behavior are source-equivalent, so the report card was not applicable. STRUCT-001FX then deleted the unconsumed public host-reduced filtered-int4 stats facade (17 production lines) and removed its three stale live-source symbol references. Whole-tree and history audit proves its sole engine caller moved in `6bc6b188` to the surviving direct GPU scalar reducer, which filters and reduces to four scalars on-device; archived history remains untouched. Both execution modes passed 54/81, both engine modes passed 505/487, workspace all-target/all-feature check, strict execution/engine clippy, source/diff/docs gates, and independent audit are clean. No live path, kernel, layout, or result behavior changed, so HAZARD and report-card gates were not applicable. STRUCT-001FY then isolated the normalized-exact resident int4 SUM/scalar-statistics owner in the rustfmt-clean 1,083-line `resident_scalar.rs` child, reducing the root to 8,244 lines. The six stable inherent APIs and crate-root `CudaI32Stats` path remain unchanged; `CudaI32Comparison` stays root-owned to avoid a count/scalar cycle, with a one-way dependency on the existing bitmap validator and parent context/stream services. Five direct GPU families passed 15 sequential plus ten concurrent invocations without device faults. Both execution modes passed 54/81, both engine modes passed 505/487, workspace all-target/all-feature check, strict execution/engine clippy, source/reference/format/diff gates, and independent audit are clean. PTX, ABIs, checked windows, NULL/empty/sentinel/overflow behavior, geometry, initialization, stream/lease/error lifetime, bounded scalar readback, and callers are source-equivalent, so the report card was not applicable. STRUCT-001FZ then deleted the obsolete public whole-column int4 D2H facade/private transfer and their sole remaining consumer: the test-only serial identity-copy launcher/PTX and ignored migration A/B gate. The execution root fell by 312 lines to 7,932 and `context_aggregate.rs` by 94 lines to 2,508; execution inventory is now 54 active/80 GPU-ignored. Whole-tree/history audit proves all product consumers had moved by 2026-06-25 to selected-row gather or on-device GROUP BY/general-expression bridges; no engine, example, benchmark, roofline, script, or nonarchive-doc consumer remained. Selected-row gather and ordered compare/project routes are unchanged. Both execution modes passed 54/80, both engine modes passed 505/487, workspace all-target/all-feature check, strict execution/engine clippy, source/history/diff/docs gates, and independent audit are clean. With no live path or surviving kernel/result change, HAZARD and report-card gates were not applicable. STRUCT-001GA then deleted the unconsumed 29-line `project_i32_compare_ordered_from_payload` facade, which materialized all matches, CPU-sorted them, and host-windowed OFFSET/LIMIT. History proves `36470be8` moved the route to the general GPU expression/sort path and `24ebe8cf` removed its sole engine probe. The Layer-1 compare/project benchmark, device-ordered index compaction, and selected-row gathers are unchanged. The root is 7,903 lines; both execution modes passed 54/80, both engine modes passed 505/487, all static/source/history/docs gates and independent audit are clean. With no live caller or surviving runtime behavior, HAZARD and report-card gates were not applicable. STRUCT-001GB then made every surviving ordered int4 compaction boundary total: predicate codes fail closed, index emission rejects row domains above `u32::MAX`, a private input-window descriptor couples each base with its exact extent/offset, resident offsets are naturally aligned and bounded, and typed pooled leases prove same-context ownership and capacity before either CUDA pass. Permanent host/GPU gates cover code/domain/context/short/misaligned/overflow/zero-boundary failures and post-error reuse. The root is 7,951 lines; execution passes 55/81, engine passes 505/487, the five-family final-tree HAZARD matrix passes 15 sequential plus ten concurrent invocations, all workspace/static/source/diff/docs gates and independent re-audit are clean, and the canonical report card remains stable. PTX/ABI/geometry/order and valid results are unchanged. STRUCT-001GC then isolated that total boundary in the rustfmt-clean 1,088-line `resident_compare_ordered.rs` child and byte-exact 803-line `resident_compare_ordered.ptx`, reducing the root to 6,077 lines. Stable inherent APIs and `CudaI32Comparison` remain at the crate root; nine narrow production/test `pub(super)` bridges preserve the prior effective scope without a dependency cycle or public child surface. The five-family HAZARD matrix passes 15 sequential plus ten concurrent invocations; execution passes 55/81 and engine passes 505/487. Workspace all-target/all-feature check, strict execution/engine clippy, exact PTX and normalized Rust/source/reference/format/diff gates, the canonical two-layer/two-cache report card, and independent audit are clean. Valid-path PTX, ABI, geometry, ordering, safety validation, drains, and lease lifetime are source-equivalent. STRUCT-001GD then isolated the host-staged CUDA compatibility family in the rustfmt-clean 1,447-line `staged_filter.rs` and 592-line `staged_mvcc.rs` leaves, reducing the root to 4,067 lines. `CudaDriverRuntime` remains the stable public facade and imports the seven narrow `pub(super)` launchers directly; dependencies are one-way and old root ownership is absent. Both leaves explicitly mark their per-call device-0 context/PTX-load/allocation/H2D/sync/D2H behavior and host-materialized engine consumers as RETIRE-003 bootstrap debt, not GPU-native direction. All seven direct GPU families passed 21 sequential plus 14 concurrent invocations; execution passed 55/81 and engine passed 505/487. Workspace all-target/all-feature check, strict execution/engine clippy, normalized-exact source/reference/Rust-2021-format/diff gates, full ignored GPU suites, and independent audit are clean. Runtime behavior is source-equivalent, so the report card was not applicable. STRUCT-001GE then replaced the unchecked two-column raw-offset/atomic-append special path with the typed postfix VM plus ordered compaction before isolating the expression filter/materialization owner in rustfmt-clean `expression_filter.rs` (393), reducing the root to 3,502 lines. Invalid op/comparison/u32 domains, misaligned/short windows, and post-error reuse are permanently covered; valid add/multiply/zero/all-result parity and checked overflow remain intact. The two now-unreferenced legacy PTX entries were deleted, leaving `expression_i32.ptx` at 520 lines and six live entries. Five GPU families passed 15 sequential plus ten concurrent invocations; execution passed 55/81 and engine passed 505/487. Workspace/static/source/reference/scoped-format/diff gates, `ptxas`, ASCII, full suites, canonical report card, and independent audit are clean. The card reports 1,405.1/1,449.9 GB/s IN-L2/OUT-OF-L2 rooflines, 1,679.0M grouped elements/s, and batch-65,536 point reads of 237.0M/s at p50 140us IN-L2 and 251.8M/s at p50 134us OUT-OF-L2. Full-column expression D2H plus host selected gather remains explicit RETIRE-003 result-path debt. STRUCT-001GF then completed the execution-root disposition by moving the sole product-live LSD-radix owner behind a borrowed same-context pooled lease with pre-CUDA u32-domain, checked n*8, alignment, capacity, and address validation. Its four-entry PTX is external and byte-identical; useful bitonic/serial parity remains test-only, while product-dead adaptive, ORDER-BY-LIMIT, and raw-HAVING code, PTX, and self-only tests are deleted. The stable ORDER BY facade and semantics remain unchanged; host-key H2D and permutation D2H remain RETIRE-003 debt. Five families passed 15 sequential plus ten concurrent invocations; execution passed 56/77 and engine 505/487, the canonical card stayed healthy, and static, ptxas, exact-source, and independent audit gates are clean. |
| 31,858 | `crates/protocol/src/bin/gpu-db-server.rs` | **DECOMPOSE COMPLETE — backend, bootstrap/security, transport, tests, bind/describe, dispatch, catalog, and compatibility owners isolated; root now 1,993 lines and satisfies the production envelope.** Handwritten legacy compatibility binary: 21,203 production lines plus a 10,654-line inline 123-test module, accumulated through 312 file-touching commits. Responsibilities are independently separable: host-backed compatibility session/catalog/DML and constraint model (lines 1–4,455), prepared/portal/cursor and security state (4,456–4,568), listener/startup/SCRAM/ready-loop and extended protocol (4,569–8,184), monolithic statement and catalog/`pg_dump` compatibility dispatch (8,185–19,756), bind/describe parameter rewriting (19,757–21,040), thin `BackendWriter` adapter (21,041–21,202), and inline tests (21,205–31,858). The binary consumes only `gpu_db_protocol` plus wire/security libraries and is directly exercised by protocol unit tests, tokio-postgres/sqlx and external driver smokes, pg_dump/pg_dumpall/psql goldens, benchmark scripts, and the security-posture source guard. It is explicitly the legacy host-relational endpoint pending **PRODUCT-001**; it is not the product GPU execution path and must not gain new relational behavior during decomposition. Target ownership is `backend_adapter`, `security`, `session_state`, `extended_query`, `bind_describe`, catalog metadata/query-family modules, statement dispatch, and invariant-grouped test modules, all behind the unchanged binary boundary with dependencies directed toward protocol library primitives. The private 194-line `backend_adapter` now owns the 28 wrappers/conversions that already delegate framing to `gpu_db_protocol::backend`; normalized definitions, all checks/smokes, the repaired security preflight, and independent audit are clean. The private 798-line `server_bootstrap` now owns configuration, listener/TLS/SCRAM startup, startup framing, and its two focused tests behind a root `main` delegate; exactness, full protocol/driver/security gates, and independent audit are clean, with no relational/session type crossing the seam. The private 127-line `frontend_transport` owns the shared connection trait and length-validated tagged reader. STRUCT-001AK now caps startup/tagged declarations at 64 KiB/64 MiB before allocation, constructs one exact buffer, and closes under 127 binary tests, full driver/security gates, and clean independent audit; aggregate slow-client memory remains **SCALE-001**. STRUCT-001AL externalized the canonically exact 121-test body behind the unchanged `tests` module name; the 127-name inventory and all gates/audit remain clean. STRUCT-001AM split that intermediate owner into a 120-line support parent plus seven coherent 360–2,125-line modules; canonical bodies, the exact 127-item inventory/name map, sequential+concurrent runs, full gates, and audit are clean. STRUCT-001AN isolated the exact 1,152-line bind/describe body in a 1,179-line private module with 15 production entry points, 32 private helpers, three test-only delegates, and clean full gates/audit. STRUCT-001AO isolated the exact ready-loop/frontend state machine in a 261-line private module with one production entry point, two test-only delegates, repaired security guards, and clean full gates/audit. STRUCT-001AP isolated the exact 590-line extended-query body in a 622-line private module with five production entry points and one test-only delegate; statement/portal replacement, bind formats, skip-until-Sync, suspension/resume, COPY delegation, exact errors, full gates, and independent audit are clean. STRUCT-001AQ isolated the canonical-exact SQL compatibility body behind an acyclic 913-line syntax leaf and 301-line PREPARE/DEALLOCATE execution bridge; seven proven exports per module, full gates, and independent audit are clean. STRUCT-001AR isolated the exact cursor parser/executor blocks in a 407-line private module with ten proven entry points, acyclic dependencies, full gates, and independent audit. STRUCT-001AS isolated the exact 197-line legacy extended-DML body in a 205-line private leaf with three one-consumer exports, full gates, and independent audit; inherited integrity/partial-mutation limitations remain compatibility debt rather than claimed guarantees. STRUCT-001AT isolated the exact 273-line COPY state/execution body in a 283-line private module with four proven entry points, full gates, and independent audit. STRUCT-001AU isolated the canonical-exact 150-line legacy DDL syntax body in a 154-line private leaf with four parser/predicate entry points, minimal returned-record visibility, full gates, and independent audit. STRUCT-001AW moved ordered COPY TO/FROM/unsupported routing behind one tri-state delegate in the existing COPY owner with exact behavior, full gates, and independent audit. STRUCT-001AX moved ordered cursor routing behind one tri-state delegate in the existing cursor owner, moved its target enum out of the root, narrowed production visibility, and passed full gates/audit. STRUCT-001AY moved the complete SQL prepared/pg_dump/DEALLOCATE family behind one tri-state delegate in `sql_prepare`, moved its target enum out of the root, preserved exact behavior, and passed full gates/audit. STRUCT-001AZ moved the exact TRUNCATE, DROP TABLE, and ALTER TABLE DROP CONSTRAINT mutation families into the private 291-line `ddl_execution` owner behind one tri-state delegate; syntax dependency remains one-way and full gates/audit are clean. STRUCT-001BA moved bounded session compatibility into a private 101-line owner and deleted a 37-line post-parse subset proven fully dominated by earlier unconditional returns; full gates/audit are clean. STRUCT-001BB moved the normalized-exact 527-line pg_dump/pg_dumpall prelude into a private 568-line owner with full internal gates and clean audit; PostgreSQL 18.4 client query drift remains PRODUCT-002 rather than extraction scope. STRUCT-001BC moved parsed role and transaction state into a private 58-line owner with exact behavior, full gates, and clean audit. STRUCT-001BD moved the normalized-exact bootstrap extension/schema arms into a private 165-line owner with full gates and clean audit. STRUCT-001BE moved normalized-exact database/tablespace DDL into a private 303-line owner with full gates and clean audit. STRUCT-001BF extended `ddl_execution` to 609 lines with the normalized-exact parsed table-definition family; full gates/audit are clean. STRUCT-001BG moved normalized-exact CREATE/RENAME/DROP INDEX into a private 161-line owner with full gates and clean audit. STRUCT-001BH moved the normalized-exact view/materialized-view lifecycle into a private 514-line owner with full gates and clean audit. STRUCT-001BI moved normalized-exact bounded function lifecycle/invocation into a private 113-line owner with full gates and clean audit. STRUCT-001BJ moved normalized-exact sequence lifecycle/value functions into a private 220-line owner with full gates and clean audit; inherited missing default-dependency enforcement/retargeting remains PRODUCT-002. STRUCT-001BK moved normalized-exact domain lifecycle into a private 127-line owner with full gates and clean audit. STRUCT-001BL moved token-equivalent publication/subscription lifecycle into a private 56-line owner with full gates and clean audit. STRUCT-001BM moved normalized-exact role lifecycle into a private 203-line owner with full gates and clean extraction audit; inherited stale active-role identity and absent membership state remain PRODUCT-002. STRUCT-001BN extended `ddl_execution` to 914 lines with the token-equivalent remaining table/column mutation family; full gates/audit are clean. STRUCT-001BO moved the normalized-exact complete comment family into a private 336-line owner with full gates/audit. STRUCT-001BP moved the token-equivalent twelve-arm ACL family into a private 146-line owner with full gates/audit. STRUCT-001BQ moved normalized-exact simple-query relational DML into a private 274-line owner with full gates/audit; inherited duplicate-target compatibility debt remains PRODUCT-002. STRUCT-001BR moved normalized-exact legacy query execution into a private 1,432-line owner with seven proven exports, full gates/audit, and a fully rustfmt-clean root. STRUCT-001BS moved the exact bounded function evaluator into the existing 282-line owner with full gates/audit. STRUCT-001BT moved exact recursive view-dependency helpers into the existing 543-line owner with full gates/audit. STRUCT-001BU localized four exact rename helpers in existing owners with full gates/audit; inherited sequence-default and materialized-view dependency gaps remain PRODUCT-002. STRUCT-001BV moved exact shared integrity validation into a private 109-line owner with three proven exports and full gates/audit; inherited NULL/missing-metadata/generic-error semantics remain PRODUCT-002. STRUCT-001BW moved exact table/FK/column/constraint mutations into the existing 1,467-line DDL owner with full gates/audit; inherited inbound-FK, view-column, and implicit-sequence dependency gaps remain PRODUCT-002. STRUCT-001BX moved exact table-constraint construction into a private 213-line owner with three proven exports and full gates/audit; inherited OID-gap/transient-dirty rollback artifacts remain PRODUCT-002. STRUCT-001BY moved exact sequence primitives into the existing 357-line owner with three proven parent consumers and full gates/audit; inherited name-dependency/index-namespace gaps remain PRODUCT-002. STRUCT-001BZ moved exact column-default semantics into a private 130-line owner with six proven exports and full gates/audit; inherited narrow-type/sequence-overflow mutation remains PRODUCT-002. STRUCT-001CA moved normalized-exact ACL policy/mutations into the existing 740-line owner with eight production and five test-only proven aliases plus full gates/audit; inherited membership/grant-option/PUBLIC-default gaps remain PRODUCT-002. STRUCT-001CB moved exact publication/subscription helpers into the existing 245-line owner with full gates/audit; inherited stale subscription publication lists and missing-name dirty marks remain PRODUCT-002. STRUCT-001CC moved the five exact shared-catalog comment lookups into the existing 386-line owner with full gates/audit; inherited foreign-key omission, mixed-generation checks, and mutex-poison panic remain PRODUCT-002. STRUCT-001CD moved the exact final session/golden compatibility match into the existing 205-line owner with full gates/audit; task-critical real-psql scenarios pass and inherited PostgreSQL 18 suite drift remains PRODUCT-002. STRUCT-001CE moved eleven exact replication-catalog read routes and nineteen private helpers into the existing 672-line owner with stage-preserving delegates and full gates/audit; inherited hard-coded metadata, synthetic OIDs, stale-name filtering, and PostgreSQL 18 drift remain PRODUCT-002. STRUCT-001CF moved three exact domain catalog routes and four private helpers into the existing 245-line owner with full gates/audit; direct domain metadata passes while inherited PostgreSQL 18 `\\dD` drift remains PRODUCT-002. STRUCT-001CG moved two exact psql sequence-list routes and four private helpers into the existing 459-line owner with full gates/audit; sequence rows/comments/state pass while inherited PostgreSQL 18 wording drift remains PRODUCT-002. STRUCT-001CH moved four exact normal/verbose psql view and materialized-view list routes plus eight private helpers into the existing 722-line owner with full gates/audit; rows/comments/storage fields pass while inherited exact literals, hard-coded metadata, unapplied visibility predicates, and PostgreSQL 18 drift remain PRODUCT-002. STRUCT-001CI moved four exact psql/direct function-catalog routes plus six private helpers into the existing 508-line owner with full gates/audit; metadata/comments/ACL/rename behavior pass while inherited broad matching, hard-coded and omitted metadata, absent overload identity, unapplied visibility, and legacy host execution remain PRODUCT-002. STRUCT-001CJ moved two exact psql index-list routes plus five private helpers into the existing 346-line owner with full gates/audit; filtered/verbose/comment/rename/drop behavior passes while inherited exact literals, unusual relkind lists, public-only and unapplied visibility, hard-coded metadata, hidden orphans, and null size remain PRODUCT-002. STRUCT-001CK moved two exact direct `pg_indexes` routes plus four private helpers into the existing 427-line owner with full gates/audit; definition/rename/drop behavior passes while inherited exact routing, host public-only data, hidden orphans, simple unquoted definitions, and positional projection remain PRODUCT-002. STRUCT-001CL moved the exact direct sequence `pg_class` route plus two private helpers into the existing 506-line owner with full gates/audit; OID/rename/drop/state behavior passes while inherited exact routing, host OIDs, public-only and hard-coded metadata, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001CM moved the exact direct materialized-view `pg_class` route plus two private helpers into the existing 764-line owner with full gates/audit; OID/lifecycle behavior passes while inherited exact routing, host OIDs, public-only and hard-coded metadata, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001CN moved two exact view-relation routes plus four private helpers into the existing 869-line owner with full gates/audit; definitions/lifecycle behavior passes while inherited exact routing, host metadata, hard-coded capability fields, limited visibility, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001CO moved six exact later relation-description routes plus sixteen private helpers into the existing 888-line catalog-comment owner with full gates/audit; inherited brittle matching, host sorting, broader shared row builders, partial psql class emulation, hidden orphan-index comments, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001CP moved the exact public-schema description route plus two private helpers into the existing 929-line catalog-comment owner with full gates/audit; inherited exact matching, hard-coded public metadata, host optional comments, CPU construction, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001CQ moved the byte-identical pg-dump description row builder into the existing 1,192-line catalog-comment owner with full protocol and PostgreSQL 16 pg-dump gates/audit; inherited exact routing, host/synthetic OIDs, CPU construction, liveness suppression, anomalous function tuples, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001CR moved seven exact role catalog helpers behind two delegates in the existing 420-line role owner, removed one proven dominated duplicate, retargeted its source guard, and passed full PostgreSQL 16 psql/pg-dumpall gates/audit; inherited hard-coded host roles/OIDs/capabilities, ordering and field-shape quirks, absent membership/grant-option modeling, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001CS moved ten exact database catalog helpers behind two delegates in the existing 580-line cluster owner, removed one proven dominated duplicate, and passed full PostgreSQL 16 psql/pg-dump gates/audit; inherited hard-coded host database/OID/metadata/ACL modeling, bootstrap-only pg-dump rows, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001CT moved eight exact tablespace catalog helpers behind two delegates in the existing 820-line cluster owner, removed two proven dominated branches, and passed full PostgreSQL 16 psql/pg-dumpall gates/audit; inherited host/synthetic metadata, simplified ACLs/defaults, bootstrap pg_dumpall omission, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001CU moved four exact extension catalog helpers behind two delegates in the existing 257-line bootstrap owner, removed one proven dominated duplicate, and passed full PostgreSQL 16 psql/pg-dump gates/audit; inherited synthesized fixed bootstrap metadata, fallback descriptions, CPU row construction, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001CV moved eleven exact schema/namespace catalog helpers behind four delegates in the existing 504-line bootstrap owner, removed two proven dominated duplicates, and passed full PostgreSQL 16 psql/pg-dump gates/audit; inherited public-only fixed namespace metadata, simplified ACLs, unconditional pg-dump public rows, CPU construction, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001CW moved four exact language catalog helpers behind two delegates in the existing 588-line bootstrap owner, removed two proven dominated fallbacks, and passed full PostgreSQL 16 psql/pg-dump gates/audit; inherited synthesized fixed language metadata, simplified ACL/defaults, CPU construction, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001CX moved two exact access-method catalog helpers behind two delegates in the existing 642-line bootstrap owner, removed the dominated pg_am classifier branch, and passed full PostgreSQL 16 psql/pg-dump gates/audit; inherited inconsistent fixed/empty host metadata and absent GPU catalog execution remain PRODUCT-002. STRUCT-001CY moved six exact default-ACL catalog helpers behind two delegates in the existing 843-line ACL owner, removed one proven dominated duplicate, and passed full PostgreSQL 16 psql/pg-dump gates/audit; inherited table-only fixed host metadata, broad routing, CPU construction, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001CZ moved the exact aggregate query plus both empty catalog routes behind two delegates in the existing 563-line function owner, removed the dominated aggregate classifier branch, and passed full PostgreSQL 16 psql/pg-dump gates/audit; inherited broad empty host metadata and absent GPU catalog execution remain PRODUCT-002. STRUCT-001DA isolated four empty type-system psql/pg-dump families in a bounded 174-line owner, removed four dominated classifier branches, and passed full PostgreSQL 16 psql/pg-dump gates/audit; inherited exact/broad empty CPU metadata and absent GPU catalog execution remain PRODUCT-002. STRUCT-001DB moved twelve exact built-in type and pg-dump metadata helpers behind two delegates in the existing type-system owner, removed one dominated root route, reconciled the obsolete two-type scenario-58 golden to the pre-existing nine-type registry, and passed full PostgreSQL 16 psql/pg-dump gates; inherited exact host/synthetic metadata, incomplete type-system semantics, CPU construction, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001DC moved three direct `pg_type` routes plus four registry helpers behind one delegate in the type-system owner and passed the full protocol and PostgreSQL 16 scenario-04 gates; inherited exact hard-coded discovery, host filtering/sorting, the narrow empty extension probe, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001DD moved the exact psql `\\gdesc` OID formatter/parser behind one delegate in the 590-line type-system owner and passed the full protocol and PostgreSQL 16 scenario-69 gates; inherited generated-SQL parsing, host type-display decisions, ignored typmod, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001DE moved the complete early direct/psql table-list and privilege family into a coherent new table-catalog owner with full protocol and 17 PostgreSQL 16 scenario gates; inherited exact SQL matching, host filtering/sorting/sizing, public-only metadata, simplified ACL fields, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001DF moved three later direct table/class routes plus seven helpers behind three stage-preserving delegates in the now-715-line table-catalog owner and passed full protocol and PostgreSQL 16 scenarios 22/23/29; inherited exact host routing/filtering/sorting, hard-coded public metadata, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001DG moved the complete six-route `information_schema.tables` family plus twelve exact helpers behind one delegate in the existing 1,016-line table-catalog owner and passed full protocol and PostgreSQL 16 scenarios 08/20/27/38/45/73/324 plus clean audit; inherited exact host routing, CPU filtering/sorting, hard-coded catalog/schema/type fields, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001DH moved the complete eleven-route `information_schema.columns` family plus twenty-three exact helpers behind one delegate in a new 648-line column-catalog owner and passed full protocol and 17 PostgreSQL 16 scenario gates plus clean audit; inherited exact host routing, CPU filtering/sorting, simplified metadata, hard-coded catalog/schema fields, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001DI moved four exact constraint/default catalog routes plus eight helpers behind two stage-preserving delegates in a new 248-line constraint-catalog owner and passed full protocol and 14 PostgreSQL 16 scenario gates plus clean audit; inherited exact host routing, CPU construction/sorting, simplified constraint metadata, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001DJ moved three direct `pg_attribute` routes plus six exact helpers behind one delegate in the existing 843-line column-catalog owner and passed full protocol and 18 PostgreSQL 16 scenario gates plus clean audit; inherited exact host routing, CPU construction, simplified not-null metadata, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001DK deleted exactly 52 lines across eight post-parse pg-dump branches proven fully dominated by the earlier unconditional compatibility prelude; all canonical helpers remain and full protocol, PostgreSQL 16 pg-dump/pg-dumpall restore, scenario-295, and independent dominance gates are clean. STRUCT-001DL moved fourteen psql relation-introspection arms plus twenty-six exact helpers behind one delegate in a new 800-line owner and passed full protocol and 22 PostgreSQL 16 scenario gates plus clean audit; inherited brittle host routing, CPU filtering/sorting, synthetic metadata, incomplete psql semantics, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001DM moved twenty-three exact pg-dump relation-metadata helpers plus their private record into a new 619-line owner, retargeted the byte-identical compatibility prelude, and passed full protocol, PostgreSQL 16 pg-dump/pg-dumpall/scenario-295, and clean audit gates; inherited exact host routing, synthetic metadata/OIDs, CPU construction/sorting, simplified catalog semantics, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001DN moved fifteen exact ACL rendering/privilege-letter helpers into the existing 1,084-line ACL owner with nine proven parent-private exports and six owner-private helpers, passed full protocol, twelve PostgreSQL 16 ACL scenarios, pg-dump/pg-dumpall, and clean audit gates, and reduced the root to 2,543 lines; inherited simplified host ACL semantics, CPU metadata construction, and absent GPU catalog execution remain PRODUCT-002. STRUCT-001DO moved nineteen exact sequence/function/empty-catalog/domain helpers into the existing 1,031-line pg-dump compatibility owner, preserved a byte-identical prelude, passed full protocol, seven PostgreSQL 16 scenarios, pg-dump/pg-dumpall, and clean audit gates, and reduced the root to 1,993 lines. The remaining 400-line statement dispatcher is the ordered private facade over extracted owners; the protocol disposition and STRUCT-001AV are closed, while all inherited host compatibility behavior remains PRODUCT-001/PRODUCT-002 debt. **Next inventory:** analyze the replication outlier under STRUCT-001DP. All source guards and driver/golden consumers were preserved; every affected boundary ran protocol all-target checks, the exact binary-test inventory, tokio-postgres/SQLx smokes, connection-security preflight, and relevant compatibility goldens. No extraction migrated catalog filtering/joining into a new host product API or conflicted with **PRODUCT-001/PRODUCT-002** GPU-native system relations. |
| 20,164 | `crates/replication/src/lib.rs` | **DECOMPOSE COMPLETE — stable facade, production leaves, and all consensus/repair tests isolated; root now 877 lines.** The original handwritten facade contained 2,021 production lines plus an 18,143-line inline 187-test module, with no feature gates, generated code, or unsafe blocks. Production ownership separates into operational smoke-report contracts, request-vote/AppendEntries RPC data and codec, TCP/TLS/mTLS transport, progress/status/recovery invariants, stable `LogReplicator`/`ReplicatedStateMachine` traits, local replication, and Raft consensus/snapshot behavior. Engine consumes only `LocalReplicator` and the two traits; operational examples consume the public Raft/RPC/transport/report facade. Target modules are `operational`, `rpc`, `transport`, `progress`, `local`, and `raft`, with stable crate-root re-exports and dependency direction `transport -> rpc + raft`, `raft -> rpc + progress`, `local -> progress`, never consensus -> transport. The tests partition into support; operational/election/transport; local; baseline Raft/progress; append/snapshot install; snapshot identity refresh; and seven bounded repair-phase modules while preserving all 187 harness names. STRUCT-001DQ isolated the exact 259-line `transport` owner and four transport tests; STRUCT-001DR isolated the byte-identical 208-line `operational` owner and exact smoke test; STRUCT-001DS isolated the exact 202-line acyclic `rpc` owner and two frame tests while keeping `apply_to` at the root. STRUCT-001DT moved four records, three invariant errors, and all exact progress/status/recovery behavior into a 410-line `progress` owner with only the five planned type dependencies; one single-consumer helper was normalized to `pub(super)`, and its exact 16-test block moved behind the unchanged test facade. STRUCT-001DU isolated the exact 294-line `LocalReplicator` owner and its `LogReplicator` implementation with the stable root re-export; only the single-consumer `entry_at` helper was normalized to `pub(super)`. Its selected exact 20-test family now lives in a 354-line included file with unchanged names. Each slice passed focused gates, the full 187-test serial/16-thread matrix, all-target check and strict clippy, applicable downstream/operational/security gates, targeted formatting/diff/reference checks, and clean independent audit/re-audit. STRUCT-001DV then moved the exact 11-test baseline Raft/progress family into a 272-line included owner at the same parent position with byte-identical bodies and unchanged `tests::*` harness names; production and visibility are untouched. STRUCT-001DW then moved the exact 27-test replicator lifecycle/progress family into a 551-line included owner with byte-identical bodies, unchanged `tests::*` names, and coherent shared `LogReplicator::wait_committed` coverage. STRUCT-001DX then moved the exact 28-test AppendEntries family into a 1,029-line included owner with byte-identical bodies and unchanged `tests::*` names; the coherent owner covers heartbeat commit, term/role validation, conflict handling, newer-leader repair, and snapshot-boundary append invariants. STRUCT-001DY then moved the exact 24-test snapshot-identity family into a rustfmt-normalized 2,133-line included owner with unchanged bodies, attributes, and `tests::*` names; only two long declarations rewrapped. STRUCT-001DZ then moved the exact seven-test advanced-snapshot repair family into a rustfmt-normalized 913-line included owner with unchanged bodies, attributes, and `tests::*` names. STRUCT-001EA then moved the exact seven-test second-refresh advanced-replacement family into a rustfmt-normalized 1,107-line included owner with unchanged bodies, attributes, and `tests::*` names. STRUCT-001EB then moved the exact ten-test post-rejection repair family into a formatting-exact 2,256-line included owner with unchanged bodies, attributes, and `tests::*` names. STRUCT-001EC then moved the exact nine-test refresh-rejection repair cycle into a formatting-exact 2,830-line included owner with unchanged bodies, attributes, and `tests::*` names. STRUCT-001ED then moved the exact eight-test deep refresh/rejection cycle into a formatting-exact 2,838-line included owner with unchanged bodies, attributes, and `tests::*` names. STRUCT-001EE then moved the exact six-test stale-install unwind family into a formatting-exact 1,645-line included owner with unchanged bodies, attributes, and `tests::*` names. STRUCT-001EF then moved the exact final six-test advanced repair cleanup family into a rustfmt-normalized 1,023-line included owner with unchanged bodies, attributes, and `tests::*` names. The root is now 877 lines with only 683 production lines before the test facade. The small root coherently retains the stable traits, public re-exports, Raft consensus/orchestration, and cross-type `AppendEntriesRequest::apply_to` seam; extracting a separate `raft` leaf would create artificial visibility and dependency pressure rather than reduce an outlier. All 187 test harness names remain stable, every focused/full/check/clippy/format/reference gate and independent audit is clean, and the replication disposition is complete. |
| 671 | `crates/engine/src/engine_residency.rs` | **DECOMPOSE COMPLETE — all residency test and production owners isolated; root now 671 lines and satisfies the production envelope.** Baseline was 12,964 lines of handwritten, non-generated, non-archived source with no unsafe block or embedded PTX: 6,145 production/support lines plus a 6,819-line inline `capacity_payload_tests` owner containing 76 tests (71 ignored GPU, five ordinary). Baseline responsibilities were distinct: typed payload/key encoding and open-shard append chunks (lines 1–813); the inline residency differential matrix (814–7,632); admission/publication (7,634–8,317); feature policy, counters, and elision eligibility (8,318–8,936); shard append/rollover, sparse visibility regions, row identity, tombstone, and fused apply (8,937–10,162); auto-admit, transient relations, and benchmark installation (10,163–10,927); vacuum, serialized rehydration, and device gather (10,929–11,553); retained snapshot handles, byte accounting, warmup/maintenance, route planning, and status (11,554–12,964). The private `engine_residency` module imports the crate engine surface through `super::*`; its crate-visible payload/key helpers are consumed by expression, DDL/DML, commit/write, retained-read, and streaming owners, while public inherent `Engine` methods are consumed by the facade, server/examples, and engine tests. There are no feature-conditional implementations beyond `cfg(test)`; runtime switches live in shared `Engine` state, and CUDA ownership is delegated through typed execution/resident-storage APIs. In 132 file-touching commits, the strongest co-change is with `engine_expr` (41), `engine_state`/`engine_retained_read` (36 each), `engine_lifecycle`/`engine_commit` (32 each), `engine_dml_prepare` (31), execution/resident-storage (30 each), and concurrent DML (28), proving accumulated cross-subsystem growth rather than one indivisible invariant. Preserve the private facade and public inherent APIs; target leaves are `residency_payload`, `residency_admission`, `residency_policy`, `residency_mutation`, `residency_transient`, `residency_maintenance`, and `residency_routes`, with tests partitioned into bounded payload/append, baseline shard, sparse-visibility, index/route, elision/type, write/vacuum, and sharded-read owners. Dependencies point from orchestration leaves to payload and resident-storage/execution primitives; direct child cycles and broad visibility are forbidden, and proven cross-owner private helpers remain at the facade or receive only narrow `pub(super)` access. Load-bearing invariants are capacity-strided layout; append values→birth/identity stamps→atomic row-count/HWM/zone-map publication; one-load generation-consistent descriptors; shard XOR unified layout; allocate-before-evict accounting; NULL/3VL device handling; fail-safe optional-index decline; and no host relational fallback. Each pure move preserves symbols/harness names and runs focused plus full engine gates, scoped formatting/reference checks, and independent audit; any runtime/layout/device change additionally runs NULL differential, HAZARD 3× sequential + 2× concurrent, and the canonical report card. STRUCT-001EH externalized that exact four-test payload/append block into a formatting-exact 386-line `tests/residency_payload.rs` owner with unchanged attributes and `capacity_payload_tests::*` names; the root is 12,583 lines and expanding the include reconstructs the baseline source exactly. Focused ordinary tests, the moved GPU gate’s 3× sequential/2× concurrent zero-fault matrix, 992/76 inventory, all-target check, scoped source/format/reference checks, and independent audit are clean; the report card was not applicable. Its broad-suite investigation exposed a pre-existing nullable-text `LIKE` CUDA 700, now closed by READ-004 with serial/16-thread suites and performance gates green. READ-004 and QUALITY-001 are closed. QUALITY-001 restored the strict engine lint gate and repaired the stale explicit-admission fixture without runtime changes. STRUCT-001EI then externalized the exact five-test baseline shard family into a rustfmt-clean 429-line `tests/residency_shard_baseline.rs` owner at the same parent position; the root is 12,154 lines. Normalized source differs only by one rustfmt line wrap, all five names/attributes and the 992/76 inventories are unchanged, and the GPU gates passed 15 sequential plus 10 concurrent invocations with zero CUDA 700/716/717. Both ordinary-suite modes, all-target check, strict clippy, scoped formatting/diff/reference checks, and independent audit are clean; the report card was not applicable. STRUCT-001EJ then externalized the byte-exact eight-test sparse `deleted_by` family into a rustfmt-clean 524-line `tests/residency_sparse_visibility.rs` owner at the same parent position while leaving five shared sparse-region helpers at the facade; the root is 11,631 lines. All eight names/attributes and the 992/76 inventories are unchanged. The GPU gates passed 24 sequential plus 16 concurrent invocations with zero CUDA 700/716/717; both ordinary-suite modes, all-target check, strict clippy, scoped formatting/diff/reference checks, and independent audit are clean. Production behavior is untouched, so the report card was not applicable. STRUCT-001EK then externalized the normalized-exact seven-test SQL DELETE/UPDATE and `created_by` family plus its sole `device_row_id_for` helper into a rustfmt-clean 748-line `tests/residency_update_visibility.rs` owner at the same parent position; the root is 10,886 lines. One long call was rustfmt-wrapped; all names/attributes/order and the 992/76 inventories are unchanged. The GPU gates passed 21 sequential plus 14 concurrent invocations with zero CUDA 700/716/717; both ordinary-suite modes, all-target check, strict clippy, scoped formatting/diff/reference checks, and independent audit are clean. Production behavior is untouched, so the report card was not applicable. STRUCT-001EL then externalized the normalized-exact seven-test cross-shard primary-key index/cache/route family into a rustfmt-clean 568-line `tests/residency_pk_index.rs` owner at the same parent position; the root is 10,317 lines. Two closures were rustfmt-normalized; all names/attributes/order and the 992/76 inventories are unchanged. The GPU gates passed 21 sequential plus 14 concurrent invocations with zero CUDA 700/716/717; both ordinary-suite modes, all-target check, strict clippy, scoped formatting/diff/reference checks, and independent audit are clean. Production cache/route behavior is untouched, so the report card was not applicable. STRUCT-001EM then externalized the normalized-exact three-test sharded NULL/filter/join route-parity family into a rustfmt-clean 159-line `tests/residency_route_parity.rs` owner at the same parent position; the root is 10,159 lines. One expression was rustfmt-wrapped; all names/attributes/order and the 992/76 inventories are unchanged. The GPU gates passed nine sequential plus six concurrent invocations with zero CUDA 700/716/717 and explicitly reported GPU execution for the filtered shapes; both ordinary-suite modes, all-target check, strict clippy, scoped formatting/diff/reference checks, and independent audit are clean. Production predicate/join/route behavior is untouched, so the report card was not applicable. STRUCT-001EN then externalized the normalized-exact six-test device-resolve and core elision lifecycle/concurrency family into a rustfmt-clean 611-line `tests/residency_elision_core.rs` owner at the same parent position; the root is 9,549 lines. Only two array comments were rustfmt-aligned; all names/attributes/order and the 992/76 inventories are unchanged. The GPU gates passed 18 sequential plus 12 concurrent invocations with zero CUDA 700/716/717; both ordinary-suite modes, all-target check, strict clippy, scoped formatting/diff/reference checks, and independent audit are clean. Production resolver/elision/concurrency behavior is untouched, so the report card was not applicable. STRUCT-001EO then externalized the byte-exact four-test DATE/INT2 and INT8 shard/elision family into a rustfmt-clean 426-line `tests/residency_type_coverage.rs` owner at the same parent position; the root is 9,124 lines. All names/attributes/order and the 992/76 inventories are unchanged. The GPU gates passed 12 sequential plus eight concurrent invocations with zero CUDA 700/716/717; both ordinary-suite modes, all-target check, strict clippy, scoped formatting/diff/reference checks, and independent audit are clean. Production encoding/append/elision behavior is untouched, so the report card was not applicable. STRUCT-001EP then externalized the byte-exact three-test device locate/reinsert/versioned-scan family into a rustfmt-clean 214-line `tests/residency_device_locate.rs` owner at the same parent position; the root is 8,911 lines. All names/attributes/order and the 992/76 inventories are unchanged. The GPU gates passed nine sequential plus six concurrent invocations with zero CUDA 700/716/717; both ordinary-suite modes, all-target check, strict clippy, scoped formatting/diff/reference checks, and independent audit are clean. Production locate/reinsert/scan behavior is untouched, so the report card was not applicable. STRUCT-001EQ then externalized the normalized-exact five-test text/numeric/bool/b128 and grouped/versioned read family into a rustfmt-clean 448-line `tests/residency_wide_type_reads.rs` owner at the same parent position; the root is 8,463 lines. One `if let` layout was rustfmt-normalized; all names/attributes/order and the 992/76 inventories are unchanged. The GPU gates passed 15 sequential plus 10 concurrent invocations with zero CUDA 700/716/717; both ordinary-suite modes, all-target check, strict clippy, scoped formatting/diff/reference checks, and independent audit are clean. Production type/rehydration/grouping/read behavior is untouched, so the report card was not applicable. STRUCT-001ER then externalized the byte-exact five-test wave validation/duplicate-race/multi-writer family into a rustfmt-clean 407-line `tests/residency_elision_waves.rs` owner at the same parent position; the root is 8,057 lines. All names/attributes/order and the 992/76 inventories are unchanged. The GPU gates passed 15 sequential plus 10 concurrent invocations with zero CUDA 700/716/717; both ordinary-suite modes, all-target check, strict clippy, scoped formatting/diff/reference checks, and independent audit are clean. Production validation/elision/write-wave/concurrency behavior is untouched, so the report card was not applicable. STRUCT-001ES then externalized the normalized-exact six-test vacuum/gather/multi-row DML/concurrency/materialization family into a rustfmt-clean 695-line `tests/residency_maintenance_materialization.rs` owner at the same parent position; the root is 7,360 lines. One assertion layout was rustfmt-normalized; all names/attributes/order and the 992/76 inventories are unchanged. The GPU gates passed 18 sequential plus 12 concurrent invocations with zero CUDA 700/716/717; both ordinary-suite modes, all-target check, strict clippy, scoped formatting/diff/reference checks, and independent audit are clean. Production vacuum/gather/DML/materialization behavior is untouched, so the report card was not applicable. STRUCT-001ET then externalized the normalized-exact three-test A3/A2/A1 validation/update-chain/row-identity family into a rustfmt-clean 377-line `tests/residency_identity_validation.rs` owner at the same parent position; the root is 6,985 lines. Only comment and panic/closure layouts were rustfmt-normalized; all names/attributes/order and the 992/76 inventories are unchanged. The GPU gates passed nine sequential plus six concurrent invocations with zero CUDA 700/716/717; both ordinary-suite modes, all-target check, strict clippy, scoped formatting/diff/reference checks, and independent audit are clean. Production validation/update-chain/row-identity behavior is untouched, so the report card was not applicable. STRUCT-001EU then externalized the normalized-exact seven-test mixed-type/NULL/3VL/batched-point/binary/deletion-gate family into a rustfmt-clean 625-line `tests/residency_sharded_point_reads.rs` owner at the same parent position; the root is 6,355 lines. Three gather-call layouts were rustfmt-normalized; all names/attributes/order and the 992/76 inventories are unchanged. The GPU gates passed 21 sequential plus 14 concurrent invocations with zero CUDA 700/716/717; both ordinary-suite modes, all-target check, strict clippy, scoped formatting/diff/reference checks, and independent audit are clean. The inherited CPU-pinned mixed-type oracle was test-only at that structural checkpoint; RETIRE-001 later replaced it with GPU execution plus rows-only specifications, and no product route behavior changed in STRUCT-001EU, so the report card was not applicable. STRUCT-001EV then externalized the normalized-exact final capacity/open-payload/residency-budget family into a rustfmt-clean 87-line `tests/residency_capacity_budget.rs` owner immediately before the parent module close; the root is 6,267 lines. Two bindings were rustfmt-normalized; all names/attributes/order and the 992/76 inventories are unchanged. Both focused ordinary tests passed; the budget GPU gate passed three sequential invocations and two concurrent rounds with zero CUDA 700/716/717; both ordinary-suite modes, all-target check, strict clippy, scoped formatting/diff/reference checks, and independent audit are clean. Production capacity/payload/budget behavior is untouched, so the report card was not applicable. All 76 residency tests are now partitioned into bounded owners. STRUCT-001EW then isolated the normalized-exact payload/key/open-append production owner in the rustfmt-clean 806-line `engine_residency/payload.rs` child, reducing the root to 5,478 lines. The only moved-source change is the narrow `pub(super)` visibility required for `AppendCreatedBy::stamps_for`; explicit proven re-exports preserve every consumed facade path, while the unconsumed payload alias and hash helper stay leaf-private. Five focused ordinary tests, 12 sequential plus eight concurrent representative GPU invocations with zero CUDA 700/716/717, both 505/487 ordinary modes, exact 992/76 inventories, all-target check, strict clippy, scoped source/format/reference checks, and independent audit are clean. Runtime, layout, and device behavior are unchanged, so the report card was not applicable. STRUCT-001EX then isolated the normalized-exact six-method snapshot construction/admission/publication owner in the rustfmt-clean 691-line `engine_residency/admission.rs` child, reducing the facade root to 4,793 lines. Rustfmt changed four layouts; the only semantic-text changes are three narrow `pub(super)` bridges required by existing parent consumers, with every public/crate API and call site unchanged. Six focused ordinary tests, 12 sequential plus eight concurrent admission-sensitive GPU invocations with zero CUDA 700/716/717, both 505/487 ordinary modes, exact 992/76 inventories, all-target check, strict clippy, scoped source/format/reference checks, and independent audit are clean after correcting the child ownership comments. Allocation-before-evict, deterministic admission, payload/layout/NULL/MVCC semantics, publication order, errors, and device behavior are unchanged, so the report card was not applicable. STRUCT-001EY then isolated the byte-exact 60-method feature-policy, elision-eligibility, shard-sizing, and telemetry owner in the rustfmt-clean 624-line `engine_residency/policy.rs` child, reducing the facade root to 4,176 lines. Four focused ordinary tests, 12 sequential plus eight concurrent policy-sensitive GPU invocations with zero CUDA 700/716/717, both 505/487 ordinary modes, exact 992/76 inventories, all-target check, strict clippy, scoped source/format/reference checks, and independent audit are clean. Every method name/visibility, default, atomic ordering, predicate, cache/state mutation, and counter source is source-identical, with no dependency cycle or CPU-first drift, so the report card was not applicable. STRUCT-001EZ then isolated the normalized-exact eight-method append/rollover, sparse-version stamping, row-identity, tombstone, and fused-apply owner in the rustfmt-clean 1,230-line `engine_residency/mutation.rs` child, reducing the facade root to 2,952 lines. Rustfmt joined one offset expression; only `stamp_created_by_resident_shard_slots` gained narrow `pub(super)` for its existing parent differential. Four focused ordinary tests, 15 sequential plus ten concurrent mutation-sensitive GPU invocations with zero CUDA 700/716/717, both 505/487 ordinary modes, exact 992/76 inventories, all-target check, strict clippy, scoped source/format/reference checks, and independent audit are clean. Publication order, sparse MVCC regions, fused apply, tombstones, offsets, launches, counters, and fail-safe behavior are source-equivalent, so the report card was not applicable. STRUCT-001FA then isolated the normalized-exact nine-method auto-admit, synchronous/payload-only/async transient relation, shared transient construction, and benchmark chunk/shard installation owner in the 765-line `engine_residency/transient.rs` child, reducing the facade root to 2,188 lines. The only visibility change is the required narrow `pub(super)` bridge for `relational_residency_device_memory`; rustfmt changed two final shard-install call layouts. Five focused ordinary gates, the sync transient route's three sequential/two concurrent READ-005 matrix, 15 sequential plus ten concurrent additional auto-admit/async/install/sharded-read GPU invocations with zero CUDA 700/716/717, both 505/487 ordinary modes, exact 992-test inventory, all-target check, strict clippy, dependency-boundary/scoped source/reference/format checks, and independent audit are clean. Runtime, layout, admission, residency, transient, and result-path behavior are unchanged, so the report card was not applicable. STRUCT-001FB then isolated the normalized-exact 14-method vacuum/churn, serialized rehydration, int4 identity resolution, host-store reconciliation, and typed device-gather owner in the 607-line `engine_residency/maintenance.rs` child, reducing the facade root to 1,590 lines. Audit found and the slice corrected one child cycle by retaining the byte-exact shared `reset_tombstone_churn` helper at the parent facade; every remaining moved segment reconstructs HEAD exactly with no visibility change. Six representative GPU routes passed 18 sequential plus 12 concurrent invocations, then the post-correction minimal matrix passed another 12 sequential plus eight concurrent invocations, all with zero CUDA 700/716/717. Both 505/487 ordinary modes, exact 992-test inventory, all-target check, strict clippy, dependency-boundary/scoped source/reference/format checks, and independent re-audit are clean. Lock/catalog/snapshot, NULL/MVCC/identity, repair, and fail-loud behavior are source-identical, so the report card was not applicable. STRUCT-001FC then isolated the byte-exact nine-method warmup/maintenance-policy, cache-state readiness, single/sharded route-planning, rejection-evidence, and status owner in the rustfmt-clean 988-line `engine_residency/routes.rs` child, reducing the facade root to 609 lines. Eight focused ordinary gates and four representative single/sharded GPU routes passed 12 sequential plus eight concurrent invocations with zero CUDA 700/716/717. Both 505/487 ordinary modes, the exact 992-test inventory, all-target check, strict clippy, dependency-boundary/scoped source/reference/format checks, and independent audit are clean. Route decisions, reasons, estimates, counters, status, fallback, runtime, layout, and result behavior are byte-identical, so the report card was not applicable. The disposition is complete. |
| 2,433 | `crates/engine/src/engine_expr.rs` | **ACCEPT EXCEPTION — disposition complete; root now 2,433 lines.** Handwritten production hub with embedded tests, not generated/archive. Baseline responsibility map: lines 1–522 own scalar/join IR contracts plus query-normalization, shard-prune, and HAVING conversion helpers; 523–700 own the standalone grouped-result GPU permutation; 702–2,519 own grouped value helpers and predicate type/VM compilation; 2,520–4,293 own visibility, resident DML locate/apply, unified-source construction, and sharded routing; 4,294–4,484 own distinct/select/group benchmark entries; 4,485–6,382 own join resolution, predicate/coordinate filtering, projection materialization, and device-coordinate orchestration; 6,383–9,570 are the general grouped/scalar/ordered select pipeline; 9,571–11,294 own type-specific predicate lowering; 11,295–11,414 are focused zone-map tests. Explicit cross-module consumers are `engine_sql_pg`, `engine_streaming_exec`, `engine_select_exec`, `engine_retained_read`, `engine_dml_prepare`, and tests; crate-private inherent `Engine` methods and `crate::engine_expr::*` contracts are the stable internal facade, while public entry points live in sibling owners such as `engine_sql_pg`. The file is built unconditionally through private `mod engine_expr`; its final unit tests and two bench helpers are `cfg(test)`, probe instrumentation is `cfg(feature = "probe-timing")`, it contains no unsafe block or embedded PTX, and every CUDA boundary is a typed execution API. The broad `use super::*` import is facade debt to narrow as each leaf moves. History/co-change shows rapid cross-subsystem accumulation, while result sorting was introduced and evolved specifically with grouped alignment, LIMIT/OFFSET, join ordering, and STRATA work (`42138340`, `237f3e34`, `70758557`, `c5be0c96`). Target ownership proceeds by invariant: `engine_result_sort`, contract/IR, predicate type/compiler, resident mutation/source/sharded routing, join projection/device orchestration, grouped/select stages, and type-family lowerers; the 3,188-line select orchestrator remains at the facade until its internal stage seams are separately proven rather than moved into another outlier. Dependencies must flow from orchestrators to contracts/leaves, never through sibling cycles or broad visibility. First slice: the approximately 177-line `gpu_sort_permutation` has exactly two live callers (multi-pass group alignment and grouped final ORDER BY), depends only on SQL/error types, residency payload construction, and execution sort APIs, and uses no other private `engine_expr` helper. Move it normalized-exact to `engine_result_sort.rs`, preserve `crate::engine_expr::gpu_sort_permutation` via a narrow re-export, and retain host key/payload construction plus permutation readback as RETIRE-003 debt. STRUCT-001GG isolated the standalone grouped-result GPU permutation in the 186-line `engine_result_sort.rs` leaf; executable source is exact, the existing crate-private facade is preserved by one narrow re-export, and dependencies flow one-way to residency payload construction, typed execution sorting, and SQL/error types. Exactly two callers remain: multi-pass group alignment and grouped final ORDER BY. Inherited stale join attribution was corrected to the live device-coordinate sort/window route. Six GPU families passed 18 sequential plus 12 concurrent invocations, both engine modes passed 505/487, all-target/strict-clippy/source/reference/format/diff gates and independent audit are clean, and the report card was not applicable. Host key/payload construction, H2D, and permutation D2H remain RETIRE-003 debt. STRUCT-001GH isolated the state-free scalar contract in the 63-line `engine_expr_ir.rs` leaf. Both derives, every `ResidentBinaryOp`/`ResidentExpr` variant and field, field order, and variant docs are exact; only inherited int4/prototype-era ownership prose was corrected. The stable `crate::engine_expr` paths remain narrow crate-private re-exports, and dependencies flow one-way from SQL/DML/streaming/execution orchestrators to the neutral IR and `Decimal128`, with no runtime state, unsafe, PTX, feature gate, cycle, or visibility expansion. Four focused GPU SQL/numeric/DML/streaming routes, both 505/487 engine modes, all-target/strict-clippy/source/consumer/format/diff gates, and independent audit are clean; HAZARD and report card were not applicable. STRUCT-001GI isolated the five state-free join-plan types in the 76-line `engine_join_ir.rs` leaf. Definitions, derives, variants, fields, visibility, and order are exact; the stable `crate::engine_expr` paths remain narrow crate-private re-exports. Dependencies flow one-way from SQL binding, resident/streaming execution, and tests to the prelude-only contract, while sentinels, device ownership, parsing, projection, sorting/windowing, and execution remain at their established owners. Obsolete transient-intermediate and retired result-sort prose was corrected to the live device-coordinate pipeline. Focused parsed/user/catalog/outer/streaming GPU joins, both 505/487 engine modes, all-target/strict-clippy/source/consumer/format/diff/docs gates, and independent audit are clean; HAZARD and report card were not applicable. STRUCT-001GJ isolated the five state-free select/predicate normalization helpers in the 238-line private `engine_expr/normalization.rs` leaf. Bodies and docs are source-equivalent apart from future-proof ownership prose; the two parent-only helpers gained only `pub(super)`, while the three existing `crate::engine_expr` crate-private paths remain narrow re-exports. Dependencies flow one-way to scalar IR, bound-select, error, and SQL value/projection contracts; pruning, compilation/lowering, grouped materialization, `Engine` methods, and all runtime/device state remain at their established owners. Six focused legacy-group, LIKE, HAVING, DML, and streaming GPU/active routes, both 505/487 engine modes, all-target/strict-clippy/source/consumer/visibility/format/diff/docs gates, and independent audit are clean; HAZARD and report card were not applicable. STRUCT-001GK isolated the three pure shard-pruning/point-shape helpers and their two exact unit tests in the 213-line private `engine_expr/shard_pruning.rs` leaf. Bodies, docs, and tests are exact; the two parent-only helpers gained only `pub(super)`, while the existing `crate::engine_expr::shard_point_lookup_int4_eq` path remains one narrow crate-private re-export. Dependencies flow one-way to scalar IR, bound-select, relational table/stat, and SQL filter/type/value contracts; source construction, lookup execution, device/runtime state, and MULTI remain in place. Both unit gates, three focused GPU parity routes, final 505/487 engine modes, all-target/strict-clippy/source/consumer/visibility/format/diff/docs gates, and independent audit are clean. The first GPU sweep exposed 547.4 GiB of obsolete generated `target/tmp/gpu-db-*` WAL/FUA residue; scoped cleanup restored 566 GiB free, all 11 ENOSPC failures passed in isolation, the complete 487-test GPU rerun passed, and fresh generated residue was removed. HAZARD and report card were not applicable. STRUCT-001GL isolated exact grouped-value reconstruction and representative-row grouping in the 174-line private `engine_expr/grouped_values.rs` leaf. The two bodies are exact and gained only `pub(super)` for parent use; explicit dependencies name relational offsets/layouts, snapshot/table, SQL/error, and typed execution contracts. The select orchestrator, predicate compiler/lowerers, `Engine` methods, residency construction, runtime/device owners, and MULTI remain in place. One inaccurate inherited claim that the appended DISTINCT member was always non-int was corrected to the live int8/bool-capable route. Nine typed GPU routes, both 505/487 engine modes, all-target/strict-clippy/source/consumer/visibility/format/diff/docs gates, and independent audit are clean; fresh generated test residue was removed. HAZARD and report card were not applicable. STRUCT-001GM isolated the resident execution-source and on-device MVCC visibility contracts in the 76-line private `engine_expr/execution_source.rs` leaf. The three structs, derives, fields, visibility, docs, and visibility instruction order/opcodes are source-equivalent apart from explicit import/path normalization and durable ownership prose. Existing `crate::engine_expr` paths remain narrow crate-private re-exports; construction, routing, join/source orchestration, every `Engine` method, runtime/device action, and MULTI remain in place. Three focused MVCC visibility gates plus streaming injected-source and resident-select GPU routes, both 505/487 engine modes, all-target/strict-clippy/dependency/source/consumer/visibility/format/diff/docs gates, and independent audit are clean. Another 513 MiB of fresh generated test residue was removed. HAZARD and report card were not applicable. STRUCT-001GN isolated the exact join NULL sentinel, device-memory owner/accessor, NULL-pad mask owner, and execution-side alias in the 44-line private `engine_expr/join_source.rs` leaf, reducing the root to 10,410 lines. Existing crate-private paths remain narrow re-exports; only parent-equivalent `pub(super)` exposure and explicit imports differ. Four focused resident/transient/OUTER/streaming GPU joins, both 505/487 engine modes, the complete 992-test GPU suite, all-target check, strict clippy, source/consumer/visibility/scoped-format/diff/docs gates, and independent audit are clean; fresh generated test residue was removed. Runtime behavior is unchanged, so HAZARD and report-card gates were not applicable. Next slice analysis: the 25 state-free helpers from `is_int4_literal` through `column_numeric_scale` own operand/type recognition, literal normalization, and LIKE tokenization across the predicate compiler and type lowerers. Their callers are confined to the parent, dependencies are limited to neutral expression/relational/SQL/error contracts and date/UUID parsers, and history groups them with the June predicate type-matrix series plus later bound temporal DML semantics. Move them exact to `engine_expr/predicate_operands.rs` as `pub(super)` helpers with explicit imports; keep opcode mapping, numeric rescale/compiler work, every `Engine` method, runtime/device work, orchestration, joins, mutation, R3 decisions, and MULTI in place. STRUCT-001GO isolated the 25 exact state-free predicate operand/type-recognition, literal-normalization, and LIKE-tokenization helpers in the rustfmt-clean 400-line private `engine_expr/predicate_operands.rs` leaf, reducing the root to 10,030 lines. All consumers remain parent-local behind explicit `pub(super)` imports; dependencies are one-way to neutral expression/relational/SQL/error contracts, while every compiler/lowerer body, `Engine` method, runtime/device action, orchestration, routing, mutation, join, R3 decision, and MULTI remains in place. Fourteen typed GPU predicate/DML routes, both 505/487 engine modes, the complete 992-test GPU suite, static/scoped gates, and independent audit are clean; fresh generated residue was removed. Runtime behavior is unchanged, so HAZARD and report-card gates were not applicable. Next slice analysis: the current 25-function block from `arith_op_code` through `compile_bool_leaf` is one mutually coupled predicate compiler owner: generic/numeric/typed leaf compilation shares opcode, postfix stack, validity, width-routing, and literal behavior, and splitting it smaller would create reverse edges or duplication. Move the exact block to a bounded `engine_expr/predicate_compiler.rs` leaf with dependency direction `engine_expr -> predicate_compiler -> predicate_operands`; keep seven leaf-internal helpers private, expose only 18 proven parent-used functions as `pub(super)`, and leave every `Engine` method and runtime/orchestration concern in place. STRUCT-001GP isolated the exact complete predicate compiler in the rustfmt-clean 1,252-line private `engine_expr/predicate_compiler.rs` leaf. Seven helpers remain leaf-private and only 18 proven parent-used functions are `pub(super)`; dependencies flow one-way to predicate operands and neutral contracts. Twenty-three focused typed GPU predicate/DML tests, both 505/487 engine modes, the complete 992-test GPU suite, static/scoped gates, and independent audit are clean; fresh generated residue was removed. Runtime behavior is unchanged, so HAZARD and report-card gates were not applicable. Next slice analysis: the eight current resident DML methods from `locate_resident_delete_slots` through `try_update_resident_commit` form one transition owner for physical old-version locate, generation gating, tombstoning, fingerprint verification, conservative re-admit fallback, and created-by-stamped UPDATE append. Move them exact to private `engine_expr/resident_dml.rs`, retaining five `pub(crate)` inherent paths and three leaf-private helpers. Dependencies flow to shard pruning, predicate lowering, and established resident mutation/read primitives; the extraction must not decide R3-001's future version/index/concurrency model. STRUCT-001GQ isolated the exact current resident DML transition block in the rustfmt-clean 486-line private `engine_expr/resident_dml.rs` leaf. Five inherent methods remain `pub(crate)` and three helpers remain private; all locate, generation, tombstone, fingerprint, re-admit, update-append, SV6, and churn semantics are byte-exact. Twelve focused GPU transition tests, both 505/487 engine modes, the complete 992-test GPU suite, static/scoped gates, and independent audit are clean; 15 GiB of generated residue was removed. Runtime behavior is unchanged, so HAZARD and report-card gates were not applicable. Next slice analysis: private `TextRebaseOp` and the complete `build_sharded_unified_exec_source` method form the source-layout/recompaction owner, distinct from route selection and point-result framing. Move them exact to private `engine_expr/sharded_source.rs`, retain the stable `pub(crate)` inherent path, explicit source-contract/pruning/resident/typed-execution dependencies, every proof/source-owner lifetime, and all layout/error behavior. Run the standard read-kernel roofline before/after because the moved code owns residency layout; leave the full report card inapplicable if source equivalence is exact. STRUCT-001GR isolated private `TextRebaseOp` and exact sharded unified-source construction in the rustfmt-clean 727-line private `engine_expr/sharded_source.rs` leaf. Layout/error/order semantics and every proof/source-owner/sidecar/`Arc` lifetime remain equivalent; only two rustfmt wraps differ. Seventeen focused sharded-layout GPU routes, both 505/487 engine modes, the complete 992-test GPU suite, static/scoped gates, independent audit, and before/after roofline comparison are clean; 15 GiB of generated residue was removed. Runtime behavior is unchanged, so HAZARD and the full report card were not applicable. Next slice analysis: the complete `execute_resident_sharded_via_general` bridge and private `try_shard_index_point_route` are one route-selection/result-framing owner, distinct from source layout and downstream executors. Move them exact to private `engine_expr/sharded_route.rs`, retain one stable `pub(crate)` inherent path and one private helper, explicit dependencies, generation/visibility/fallback/dispatch/result semantics, and all exact errors. STRUCT-001GS isolated the exact sharded general bridge and private point-index helper in the rustfmt-clean 421-line private `engine_expr/sharded_route.rs` leaf. Normalized old/new source hashes match; visibility, fallback, dispatch, ownership, counters, and result framing are unchanged. Twenty-four focused GPU routes, both 505/487 engine modes, the complete 992-test GPU suite, static/scoped gates, and independent audit are clean; 15 GiB of generated residue was removed. Runtime behavior is unchanged, so HAZARD and report-card gates were not applicable. Next slice analysis: `execute_resident_distinct_via_general` and `execute_resident_select_via_general` form one production bridge owner for DISTINCT synthesis and declined-shape dispatch. Move them exact to private `engine_expr/select_bridge.rs`, retain both stable `pub(crate)` inherent paths, explicit source/result/SQL dependencies, visibility forwarding, schema/row framing, and exact errors; leave the two test-only GROUP BY benchmark helpers for the separately sequenced STRUCT-001GU. STRUCT-001GT isolated the exact production DISTINCT/select bridges in the rustfmt-clean 91-line private `engine_expr/select_bridge.rs` leaf. Normalized old/new hashes match; DISTINCT synthesis, source/visibility forwarding, schema/row framing, dispatch, errors, and consumers are unchanged. Thirteen focused GPU/route tests, both 505/487 engine modes, the complete 992-test GPU suite, static/scoped gates, and independent audit are clean; 15 GiB of generated residue was removed. Runtime behavior is unchanged, so HAZARD and report-card gates were not applicable. Next slice analysis: the two `cfg(test)` GROUP BY benchmark methods form a separate test-only owner. Move their exact docs/attributes/bodies to private test-gated `engine_expr/group_bench.rs`, retaining both `pub(crate)` inherent paths, typed execution/offset dependencies, inputs/timing/results/errors, and sole ignored benchmark consumer; keep production and join code out. STRUCT-001GU isolated the exact two test-only GROUP BY benchmark helpers in the rustfmt-clean 115-line private `engine_expr/group_bench.rs` leaf behind module- and method-level test gates. Inputs, offsets, bounds, controls, event timing, results, errors, and the sole ignored benchmark consumer remain exact apart from import normalization. The ignored GPU benchmark, both 505/487 engine modes, the complete 992-test GPU suite, static/scoped gates, and independent audit are clean; 15 GiB of generated residue was removed. Runtime behavior is unchanged, so HAZARD and report-card gates were not applicable. Next slice analysis: history proves five select-orchestrator rustdoc lines were orphaned onto `resolve_join_side` by later insertion; re-home them to their actual method, then move only exact join-side resident/transient source resolution to private `engine_expr/join_side.rs`. Retain the stable inherent path, explicit join-source/residency dependencies, ownership/lifetime/error behavior, and leave every later join invariant in place. STRUCT-001GV isolated exact join-side source resolution in the rustfmt-clean 81-line private `engine_expr/join_side.rs` leaf. Resident/sharded/transient source precedence, ownership/lifetimes, errors, visibility, and consumers remain equivalent. Five historically proven orphaned select-orchestrator rustdoc lines were moved byte-for-byte back to their intended method. Seven focused GPU joins, both 505/487 engine modes, the complete 992-test GPU suite, static/scoped gates, and independent audit are clean; 15 GiB of generated residue was removed. Runtime behavior is unchanged, so HAZARD and report-card gates were not applicable. Next slice analysis: the four methods from `execute_resident_expr_inner_join` through private `execute_resident_expr_join_with_device_overrides` form one bounded join entry/adapter/plan owner. Move them exact to private `engine_expr/join_plan.rs`, retain three `pub(crate)` paths and one private helper, explicit plan/source/alias/coordinate dependencies, every validation/binding/dispatch invariant, and leave predicate masks, projection, and coordinate execution in place. STRUCT-001GW isolated the exact four-method join entry/adapter/plan owner in the rustfmt-clean 454-line private `engine_expr/join_plan.rs` leaf. Three methods remain `pub(crate)` and the override private; argument ordering, validation, classification, NATURAL/USING, projection/order, sentinel, source, and final dispatch semantics are equivalent. Twenty distinct focused GPU join-plan routes, both 505/487 engine modes, the complete 992-test GPU suite, static/scoped gates, and independent audit are clean; 15 GiB of generated residue was removed. Runtime behavior is unchanged, so HAZARD and report-card gates were not applicable. Next slice analysis: contiguous `predicate_mask_on_null_pad` and `resident_predicate_device_mask` form one shared device-mask construction owner. Move them exact to private `engine_expr/predicate_mask.rs`, retain the resident method `pub(crate)` and expose only the parent-used pad helper as `pub(super)`, preserving compiler/type/visibility order, device/source/allocation lifetimes, probe behavior, text launch, and exact errors; leave join/rank orchestration and predicate lowering in place. STRUCT-001GX isolated exact NULL-pad and resident predicate-device mask construction in the rustfmt-clean 107-line private `engine_expr/predicate_mask.rs` leaf. The resident builder remains `pub(crate)` and the pad helper gained only parent visibility; compiler/type/visibility order, feature probe, text launch, errors, and retained lifetimes are exact apart from import normalization. Twelve focused GPU mask consumers, both 505/487 engine modes, the complete 992-test GPU suite, static/feature/scoped gates, and independent audit are clean; 15 GiB of generated residue was removed. Runtime behavior is unchanged, so HAZARD and report-card gates were not applicable. Next slice analysis: `resident_join_identity_coordinates` and `execute_resident_join_coordinate_matches` form the streaming/incremental coordinate owner. Move them exact to private `engine_expr/join_incremental.rs`, retain both stable `pub(crate)` paths, explicit mask/layout/typed-execution dependencies, all visibility/range/key payload/orientation/argument-order semantics, and leave the main coordinate executor and projection materialization in place. STRUCT-001GY isolated the exact two streaming/incremental coordinate builders in the rustfmt-clean 248-line private `engine_expr/join_incremental.rs` leaf. Visibility-mask/range order, identity, payload widths, orientation, key/relation order, bitmap arguments, errors, lifetimes, and streaming-only consumers are exact apart from type-path normalization. Seven focused GPU controls, both 505/487 engine modes, the complete 992-test GPU suite, static/scoped gates, and independent audit are clean; 15 GiB of generated residue was removed. Runtime behavior is unchanged, so HAZARD and report-card gates were not applicable. STRUCT-001GZ isolated the exact complete join projection resolution/materialization owner in the rustfmt-clean 478-line private `engine_expr/join_projection.rs`, reducing the root to 5,727 lines. All four inherent methods remain `pub(crate)`; resolution/errors, coalesced/star ordering, aliases/attnum, device specs/layouts, side-0 ownership, typed readback/endian/numeric/UUID/NULL behavior, consumers, and dependency direction are source-equivalent. Eleven focused GPU routes, both 505/487 engine modes, the complete 992-test GPU suite, all-target check, strict clippy, static/scoped gates, and independent audit are clean; generated residue was removed. Raw roofline and the canonical two-layer/two-cache report card are stable, including out-of-L2 batched point reads from 251.6M to 253.1M lookups/s at p50 132us and indexed single-flight from 3.20x to 3.25x scan. Runtime behavior is unchanged, so HAZARD was not applicable. STRUCT-001HA isolated the exact streaming-only coordinate post-filter owner in the rustfmt-clean 84-line private `engine_expr/join_coordinate_filter.rs`, reducing the root to 5,657 lines. Both methods remain `pub(crate)`; arity-before-work, relation-order predicate/visibility masks, retained NULL-pad guards, temporary option-vector order, side-0 context, real/pad argument order, all-None visibility pads, errors, consumers, and dependency direction are source-equivalent. Five focused GPU OUTER/NULL/streaming controls, both 505/487 engine modes, the complete 992-test GPU suite, all-target check, strict clippy, static/scoped gates, and independent audit are clean; generated residue was removed. Runtime behavior is unchanged, so HAZARD and report card were not applicable. STRUCT-001HB isolated the exact main non-streaming/direct device-coordinate join executor in the rustfmt-clean 525-line private `engine_expr/join_coordinate_exec.rs`, reducing the root to 5,143 lines. The sole sibling caller retains the former root-private visibility envelope through one authorized `pub(super)` token; every body invariant, error, consumer, import, and one-way dependency is otherwise source-equivalent, and the obsolete parent-only `JoinNullPadMask` import is gone. Eleven focused GPU direct/n-way/composite/text/numeric/UUID/typed-NULL/OUTER/order/window/streaming routes, both 505/487 engine modes, the complete 992-test GPU suite, static/scoped gates, and independent audit are clean; generated residue was removed. Raw roofline and the canonical report card are stable, including out-of-L2 batched point reads from 253.2M at p50 131us to 250.2M at p50 132us and indexed single-flight from 3.24x to 3.23x scan. Runtime behavior is unchanged, so HAZARD was not applicable. STRUCT-001HC consolidated the exact three general-select/grouped entry bridges in the rustfmt-clean 226-line existing private `engine_expr/select_bridge.rs`, reducing the root to 5,014 lines. All three methods remain `pub(crate)` and their rustdocs, wrapper attribute, single binding, normalization timing, injected source/visibility, predicate-before-clear order, group keys, order vectors, final arguments, consumers, imports, and dependency direction are source-equivalent. Ten focused GPU predicate/grouped/plain/ordered/DISTINCT/sharded/versioned-visibility controls, both 505/487 engine modes, the complete 992-test GPU suite, static/scoped gates, and independent audit are clean; generated residue was removed. Runtime behavior is unchanged, so HAZARD and report card were not applicable. STRUCT-001HD re-homed the history-proven dispatcher rustdocs and isolated exact predicate dispatch plus bool/NULL fast paths in the rustfmt-clean 639-line private `engine_expr/predicate_dispatch.rs`, reducing the root to 4,400 lines and below the mandatory 5,000-line threshold. Three helpers remain private and the dispatcher remains `pub(crate)`; the only body change is location, the general and bool-specific docs are byte-identical at their proven owners, root compiler imports were narrowed, and all dispatch order/bounds/validity/errors/consumers/dependencies are exact. Fifteen focused GPU visibility/bool/NULL/3VL/mixed-width/typed/arithmetic/DML/streaming controls, both 505/487 engine modes, the complete 992-test GPU suite, static/scoped gates, and independent audit are clean; generated residue was removed. Runtime behavior is unchanged, so HAZARD and report card were not applicable. STRUCT-001HE isolated the exact complete typed predicate-lowering owner in the rustfmt-clean 1,147-line private `engine_expr/predicate_typed_lowering.rs`, reducing the root to 3,262 lines. The nine dispatcher-called lowerers gained only `pub(super)` and the numeric scale helper remains private; normalized executable hashes match after those authorized visibility tokens, and fallthrough/error, type/width/scale/orientation/validity/layout/program/device-error behavior is exact. Twenty-four focused GPU controls, 33 independent-audit GPU controls, both 505/487 engine modes, the complete 992-test GPU suite, all-target check, strict clippy, static/scoped gates, and independent audit are clean; generated residue was removed. Runtime behavior is unchanged, so HAZARD and report card were not applicable. STRUCT-001HF isolated the shared GPU COUNT(DISTINCT) sort/mark/group owner in the rustfmt-clean 161-line private `engine_expr/grouped_count_distinct.rs`, reducing the root to 3,127 lines. A rustfmt-normalized reconstruction from the old closure matches exactly after explicit captures and four redundant snapshot-borrow removals; both call sites preserve argument order and all fixed/text descriptors, numeric/UUID high-low layout, sort/mark/SUM behavior, empty/NULL/errors, buffer lifetimes, and constant scalar group semantics. Twenty-seven focused GPU controls, 25 independent-audit GPU controls, both 505/487 engine modes, the complete 992-test GPU suite, all-target check, strict clippy, static/scoped gates, and independent audit are clean; generated residue was removed. Runtime behavior is unchanged, so HAZARD and report card were not applicable. STRUCT-001HG isolated the complete scalar aggregate phase in the rustfmt-clean 231-line private `engine_expr/scalar_aggregate.rs`, reducing the root to 2,925 lines. The normalized helper is token-identical after explicit captured-borrow adaptations and tail-result syntax; selected columns and access path transfer by value and are Arc-wrapped once without clones. Ten focused GPU/spec controls, 28 independent-audit GPU/spec controls, both 505/487 engine modes, the complete 992-test GPU suite, all-target check, strict clippy, static/scoped gates, and independent audit are clean; generated residue was removed. Zero-row NULL/count, typed reductions/scales/overflow/errors, AVG, COUNT(DISTINCT), GPU metadata, and lifetimes are exact. Runtime behavior is unchanged, so HAZARD and report card were not applicable. STRUCT-001HH isolated terminal typed/nullable projected-row materialization in the rustfmt-clean 216-line private `engine_expr/projected_rows.rs`, reducing the root to 2,746 lines. The normalized body is token-identical after explicit snapshot borrowing; bound selection, access path, and the already-windowed index vector move by value without clones, while ORDER and LIMIT/OFFSET remain before every gather. Twenty-seven focused GPU controls, 35 independent-audit GPU controls, both 505/487 engine modes, the complete 992-test GPU suite, all-target check, strict clippy, static/scoped gates, and independent audit are clean; generated residue was removed. All typed/default/text/validity gathers, NULL override, row-major output, targets, metadata, and errors are exact. Runtime behavior is unchanged, so HAZARD and report card were not applicable. STRUCT-001HI isolated exact non-grouped GPU ORDER and post-sort windowing in the rustfmt-clean 354-line private `engine_expr/non_grouped_order.rs`, reducing the root to 2,433 lines. The normalized phase is token-identical after snapshot-borrow adaptation and one result wrapper; survivor indices move by value through ORDER, LIMIT/OFFSET, and projection. The now-unused crate-root `ExprStep` import was deleted. Twenty focused and 20 independent-audit GPU controls, both 505/487 engine modes, the complete 992-test GPU suite, all-target check, strict clippy, static/scoped gates, and independent audit are clean; generated residue was removed. Runtime behavior is unchanged, so HAZARD/report card were not applicable. Final disposition: the root now contains exactly one cohesive resident SELECT/grouped GPU orchestration function. Further splitting would separate derived device-buffer guards from consumers or require a catch-all context bag, so the registered `CODE_SIZE.md` exception preserves the stronger invariant. Re-review at +20% (~2,920 lines), a second production responsibility/method family, a stable grouped-pass contract, public-boundary change, or 5,000 lines. |
| 1,360 | `crates/protocol/src/lib.rs` | **DISPOSITION COMPLETE — production facade and all oversized inline-test owners are bounded.** The 1,235-line production facade owns startup/session/frontend/lifecycle/backend encoding. Thirteen private test children own 70 tests: startup (641), session commands (447), transaction commands (275), command rejections (443), terminators (140), bootstrap KV (77), lifecycle (86), valid frontend messages (1,965), malformed frontend messages (1,556), relational SQL facade (2,085 audited cohesive single-test disposition), SELECT features (349), aggregates (315), and catalog SQL compatibility (483). The root retains exactly one closest backend-writer test with root-only `backend_messages`, plus private `frontend_frame` shared by 257 frontend-child calls; further extraction adds needless test-only indirection. Exact 71-name inventory, normalized-source proofs, focused/full protocol package (71 library/127 binary/two one-test integrations/docs), protocol/server static gates, security preflight, fresh inventory, and independent audit are clean. Production bytes/API/visibility/runtime are unchanged; no protocol source is a required-analysis outlier. |

**Wave 2 — other critical production files (5,001–10,000 lines).** Start after each affected crate has a stable
module map; work may run independently across crates but GPU validation remains serialized.

| Lines | File | Disposition / evidence |
|---:|---|---|
| 1,783 | `crates/engine/src/engine_streaming_exec.rs` | **DISPOSITION COMPLETE — bounded streaming facade/control-plane owner below the 2,000-line production analysis threshold.** STRUCT-001HW–IH isolated every separable join, SELECT-fold, scalar, cold-lifecycle, DML/class, and chunk-key owner into bounded private leaves. IH moved exact key-index/Bloom/candidate/structural-uniqueness behavior into the rustfmt-clean 1,164-line `streaming_chunk_keys.rs`, preserving six public telemetry and five crate APIs, adding exactly seven sibling bridges, and keeping nine helpers private. All 80 focused GPU executions, both 505/487 modes, the complete 992 suite, all static/source/cleanup gates, and an independent 16-control audit passed. The root now cohesively owns shared cold/key/staging contracts and telemetry, durable checkpoint codec/orchestration, sanctioned final readback/reverse decode, and the scheduler helper; no exception is required. Multi-GPU remains separately deferred. |
| 1,872 | `crates/wal/src/lib.rs` | **DISPOSITION COMPLETE — bounded mixed root below the 2,000-line production threshold; no exception.** STRUCT-001II–IN isolated coherent test, buffer/group-flush, timeline/registry/prune, object-backup, and checkpoint owners; IL hardened timeline delimiters. IN moved exact checkpoint/control/lane ownership into the 371-line `checkpoint.rs` behind three type/nine function re-exports with no bridge. Both 85-test modes, 40 engine recovery tests, five GPU cold-checkpoint controls, static gates, fresh inventory, and independent audit passed. The root cohesively retains shared record/archive/recovery/commit contracts; strict/tolerant segment+tail persistence; archive manifest/recovery/retention; and shared path/validation/codec helpers used by multiple leaves. Further splitting now causes bridge/helper churn. Re-audit at 2,000 lines, a second new family, bridge/cycle/public-boundary pressure, or descendant threshold crossing. All WAL descendants are within mandatory envelopes. |
| 1,810 | `crates/sql/src/lib.rs` | **DISPOSITION COMPLETE — bounded SQL facade below the 2,000-line production threshold; no exception.** STRUCT-001IO–IV isolated COPY, fixed-point decimal, ACL, SELECT, scalar, command/control, AST, and relation/schema/table/index/DML parsing into bounded private leaves. IV moved the exact six-range relation owner into the rustfmt-clean 1,414-line `relation.rs`; normalized reconstruction differs only by separator blanks and the sole authorized `pub(super)` dispatcher bridge. The root privately aliases that entry for the existing command child; all 36 subordinate functions remain private. Both SQL/protocol/server/engine/facade test modes, all static/source/rustdoc/cleanup gates, fresh inventory, and independent audit pass. Runtime behavior is unchanged, so HAZARD/report-card/GPU-kernel gates were inapplicable. Every SQL production descendant is below 2,000 lines and every new leaf is below 1,500. |

**Wave 3 — production review outliers (2,001–5,000 lines).** Analyze after Waves 1–2 establish the relevant
facades, unless one is a safe leaf extraction that directly reduces an earlier wave.

| Lines | File | Disposition / evidence |
|---:|---|---|
| 2,083 | `crates/engine/src/engine_dml_concurrent.rs` | **DISPOSITION REOPENED UNDER PRODUCT-001 — no exception.** The accepted STRUCT-001IW/IX/IY split left this facade at 1,761 lines. PRODUCT-001's transaction-isolation, canonical-admission, transaction-private catalog, composite-publication, prepared compatibility, and pg_dump command-exhaustiveness foundations added 322 net lines of snapshot capture, program ownership, catalog stamps, coordinator integration, and typed compatibility routing, crossing the 2,000-line analysis threshold. The active PRODUCT-001 unification row owns removal or extraction of that transitional control-plane surface while it deletes the old callable write paths; this file must return below 2,000 lines before PRODUCT-001 closes. Existing `wave.rs`, `lane.rs`, and `lane_apply.rs` ownership remains unchanged, and a permanent size exception is not accepted. |
| 2,012 | `crates/engine/src/engine_mutation_admission.rs` | **DISPOSITION REOPENED UNDER PRODUCT-001 — no exception.** The accepted bounded admission owner crossed the production analysis threshold when PRODUCT-001 added typed session-control admission and the atomic same-transaction characteristic replacement needed by canonical pg_dump. It still owns one cohesive parsed-command admission match and its closest boundary tests, but the active PRODUCT-001 legacy/API deletion slice must remove obsolete arms or extract a private invariant-owned child and return this file below 2,000 lines before PRODUCT-001 closes. No public execution boundary, catch-all shard, or permanent size exception is accepted. |
| 1,952 | `crates/engine/src/mvcc_read_exec.rs` | **DISPOSITION COMPLETE — bounded GPU-native MVCC read root below 2,000 lines; no exception.** STRUCT-001IZ/JA/JB/JC isolated exact row-operation (1,441), source-resolution (400), query-capability (347), and CUDA filter (594) owners behind stable crate facades. The final JC slice passed 39 sequential plus 26 concurrent actual-CUDA filter executions, both 505/487 modes, the complete 992-test suite, static/source/cleanup gates, fresh inventory, and independent audit. Dependencies remain one-way, backend contracts remain root-owned, and no CPU hot path or API drift was introduced. |
| 1,606 | `crates/engine/src/engine_retained_read.rs` | **DISPOSITION COMPLETE — bounded retained-read facade below 2,000 lines; no exception.** STRUCT-001JD–JI isolated exact prepared-template (145), cached wave-index (133), payload-submission (150), device-index append (167), wave-locate (383), and complete sharded point-lookup backend (1,388) owners. JI retained exactly two existing `pub(crate)` contracts, six private helpers, and three narrow parent/sibling bridges; its 24-route matrix passed 24 sequential plus 48 concurrent CUDA executions, the independent audit added 60, both 505/487 modes and the complete 992 suite pass, and fresh inventory reduced to 18 actionable outliers. The root cohesively owns retained job preparation/submission/completion, public benchmark adapters, row assembly, and small cache-source/type/test contracts. |
| 1,838 | `crates/engine/src/engine_sql_pg.rs` | **DISPOSITION COMPLETE — bounded SQL-to-GPU dispatch/window root below 2,000 lines; no exception.** STRUCT-001JJ/JK isolated exact join lowering (658 lines) and SELECT lowering (975 lines). JK preserves the crate-visible parser through one facade, keeps ten internal bridges and 15 private helpers, and establishes the sole one-way `join_lowering -> select_lowering` edge. Its 20-route matrix passed 20 sequential plus 40 concurrent controls, the independent audit added 40 host/GPU runs, both 505/487 modes and the complete 992 suite pass, and fresh inventory reduced to 17 actionable outliers. The root cohesively owns catalog-bound SQL dispatch and GPU rank/window runtime execution. |
| 1,865 | `crates/write_conveyor/src/wal_segment.rs` | **DISPOSITION COMPLETE — bounded WAL format/segment/manager/recovery root below 2,000 lines; no exception.** STRUCT-001JL moved the exact 830-line inline test body into `wal_segment/tests.rs`, preserving four helpers, 31 tests, all paths, and a byte-identical 1,863-line production prefix. Focused concurrent/serial/release tests passed 31/31, complete debug/release crate suites passed 59/59, six GPU lane-recovery executions and both 505/487 engine modes passed, and static/source/cleanup/audit gates are clean. |
| 779 | `crates/engine/src/engine_write_apply.rs` | **DISPOSITION COMPLETE — bounded delta-install/batcher root below 2,000 lines; no exception.** STRUCT-001JM moved the exact complete 1,472-line executable `preflight_unique_index_constraints` method into the rustfmt-clean 1,486-line private `engine_write_apply/preflight.rs` leaf. The method hash and reconstructed full-source hash match, its `pub(crate)` inherent path and three callers are unchanged, and the sole added dependency is the explicit `TupleStore` trait formerly supplied by the parent glob. Twenty focused host controls, 57 focused actual-GPU executions, both 505/487 modes, the complete 992 suite, static/source/cleanup gates, and independent audit passed. |
| 1,996 | `crates/engine/src/engine_dml_prepare.rs` | **DISPOSITION COMPLETE — bounded DML prepare/resolve/materialize/constraint runtime owner below 2,000 lines; no exception.** STRUCT-001JN moved the exact 228-line state-free prelude into the rustfmt-clean 232-line private `engine_dml_prepare/contracts.rs` child. Its two aliases, two functions, validation enum/proof, four stable crate-private facade paths, and all typed canonicalization/DNF/decline semantics are exact apart from one scope-equivalent helper visibility token. The later R3-003 transaction-only rebind guard lives with the bounded transaction-delta owner, preserving this file's established invariant and envelope. Ten host controls, 100 focused actual-GPU executions, both 505/487 modes, the complete 992 suite, static/source/cleanup gates, and independent audit passed. |
| 1,972 | `crates/engine/src/rel_exec_helpers.rs` | **DISPOSITION COMPLETE — bounded relational helper root below 2,000 lines; no exception.** STRUCT-001JO moved the exact four-function row codec into the rustfmt-clean 176-line private `rel_exec_helpers/row_codec.rs` child. Full normalized reconstruction is byte-identical; all prefixes, NULL/text escaping, numeric scale, typed parsing/errors, four stable crate-private facade paths, 72 live invocation sites, and dependency direction are unchanged. Ten host codec/WAL/recovery controls, 40 focused actual-GPU executions, both 505/487 modes, the complete 992 suite, static/source/cleanup gates, and independent audit passed. |

**Wave 4 — test-suite outliers (over 3,000 lines).** Split by behavioral seam and fixture ownership after, or
alongside, the production module they cover; do not fragment tests merely to reduce a count.

| Lines | File | Disposition / evidence |
|---:|---|---|
| 2,695 | `crates/engine/src/tests/resident_expr.rs` | **DISPOSITION COMPLETE — bounded resident-expression test facade below 3,000 lines; no exception.** STRUCT-001JP–JZ isolated exact scalar, nullable, grouping, DISTINCT, aggregate, and ordering owners into 177–1,459-line private leaves. JZ moved the exact 15-test non-grouped ordering matrix into the rustfmt-clean 896-line `nongrouped_ordering.rs` child; payload/full reconstruction hashes match, names and two external evidence references are unchanged, and the accepted dependency is three import declarations/four names. The independent audit caught the predicted three-name undercount: six exact `RelationalSelectResult` annotations require that fourth name, and removing it fails compilation. Forty-five local plus 45 independent-audit actual-GPU executions, both 505/487 debug/release modes, the complete 992 suite, workspace check, strict Clippy, rustdoc, static/source/cleanup gates, fresh inventory, and independent audit pass. |
| 2,927 | `crates/engine/src/tests/streaming_exec.rs` | **DISPOSITION COMPLETE — bounded streaming test facade below 3,000 lines; no exception.** STRUCT-001KA–KN isolated scalar reductions, rank windows, views, projections, grouped/DISTINCT, ordered folds, cold-tier/checkpoint, DML locate, sidecars, reverse gather, chunk locate/stamp, and the P4 class lifecycle into bounded private leaves. R3-004 deleted obsolete cold-store authority suites and moved its COPY/multi-entry class regression into the existing 629-line `chunk_class_lifecycle.rs` owner when the facade briefly crossed 3,000 lines. The final 45-test streaming GPU suite and complete 1,016-test engine inventory pass; the parent retains only shared fixtures plus cohesive join/key-index/uniqueness owners. |
| 2,745 | `crates/engine/src/tests/sql_pg.rs` | **DISPOSITION COMPLETE — bounded PostgreSQL binding/GPU test facade below 3,000 lines; no exception.** STRUCT-001KO–KQ isolated trailing join-materialization audits, initial join NULL-key V1b behavior, and complete OUTER/nullable/3VL semantics into 799–1,232-line private leaves. KQ moved the exact 15-test OUTER/nullable/3VL family into the rustfmt-clean 1,232-line `outer_null_semantics.rs` child. Payload/full reconstruction hashes match; 45 local plus 45 independent-audit actual-GPU executions passed, including simultaneous processes with zero CUDA 700/716/719. Both 505/487 modes, the complete 992 suite, workspace check, strict Clippy, rustdoc, static/source/cleanup gates, fresh 10-file inventory, and independent audit pass. The bounded parent cohesively retains parse/text dispatch plus join-shape/type/catalog/ordering coverage; expected rows/truth tables remain test-only parity evidence. |
| 2,569 | `crates/engine/src/tests/mvcc_bundles.rs` | **DISPOSITION COMPLETE — bounded MVCC bundle test facade below 3,000 lines; no exception.** STRUCT-001KR–KS isolated the exact initial frame/bundle-path and occurrence-cardinality/distance owners into 1,371–1,446-line private leaves. KS moved the exact seven-test occurrence-distance family into the rustfmt-clean 1,371-line `occurrence_distance_filters.rs` child; payload/full reconstruction hashes match. Twenty-one local plus 21 independent-audit host-specification executions and nine local plus nine independent-audit actual-GPU executions passed, including simultaneous GPU processes with zero CUDA 700/716/719. Both 505/487 modes, the complete 992 suite, workspace check, strict Clippy, rustdoc, static/source/cleanup gates, fresh nine-file inventory, and independent audit pass. The bounded parent retains occurrence-offset/projection/order/mixed-occurrence owners; RETIRE-001 later replaced the moved host execution with rows-only specification fixtures. |
| 2,769 | `crates/engine/src/tests/intent_fast_path.rs` | **DISPOSITION COMPLETE — bounded GPU intent fast-path test facade below 3,000 lines; no exception.** STRUCT-001KT–KU isolated exact constraint-elision and intent-lane lifecycle owners into 945–1,499-line private leaves. KU moved the exact nine-test/seven-helper submit/poll, recovery, conflict, async-commit, delete/update, and sustained-update lifecycle into `lane_lifecycle.rs`; payload/full reconstruction hashes match. Twenty-seven local plus 27 independent-audit actual-GPU executions passed, including simultaneous processes with zero CUDA 700/716/719. Both final 505/487 modes, the complete 992 suite, workspace check, strict Clippy, rustdoc, static/source/cleanup gates, fresh eight-file inventory, and independent audit pass. The bounded parent cohesively retains route shape, compound keys, typed predicates, batching, residency, and versioning coverage. |
| 2,916 | `crates/engine/src/tests/resident_route.rs` | **DISPOSITION COMPLETE — bounded resident-route test facade below 3,000 lines; no exception.** STRUCT-001KV/KW isolated the exact sharded lookup/projection and sharded reduction owners into rustfmt-clean 832/913-line private leaves. KW moved all five SUM/AVG/filtered MIN/MAX tests with exact payload/full reconstruction hashes, 15 local plus 15 independent-audit actual-GPU executions, simultaneous GPU processes, and zero CUDA 700/716/719. Both 505/487 modes, the complete 992 suite, workspace check, strict Clippy, rustdoc, static/source/cleanup gates, fresh seven-file inventory, and independent audit pass. The bounded parent retains route planning, maintenance/status, empty/nonuniform guards, S10 grouped, and R1/R2 wave owners. |
| 2,798 | `crates/engine/src/tests/sql_catalog.rs` | **DISPOSITION COMPLETE — bounded SQL-catalog test facade below 3,000 lines; no exception.** STRUCT-001KX isolated the exact 14-test stable relation metadata, index/PK/UNIQUE/CHECK/FK enforcement/replay, index rename/drop, constraint drop, and single/batched table-drop lifecycle in the rustfmt-clean 1,183-line private `relation_lifecycle.rs` child. Payload/full reconstruction hashes match; 42 local plus 42 independent-audit host-specification executions passed. Both 505/487 modes, the complete 992 suite, workspace check, strict Clippy, rustdoc, static/source/cleanup gates, fresh six-file inventory, and independent audit pass. RETIRE-001 later replaced the CPU-oracle fixtures with rows-only semantic specifications; the bounded parent retains numeric coercion, catalog query/GPU bridge, TRUNCATE, ACL/replication/domain/function/comment, durable replay, and compound-key owners. |
| 2,347 | `crates/engine/src/tests/mvcc_query.rs` | **DISPOSITION COMPLETE — bounded MVCC-query test facade below 3,000 lines; no exception.** STRUCT-001KY isolated the exact 24-test/24-ignore initial actual-CUDA historical lookup, composition, ordering, filtering, visibility, provenance, and source-relative driver matrix in the rustfmt-clean 1,164-line private `cuda_driver_routes.rs` child. Payload/full reconstruction hashes match; 72 local plus 72 independent-audit actual-CUDA executions passed with simultaneous GPU processes and zero CUDA 700/716/719. Both 505/487 modes, the complete 992 suite, workspace check, strict Clippy, rustdoc, static/source/cleanup gates, fresh five-file inventory, and independent audit pass. The bounded parent retained capability/fallback/backend-parity seams at that structural checkpoint; RETIRE-001 later deleted those seams while preserving native set-composition, provenance, and benchmark-report owners. |
| 2,876 | `crates/execution/src/tests/cuda_paths.rs` | **DISPOSITION COMPLETE — bounded CUDA-path test corpus below 3,000 lines; no exception.** STRUCT-001KZ normalized the exact final 164-line resident-generation publish/pinned-reader/GPU-read/post-drain reclamation soundness test into the rustfmt-clean 168-line private sibling `tests/cuda_generation_lifetime.rs`. Re-indenting nonblank child payload lines by four spaces and restoring the removed separator reconstructs the old 3,041-line include fragment byte-for-byte. Three local plus three independent-audit actual-GPU executions passed with simultaneous processes and zero CUDA 700/716/719. The execution library's 56/77 ordinary and 133/133 include-ignored suites, workspace check, strict Clippy, private rustdoc, static/source/cleanup gates, fresh four-file inventory, and independent audit pass. The unchanged bounded include fragment retains all other CUDA operator/runtime tests. |

**Wave 5 — example and tool outliers (over 3,000 lines).** Determine whether each is a cohesive executable,
handwritten tool, reproducible generated artifact, or obsolete evidence before choosing modules or an exception.

| Lines | File | Disposition / evidence |
|---:|---|---|
| deleted | `scripts/generate_research_paper_mechanism_links.py` | **DISPOSITION COMPLETE — obsolete live tool deleted; archive evidence preserved.** STRUCT-001LA proved the 14,889-line handwritten file mixed a 21-line import/regex prelude, 13,495 lines of reviewed declarative corpora, and 1,373 lines of runtime logic, but every default input/output had moved to `docs/archive/research/` in the independently audited documentation consolidation. The live default invocation fails on the absent journal, no non-archive consumer references it, and an explicit invocation against the archived journal/mechanism inputs regenerated all four archived JSON/Markdown artifacts byte-for-byte. Splitting it would recreate ownership for historical, explicitly non-actionable data; deletion removes the dead tool while leaving the archived evidence untouched. |
| 2,776 | `crates/write_conveyor/examples/write_conveyor_bench.rs` | **DISPOSITION COMPLETE — bounded root plus two named scenario children; no exception.** STRUCT-001LB/LC moved the normalized-exact direct-client and coalesced-client latency owners into private 402-line `write_conveyor_bench/direct_client_latency.rs` and cohesive 1,577-line `write_conveyor_bench/coalesced_client_latency.rs` children. The root uniquely retains the shared harness/configuration/types/sampling/reporting, CLI dispatcher, simpler scenarios, coalesced label selection, and WAL-worker tail; both children use explicit one-way parent imports and root-confined entry visibility with no glob, path/include indirection, sibling cycle, context bag, duplicated helper, external target/API, or stale owner. Exact payload and reversible-root hashes pass for both slices. All direct and coalesced acknowledgement modes, segment and manager backends, two-lane durable manager recovery, matching checksums, store/durable cuts, the 59-test crate suite, default builds, strict example Clippy, scoped formatting/static/cleanup gates, fresh inventory, and two independent audits pass. The coalesced child is deliberately 77 lines above the normal 1,500-line target because all seven thread closures share one queue/manager/durability synchronization lifetime; splitting it would manufacture a broad context bag or duplicate state, and it remains far below the 3,000-line example envelope. Runtime behavior is unchanged, so HAZARD/report card was inapplicable. |
| 2,856 | `crates/server/examples/p8_engine_pgwire_benchmark_endpoint.rs` | **DISPOSITION COMPLETE — bounded root plus three named leaves; no exception.** Handwritten, auto-discovered `gpu_db_server` example, source hash `c5e68657…`, with no cfg/unsafe block and 62 follow-history commits. Responsibilities are: response cache (30–81); retained device-read route/stats/workers/fixed+text batches (83–741); COPY plus endpoint engine/fact state (742–1736); request/response/completion contracts (1737–1907); retained batch candidate owner (1908–2031); owner-thread admission/scheduler helpers (2033–2245); result-row codec (2247–2283); result/fact rendering (2285–2413); pgwire framing/client I/O (2414–2584); and main configuration, threads, scheduling, facts, and teardown (2585–3668). Imports span engine/execution/metrics/protocol plus standard file/network/thread primitives; the example is Cargo-owned only by `gpu_db_server`, while three live probe-script sites build and launch the stable target name. STRUCT-001LE corrected all three stale `gpu_db_engine` build selectors to `gpu_db_server`; the persistent runner's separate engine selector is unchanged, and the 16-row live endpoint smoke again closes with retained-route acceptance, zero H2D, nonzero CUDA event timing, and clean teardown. STRUCT-001LF moved the normalized-exact result-row codec into the 44-line private `p8_engine_pgwire_benchmark_endpoint/result_rows.rs` leaf, reducing the root to 3,636 lines; payload hash `118e6e8f…`, reversible-root hash `c5e68657…`, build/strict Clippy/server tests, live retained GPU smoke, static gates, and independent audit pass. STRUCT-001LG moved the normalized-exact retained batch contract into the 126-line private `retained_batch.rs` leaf, reducing the root to 3,515 lines; payload hash `d785e973…`, reversible-parent hash `901d9453…`, build/strict Clippy/server tests, live retained GPU smoke, static gates, and independent audit pass. STRUCT-001LH moved the normalized-exact retained route/stats/work-queue and fixed/text execution owner into the 683-line private `retained_runtime.rs` leaf, reducing the root to 2,856 lines; payload hash `1ad2b0d2…`, reversible-parent hash `1a7c6b7a…`, build/strict Clippy/server plus 505/487 engine tests, benchmark/concurrency smokes, 3-sequential/2-concurrent GPU HAZARD matrix, static gates, and independent audit pass. The complete graph is `root -> {runtime, retained_batch, result_rows}` and `runtime -> {retained_batch, result_rows}`; the response cache, endpoint/COPY state, scheduler, rendering/facts, wire I/O, and executable orchestration remain unique at root. The final root and every leaf are bounded, with no context bag, broad visibility, cycle, duplicated helper, path/include indirection, or exception. History separates initial endpoint/probe creation, retained route/batching/runtime work, later SQL/type/result-layout compatibility, and lint-only repairs. Preserve the example name, script/env/fact/output contracts, GPU-native retained path, protocol bytes, generation/in-flight fencing, synchronization, errors, and teardown through exact-source, default-build/Clippy/test, small endpoint GPU/concurrency/HAZARD, static/cleanup, inventory, and independent-audit gates recorded in the child rows. |
| 2,969 | `scripts/run_p8_ch_benchmark_residency_probe.sh` | **DISPOSITION COMPLETE — bounded executable root plus one sourced boundary-report leaf; no exception.** The analyzed 3,137-line handwritten, 700-mode operator harness had 70 follow-history commits, 53 functions, 24 CLI modes, 67 distinct `GPU_DB_*` symbols, and 24 artifact roots spanning shared/load helpers, PostgreSQL controls, protocol/pgwire smokes and metrics, identical-target orchestration, protocol/retained boundary reports, guarded readiness/execution, and dispatcher/self-check/cleanup. Process/trap ownership remains root-confined. STRUCT-001LJ corrected the boundary example's sole stale Cargo owner; LK bound two retained-runtime-view shape checks to live query/pass/route/GPU metrics; LL replaced one retired concurrency prose check with the current closed decision/scheduler/next-target metrics contract. STRUCT-001LM then moved the exact two-function boundary-report family into the 169-line `scripts/lib/p8_ch_benchmark_protocol_boundary.sh` leaf behind one source line, reducing the root to 2,969 lines. After removing the standard shell-source header and restoring the separator, payload hash `5b67f7a0…` and complete parent reconstruction hash `8935732c…` match the post-LL baseline. The leaf consumes only `OUT_DIR`, two planned row env vars, and external commands; it has no root helper, trap, process, port, back-edge, duplicate, or external consumer. All 53 functions, 24 modes, reports, metrics, blockers, exits, and source modes remain intact. Both direct boundary modes, 16-row SQL-visible retained admission/zero-H2D, complete workspace-local self-check, shell/static/reference/cleanup gates, fresh inventory, and independent audit pass. Runtime behavior is unchanged, so HAZARD/report card were inapplicable. |

STRUCT-001 closes only when every row has an audited disposition; every accepted retention appears in the
`CODE_SIZE.md` exception registry; no non-excepted production file exceeds 2,000 lines and no non-excepted
test/example/tool exceeds 3,000; all references resolve; targeted gates pass after every extraction; and a fresh
inventory finds no unowned outlier. Line-count drift is expected, so the fresh inventory—not this snapshot—is
the final acceptance source.

## Work ledger

| ID | State | Priority | Outcome and acceptance gate | Dependencies / trigger | Design or evidence |
|---|---|---:|---|---|---|
| **READ-002** | BLOCKED | P2 | The BENCH prerequisite milestone passed on 2026-07-19: the typed engine API prepares one exact-generation, O(1) GPU directory for compound `(tenant_id int4, account_id int8)` equality, exact-rechecks collisions and MVCC on-device, gathers the required fixed-width projections, proves nonzero GPU/index/cache hits, and retains zero cold accesses with GPU-scan parity. Broader bigint/text/UUID/numeric and composite point-lookup breadth remains under READ-002 after BENCH-001. | Accepted BENCH-001 architecture evidence for the broader breadth; canonical compound milestone is complete | `docs/design/non-int4-index-design-inputs.md`; 2026-07-19 STATUS evidence |
| **PRODUCT-002** | BLOCKED | P2 | The BENCH canonical SQL/type milestone passed on 2026-07-19: the immutable schema and W1 statements parse unchanged; typed placeholders and required pgwire codecs are available; every named primary/secondary index publishes and mutates in resident GPU generations; checked expression UPDATE and DML `RETURNING` execute without host relational fallback. After BENCH evidence, complete persistent GPU system catalogs, large NUMERIC, and unrelated protocol/type breadth. | Accepted BENCH-001 architecture evidence for the broader breadth; canonical SQL/type milestone is complete | ARCHITECTURE §3; `docs/design/oltp-benchmark-workload-v1.md`; 2026-07-19 STATUS evidence |
| **PRODUCT-001** | NOW | P0 | Unify serving and writes rather than adding a benchmark-only path. Carry exact prepared R1/W1 and predeclared atomic T8/T32 through the engine-backed Parse/Bind/Execute pgwire server with publication-covered crash-durable synchronous acknowledgement and preclaim enforcement of every immutable manifest dimension: operations, mutations, post-image plus logical-WAL bytes, maintained-index fanout, touched tables, cold accesses, and result bytes. Every live database-local logical mutation from autocommit, predeclared execution, interactive `COMMIT`, generic DML/DDL, or COPY crosses one typed transaction-admission interface before sequence/WAL claim. ADR-014 sequence/system effects use their own typed records in the same authoritative global-order/WAL/durable-applied/publication coordinator; reads are unsequenced, and recovery uses a replay-only interface without claiming or re-appending. Exactly one product pgwire binary and one public facade mutation/transaction boundary may reach the coordinator; covered, classic, serialized, explicit-commit, COPY, and recovery components become private strategies. Mixed traffic must remain available after intent-lane activation; delete the intent-only guard only after crash/recovery tests prove the shared order. Replace SQL-text parameter reconstruction on the prepared path. First inventory all legacy behavior, targets, public APIs, tests, and consumers; then migrate the PG16 psql golden, pg_dump/restore, tokio-postgres, SQLx, node-postgres, TLS/SCRAM, COPY, cancellation, prepared/portal, transaction-state, and catalog/introspection surface. Unsupported behavior needs an accepted deviation or PLAN owner and must fail pre-effect. Delete the legacy host-relational binary/state and P8 endpoint only after those gates pass. No slice may land a second callable product path or commit owner: move ownership and remove the old entry together. Each independently reviewable slice gets a fresh read-only adversarial audit over its gates/sabotage; findings are fixed and re-audited to ACCEPT before the next slice. Final source/build plus whole-tree audit must enumerate and prove the sole server, facade submission boundary, WAL claimant, and publication owner. RETIRE-002 repair stays private and unchanged behind that publication owner; DUR-001 remains after the accepted BENCH baseline. PRODUCT-001 closes only when serving, mixed-traffic/recovery, compatibility, deletion, and ownership gates pass. | PRODUCT-002 canonical SQL/type milestone completed 2026-07-19; user promoted full unification on 2026-07-19 | ARCHITECTURE §§2–4, 6, 8, 11; ADR-014; `docs/design/product-001-serving-write-inventory.md`; `docs/design/oltp-benchmark-workload-v1.md` |
| **COPY-001** | BLOCKED | P0 | Deliver PostgreSQL-compatible, bounded, GPU-native bulk `COPY FROM STDIN` behind the sole PRODUCT-001 admission/WAL/publication authority. Parse once; stream text/CSV/binary `CopyData` into typed columnar chunks; preserve PostgreSQL 16 NULL, column/default/sequence, constraint, transaction, cancellation, SQLSTATE, and command-count behavior; generate canonical typed WAL without SQL rendering/reparse; perform device-batch encode/validation/append/sidecar/index work; keep chunks private so autocommit COPY publishes once and explicit COPY publishes only with its enclosing transaction; and prove RPO-0 fresh-context recovery with no host relational authority or O(total rows) staging. Real `psql \copy`, simple/extended clients, autocommit/explicit transactions, failure/retry/reopen parity, bounded-memory accounting, non-vacuous GPU evidence, HAZARD, and independent audit are mandatory. Replace only report-card Layer 2 fixture construction after INSERT-versus-COPY semantic/layout equivalence: under its existing local/in-memory no-fsync profile, the unchanged 48M-row Section C fixture must load and publish in <=180s and >=270,000 rows/s on the canonical RTX PRO 6000 (>=10x versus 2,138.3s); post-build execution must finish in <=10 minutes and the complete canonical isolated clean-build invocation in <=20 minutes, with phases reported separately. | Complete PRODUCT-001 and preserve its sole server, facade, sequence/WAL, recovery, and publication ownership | CHARTER host-control-plane COPY staging; ARCHITECTURE §§1, 6–8; ADR-014/015; 2026-07-22 HANDOVER report-card baseline |
| **BENCH-001** | BLOCKED | P0 | The mutation-boundary repair plus READ-002 and PRODUCT-002 canonical milestones are complete. After PRODUCT-001 and COPY-001 pass, use the canonical bulk COPY path for the deterministic 34M-row seed, then build the remaining benchmark-owned pieces: fixed 3.3M+66M arrival generator, `B01`–`B10` driver, versioned result artifact, and reproducible tuned PostgreSQL durability/checkpoint profile. Run the complete same-host comparison in a qualified quiet window and report per-class p50/p99/p99.9/p99.99, committed TPS, logical operations/s, producer slip, queueing, WAL, saturation, and drain. The accepted artifact publishes the exact host, CPU/governor, GPU/driver/runtime, durability, and PostgreSQL configuration. Exclude warm-up, preserve the fixed schedule/mix, never add a benchmark-only write authority, and never substitute the older P8 `order_line` suite. Preserve the accepted pre-DUR result for the DUR-001 overhead comparison. | Complete PRODUCT-001 and COPY-001, including serving, mixed-traffic/recovery, compatibility-migration, deletion, source-ownership, and bulk-seed gates; then PostgreSQL profile and qualified quiet window | ADR-008; `docs/design/oltp-benchmark-workload-v1.md`; ARCHITECTURE §§6, 10; 2026-07-19 STATUS preflight |
| **DUR-001** | BLOCKED | P1 | After the accepted pre-DUR BENCH-001 result is preserved, add automatic intent-lane checkpoint policy and timestamped lane records sufficient for archive/PITR. Keep explicit operator checkpointing and refusal behavior until both are crash-gated, then rerun affected BENCH cohorts to report checkpoint-policy overhead against that exact baseline. | Accepted pre-DUR BENCH-001 artifact | Archived durable-path handover and write-conveyor record |
| **RETIRE-002** | BLOCKED | P1 | Replace chunk reverse-gather, deauthorization, and scan-build repair with device-native DDL/recovery/import repair; then delete those host relational repair operators. Acked commits remain recoverable after every injected repair failure. | Device-native DDL validation and recovery repair | ADR-006; STRATA repair boundary |
| **HA-001** | BLOCKED | P1 | Wire engine sequencing to replicated log indices; lane claims reserve Raft log-index ranges and client acknowledgement waits for quorum commit. Add follower rejection, catch-up, promotion/fencing, and snapshot-install gates. | R3 sequencing contract and multi-node runtime | ADR-001/004/005 |
| **READ-001** | VERIFY | P1 | Reproduce or close the remaining filtered-expression overflow-ordering and route-gate case findings against the current tree. R3-004 closeout resolved the previously ignored facade shape-changing-DDL/read and elided-DML races with statement generation retention, representation-safe rebinding, and commit-time FK revalidation; the complete 14-test concurrency integration suite now passes. Every remaining live defect receives a focused GPU/spec regression, while invalid CPU-era fixtures are rewritten around supported GPU routes. | None | STATUS known-debt facts and R3-004 facade stress evidence |
| **READ-003** | VERIFY | P2 | Confirm an empty filtered SUM/AVG/MIN/MAX reaches a real pgwire client as a typed SQL NULL. Add one end-to-end GPU test if the existing engine and wire-unit coverage do not cross that seam. | Current aggregate and pgwire paths | Archived M3 proposal acceptance criteria |
| **CFG-001** | NEXT | P2 | Reckon remaining product/runtime flags and setters: winner becomes unconditional, losing arm and knob are deleted together. This is touch-triggered work, not a competing stream: remove a superseded sequencer/classic-path or benchmark knob when its owning subsystem is already being changed. | Owning subsystem is touched and its replacement path is complete and gated | `CONFIG.md` |
| **TOOL-001** | NEXT | P2 | CI compiles the permanent `probe-timing` instrumentation feature so probes cannot bit-rot. Agent guidance already requires reuse. | CI edit window | Archived instrumentation proposal |
| **ROUTE-001** | BLOCKED | P2 | Productize the OLTP route classes beyond PK microbenchmarks: tenant/security-filtered page reads, bounded two-table joins with a fanout contract, and computed-detail routes with resident summaries/invalidation. These broader routes do not block the immutable banking campaign. | Accepted BENCH-001 workload evidence | CHARTER transaction model; ARCHITECTURE §§6, 10 |
| **PRODUCT-003** | PARKED | P2 | Production hardening: packaging, SBOM/audit/deny, GPU CI fatbins, panic/unsafe review, Prometheus/OTLP/audit logging, mTLS/channel binding, credential management, and deployment runbooks. | Core correctness and recovery gates | Trigger: v1 release candidate |
| **SCALE-001** | BLOCKED | P2 | Replace unbounded connection/thread/queue behavior with explicit admission and bounded ownership domains; add pre-authentication read deadlines and an aggregate connection-memory budget so slow clients cannot concurrently pin the bounded 64 MiB tagged-frame allowance; validate 100k+ logical connections and bounded result streaming. This connection-scale hardening does not block the immutable banking campaign. | Accepted BENCH-001 workload model | ARCHITECTURE §§4, 10; STRUCT-001AK residual |
| **SCALE-002** | VERIFY | P2 | Disposition the legacy scalability ledger against the current tree: uncapped per-row locate loops, populate-vs-commit admission race, first-transition elision TOCTOU, eviction rollback, and the recorded 32-writer PK inversion. Keep only reproducible issues. | None | Archived pre-unification handover/reviews |
| **MEDIA-001** | BLOCKED | P2 | Re-run low-client latency and 30-second attribution on PLP-class NVMe to separate software cost from consumer-drive FUA stalls. | PLP-class storage hardware | Archived durable-path handover |
| **SIDE-001** | PARKED | P3 | GPU-resident Redis/KV product exploration. It is not part of the database v1 plan. | Trigger: explicit post-v1 product decision | `docs/archive/future/redis/` |
| **MULTI-001** | PARKED | P1 | Run and pass the existing non-vacuous STRATA scheduler/budget test on at least two physical GPUs; record per-device completed work and failure isolation. | Trigger: every non-MULTI plan item complete or explicit user promotion, plus access to a >=2-GPU host | STATUS multi-GPU gap |
| **MULTI-002** | PARKED | P0 | Partition write/visible-locate descriptor sets by owning CUDA primary context, issue one launch per GPU while retaining the exact index/version generation, and deterministically merge only bounded coordinate/count/error metadata in the host control plane. Key lookup, MVCC visibility, duplicate decisions, and mutation targeting remain device decisions; any device failure fails the whole operation and leaves every context reusable, with no host relational probe/fallback. A non-vacuous gate on at least two physical GPUs must prove both devices perform work, no descriptor crosses context, input order and duplicate/visibility semantics survive the merge, and one-device injected failure cannot yield a partial result. | Trigger: every non-MULTI plan item complete or explicit user promotion, plus access to a >=2-GPU host | ARCHITECTURE §5–6; ADR-013; STRUCT-001Z audit |
| **MULTI-003** | PARKED | P0 | Run a non-vacuous physical multi-GPU isolation matrix for the typed resident-sidecar APIs. Allocate valid bool/validity/text sources and destinations on at least two primary contexts; prove valid work fires on each device, every crossed owner is rejected before launch, and both contexts remain reusable after each rejection. Recompaction stays device-local and GPU-only; do not introduce peer copies or host bitmap/text interpretation merely to satisfy the gate. | Trigger: every non-MULTI plan item complete or explicit user promotion, plus access to a >=2-GPU host | ADR-013; STRUCT-001AD audit |

## Legacy host-engine deletion coverage

| Remaining host surface | Owning task | Deletion boundary |
|---|---|---|
| Reverse gather, deauthorization, scan-build, and DDL/recovery/import repair | **RETIRE-002** | Device-native repair preserves RPO under injected failures before host repair deletion |
| Catalog construction boundary | **PRODUCT-002** | Host retains bookkeeping and deterministic encoding/upload only; all relational catalog decisions execute on-device |

## Standing acceptance gates

- WAL-before-visibility and `commit >= applied >= visible` monotonicity.
- GPU execution must be non-vacuous; production relational decline/fault is fail-loud, never host fallback.
- Relevant GPU tests use timeouts, run serially for sweeps, and never use `--gpu-reset`.
- Read-kernel/residency/result changes run `scripts/benchmark_report_card.sh`; compare ratios, both layers,
  and both cache regimes.
- Durability/HA changes include restart, torn/failing I/O, and acknowledged-commit recovery tests.
- Documentation changes pass the single-plan audit described in `docs/README.md`.

## Explicit non-goals until post-v1

Automated multi-region reconfiguration, broad extension compatibility, and the Redis/KV side product are not
active work unless the user promotes their task IDs.
