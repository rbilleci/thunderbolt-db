# Run Reports

This directory is the destination for individual immutable report files when a
test stream is migrated out of the top-level report directory.

Keep filenames stable during migration and leave compatibility pointers at old
paths when existing links are likely.
